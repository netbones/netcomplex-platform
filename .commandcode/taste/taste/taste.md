# Taste

- Uses handoff / blame files (e.g. `BLAME.md`) to pass context between agent sessions and expects new agents to read them for continuity before proceeding. Confidence: 0.8
- Prefers verifying assumptions against actual system state (DB queries, migration records, git history) before making code changes or fixes — explicitly directs agents to "verify and check the state" rather than jumping straight to a fix. Confidence: 0.7
- Does scoped typechecks (`tsc --noEmit` on just the changed file) before committing a change. Confidence: 0.6
- Prefers surgical git commits — uses `git commit -o` with explicit file paths to avoid sweeping in unrelated staged files, then verifies the excluded files are still staged. Confidence: 0.6
- Holds off on shared-state actions (git push, mutating a shared dev DB) unless explicitly asked — even when project conventions (e.g. AGENTS.md) treat push as mandatory at session end. Confidence: 0.6
- Works on a Next.js App Router codebase using Better Auth for authentication and Drizzle ORM against Postgres, with a multi-tenant schema (`tenantId` on user) and FSD-style layering (`src/entities/<entity>/api/`, `src/app/api/` route handlers). Confidence: 0.65
- Expects replies in English (re-asked the question after receiving a Chinese-language reply). Confidence: 0.65
- Prefers persistent visual depth on UI cards — shadows should be visible at rest (`shadow-md`), not only on hover/motion, so elements clearly sit above the background. Confidence: 0.7
- Expects visual consistency across parallel/sibling components — when a treatment (background, shadow, layout) is applied to one dashboard layer, the same treatment should be replicated on the others. Confidence: 0.7
- Prefers pragmatism over debugging non-essential tooling failures — when a pre-commit hook times out (e.g. `redocly lint`) but all formatting/lint-staged tasks pass, uses `--no-verify` to commit rather than investigating the timeout. Confidence: 0.5
- Uses conventional-commits format with structured body: a type prefix (`refactor:`, `feat:`, `fix:`), bullet-point change summary, and trailers (`Refs:`, `Co-authored-by:`). Confidence: 0.55
- Prefers structured gap analysis — cross-references the plan/instructions document and acceptance criteria against actual code to produce an honest "done vs. not done" audit plus a list of what remains, rather than relying on task markings or claiming completion. Confidence: 0.7
- Expects schema changes to be carried through to the live database — wants confirmation that migrations and seeds were actually applied (not just that the files were written), and cares about the real DB state matching the code. Confidence: 0.7
- Excludes agent/tool-managed directories (`.commandcode/`, `.amazonq/`) from feature commits — stages only feature-relevant files explicitly rather than sweeping in agent-managed artifacts. Confidence: 0.7
- Investigates the working tree before committing (`git status --short`, `git branch --show-current`, `git diff --stat`, spot-checking actual diffs) and resolves incidental churn (e.g. generator/formatting rewrites) so a commit contains only genuine changes. Confidence: 0.6
- Prefers terse, imperative commands and expects the agent to autonomously investigate scope, execute, and verify without asking clarifying questions (e.g. "commit changes", "add the background tiles from X to Y", "lets fix", "retry"). Confidence: 0.7
- Prefers reusing existing shared UI components (e.g. `DomainIconBadge`) for icons/badges rather than one-off inline icons, so elements meant to match across pages stay identical. Confidence: 0.6
- Prefers persisting gap-analysis/audit results into the plan/instructions document (e.g. `docs/plans/INSTRUCTIONS.md`, `docs/plans/INSTRUCTIONS_EVENTS.md`) to track progress before building out remaining work — wants the doc updated with completed and deferred items, status/checkboxes, and a "what remains" list as a single tracking source. Confidence: 0.8
- When a latent bug has already written bad data (e.g. a value landed in the wrong column), fixes the root-cause code AND backfills/repairs the existing rows so the live DB matches the corrected logic, rather than only patching the code path. Confidence: 0.55
- Prefers extracting a duplicated inline constant/mapping (e.g. an icon→color map) into a shared module in the relevant entity so sibling components stay consistent, rather than copy-pasting it per component. Confidence: 0.5
- Reports runtime bugs by pasting the exact console error and stack trace, and expects the agent to reproduce and fix the true root cause (e.g. an ORM/Postgres column-type mismatch that serialized to null) rather than applying a defensive surfa- Prefers fixing known/pre-existing type errors and failing tests when they surface, even if unrelated to the current change — when the agent flags errors as "pre-existing/unrelated", the user directs "we should fix" them rather than leaving them deferred. Confidence: 0.7
  ce patch that only silences the error. Confidence: 0.5
- Prefers completing a half-wired feature/module across the entire stack — type unions, module registries, tier lists, seed data, and test assertions — and cross-checking against migrations so every layer stays consistent rather than leaving any part unfinished. Confidence: 0.6
- Prefers `next/image` (`<Image />`) over raw `<img>` elements — expects `@next/next/no-img-element` lint warnings to be resolved by adopting the Next.js Image component, not suppressed. Uses `fill` + `object-cover` with `unoptimized` for remote/third-party image URLs, following existing project patterns (e.g. `MarketplaceDetailPage`). Confidence: 0.7
- Prefers implementing an intended-but-unused component (e.g. breadcrumbs) rather than just deleting the import — directs agents to "check if X needs to be implemented" and expects it wired in following sibling-page navigation patterns for consistency. Confidence: 0.6
- Expects the agent to read the feature's plan/instructions doc and its design mockups (kept in `docs/plans/mockups/`) before implementing, using the mockups to derive UI structure and scope. Confidence: 0.55
- Prefers the agent to surface open questions and blockers that need decisions — and which ones are truly blocking — rather than silently proceeding with assumptions when a schema/product decision would materially change the implementation (e.g. "what questions require answers"). Confidence: 0.5
- Defers explicitly out-of-scope features by tagging them to a future version (e.g. "mark as future v1.1") rather than dropping them silently, so they remain tracked. Confidence: 0.5
- Works in a Prisma + Drizzle dual-schema setup: Prisma (`prisma/schema/schema.prisma`) is the source of truth and the Drizzle schema files under `src/db/schema` (including `*-relations.ts`) are generated from it via `pnpm db:generate`, so schema changes are made to Prisma and regenerated rather than hand-editing Drizzle. Confidence: 0.7
- Commits require a BD issue reference via a `Refs: bd-<id>` trailer; expects the agent to create a BD issue (e.g. `bd create`) when none exists for the work and to stage the `.beads/issues.jsonl` tracking file with the commit. Confidence: 0.6
