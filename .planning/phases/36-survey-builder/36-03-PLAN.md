---
phase: 36-survey-builder
plan: 03
type: execute
wave: 3
depends_on:
  - 36-02
files_modified:
  - src/app/(tenant)/admin/surveys/[id]/edit/page.tsx
  - src/components/surveys/builder/SurveyEditor.tsx
  - src/components/surveys/builder/SurveyEditorHeader.tsx
  - src/components/surveys/builder/QuestionList.tsx
  - src/components/surveys/builder/QuestionBlock.tsx
  - src/components/surveys/builder/QuestionConfigPanel.tsx
  - src/components/surveys/builder/BlockPalette.tsx
  - src/components/surveys/builder/SectionBlock.tsx
  - src/components/surveys/builder/question-types/SingleChoiceBlock.tsx
  - src/components/surveys/builder/question-types/MultipleChoiceBlock.tsx
  - src/components/surveys/builder/question-types/TextBlock.tsx
  - src/components/surveys/builder/question-types/RatingBlock.tsx
  - src/components/surveys/builder/question-types/YesNoBlock.tsx
  - src/components/surveys/builder/question-types/LinearScaleBlock.tsx
  - src/app/(tenant)/admin/surveys/page.tsx
  - src/app/(tenant)/admin/surveys/[id]/page.tsx
autonomous: true
requirements:
  - SURVEY-BUILD-03
user_setup: []

must_haves:
  truths:
    - "Admin sees a Google Forms-like builder page at /admin/surveys/:id/edit"
    - "Admin can click a + button to see available question types and add one"
    - "Each of the 6 question types shows a live preview in the builder"
    - "Admin can click a question to see/configure its type-specific settings"
    - "Sections show as collapsible accordion headers with title and description"
    - "Admin can navigate from surveys list and survey detail to the edit page"
  artifacts:
    - path: "src/app/(tenant)/admin/surveys/[id]/edit/page.tsx"
      provides: "Builder route — loads survey data and renders SurveyEditor"
      min_lines: 20
    - path: "src/components/surveys/builder/SurveyEditor.tsx"
      provides: "Main editor component orchestrating question list, palette, sections"
      min_lines: 100
    - path: "src/components/surveys/builder/BlockPalette.tsx"
      provides: "Floating + button + question type picker overlay"
      min_lines: 50
    - path: "src/components/surveys/builder/question-types/SingleChoiceBlock.tsx"
      provides: "Radio/dropdown question preview + options config"
      min_lines: 30
  key_links:
    - from: "SurveyEditor.tsx"
      to: "/api/surveys/:id"
      via: "useEffect fetch on mount"
      pattern: "fetch.*api/surveys"
    - from: "BlockPalette.tsx"
      to: "POST /api/surveys/:id/questions"
      via: "onSelect handler"
      pattern: "createQuestion|POST.*questions"

---

<objective>
Build the visual survey builder — a Google Forms-like editing page where admins can see their survey, add questions of 6 types, configure them, and organize into sections.

Purpose: The core user-facing feature — the admin tool for creating rich surveys.
Output: Working builder page at /admin/surveys/:id/edit with all question types and section support.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-survey-builder/36-CONTEXT.md
@src/components/surveys/builder/  (will create)
</context>

<tasks>

<task type="auto">
  <name>Create builder page shell with data loading and block palette</name>
  <files>
    src/app/(tenant)/admin/surveys/[id]/edit/page.tsx
    src/components/surveys/builder/SurveyEditor.tsx
    src/components/surveys/builder/SurveyEditorHeader.tsx
    src/components/surveys/builder/BlockPalette.tsx
  </files>
  <action>
    Create the builder infrastructure:

    **1. `edit/page.tsx`** — 'use client' page component:
    - Extract `surveyId` from `useParams()`
    - Render `<SurveyEditor surveyId={surveyId} />`
    - Add breadcrumb navigation: Home → Admin → Surveys → Survey Title → Edit
    - Wrap in ErrorBoundary

    **2. `SurveyEditor.tsx`** — Main orchestrator component:

    On mount, fetch `GET /api/surveys/${surveyId}` → unwrap canonical response → store survey, questions, sections in state.

    Render:
    - `<SurveyEditorHeader />` at top
    - List of sections with their questions, rendered in order
    - Questions without a sectionId rendered as "ungrouped" at top
    - `<BlockPalette />` floating "+" button

    Provide `onAddQuestion`, `onUpdateQuestion`, `onDeleteQuestion` callbacks that call the API and update local state (optimistic update).

    **3. `SurveyEditorHeader.tsx`** — Header bar with:
    - Survey title (editable inline — PUT on blur)
    - Status badge (DRAFT/ACTIVE/CLOSED) with toggle?
    - "Survey Settings" button that opens config panel (metadata tags)
    - "Preview" button (opens survey in new tab)
    - "Back to results" link

    **4. `BlockPalette.tsx`** — Floating "+" button:
    - Fixed position button (bottom-right in the builder area, similar to Google Forms FAB)
    - On click, opens a popover/menu showing all 6 question types with icons and labels
    - Each type option shows: icon + name + brief description
    - On select, calls `onAddQuestion(type)` which POSTs to the API
    - Types listed: "Multiple Choice", "Checkbox", "Short Answer / Paragraph", "Rating", "Yes / No", "Linear Scale"
    - Closes after selection or on click outside

    All API calls must unwrap the canonical `{success, data}` envelope. Use consistent Tailwind styling — indigo-600 primary, gray backgrounds, rounded corners, shadows.
  </action>
  <verify>
    <automated>ls src/app/\(tenant\)/admin/surveys/\[id\]/edit/page.tsx && ls src/components/surveys/builder/SurveyEditor.tsx && ls src/components/surveys/builder/BlockPalette.tsx</automated>
    <manual>Navigate to /admin/surveys/:id/edit — page loads, shows survey title, floating + button visible</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Builder page loads survey data, displays header, shows floating + button that opens type palette</done>
</task>

<task type="auto">
  <name>Create QuestionBlock components for all 6 types</name>
  <files>
    src/components/surveys/builder/QuestionBlock.tsx
    src/components/surveys/builder/QuestionConfigPanel.tsx
    src/components/surveys/builder/question-types/SingleChoiceBlock.tsx
    src/components/surveys/builder/question-types/MultipleChoiceBlock.tsx
    src/components/surveys/builder/question-types/TextBlock.tsx
    src/components/surveys/builder/question-types/RatingBlock.tsx
    src/components/surveys/builder/question-types/YesNoBlock.tsx
    src/components/surveys/builder/question-types/LinearScaleBlock.tsx
  </files>
  <action>
    Create the question block components that render inside the builder:

    **1. `QuestionBlock.tsx`** — Container wrapper for each question in the builder:
    Renders a white card with:
    - Drag handle icon (left, 8 dots icon) — wired in Plan 04
    - Question type badge (top-right, colored pill)
    - Required toggle switch
    - Delete button (trash icon, top-right, with confirm)
    - Question text input (editable, updates on blur via PATCH)
    - Below: renders the appropriate type-specific block component based on `question.type`
    - On click/focus: shows the `<QuestionConfigPanel>` below the block
    - Collapsed by default — click to expand config

    **2. `QuestionConfigPanel.tsx`** — Config panel shown when a question is selected:
    - Renders the type-specific config UI from the question-type component
    - Shows `required` toggle
    - All changes call `onUpdateQuestion(q.id, changes)` PATCH on blur/change

    **3. Type-specific block components** — Each is a two-part component: preview + config panel.

    Each type component exports a `Preview` sub-component (shows what admin sees in the builder — a static representation) and a `ConfigPanel` sub-component (type-specific settings form).

    - **`SingleChoiceBlock.tsx`**: 
      - Preview: renders radio buttons with option labels. Shows dropdown variant if `config.displayAs === 'dropdown'`.
      - Config: options list editor (add/remove/reorder options), displayAs toggle (radio/dropdown)

    - **`MultipleChoiceBlock.tsx`**:
      - Preview: renders checkboxes with option labels
      - Config: options list editor (same pattern as SingleChoice)

    - **`TextBlock.tsx`**:
      - Preview: renders a text input (single line) or textarea (paragraph)
      - Config: charLimit slider/input, isParagraph toggle

    - **`RatingBlock.tsx`**:
      - Preview: renders star icons (filled yellow, outlined gray), shows current maxStars value
      - Config: maxStars input (1-10 range)

    - **`YesNoBlock.tsx`**:
      - Preview: renders two radio options "Yes" and "No"
      - Config: no type-specific config (just show "Yes/No question — no additional settings")

    - **`LinearScaleBlock.tsx`**:
      - Preview: renders clickable numbered bubbles from minValue to maxValue with labels below
      - Config: minValue (1-10), maxValue (1-10, must be > minValue), minLabel text, maxLabel text

    Option management pattern: use a local `useFieldArray`-style pattern with add/remove/reorder buttons for each option. Changes save on blur or on "Save" button click per option.

    All components use `@shared/ui` patterns where applicable. Import types from a shared `survey-types.ts` or define inline interfaces.
  </action>
  <verify>
    <automated>ls src/components/surveys/builder/question-types/SingleChoiceBlock.tsx && ls src/components/surveys/builder/question-types/LinearScaleBlock.tsx && ls src/components/surveys/builder/QuestionBlock.tsx</automated>
    <manual>Add multiple question types via the palette — each renders correctly in the builder, click to see config panel</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All 6 question types render with preview and config panels; options editable; changes save via PATCH API</done>
</task>

<task type="auto">
  <name>Create SectionBlock component and link from surveys list/detail pages</name>
  <files>
    src/components/surveys/builder/SectionBlock.tsx
    src/app/(tenant)/admin/surveys/page.tsx
    src/app/(tenant)/admin/surveys/[id]/page.tsx
  </files>
  <action>
    **1. `SectionBlock.tsx`** — Collapsible section component:
    Renders as a visual divider within the builder:
    - Section header bar with left accent border (indigo-400), drag handle, section title (editable input)
    - Expand/collapse chevron icon — toggles visibility of questions inside this section
    - Section description text area (below title, editable)
    - Section image placeholder (shows image if set, upload UI added in Plan 04)
    - "Add question to this section" button at the bottom of the section (adds question with sectionId)
    - Delete section button (with confirm — warns that questions will be ungrouped, not deleted)
    
    Sections are rendered by `SurveyEditor` in order. Each section wraps its questions (fetched with matching sectionId). A "Add Section" button exists between sections and at the bottom.

    **2. Link from surveys list page** — `admin/surveys/page.tsx`:
    - Add an "Edit" button/link in each survey row (beside the existing "View" link)
    - Links to `/admin/surveys/${survey.id}/edit`

    **3. Link from survey detail page** — `admin/surveys/[id]/page.tsx`:
    - The existing page shows survey results
    - Add an "Edit Survey" button in the header (when survey is DRAFT status)
    - Links to `/admin/surveys/${surveyId}/edit`
  </action>
  <verify>
    <automated>ls src/components/surveys/builder/SectionBlock.tsx</automated>
    <manual>Navigate from surveys list via "Edit" link → builder page loads. Section accordions collapse/expand.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Sections render as collapsible accordions in the builder; navigation links connect list → edit → builder</done>
</task>

</tasks>

<verification>
1. `/admin/surveys/:id/edit` loads with survey data from API
2. Floating + button opens palette with 6 question types
3. Selecting a type creates a question and shows it in the builder
4. Each question type's preview looks correct (radio, checkbox, text input, stars, yes/no, scale)
5. Clicking a question shows its config panel with type-specific settings
6. Sections render as accordion headers with collapsible question lists
7. "Edit" links on surveys list and detail page navigate to the builder
8. npx tsc --noEmit passes with no errors
</verification>

<success_criteria>
- Builder page loads and renders survey data
- Block palette lets admin add any of 6 question types
- Each question type has a working preview and config panel
- Questions can be edited, deleted, required-toggled
- Sections render as accordions with title, description, questions inside
- Admin can add sections and delete sections
- Navigation: surveys list → edit → builder works
- All state changes persist via API calls
</success_criteria>

<output>
After completion, create `.planning/phases/36-survey-builder/36-03-SUMMARY.md`
</output>
