---
phase: 25-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/platform/onboarding/route.ts
  - src/widgets/admin/ui/ModerationQueueWidget.tsx
  - src/widgets/admin/ui/AdminWidgetRenderer.tsx
  - src/widgets/admin/index.ts
autonomous: true
requirements: [INCEPT-01, GAP-09, GAP-12]
user_setup: []

must_haves:
  truths:
    - 'If tenant creation fails during onboarding, no user record is left in database'
    - 'If role assignment fails, neither user nor tenant record persists'
    - 'Only one moderation widget component is registered and used'
    - 'Approving/rejecting membership requests works correctly with surviving widget'
  artifacts:
    - path: 'src/app/api/platform/onboarding/route.ts'
      provides: 'Atomic onboarding transaction'
      contains: 'db.transaction'
    - path: 'src/widgets/admin/ui/GroupModerationWidget.tsx'
      provides: 'Surviving moderation widget'
      contains: 'APPROVED|REJECTED'
    - path: 'src/widgets/admin/ui/ModerationQueueWidget.tsx'
      provides: 'Deprecated widget (marked or removed)'
      contains: 'DEPRECATED'
  key_links:
    - from: 'src/app/api/platform/onboarding/route.ts'
      to: 'src/db/schema'
      via: 'db.transaction wrapping user+tenant+role creation'
      pattern: "db\\.transaction"
    - from: 'src/widgets/admin/ui/AdminWidgetRenderer.tsx'
      to: 'src/widgets/admin/ui/GroupModerationWidget.tsx'
      via: 'import statement'
      pattern: 'GroupModerationWidget'
---

<objective>
Wrap onboarding signup in database transaction and deduplicate moderation widgets.

Purpose: Prevent broken state where user exists but tenant creation failed, and eliminate code duplication from two moderation widget components.
Output: Atomic onboarding flow, single moderation widget
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/GAPS.md
@.planning/phases/20-self-service-inception/20-01-SUMMARY.md
@.planning/phases/24-dashboard-enhancement/24-02-SUMMARY.md
@src/app/api/platform/onboarding/route.ts
@src/widgets/admin/ui/GroupModerationWidget.tsx
@src/widgets/admin/ui/ModerationQueueWidget.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap onboarding flow in database transaction</name>
  <files>src/app/api/platform/onboarding/route.ts</files>
  <action>
    1. Read `src/app/api/platform/onboarding/route.ts` fully to understand the current flow.
    2. Identify the three operations that must be atomic:
       - Create User record
       - Create Tenant record with ownerId set to new user's id
       - Assign ADMIN role to the new user for the new tenant
    3. Wrap these operations in a Drizzle transaction:
       ```ts
       const result = await db.transaction(async (tx) => {
         // 1. Create user
         const [user] = await tx.insert(users).values({...}).returning();
         
         // 2. Create tenant with ownerId = user.id
         const [tenant] = await tx.insert(tenants).values({
           ...tenantData,
           ownerId: user.id,
         }).returning();
         
         // 3. Assign ADMIN role
         await tx.insert(userRoles).values({
           userId: user.id,
           tenantId: tenant.id,
           role: 'ADMIN',
         });
         
         return { user, tenant };
       });
       ```
    4. Add error handling: if the transaction fails, return a clear error message and do not leave partial state.
    5. Verify that `ownerId` on the Tenant is set within the same transaction.
    6. Check existing imports for `db` from `@api/db` or equivalent Drizzle client.
  </action>
  <verify>
    <automated>grep -n "db.transaction\|tx\." src/app/api/platform/onboarding/route.ts</automated>
    <manual>Verify: transaction wraps all three operations, error handling returns clear message</manual>
  </verify>
  <done>Onboarding route wraps user+tenant+role creation in db.transaction, fails atomically if any step errors</done>
</task>

<task type="auto">
  <name>Task 2: Deduplicate moderation widgets — keep GroupModerationWidget, deprecate ModerationQueueWidget</name>
  <files>src/widgets/admin/ui/ModerationQueueWidget.tsx, src/widgets/admin/ui/AdminWidgetRenderer.tsx, src/widgets/admin/index.ts</files>
  <action>
    1. Read both `ModerationQueueWidget.tsx` and `GroupModerationWidget.tsx` to confirm which is actively used.
    2. Run `grep -rn "ModerationQueueWidget\|GroupModerationWidget" src/` to find all imports.
    3. Keep `GroupModerationWidget.tsx` — it was created in Phase 24 and connects to `/api/groups/membership-requests`.
    4. Deprecate `ModerationQueueWidget.tsx`:
       - Add a comment at the top: `// DEPRECATED: use GroupModerationWidget instead. This file is kept for reference only.`
       - Remove it from `src/widgets/admin/index.ts` exports
       - Remove it from `src/widgets/admin/ui/AdminWidgetRenderer.tsx` imports and rendering logic
    5. Verify `GroupModerationWidget.tsx` renders PENDING membership requests with Approve/Reject actions that call `PATCH /api/groups/membership-requests/[id]`.
    6. Do NOT delete `ModerationQueueWidget.tsx` entirely — deprecate it with a comment so it can be cleaned up later if needed.
  </action>
  <verify>
    <automated>grep -rn "ModerationQueueWidget" src/ --include="*.ts" --include="*.tsx" | grep -v "DEPRECATED"</automated>
    <manual>Verify: only GroupModerationWidget is imported and used, ModerationQueueWidget has DEPRECATED comment</manual>
  </verify>
  <done>Only GroupModerationWidget is registered and used, ModerationQueueWidget is deprecated with clear comment</done>
</task>

</tasks>

<verification>
- Onboarding route uses db.transaction for atomic user+tenant+role creation
- Only one moderation widget is registered (GroupModerationWidget)
- ModerationQueueWidget is deprecated with clear comment
- TypeScript compiles without errors
</verification>

<success_criteria>

- Onboarding failure leaves no partial user/tenant records
- Admin dashboard shows only one moderation widget
- Approving/rejecting membership requests works via GroupModerationWidget
  </success_criteria>

<output>
After completion, create `.planning/phases/25-gap-closure/25-02-SUMMARY.md`
</output>
