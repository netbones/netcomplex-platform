---
phase: 25-gap-closure
plan: 03
type: execute
wave: 2
depends_on: ['25-01']
files_modified:
  - src/test/resources.test.ts
  - src/test/competitions.test.ts
  - src/test/platform-admin.test.ts
autonomous: true
requirements: [GAP-14]
user_setup: []

must_haves:
  truths:
    - 'Resource API tests verify tenantId scoping and visibility filtering'
    - 'Competition API tests verify public access returns only ACTIVE status'
    - 'Platform Admin API tests verify isPlatformAdmin guard returns 403 for non-admin'
    - 'All tests pass when run with vitest'
  artifacts:
    - path: 'src/test/resources.test.ts'
      provides: 'Resource API test suite'
      min_lines: 40
    - path: 'src/test/competitions.test.ts'
      provides: 'Competition API test suite'
      min_lines: 30
    - path: 'src/test/platform-admin.test.ts'
      provides: 'Platform Admin API test suite'
      min_lines: 30
  key_links:
    - from: 'src/test/resources.test.ts'
      to: 'src/app/api/resources/route.ts'
      via: 'mocked API route handlers'
      pattern: 'describe.*resources'
    - from: 'src/test/competitions.test.ts'
      to: 'src/app/api/competitions/route.ts'
      via: 'mocked API route handlers'
      pattern: 'describe.*competitions'
    - from: 'src/test/platform-admin.test.ts'
      to: 'src/app/api/admin/platform/tenants/route.ts'
      via: 'mocked API route handlers'
      pattern: 'describe.*platform.*admin'
---

<objective>
Create test suites for Resource, Competition, and Platform Admin API routes.

Purpose: Ensure new models and APIs have test coverage matching the patterns established in the existing test suite.
Output: Three test files with comprehensive coverage of tenant scoping, visibility, and auth guards
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/GAPS.md
@src/test/permissions.test.ts
@src/test/flags.test.ts
@src/test/setup.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Resource API test suite</name>
  <files>src/test/resources.test.ts</files>
  <action>
    1. Read `src/test/permissions.test.ts` and `src/test/flags.test.ts` to understand existing test patterns.
    2. Create `src/test/resources.test.ts` with the following test cases:
       - `GET /api/resources` returns only resources matching caller's visibility tier
       - `GET /api/resources` returns only resources for caller's tenantId
       - `POST /api/resources` sets tenantId from session, ignores tenantId in request body
       - `GET /api/resources/[id]` returns 403 for resource belonging to different tenant
       - `PATCH /api/resources/[id]` requires ADMIN or MANAGER role
       - `DELETE /api/resources/[id]` requires ADMIN role
    3. Use vitest mocking patterns consistent with existing tests.
    4. Mock the database layer (`@api/db`) and auth utilities (`getSessionAndRole`, `withTenant`).
    5. Include setup/teardown for test data if needed.
    6. Follow the existing test file structure: describe blocks for each endpoint, it blocks for each scenario.
  </action>
  <verify>
    <automated>npx vitest src/test/resources.test.ts --run</automated>
    <manual>Verify: all 6 test cases pass, follows existing test patterns</manual>
  </verify>
  <done>Resource test file created with 6 test cases covering visibility, tenant scoping, and role requirements</done>
</task>

<task type="auto">
  <name>Task 2: Create Competition and Platform Admin test suites</name>
  <files>src/test/competitions.test.ts, src/test/platform-admin.test.ts</files>
  <action>
    1. Create `src/test/competitions.test.ts` with:
       - Public `GET /api/competitions` only returns `status: 'ACTIVE'` competitions
       - Cross-tenant access returns 403
       - Admin can create, update, and delete competitions
       - Unauthenticated access to upcoming competitions returns 200 (public endpoint)
    2. Create `src/test/platform-admin.test.ts` with:
       - `GET /api/admin/platform/tenants` returns 403 for user with `isPlatformAdmin: false`
       - `GET /api/admin/platform/tenants` returns 200 for user with `isPlatformAdmin: true`
       - `POST /api/admin/platform/assist` creates AssistSession with correct expiry
       - `PATCH /api/admin/platform/assist/[id]` (revoke) sets `isActive: false`
       - Expired AssistSession is rejected with 403
    3. Use the same mocking patterns as Task 1 and existing tests.
    4. Ensure both files follow the existing test suite conventions.
  </action>
  <verify>
    <automated>npx vitest src/test/competitions.test.ts src/test/platform-admin.test.ts --run</automated>
    <manual>Verify: all test cases pass, follows existing test patterns</manual>
  </verify>
  <done>Competition test file (4+ cases) and Platform Admin test file (5+ cases) created and passing</done>
</task>

</tasks>

<verification>
- All three test files exist and pass with vitest
- Tests follow existing patterns from permissions.test.ts and flags.test.ts
- Coverage includes tenant scoping, visibility filtering, auth guards, and role requirements
- No test uses `any` type — all mocks are properly typed
</verification>

<success_criteria>

- `npx vitest src/test/resources.test.ts src/test/competitions.test.ts src/test/platform-admin.test.ts --run` passes with 0 failures
- Test coverage report shows resources, competitions, and platform-admin routes are covered
- Tests can run independently without requiring a live database
  </success_criteria>

<output>
After completion, create `.planning/phases/25-gap-closure/25-03-SUMMARY.md`
</output>
