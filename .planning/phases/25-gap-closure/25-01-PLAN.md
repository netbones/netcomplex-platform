---
phase: 25-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/admin/platform/tenants/route.ts
  - src/app/api/admin/platform/tenants/[id]/route.ts
  - src/shared/ui/MobileMenu.tsx
  - src/entities/tenant/api/permissions.ts
autonomous: true
requirements: [SCHEMA-04, GAP-03, GAP-04]
user_setup: []

must_haves:
  truths:
    - 'Unauthenticated requests to /api/admin/platform/tenants/* receive 401'
    - 'Non-platform-admin requests to /api/admin/platform/tenants/* receive 403'
    - 'Platform admin can list, create, update, and delete tenants'
    - 'Mobile menu shows admin link for users with role ADMIN (case-insensitive match)'
  artifacts:
    - path: 'src/app/api/admin/platform/tenants/route.ts'
      provides: 'Auth guard for tenant list/create'
      contains: 'isPlatformAdmin'
    - path: 'src/app/api/admin/platform/tenants/[id]/route.ts'
      provides: 'Auth guard for tenant read/update/delete'
      contains: 'isPlatformAdmin'
    - path: 'src/shared/ui/MobileMenu.tsx'
      provides: 'Role comparison using isAdmin helper'
      contains: 'isAdmin'
    - path: 'src/entities/tenant/api/permissions.ts'
      provides: 'requirePlatformAdmin helper (if created)'
      exports: ['requirePlatformAdmin', 'isAdmin']
  key_links:
    - from: 'src/app/api/admin/platform/tenants/route.ts'
      to: 'src/entities/tenant/api/permissions.ts'
      via: 'requirePlatformAdmin or isPlatformAdmin check'
      pattern: 'isPlatformAdmin|requirePlatformAdmin'
    - from: 'src/shared/ui/MobileMenu.tsx'
      to: 'src/entities/tenant/api/permissions.ts'
      via: 'isAdmin helper import'
      pattern: 'import.*isAdmin'
---

<objective>
Add missing platform admin auth guards to tenant CRUD routes and fix MobileMenu role case-sensitivity bug.

Purpose: Close critical security gap where tenant API routes are completely unguarded, and fix UI bug where admin users don't see admin links in mobile menu.
Output: Secured platform admin routes, corrected mobile menu role comparison
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/GAPS.md
@.planning/phases/19-schema-corrections/19-03-SUMMARY.md
@src/app/api/admin/platform/assist/route.ts
@src/shared/ui/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isPlatformAdmin guards to platform tenant routes</name>
  <files>src/app/api/admin/platform/tenants/route.ts, src/app/api/admin/platform/tenants/[id]/route.ts, src/entities/tenant/api/permissions.ts</files>
  <action>
    1. Read `src/app/api/admin/platform/assist/route.ts` to see the existing `isPlatformAdmin` guard pattern used there.
    2. In `src/app/api/admin/platform/tenants/route.ts`:
       - Add session check at the top of GET and POST handlers
       - Query user table for `isPlatformAdmin` flag using the session userId
       - Return `403` with `{ error: 'Forbidden' }` if `!user.isPlatformAdmin`
       - Return `401` if no session exists
    3. In `src/app/api/admin/platform/tenants/[id]/route.ts`:
       - Add the same `isPlatformAdmin` guard to GET, PATCH, and DELETE handlers
       - Guard must come before any database query
    4. If a `requirePlatformAdmin()` helper doesn't exist in `src/entities/tenant/api/permissions.ts`, create it following the pattern of existing permission helpers. It should:
       - Accept the session/request
       - Query user table for `isPlatformAdmin`
       - Return `{ error: 'Forbidden', status: 403 }` if not platform admin
       - Return `{ error: 'Unauthorized', status: 401 }` if no session
    5. Use the helper in both tenant route files to avoid duplication.

    Reference the assist route pattern:
    ```ts
    const user = await db.select({ isPlatformAdmin: users.isPlatformAdmin })
      .from(users)
      .where(eq(users.id, session.userId));
    if (!user[0]?.isPlatformAdmin) {
      return NextResponse.json({ error: 'Forbidden' }, { status: 403 });
    }
    ```

  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/admin/platform/tenants</automated>
    <manual>Verify: returns 401 without auth, 403 with non-platform-admin user, 200 with platform admin</manual>
  </verify>
  <done>Both tenant route files have isPlatformAdmin guards at the top of every handler, returning 401/403 appropriately</done>
</task>

<task type="auto">
  <name>Task 2: Fix MobileMenu role case-sensitivity bug</name>
  <files>src/shared/ui/MobileMenu.tsx</files>
  <action>
    1. Read `src/shared/ui/Header.tsx` line 9 and 86 to see how it imports and uses `isAdmin()` helper.
    2. In `src/shared/ui/MobileMenu.tsx`:
       - Add import: `import { isAdmin } from '@entities/tenant/api/permissions';`
       - Replace line 26 `const isAdmin = session?.user?.role === 'admin';` with `const isAdminUser = isAdmin(session?.user?.role);`
       - Update all references from `isAdmin` to `isAdminUser` to avoid naming collision with the imported helper
       - Verify line 96 `{(isAdmin || isBoard) && (` uses the correct variable name
    3. Do NOT change the Role enum values. Fix the comparison only.
  </action>
  <verify>
    <automated>grep -n "isAdmin\|'admin'" src/shared/ui/MobileMenu.tsx</automated>
    <manual>Verify: no lowercase 'admin' string comparison, uses isAdmin() helper instead</manual>
  </verify>
  <done>MobileMenu uses isAdmin() helper from permissions.ts, no lowercase 'admin' string comparisons remain</done>
</task>

</tasks>

<verification>
- Both tenant route files have isPlatformAdmin guards at the top of every handler
- MobileMenu uses isAdmin() helper, no lowercase role comparisons
- TypeScript compiles without errors
- No new files created except possibly requirePlatformAdmin helper in permissions.ts
</verification>

<success_criteria>

- Unauthenticated request to GET /api/admin/platform/tenants returns 401
- Authenticated non-platform-admin request returns 403
- Platform admin request returns 200 with tenant list
- Mobile menu shows admin navigation for users with role ADMIN
  </success_criteria>

<output>
After completion, create `.planning/phases/25-gap-closure/25-01-SUMMARY.md`
</output>
