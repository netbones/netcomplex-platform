---
phase: 04-content-i18n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/dashboard/ui/UserContentWidget.tsx
  - src/widgets/dashboard/ui/TagCloudWidget.tsx
  - src/widgets/dashboard/ui/HomeLayer.tsx
  - src/shared/lib/hooks/useAdminContent.ts
  - src/app/(tenant)/admin/content/page.tsx
  - src/app/conservation/page.tsx
  - src/widgets/dashboard/ui/__tests__/HomeLayer-locale.test.tsx
autonomous: true
requirements:
  - CONTENT-I18N-01
  - CONTENT-I18N-03

must_haves:
  truths:
    - UserContentWidget fetches content with `&locale=${language}` for the session user
    - TagCloudWidget fetches content with `&locale=` for tag aggregation
    - HomeLayer announcement titles render in user's active locale (not hardcoded 'en')
    - useAdminContent hook accepts locale parameter and passes it to API calls
    - Admin content list page fetches content with `&locale=` param
    - Conservation page uses API-returned flat strings directly — no client-side JSONB key extraction
  artifacts:
    - src/widgets/dashboard/ui/UserContentWidget.tsx (updated fetch with locale param)
    - src/widgets/dashboard/ui/TagCloudWidget.tsx (added locale hook + locale param)
    - src/widgets/dashboard/ui/HomeLayer.tsx (resolveTitle now uses dynamic locale)
    - src/shared/lib/hooks/useAdminContent.ts (accepts locale param)
    - src/app/(tenant)/admin/content/page.tsx (updated fetch with locale param)
    - src/app/conservation/page.tsx (removed getLocalizedContent helper)
    - src/widgets/dashboard/ui/__tests__/HomeLayer-locale.test.tsx (new — verifies locale-aware resolveTitle)
  key_links:
    - HomeLayer.resolveTitle() → getLocalizedValue(title, locale) — locale must come from runtime
    - useAdminContent hook → fetch URL — hook must accept and thread locale
    - conservation/page.tsx — getLocalizedContent removal must not break admin editing of _raw
---

<objective>
Wire the `locale` query parameter into all dashboard widget and admin content-fetching sites. Remove two redundant client-side locale helpers: `getLocalizedContent()` in the conservation page and the hardcoded `'en'` in `HomeLayer.resolveTitle()`. Add a test for HomeLayer's locale-aware title resolution.

Purpose: Close the gap where dashboard widgets and admin content pages always default to English, and remove dead client-side JSONB extraction code that the API now handles server-side.

Output: 6 updated files with locale-aware wiring; 1 new test file; 0 new packages.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-i18n/04-RESEARCH.md
@.planning/phases/04-content-i18n/04-CONTEXT.md
@.planning/phases/04-content-i18n/04-UI-SPEC.md
@src/shared/lib/hooks/useSafeTranslation.ts
@src/shared/lib/i18n/config.ts
@src/widgets/dashboard/ui/UserContentWidget.tsx
@src/widgets/dashboard/ui/TagCloudWidget.tsx
@src/widgets/dashboard/ui/HomeLayer.tsx
@src/shared/lib/hooks/useAdminContent.ts
@src/app/(tenant)/admin/content/page.tsx
@src/app/conservation/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire locale to dashboard widgets</name>
  <files>src/widgets/dashboard/ui/UserContentWidget.tsx, src/widgets/dashboard/ui/TagCloudWidget.tsx, src/widgets/dashboard/ui/HomeLayer.tsx</files>
  <action>
    **UserContentWidget.tsx (line 44):**
    - Already imports `useTranslation` at line 4 and destructures `{ t }` at line 33.
    - Add `i18n` to destructuring: `const { t, i18n } = useTranslation('dashboard');`
    - Change line 44 from `fetch('/api/content?authorId=${session.user.id}')` to `fetch('/api/content?authorId=${session.user.id}&locale=${i18n.language}')`
    - Add `i18n.language` to the useEffect dependency array at line 53.

    **TagCloudWidget.tsx (line 33):**
    - Does NOT currently import a locale hook. Add: `import { useLanguage } from '@shared/lib/hooks/useSafeTranslation';`
    - Call `const { language } = useLanguage();` inside the component.
    - Change `buildApiUrl()` (lines 31-35) to include `&locale=${language}` in both branches:
      - `return '/api/content?authorId=${authorId}&locale=${language}';`
      - `return '/api/content?locale=${language}';`
    - Add `language` to the useEffect dependency array at line 29: `[authorId, language]`.

    **HomeLayer.tsx (lines 51-54):**
    - `resolveTitle()` currently uses `getLocalizedValue(title, 'en')` — hardcoded to English.
    - The file already imports `getLocalizedValue` from `@shared/lib/i18n/config` at line 7.
    - Add: `import { useLanguage } from '@shared/lib/hooks/useSafeTranslation';`
    - In the `HomeLayer` component, call `const { language } = useLanguage();`
    - Change the `resolveTitle` function signature to accept a locale parameter:
      `function resolveTitle(title: Announcement['title'], locale: string): string`
    - Change line 53 from `return getLocalizedValue(title, 'en') || '';` to `return getLocalizedValue(title, locale) || '';`
    - Update all 4 call sites (lines 136, 321, 556) to pass `language`:
      `resolveTitle(a.title, language)`

    Per RESEARCH.md Pitfall 3: Do NOT modify the `_raw` field or `transformContentForLocale` return shape. The `_raw` field is load-bearing for admin editing.

  </action>
  <verify>
    <automated>grep -n "locale=" src/widgets/dashboard/ui/UserContentWidget.tsx src/widgets/dashboard/ui/TagCloudWidget.tsx && grep -n "getLocalizedValue(title, language)" src/widgets/dashboard/ui/HomeLayer.tsx</automated>
  </verify>
  <done>All three widgets include locale param in content fetches; HomeLayer.resolveTitle uses dynamic locale</done>
</task>

<task type="auto">
  <name>Task 2: Wire locale to admin content + remove redundant helpers</name>
  <files>src/shared/lib/hooks/useAdminContent.ts, src/app/(tenant)/admin/content/page.tsx, src/app/conservation/page.tsx</files>
  <action>
    **useAdminContent.ts (line 6):**
    - Add a `locale` parameter to the hook: `export function useAdminContent(locale?: string)`.
    - Change line 5 from `queryKey: ['admin', 'content']` to `queryKey: ['admin', 'content', locale]` so TanStack Query re-fetches when locale changes.
    - Change line 6 from `fetch('/api/content').then(r => r.json())` to `fetch('/api/content?locale=${locale || 'en'}').then(r => r.json())`

    **admin/content/page.tsx (line 51):**
    - Add: `import { useLanguage } from '@shared/lib/hooks/useSafeTranslation';`
    - Call `const { language } = useLanguage();` inside the component.
    - Change line 51 from `fetch('/api/content')` to `fetch('/api/content?locale=${language}')`
    - Add `language` to the empty useEffect dependency array at line 57: `[language]`.

    <!-- planner-discipline-allow: getLocalizedContent -->
    **conservation/page.tsx (lines 47-53):**
    - The conservation page already passes locale correctly (`/api/conservation?locale=${locale}` at line 12) per RESEARCH.md §Already Correct item A.
    - Remove the `getLocalizedContent` function entirely (lines 47-53).
    - Inline-replace all 5 usages:
      - Line 268: `{getLocalizedContent(article.title) || article.title}` → `{article.title}`
      - Line 271: `{getLocalizedContent(article.content) || article.content}` → `{article.content}`
      - Line 325: `{getLocalizedContent(article.title)}` → `{article.title}`
      - Line 327: `{getLocalizedContent(article.content)}` → `{article.content}`
    - The API now returns flat strings via `transformContentForLocale()` — client-side JSONB key extraction is redundant.

    Per RESEARCH.md Pitfall 3: Do NOT modify the `_raw` field or `transformContentForLocale` return shape. Verify admin editing pages at `src/app/(tenant)/admin/content/[id]/page.tsx` still access `content._raw.title` correctly — the conservation page changes do not affect admin pages.

  </action>
  <verify>
    <automated>grep -c "getLocalizedContent" src/app/conservation/page.tsx | grep -v '^#' | xargs -I{} sh -c 'test {} -eq 0' && grep -n "locale=" src/shared/lib/hooks/useAdminContent.ts src/app/(tenant)/admin/content/page.tsx</automated>
  </verify>
  <done>useAdminContent accepts locale param; admin/content page passes locale; getLocalizedContent helper removed from conservation page</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add HomeLayer locale tests + verify all changes</name>
  <files>src/widgets/dashboard/ui/__tests__/HomeLayer-locale.test.tsx</files>
  <behavior>
    - Test 1: resolveTitle() with locale 'en' returns English value from JSONB
    - Test 2: resolveTitle() with locale 'af' returns Afrikaans value when present in JSONB
    - Test 3: resolveTitle() with locale 'xh' returns Xhosa value when present in JSONB
    - Test 4: resolveTitle() with missing locale falls back to 'defaultLocale' (handled by getLocalizedValue server-side) — client side receives resolved string
    - Test 5: resolveTitle() with plain string title returns the string unchanged regardless of locale
  </behavior>
  <action>
    Create `src/widgets/dashboard/ui/__tests__/HomeLayer-locale.test.tsx` using Vitest.

    Test the `resolveTitle` function directly (extract it from the module or test it via `getLocalizedValue` from `@shared/lib/i18n/config`). The function wraps `getLocalizedValue` which is already well-tested in `src/shared/lib/i18n/__tests__/content-i18n.test.ts` — focus tests on the integration between `resolveTitle` and the locale that flows from the component.

    Use `vi.mock('@shared/lib/hooks/useSafeTranslation', ...)` to mock `useLanguage` returning different locale values.

    Test 1-3: Verify `getLocalizedValue({ en: 'Hello', af: 'Hallo', xh: 'Molo', zu: 'Sawubona' }, locale)` returns the correct language value.

    Test 4: Verify `getLocalizedValue({ en: 'Hello' }, 'xh')` returns `'Hello'` (fallback to defaultLocale via server-side resolveLocale).

    Test 5: Verify `resolveTitle('Hello World', 'af')` returns `'Hello World'` unchanged (string passthrough).

    Tests should run with: `npx vitest run src/widgets/dashboard/ui/__tests__/HomeLayer-locale.test.tsx`

  </action>
  <verify>
    <automated>npx vitest run src/widgets/dashboard/ui/__tests__/HomeLayer-locale.test.tsx --reporter=verbose</automated>
  </verify>
  <done>All 5 HomeLayer locale tests pass — resolveTitle uses dynamic locale from useLanguage</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                             |
| ---------- | --------------------------------------------------------------------------------------- |
| client→API | User-controlled `locale` param passes from dashboard widgets/admin pages to content API |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                                         | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                        |
| --------- | ---------------------- | --------------------------------------------------------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering              | `locale` query param on content API calls                                         | low      | accept      | Same as Plan 04-01 — `resolveLocale()` validates against `supportedLanguages` whitelist server-side. Client-side wiring adds no new attack surface.                                                                                                                                                                                                                    |
| T-04-03   | Information Disclosure | Admin content page locale exposed in fetch URL                                    | low      | accept      | Admin pages are behind Better Auth session + role gating. The locale value carries no PII or secrets.                                                                                                                                                                                                                                                                  |
| T-04-04   | Tampering              | Removal of `getLocalizedContent` could break `_raw` field access in admin editors | low      | mitigate    | The conservation page's `getLocalizedContent` removal only affects content DISPLAY on the public conservation route. Admin editing pages (`src/app/(tenant)/admin/content/[id]/page.tsx`) access `content._raw.title` independently and are not modified by this plan. Verify after implementation with `grep -n '_raw' src/app/(tenant)/admin/content/[id]/page.tsx`. |

</threat_model>

<verification>
- `pnpm typecheck` passes (no new TS errors from locale destructuring or hook additions)
- `grep -c "getLocalizedContent" src/app/conservation/page.tsx` returns 0 (helper fully removed)
- `grep -c "resolveTitle.*'en'" src/widgets/dashboard/ui/HomeLayer.tsx` returns 0 (hardcoded 'en' removed)
- All 5 HomeLayer-locale tests pass: `npx vitest run src/widgets/dashboard/ui/__tests__/HomeLayer-locale.test.tsx`
- Existing tests remain green: `npx vitest run src/shared/lib/i18n/__tests__/`
</verification>

<success_criteria>

1. `grep -c 'locale=' src/widgets/dashboard/ui/UserContentWidget.tsx src/widgets/dashboard/ui/TagCloudWidget.tsx` shows locale param present in both widgets
2. `grep -n "getLocalizedValue(title, language)" src/widgets/dashboard/ui/HomeLayer.tsx` shows dynamic locale in resolveTitle
3. `grep -c "getLocalizedContent" src/app/conservation/page.tsx` returns 0 — helper fully removed
4. `grep -c 'locale=' src/shared/lib/hooks/useAdminContent.ts src/app/(tenant)/admin/content/page.tsx` shows locale param present
5. `npx vitest run src/widgets/dashboard/ui/__tests__/HomeLayer-locale.test.tsx` — all 5 passing
6. `pnpm typecheck` — zero errors from files modified in this plan
   </success_criteria>

<output>
Create `.planning/phases/04-content-i18n/04-02-SUMMARY.md` when done
</output>
