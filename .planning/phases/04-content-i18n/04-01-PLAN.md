---
phase: 04-content-i18n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/news/page.tsx
  - src/app/news/[id]/page.tsx
  - src/page-modules/service/ui/ServicesPage.tsx
  - src/app/resident/[id]/page.tsx
  - src/app/news/__tests__/news-locale.test.tsx
autonomous: true
requirements:
  - CONTENT-I18N-01
  - CONTENT-I18N-02

must_haves:
  truths:
    - News listing page fetches content with `&locale=${i18n.language}` and displays locale-resolved titles/excerpts
    - News detail page fetches content with `&locale=${i18n.language}` and displays locale-resolved content
    - ServicesPage fetches content with `&locale=${language}` and displays locale-resolved service listings
    - Resident profile page fetches content with `&locale=${language}` for tag cloud content
    - When user switches locale via LanguageSwitcher, content re-fetches in the new language
  artifacts:
    - src/app/news/page.tsx (updated fetch with locale param)
    - src/app/news/[id]/page.tsx (updated fetch with locale param)
    - src/page-modules/service/ui/ServicesPage.tsx (updated fetch with locale param)
    - src/app/resident/[id]/page.tsx (updated fetch with locale param)
    - src/app/news/__tests__/news-locale.test.tsx (new — verifies locale propagation)
  key_links:
    - i18n.language → fetch URL — locale must be threaded from i18next runtime
    - useEffect dependency array — must include language to trigger re-fetch on locale switch
    - API response shape — flat strings from transformContentForLocale() replace JSONB objects
---

<objective>
Wire the `locale` query parameter into all public-facing content-fetching pages so content renders in the user's active language. Add tests verifying locale propagation end-to-end.

Purpose: Close the gap where content cards and detail pages always default to English because they omit `?locale=` from API calls, even though the API already supports `transformContentForLocale()`.

Output: 4 updated page files with locale-aware fetch URLs; 1 new test file verifying locale propagation.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-i18n/04-RESEARCH.md
@.planning/phases/04-content-i18n/04-CONTEXT.md
@.planning/phases/04-content-i18n/04-UI-SPEC.md
@src/shared/lib/hooks/useSafeTranslation.ts
@src/app/news/page.tsx
@src/app/news/[id]/page.tsx
@src/page-modules/service/ui/ServicesPage.tsx
@src/app/resident/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire locale to news listing page</name>
  <files>src/app/news/page.tsx</files>
  <action>
    In `src/app/news/page.tsx`, destructure `i18n` from the existing `useTranslation` call at line 31. Then modify the fetch URL at line 51 from:
    `fetch('/api/content?published=true${categoryParam}')`
    to:
    `fetch('/api/content?published=true&locale=${i18n.language}${categoryParam}')`

    Add `i18n.language` to the useEffect dependency array at line 46 so content re-fetches on locale switch:
    Change `[selectedCategory]` to `[selectedCategory, i18n.language]`.

    Per CONTEXT.md pattern: use `i18n.language` from the existing `useTranslation()` call — no new imports needed.

    Do NOT change the component's visual appearance or add new UI elements. The ContentItem interface and rendering logic remain unchanged since the API now returns flat `title`/`content`/`excerpt` strings.

  </action>
  <verify>
    <automated>grep -n "locale=\${i18n.language}" src/app/news/page.tsx</automated>
  </verify>
  <done>news/page.tsx fetch URL includes `&locale=${i18n.language}` and useEffect re-fetches when `i18n.language` changes</done>
</task>

<task type="auto">
  <name>Task 2: Wire locale to news detail, services, and resident pages</name>
  <files>src/app/news/[id]/page.tsx, src/page-modules/service/ui/ServicesPage.tsx, src/app/resident/[id]/page.tsx</files>
  <action>
    **news/[id]/page.tsx (line 89):**
    - Already imports `useTranslation` at line 4 and destructures `{ t }` at line 64.
    - Add `i18n` to the destructuring: `const { t, i18n } = useTranslation(['common', 'news']);`
    - Change line 89 from `fetch('/api/content/${id}?published=true')` to `fetch('/api/content/${id}?published=true&locale=${i18n.language}')`

    **ServicesPage.tsx (line 100):**
    - Already imports `useTranslation` at line 4 and destructures `{ t }` at line 79.
    - Add `i18n` to destructuring at line 79: `const { t, i18n } = useTranslation('services');`
    - Change line 100 from `fetch('/api/content?category=SERVICES&published=true')` to `fetch('/api/content?category=SERVICES&published=true&locale=${i18n.language}')`
    - Per RESEARCH.md pattern: use `i18n.language` from existing `useTranslation` call.

    **resident/[id]/page.tsx (line 68):**
    - Already imports `useSafeTranslation` at line 5 which returns `language`.
    - The main component destructures `{ tx: txCommon }` at line 207. Add `language` to destructuring: `const { tx: txCommon, language } = useSafeTranslation('common');`
    - The content fetch is inside `TagCloudWidgetForUser` (line 68). This inner component does NOT have direct access to `language`. Pass `language` as a prop from the parent component. Add a `locale: string` prop to `TagCloudWidgetForUser`, wire it in the parent, and change line 68 from `fetch('/api/content?authorId=${userId}&published=true')` to `fetch('/api/content?authorId=${userId}&published=true&locale=${locale}')`

    For each page with a useEffect fetching content, add the language/i18n.language value to the dependency array so the content re-fetches when the user switches locale.

  </action>
  <verify>
    <automated>grep -n "locale=" src/app/news/[id]/page.tsx src/page-modules/service/ui/ServicesPage.tsx src/app/resident/[id]/page.tsx</automated>
  </verify>
  <done>All three pages include `locale` param in content-fetch URLs and re-fetch on locale change</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add news locale propagation tests + verify all pages</name>
  <files>src/app/news/__tests__/news-locale.test.tsx</files>
  <behavior>
    - Test 1: news/page.tsx fetch URL includes `&locale=en` when i18n.language is 'en'
    - Test 2: news/page.tsx fetch URL includes `&locale=af` when i18n.language is 'af'
    - Test 3: news/page.tsx re-fetches content when language changes (useEffect dependency)
    - Test 4: news/[id]/page.tsx fetches with `&locale=` query param
  </behavior>
  <action>
    Create `src/app/news/__tests__/news-locale.test.tsx` using Vitest + React Testing Library.

    Mock the global `fetch` to capture the URL string. Mock `useTranslation` from `react-i18next` to return `{ t: vi.fn(), i18n: { language: 'en' } }`.

    Test 1-2: Render `<NewsPage />` (or the fetch function), verify the URL passed to `fetch` contains `locale=en` or `locale=af`.

    Test 3: Re-render with `language: 'af'`, verify fetch is called with `locale=af`.

    Test 4: Render news detail page, verify fetch URL includes `locale=`.

    Use `vi.mock('react-i18next', ...)` to control the locale value. Tests should run with: `npx vitest run src/app/news/__tests__/news-locale.test.tsx`

  </action>
  <verify>
    <automated>npx vitest run src/app/news/__tests__/news-locale.test.tsx --reporter=verbose</automated>
  </verify>
  <done>All 4 tests pass — locale param propagation verified for news pages</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                       |
| ---------- | ----------------------------------------------------------------- |
| client→API | User-controlled `locale` param passes from browser to content API |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                                                              |
| --------- | ---------------------- | ----------------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-04-01   | Tampering              | `locale` query param on content API calls | low      | accept      | `resolveLocale()` in `src/entities/content/services/index.ts` validates against `supportedLanguages` whitelist server-side — any injected value falls back to `'en'`. The client-side wiring adds no new attack surface beyond what the API already handles. |
| T-04-02   | Information Disclosure | `i18n.language` exposed in fetch URL      | low      | accept      | The locale value is already visible in the LanguageSwitcher UI, cookies, and `x-locale` header. Adding it to a GET query parameter exposes no new information.                                                                                               |

</threat_model>

<verification>
- `pnpm typecheck` passes (no new TS errors from locale destructuring additions)
- All 4 news-locale tests pass (`npx vitest run src/app/news/__tests__/news-locale.test.tsx`)
- Existing tests remain green: `npx vitest run src/shared/lib/i18n/__tests__/ src/entities/content/__tests__/`
</verification>

<success_criteria>

1. `grep -c 'locale=' src/app/news/page.tsx src/app/news/[id]/page.tsx` shows locale param present in both news pages
2. `grep -c 'locale=' src/page-modules/service/ui/ServicesPage.tsx` shows locale param present
3. `grep -c 'locale=' src/app/resident/[id]/page.tsx` shows locale param present
4. `npx vitest run src/app/news/__tests__/news-locale.test.tsx` — all passing
5. `pnpm typecheck` — zero errors from files modified in this plan
   </success_criteria>

<output>
Create `.planning/phases/04-content-i18n/04-01-SUMMARY.md` when done
</output>
