---
phase: 102-achievements-system
plan: 03
type: execute
wave: 3
depends_on:
  - 102-02
files_modified:
  - src/widgets/dashboard/ui/AchievementsWidget.tsx
  - src/widgets/dashboard/ui/AdminAchievementsWidget.tsx
  - src/widgets/dashboard/model/widgets.ts
  - src/entities/directory/ui/AchievementBadgeGrid.tsx
  - src/entities/directory/ui/UnifiedResidentCard.tsx
autonomous: true
requirements:
  - ADVISORY-013-D
must_haves:
  truths:
    - 'Dashboard widget shows badge grid — locked (gray/muted) and unlocked (colored with icon)'
    - 'Hover tooltip shows achievement name + unlock date'
    - 'Widget registered in widgets.ts with home space assignment'
    - 'Directory profile has separate Achievements section'
    - 'Admin widget mirrors PageSettingsWidget pattern for catalog management'
  artifacts:
    - path: 'src/widgets/dashboard/ui/AchievementsWidget.tsx'
      provides: 'Badge grid widget — locked/unlocked states with tooltip'
    - path: 'src/widgets/dashboard/ui/AdminAchievementsWidget.tsx'
      provides: 'Admin catalog management widget (enable/disable, threshold override)'
    - path: 'src/entities/directory/ui/AchievementBadgeGrid.tsx'
      provides: 'Reusable badge grid component for profile display'
    - path: 'src/widgets/dashboard/model/widgets.ts'
      provides: "Achievements widget registration (id: 'achievements', spaces: ['home'])"
  key_links:
    - from: 'src/widgets/dashboard/model/widgets.ts'
      to: 'src/widgets/dashboard/ui/AchievementsWidget.tsx'
      via: 'lazy import in registry.register()'
      pattern: "import\\(.*AchievementsWidget"
    - from: 'src/widgets/dashboard/ui/AchievementsWidget.tsx'
      to: '/api/achievements'
      via: 'fetch achievements data'
      pattern: 'fetch.*api/achievements'
    - from: 'src/entities/directory/ui/UnifiedResidentCard.tsx'
      to: 'src/entities/directory/ui/AchievementBadgeGrid.tsx'
      via: 'import and render in profile section'
      pattern: 'AchievementBadgeGrid'
---

<objective>
Build the dashboard badge grid widget (resident + admin variants), register it in the widget system, and add an achievements section to the directory profile view.

Purpose: Makes achievements visible and aspirational. Residents see their trophy case on the home dashboard; admins manage the catalog; directory profiles showcase unlocked achievements.
Output: 3 widget files + 1 shared component + widget registration.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-achievements-system/102-CONTEXT.md
@.planning/phases/102-achievements-system/102-RESEARCH.md
@.planning/phases/102-achievements-system/102-02-SUMMARY.md
@src/widgets/dashboard/model/widgets.ts
@src/widgets/dashboard/ui/WidgetCard.tsx
@src/entities/directory/ui/UnifiedResidentCard.tsx
@src/widgets/dashboard/ui/NotificationsWidget.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: AchievementBadgeGrid component + AchievementsWidget + widget registration</name>
  <files>src/entities/directory/ui/AchievementBadgeGrid.tsx, src/widgets/dashboard/ui/AchievementsWidget.tsx, src/widgets/dashboard/model/widgets.ts</files>
  <read_first>
    - src/widgets/dashboard/model/widgets.ts (registration pattern: lazy import, manifest fields, spaces assignment, lines 58-73 for example)
    - src/widgets/dashboard/ui/NotificationsWidget.tsx (existing widget pattern for data fetching + loading state)
    - src/entities/directory/ui/UnifiedResidentCard.tsx (profile structure — where to add achievements section)
    - src/app/api/achievements/route.ts (API response shape from Plan 02)
    - src/widgets/dashboard/ui/WidgetCard.tsx (WidgetCard wrapper pattern)
  </read_first>
  <action>
Create 3 files:

**src/entities/directory/ui/AchievementBadgeGrid.tsx — Shared badge grid component:**

- Reusable component accepting `achievements: Array<{ key, label, icon?, category, unlocked, unlockedAt? }>`
- Renders responsive grid of achievement badges (CSS grid, 3-4 columns on desktop, 2 on mobile)
- Unlocked: colored background with icon + label, hover tooltip shows "Unlocked: {date}"
- Locked: gray/muted background with lock icon, hover tooltip shows achievement name (aspirational pull per D-07)
- Uses `Trophy`, `Award`, `Star`, `Lock` icons from lucide-react (already installed)
- Categories rendered as section headers: ENGAGEMENT, CONTRIBUTION, MILESTONE
- Pure presentational component — no data fetching

**src/widgets/dashboard/ui/AchievementsWidget.tsx — Dashboard widget:**

- Fetches from `/api/achievements` using fetch (matches existing widget patterns)
- Loading state: skeleton grid of 6 placeholder badges
- Error state: muted error message (non-critical widget)
- Renders `AchievementBadgeGrid` with the fetched data
- Widget header: "Achievements" with Trophy icon, count of unlocked / total
- Default size: { width: 3, height: 2 }, minSize: { width: 2, height: 1 } per D-07

**src/widgets/dashboard/model/widgets.ts — Registration:**

- Add registration block for `achievements` widget after the existing core widgets section
- id: `'achievements'`, version: `'1.0.0'`, name: `'Achievements'`, description: `'Your achievement badges and progress'`
- author: `'internal'`, category: `'content'`, icon: `Trophy` (already imported in widgets.ts)
- component: `lazy(() => import('../ui/AchievementsWidget').then(m => ({ default: m.AchievementsWidget })))`
- defaultSize: `{ width: 3, height: 2 }`, minSize: `{ width: 2, height: 1 }`
- spaces: `['home']` per D-09
- No `permissions` field — available to all roles per D-13 (always-on, not gated)
- No `featureFlag` field — not feature-flagged per D-13
  </action>
  <verify>
  <automated>npx tsc --noEmit src/entities/directory/ui/AchievementBadgeGrid.tsx src/widgets/dashboard/ui/AchievementsWidget.tsx src/widgets/dashboard/model/widgets.ts 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria> - AchievementBadgeGrid renders locked badges with gray/muted style + lock icon - AchievementBadgeGrid renders unlocked badges with colored background + icon + label - Hover tooltip shows achievement name + unlock date for unlocked badges - Hover tooltip shows achievement name for locked badges - AchievementsWidget fetches from /api/achievements and renders AchievementBadgeGrid - AchievementsWidget has loading skeleton and error states - widgets.ts has registry.register() for 'achievements' with spaces: ['home'] - Widget uses Trophy icon (already imported in widgets.ts) - No permissions or featureFlag on achievements widget (per D-13: always-on) - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Achievements widget shows badge grid on home dashboard. Locked badges are gray/muted with lock icon; unlocked are colored with icon. Tooltip shows name + date. Widget registered in widgets.ts with home space assignment.</done>
  </task>

<task type="auto">
  <name>Task 2: Admin achievements widget + directory profile section</name>
  <files>src/widgets/dashboard/ui/AdminAchievementsWidget.tsx, src/entities/directory/ui/UnifiedResidentCard.tsx</files>
  <read_first>
    - src/widgets/dashboard/model/widgets.ts (admin widget registration pattern — see 'page-settings' at lines 598-612)
    - src/entities/directory/ui/UnifiedResidentCard.tsx (full file — understand profile structure for achievements section insertion)
    - src/app/api/admin/achievements/[id]/route.ts (admin PATCH API from Plan 02)
    - src/app/api/achievements/route.ts (GET API for listing definitions)
  </read_first>
  <action>
Create/edit 2 files:

**src/widgets/dashboard/ui/AdminAchievementsWidget.tsx — Admin catalog management (per D-12):**

- Mirrors PageSettingsWidget pattern: list of achievement definitions with toggle switches
- Fetches all achievement definitions from `/api/achievements` (admin sees all, including disabled)
- For each definition: display label, category badge, current threshold, enabled toggle
- Toggle calls PATCH `/api/admin/achievements/:id` with `{ enabled: !current }`
- Inline threshold override: number input that PATCHes `{ customThreshold: value }` on blur
- Uses Sonner toast on success/error (existing toast system per ADR-018)
- Loading skeleton, error state
- Category filter tabs: All, Engagement, Contribution, Milestone

**Register admin widget in widgets.ts:**

- Add registration block for `admin-achievements` after existing admin widgets
- id: `'admin-achievements'`, permissions: `['admin']`, icon: `Trophy`
- component: `lazy(() => import('../ui/AdminAchievementsWidget').then(m => ({ default: m.AdminAchievementsWidget })))`
- spaces: `['admin']`, defaultSize: `{ width: 4, height: 3 }`

**src/entities/directory/ui/UnifiedResidentCard.tsx — Add achievements section (per D-08):**

- Add import for `AchievementBadgeGrid` from `./AchievementBadgeGrid`
- Add state/fetch for achievements data (fetch from `/api/achievements` — same data source as widget)
- Add "Achievements" section after existing profile sections (interests, standing badge)
- Renders `AchievementBadgeGrid` inside a collapsible section
- Only renders if there are any achievements to show (no empty section)
- Per D-08: separate section in profile detail view, no changes to directory listing cards
  </action>
  <verify>
  <automated>npx tsc --noEmit src/widgets/dashboard/ui/AdminAchievementsWidget.tsx src/entities/directory/ui/UnifiedResidentCard.tsx src/widgets/dashboard/model/widgets.ts 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria> - AdminAchievementsWidget lists all achievement definitions with toggle switches - Toggle calls PATCH /api/admin/achievements/:id with { enabled } body - Inline threshold override input PATCHes customThreshold on blur - Category filter tabs work (All/Engagement/Contribution/Milestone) - Toast notification on success/error - widgets.ts has registry.register() for 'admin-achievements' with permissions: ['admin'] - UnifiedResidentCard imports and renders AchievementBadgeGrid - Achievements section appears after existing profile sections - Section is collapsible and only renders when achievements exist - No changes to directory listing cards (per D-08) - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Admin can manage achievement catalog from admin widget. Directory profile shows achievements section with badge grid. Widget registered with admin permissions. No directory listing card changes.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                       |
| ---------------------- | ----------------------------------------------------------------- |
| Resident → Widget data | Widget fetches from /api/achievements (auth-gated, tenant-scoped) |
| Admin → Admin widget   | Admin widget calls admin PATCH API (permission-gated)             |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation                                         |
| --------- | ---------------------- | ---------------------- | ----------- | -------------------------------------------------- |
| T-102-07  | Information Disclosure | widget data fetch      | mitigate    | Widget calls auth-gated /api/achievements endpoint |
| T-102-08  | Elevation of Privilege | admin widget           | mitigate    | Widget registration uses permissions: ['admin']    |
| T-102-SC  | Tampering              | npm/pip/cargo installs | mitigate    | No new packages installed                          |

</threat_model>

<verification>
- Achievements widget renders on home dashboard
- Badge grid shows locked (gray) and unlocked (colored) states
- Admin widget allows toggle and threshold override
- Directory profile shows achievements section
- Widget registered in widgets.ts correctly
- TypeScript clean on all files
</verification>

<success_criteria>

- Badge grid widget registered in widgets.ts with home space assignment
- Locked badges gray/muted, unlocked badges colored with icon
- Tooltip shows name + unlock date on hover
- Admin widget lists definitions with toggle + threshold override
- Directory profile has collapsible achievements section
- No directory listing card changes (per D-08)
- TypeScript clean
  </success_criteria>

<output>
Create `.planning/phases/102-achievements-system/102-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol                                   | Type            | File                                                 | Description                      |
| ---------------------------------------- | --------------- | ---------------------------------------------------- | -------------------------------- |
| `AchievementBadgeGrid`                   | React component | src/entities/directory/ui/AchievementBadgeGrid.tsx   | Reusable badge grid for profiles |
| `AchievementsWidget`                     | React component | src/widgets/dashboard/ui/AchievementsWidget.tsx      | Dashboard badge grid widget      |
| `AdminAchievementsWidget`                | React component | src/widgets/dashboard/ui/AdminAchievementsWidget.tsx | Admin catalog management widget  |
| `achievements` widget registration       | registry entry  | src/widgets/dashboard/model/widgets.ts               | Widget manifest with home space  |
| `admin-achievements` widget registration | registry entry  | src/widgets/dashboard/model/widgets.ts               | Admin widget manifest            |
