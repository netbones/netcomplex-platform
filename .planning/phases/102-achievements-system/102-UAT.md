---
status: diagnosed
phase: 102-achievements-system
source: 102-01-SUMMARY.md, 102-02-SUMMARY.md, 102-03-SUMMARY.md
started: 2026-06-23T19:00:00Z
updated: 2026-06-23T19:20:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Achievement Definitions Seeded

expected: After running the seed, 12 achievement definitions exist in the database. Categories include ENGAGEMENT, CONTRIBUTION, and MILESTONE. Includes both one-shot (threshold=1) and cumulative achievements.
result: issue
reported: "no achievement definitions visible in our db after seed"
severity: major

### 2. View Achievements as Resident

expected: GET /api/achievements returns list of enabled achievements with unlock status. Each has id, key, label, description, icon, category, threshold, unlocked (boolean), unlockedAt.
result: pass

### 3. View Achievement Progress

expected: GET /api/achievements/progress returns current user's progress counters with definitionKey, label, count, threshold, and percentage (0-100).
result: pass

### 4. Achievement Unlock on Event

expected: When a user performs an action that triggers an achievement event (e.g., creates a booking → first_booking), their progress counter increments. When threshold is reached, a UserAchievement record is created and a notification is sent.
result: blocked
blocked_by: prior-phase
reason: "Bookings system shows 'This feature is not available for your community' - bookings module not enabled for tenant"

### 5. Dashboard Achievements Widget

expected: Home dashboard shows AchievementsWidget with Trophy icon. Displays badge grid grouped by category. Unlocked achievements have indigo background with colored icon. Locked achievements are grayed out with lock icon. Shows unlocked/total count header.
result: issue
reported: "no Achievements widget on dashboard even after RLS fix"
severity: major

### 6. Directory Profile Shows Achievements

expected: Viewing a resident's profile card shows their achievement badges after the interests section. Only renders when achievements exist.
result: pass

### 7. Admin Achievement Configuration

expected: Admin can PATCH /api/admin/achievements/:id to toggle enabled, set customThreshold, change icon, or update category. Changes are persisted in TenantAchievement table. Audit log records ACHIEVEMENT_CONFIG_CHANGED.
result: issue
reported: "no /admin/achievements page exists; no achievements domain card on /admin dashboard"
severity: major

### 8. Admin Achievement Widget

expected: Admin dashboard shows AdminAchievementsWidget with toggle switches per achievement, inline threshold override inputs, and category filter tabs (All, ENGAGEMENT, CONTRIBUTION, MILESTONE). Shows toast on success/error.
result: issue
reported: "should be on /admin/achievements page which doesn't exist"
severity: major

## Summary

total: 8
passed: 3
issues: 4
pending: 0
skipped: 0
blocked: 1

## Gaps

- truth: "12 achievement definitions exist in database after seed"
  status: failed
  reason: "User reported: no achievement definitions visible in our db after seed"
  severity: major
  test: 1
  root_cause: "seedAchievementDefinitions() is never called - function exists but has zero invocations in codebase"
  artifacts:
  - path: "src/shared/api/achievements/seed.ts"
    issue: "Function defined but never called"
  - path: "scripts/seed-drizzle.ts"
    issue: "Main seed orchestrator missing call to seedAchievementDefinitions()"
    missing:
  - "Add seedAchievementDefinitions() call to scripts/seed-drizzle.ts"
  - "Add unique constraint on key column in AchievementDefinition table"
  - "Change seed to use onConflictDoNothing({ target: achievementDefinitions.key })"
    debug_session: ""

- truth: "GET /api/achievements returns achievements list with unlock status"
  status: pass
  test: 2

- truth: "GET /api/achievements/progress returns progress counters"
  status: pass
  test: 3

- truth: "AchievementsWidget registered and visible on home dashboard"
  status: failed
  reason: "User reported: no achievement widget on dashboard even after RLS fix"
  severity: major
  test: 5
  root_cause: "Widget registered but not in default home layout for any role - RESIDENT/BOARD/ADMIN default layouts missing 'achievements'"
  artifacts:
  - path: "src/entities/widget/model/default-layouts.ts"
    issue: "achievements not in RESIDENT_USER_WIDGETS.home, BOARD_USER_WIDGETS.home, or ADMIN_USER_WIDGETS.home"
    missing:
  - "Add 'achievements' to home widget list in all three default layouts"
  - "Add achievements layout entry to RESIDENT_LAYOUTS.home, BOARD_LAYOUTS.home, ADMIN_LAYOUTS.home"
    debug_session: ""

- truth: "Resident profile card shows achievement badges"
  status: pass
  test: 6

- truth: "Admin can configure achievements via /admin/achievements page"
  status: failed
  reason: "User reported: no /admin/achievements page exists; no achievements domain card on /admin dashboard"
  severity: major
  test: 7
  root_cause: "Two admin systems exist, achievements wired into neither - ADMIN_DOMAINS missing 'achievements', no page route created"
  artifacts:
  - path: "src/widgets/dashboard/model/spaces.ts"
    issue: "ADMIN_DOMAINS array missing 'achievements'; ADMIN_DOMAIN_WIDGET_MAP missing achievements entry"
  - path: "src/widgets/dashboard/ui/AdminSubLauncher.tsx"
    issue: "ADMIN_DOMAIN_DEFINITIONS missing achievements entry"
  - path: "src/app/(tenant)/admin/achievements/"
    issue: "Directory does not exist - no page.tsx"
    missing:
  - "Add 'achievements' to ADMIN_DOMAINS in spaces.ts"
  - "Add achievements: ['admin-achievements'] to ADMIN_DOMAIN_WIDGET_MAP"
  - "Add achievements entry to ADMIN_DOMAIN_DEFINITIONS in AdminSubLauncher.tsx"
  - "Create src/app/(tenant)/admin/achievements/page.tsx"
    debug_session: ""

- truth: "AdminAchievementsWidget visible on /admin/achievements page"
  status: failed
  reason: "User reported: should be on /admin/achievements page which doesn't exist"
  severity: major
  test: 8
  root_cause: "Same as test 7 - /admin/achievements page and domain registration not created"
  artifacts:
  - path: "src/widgets/dashboard/ui/AdminAchievementsWidget.tsx"
    issue: "Widget exists (151 lines) but is orphaned - never rendered"
    missing:
  - "Same fixes as test 7"
    debug_session: ""
