---
phase: 102-achievements-system
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-drizzle.ts
  - src/entities/widget/model/default-layouts.ts
  - src/widgets/dashboard/model/spaces.ts
  - src/widgets/dashboard/ui/AdminSubLauncher.tsx
  - src/app/(tenant)/admin/achievements/page.tsx
autonomous: true
requirements:
  - ACHIEVEMENTS-01
  - ACHIEVEMENTS-05
  - ACHIEVEMENTS-07
gap_closure: true

must_haves:
  truths:
    - '12 achievement definitions exist in database after running pnpm db:seed'
    - 'AchievementsWidget visible on home dashboard for RESIDENT, BOARD, and ADMIN roles'
    - 'Admin can navigate to /admin/achievements and see AdminAchievementsWidget'
    - 'Achievements domain card visible on admin dashboard grid'
  artifacts:
    - path: 'scripts/seed-drizzle.ts'
      provides: 'seedAchievementDefinitions() call in seed orchestrator'
    - path: 'src/entities/widget/model/default-layouts.ts'
      provides: 'achievements widget in home layouts for all roles'
    - path: 'src/widgets/dashboard/model/spaces.ts'
      provides: 'achievements in ADMIN_DOMAINS and ADMIN_DOMAIN_WIDGET_MAP'
    - path: 'src/widgets/dashboard/ui/AdminSubLauncher.tsx'
      provides: 'achievements entry in ADMIN_DOMAIN_DEFINITIONS'
    - path: 'src/app/(tenant)/admin/achievements/page.tsx'
      provides: 'Admin achievements page route'
  key_links:
    - from: 'scripts/seed-drizzle.ts'
      to: 'src/shared/api/achievements/seed.ts'
      via: 'import + call of seedAchievementDefinitions()'
    - from: 'src/entities/widget/model/default-layouts.ts'
      to: 'src/widgets/dashboard/ui/AchievementsWidget.tsx'
      via: "widget ID 'achievements' in default layouts"
    - from: 'src/app/(tenant)/admin/achievements/page.tsx'
      to: 'src/widgets/dashboard/ui/AdminAchievementsWidget.tsx'
      via: 'import + render'
---

<objective>
Close 4 UAT gaps from Phase 102 verification: seed definitions never called, achievements widget missing from default dashboard layouts, admin domain registration missing achievements, and admin achievements page route missing.

Purpose: Unblocks the 4 failing UAT tests (1, 5, 7, 8) so the achievements system is fully functional end-to-end.
Output: Seed produces 12 definitions, widget shows on home dashboard, admin page accessible at /admin/achievements.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-achievements-system/102-UAT.md
@.planning/phases/102-achievements-system/102-01-SUMMARY.md
@.planning/phases/102-achievements-system/102-02-SUMMARY.md
@.planning/phases/102-achievements-system/102-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed definitions + dashboard widget wiring</name>
  <files>scripts/seed-drizzle.ts, src/entities/widget/model/default-layouts.ts</files>
  <action>
## Fix 1: Wire seedAchievementDefinitions() into seed orchestrator

In `scripts/seed-drizzle.ts`:

1. Add import at top (near other schema imports around line 49):
   `import { seedAchievementDefinitions } from '@shared/api/achievements/seed';`

2. Add call in `main()` function, BEFORE the tenant loop (around line 540, after the "Seeding N tenant(s)" console.log but before the `for` loop):
   ```
   await seedAchievementDefinitions();
   console.log('  ✓ achievement definitions');
   ```
   This seeds the 12 definitions once (not per-tenant) since they're global.

## Fix 2: Add achievements widget to default home layouts

In `src/entities/widget/model/default-layouts.ts`:

1. **RESIDENT_USER_WIDGETS.home** (line 32): Add `'achievements'` to the array:
   `home: ['stats', 'quick-actions', 'notifications', 'recent-activity', 'events', 'messages', 'achievements'],`

2. **RESIDENT_LAYOUTS.home** (line 56): Add layout entry after the `messages` entry:
   `achievements: { x: 0, y: 8, width: 4, height: 2, isCollapsed: false },`

3. **BOARD_USER_WIDGETS.home** (line 95): Add `'achievements'` to the array:
   `home: ['admin-stats', 'admin-quick-links', 'admin-activity', 'notifications', 'recent-activity', 'achievements'],`

4. **BOARD_LAYOUTS.home** (line 117): Add layout entry after `recent-activity`:
   `achievements: { x: 0, y: 7, width: 4, height: 2, isCollapsed: false },`

5. **ADMIN_USER_WIDGETS.home** (line 157): Add `'achievements'` to the array:
   `home: ['admin-stats', 'admin-quick-links', 'admin-user', 'admin-activity', 'achievements'],`

6. **ADMIN_LAYOUTS.home** (line 180): Add layout entry after `admin-activity`:
   `achievements: { x: 0, y: 5, width: 4, height: 2, isCollapsed: false },`
   </action>
   <verify>
   <automated>grep -v '^#' scripts/seed-drizzle.ts | grep -c 'seedAchievementDefinitions' && grep -v '^#' src/entities/widget/model/default-layouts.ts | grep -c "'achievements'"</automated>
   </verify>
   <done>seedAchievementDefinitions() is imported and called in seed orchestrator. 'achievements' appears in all 3 role home widget lists (RESIDENT, BOARD, ADMIN) with layout entries.</done>
   </task>

<task type="auto">
  <name>Task 2: Admin domain registration + page route</name>
  <files>src/widgets/dashboard/model/spaces.ts, src/widgets/dashboard/ui/AdminSubLauncher.tsx, src/app/(tenant)/admin/achievements/page.tsx</files>
  <action>
## Fix 3: Register achievements in admin domain system

In `src/widgets/dashboard/model/spaces.ts`:

1. **ADMIN_DOMAINS** (line 329): Add `'achievements'` to the array. Insert after `'merits'` (line 337):

   ```
   'merits',
   'achievements',
   'announcements',
   ```

2. **ADMIN_DOMAIN_WIDGET_MAP** (line 350): Add achievements entry after `merits`:
   ```
   merits: ['admin-merits'],
   achievements: ['admin-achievements'],
   ```

In `src/widgets/dashboard/ui/AdminSubLauncher.tsx`:

3. **ADMIN_DOMAIN_DEFINITIONS** (line 23): Add achievements entry after the `merits` entry (after line 86):
   ```
   {
     id: 'achievements',
     labelKey: 'domains.achievements',
     descriptionKey: 'domains.descriptions.achievements',
     icon: '/platform/achievements.svg',
     description: 'Achievement configuration and catalog',
   },
   ```
   Note: If `/platform/achievements.svg` doesn't exist, use `/platform/merits.svg` as fallback (Trophy icon fits achievements). Check with `ls public/platform/achievements.svg` first.

## Fix 4: Create admin achievements page route

Create `src/app/(tenant)/admin/achievements/page.tsx`:

```tsx
'use client';

import { Breadcrumbs } from '@shared/ui';
import { AdminAchievementsWidget } from '@widgets/dashboard/ui/AdminAchievementsWidget';

export default function AchievementsAdminPage() {
  return (
    <div className="max-w-6xl mx-auto px-4 py-8">
      <Breadcrumbs items={[{ label: 'Admin', href: '/admin' }, { label: 'Achievements' }]} />
      <div className="flex items-center gap-3 mb-6">
        <h1 className="text-2xl font-bold text-gray-900">Achievements</h1>
      </div>
      <AdminAchievementsWidget />
    </div>
  );
}
```

Pattern follows other admin pages (competitions, merits) — breadcrumbs + heading + widget.
</action>
<verify>
<automated>grep -v '^#' src/widgets/dashboard/model/spaces.ts | grep -c "'achievements'" && test -f src/app/(tenant)/admin/achievements/page.tsx && echo "page exists" || echo "page missing"</automated>
</verify>
<done>'achievements' in ADMIN_DOMAINS array, ADMIN_DOMAIN_WIDGET_MAP has achievements entry, ADMIN_DOMAIN_DEFINITIONS has achievements entry, /admin/achievements/page.tsx exists and renders AdminAchievementsWidget.</done>
</task>

</tasks>

<verification>
## Post-Execution Verification

1. `pnpm db:seed` — check console output shows "✓ achievement definitions" and verify 12 rows in `achievement_definitions` table
2. Login as RESIDENT → home dashboard shows AchievementsWidget with Trophy icon
3. Login as BOARD → home dashboard shows AchievementsWidget
4. Login as ADMIN → home dashboard shows AchievementsWidget
5. Navigate to /admin → achievements domain card visible in grid
6. Click achievements card → navigates to /admin/achievements → AdminAchievementsWidget renders with toggle switches and category tabs
   </verification>

<success_criteria>

- `pnpm db:seed` produces 12 achievement definitions (no errors)
- AchievementsWidget appears in home dashboard default layout for RESIDENT, BOARD, ADMIN roles
- /admin/achievements page loads and renders AdminAchievementsWidget
- Achievements domain card visible on admin dashboard grid
  </success_criteria>

<output>
Create `.planning/phases/102-achievements-system/102-04-SUMMARY.md` when done
</output>
