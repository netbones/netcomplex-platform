---
phase: 122-workspace-context-architecture
plan: 06
type: execute
wave: 5
depends_on:
  - 122-02
  - 122-03
files_modified:
  - src/widgets/workspace/ui/WorkspaceScopePanel.tsx
  - src/widgets/workspace/ui/__tests__/WorkspaceScopePanel.test.tsx
autonomous: true
requirements:
  - WS-06
must_haves:
  truths:
    - 'WorkspaceScopePanel renders the first-class workspace object: scope line, delegator, permissions, expiry — the ONLY place workspace identity is shown (C-02 — URLs are resource-only, panel = sole identity surface)'
    - "Parametrized across PERSONAL / PROVIDER / PROPERTY / OWNER — each renders the correct fields per UI-SPEC §'Workspace Scope Panel — Content Contract' matrix; AUTOMATION NEVER renders (D-11)"
    - "'Delegated by' renders ONLY for Provider and Property workspaces; 'Expires: Never' when expiresAt is null"
    - "Missing-field behaviour per UI-SPEC Field Rules: 'Unnamed workspace' + Pino warn when name unresolved; '—No permissions—' + Pino warn when permissions empty; hide scope line when scope unset (Personal)"
    - 'Permissions chip list uses SCOPE_LABELS for human-readable labels (Pattern L); raw permission keys never shown to users'
    - 'The panel consumes only the 6 allowed WorkspaceContext fields + RLS-gated delegation data — NO domain data (tasks/messages/maintenance/billing) is rendered (C-03 lightweight)'
    - 'Pino `workspace.scope.rendered` debug event fires with workspaceId, workspaceType, fieldsMissing[] — never permissions[] or delegator PII (UI-SPEC §Observability)'
  artifacts:
    - 'src/widgets/workspace/ui/WorkspaceScopePanel.tsx — full impl replacing the plan 122-03 stub; discriminator-conditional sub-panels (Pattern L)'
    - 'src/widgets/workspace/ui/__tests__/WorkspaceScopePanel.test.tsx — parametrized across PERSONAL/PROVIDER/PROPERTY/OWNER + missing-field behaviour'
  key_links:
    - 'WorkspaceScopePanel → useWorkspaceContext via @features/workspace (current workspace)'
    - 'WorkspaceScopePanel → WORKSPACE_REGISTRY via @entities/workspace (type label + actions)'
    - 'WorkspaceScopePanel → useDelegations via @entities/delegation (delegator name + expiresAt for Property/Provider scope)'
    - 'WorkspaceScopePanel → SCOPE_LABELS via @entities/delegation (human-readable permission chips)'
---

<objective>
Build the workspace scope panel — the first-class object that makes the workspace itself visible (D-13). For a delegated property it surfaces the delegator, the permissions granted, and the expiry — the trust boundary rendered so the user always knows what they operate on. URLs stay resource-only (C-02); the panel is the SINGLE identity surface.

Purpose: WS-06 closes the abstraction loop: with registry (01), context (02), switching (03), selector (04), empty state (05), this panel makes the "current workspace" a permanently visible, auditable object rather than implicit role-based branching. It is autonomous:true — pure presentational widget; touches only `WorkspaceScopePanel.tsx` (replaces the plan 122-03 stub in place).
Output: The full `WorkspaceScopePanel` component with type-parametrized rendering and a parametrized test suite.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/122-workspace-context-architecture/CONTEXT.md
@.planning/phases/122-workspace-context-architecture/122-UI-SPEC.md
@.planning/phases/122-workspace-context-architecture/122-PATTERNS.md
@.planning/phases/122-workspace-context-architecture/122-03-SUMMARY.md

# In-repo analogs (READ FIRST)

@src/widgets/dashboard/ui/SpaceChrome.tsx # shell consumer coexisting with useGateContext (Pattern H — do NOT refactor; panel renders ALONGSIDE)
@src/entities/delegation/types.ts # SCOPE_LABELS (lines 58-79), DelegationListItem (lines 9-27)
@src/entities/delegation/api.ts # useDelegations(params?) — delegator + expiry source (Pattern G data)
@src/widgets/dashboard/model/spaces.ts # SpaceDefinition optional-field rendering pattern (Pattern L analog)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write parametrized WorkspaceScopePanel tests (4 types + missing-field + D-11)</name>
  <read_first>
    122-UI-SPEC.md §'Workspace Scope Panel — Content Contract' (Personal/Provider/Property/Owner/Automation matrices), §'Field Rules (lock for the checker)' (Required/Source/Missing behaviour table), §'Observability' (Pino workspace.scope.rendered event). 122-PATTERNS.md Pattern L (discriminator-conditional sub-panels + SCOPE_LABELS rendering). 122-VALIDATION.md §Unit Tests P1a-6 row (parametrized across 4 types; "Delegated by" only Provider/Property; "Expires: Never" when null).
  </read_first>
  <files>
    src/widgets/workspace/ui/__tests__/WorkspaceScopePanel.test.tsx
  </files>
  <behavior>
    Parametrized via `test.each` over [PERSONAL, PROVIDER, PROPERTY, OWNER]:
    - PERSONAL: renders title "Current Workspace", type label "Personal", {user.displayName}, "read profile, manage own settings", "Expires: Never", NO "Delegated by" line (scope unset → hide scope line).
    - PROVIDER: renders "Provider", {provider.businessName}, "Managing: {N} Properties..." (N=0 renderable as "0"), permissions comma list, "Expires: Never" (Provider expiry null → "Never"), "Delegated by: Direct access" (rare/Personal-like) OR hidden — assert per matrix.
    - PROPERTY: renders "Property", {property.address}, "Delegated by: {delegator.displayName}" (from delegation.grantedByName), "Expires: {date|Never}", permissions chip list joined by comma using SCOPE_LABELS labels.
    - OWNER: renders "Owner", {owner.displayName}, "Properties: {N owned} | Delegations: {N active}", permissions "Delegations, Audit, Suspend Access", "Expires: Never", NO "Delegated by".
    - AUTOMATION: the panel NEVER renders for AUTOMATION — assert `if (workspaceType === 'AUTOMATION') return null;` (D-11).
    Missing-field behaviour (UI-SPEC Field Rules):
    - Name unresolved → renders "Unnamed workspace" + fires Pino warn (spy on the logger).
    - Permissions empty → "—No permissions—" + Pino warn.
    - Expires null → "Expires: Never".
    - scope unset (Personal) → scope line hidden.
    Observability: `workspace.scope.rendered` debug event fires with `workspaceId`, `workspaceType`, `fieldsMissing[]` — assert the logged fields NEVER include `permissions[]` array values or `grantedByName`/`propertyAddress` PII (T-122-09 PII rule).
  </behavior>
  <action>
Write the failing tests FIRST (RED) using `@testing-library/react` `render` + `test.each` for the 4-type matrix. Mock `@features/workspace` `useWorkspaceContext` to return each workspace-type context. Mock `@entities/delegation` `useDelegations` to return matching delegation rows for the PROPERTY/OWNER cases. Use a typed `makeWorkspaceContext(type, overrides)` builder (Pattern K — `vi.hoisted` if needed; NO `as any`). Spy on the Pino logger (`vi.mock` the logger module or import the existing logger and `vi.spyOn`).

The component stub from plan 122-03 returns null → all assertions FAIL (RED). Commit `test(122-06): add failing parametrized WorkspaceScopePanel tests`.
</action>
<verify>
<automated>
cd ../worktrees/phase-122-workspace-context &&
npx vitest run src/widgets/workspace/ui/**tests**/WorkspaceScopePanel.test.tsx 2>&1 | tail -20
</automated>
</verify>
<done>
4-type parametrized tests + missing-field + D-11 + observability tests encoded. Run RED.
</done>
<acceptance_criteria> - `test.each` covers PERSONAL/PROVIDER/PROPERTY/OWNER. - AUTOMATION-never-renders assertion present (D-11). - "Delegated by" rendered only for Provider/Property. - "Expires: Never" when expiresAt null asserted. - Pino PII-guard: logged fields never include permissions[] values or delegator PII.
</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement WorkspaceScopePanel (replace stub, discriminator-conditional rendering)</name>
  <read_first>
    122-UI-SPEC.md §'Workspace Scope Panel — Content Contract' (verbatim field matrices), §'Workspace-Specific Actions' (D-14 — contextual actions per type), §'Visual Hierarchy' Surface 3 (focal priorities: identity → scope → permissions chip list + actions menu). 122-PATTERNS.md Pattern L (PropertyScopePanel example lift).
  </read_first>
  <files>
    src/widgets/workspace/ui/WorkspaceScopePanel.tsx
  </files>
  <behavior>
    All Task-1 tests pass (GREEN) + scoped typecheck clean + component <200 lines (split sub-panels if needed).
  </behavior>
  <action>
Implement `src/widgets/workspace/ui/WorkspaceScopePanel.tsx` (`'use client'`) replacing the plan 122-03 stub in place. Follow Pattern L (discriminator-conditional sub-panels):

- `export function WorkspaceScopePanel()`: read `useWorkspaceContext()`; if `null` (resolving) return `<LoadingSkeleton />` (existing primitive, UI-SPEC §Empty/Loading/Error Matrix Loading row). If `workspaceType === 'AUTOMATION'` return `null` (D-11 — never renders).
- Look up `WORKSPACE_REGISTRY[wctx.workspaceType]` for the type `label` (fallback to the `workspaceType` enum literal if name missing — UI-SPEC Field Rules).
- Resolve the human-readable name per type (PERSONAL: session user displayName; PROPERTY: delegation.propertyAddress; PROVIDER/OWNER: derive from scope + delegations). If unresolved → "Unnamed workspace" + `logger.warn({ workspaceId, reason: 'name_unresolved' })`.
- Render `<aside className="bg-white rounded-lg p-lg">` with:
  - Title `<h2 className="text-2xl font-bold">Current Workspace</h2>` (UI-SPEC Surface 3 Priority 1).
  - Type label `<p className="text-sm text-gray-900">{def.label}</p>`.
  - Name `<p className="text-sm text-gray-600">{resolvedName}</p>`.
  - Scope line — hidden when `scope` unset (Personal). For Property/Provider: render via sub-panels (Pattern L).
  - `PropertyScopePanel`: `useDelegations({ propertyId: scope.propertyId, status: 'ACTIVE' })` → "Delegated by: {delegation.grantedByName ?? 'Direct access'}", "Expires: {expiryLabel}" (expiryLabel = expiresAt ? new Date(expiresAt).toLocaleDateString() : 'Never'), permissions chip list `{context.permissions.map(p => SCOPE_LABELS[p] ?? p).join(', ')}` empty → "—No permissions—" + warn.
  - `ProviderScopePanel` / `OwnerScopePanel`: per UI-SPEC §'Provider'/§'Owner' contract (Managing: {N} Properties...; Properties: {N owned} | Delegations: {N active}). For P1a, if the counts require not-yet-existing endpoints (R-03 PROVIDER/OWNER data deferral), render the structure with counts sourced from useDelegations where available and "—" placeholders where the data hook is not yet built — ensure the test's mocked counts render correctly. Do NOT invent server endpoints this phase.
- D-14 actions menu: render the contextual actions for the current type from `def.actions[]` (registry-driven — C-05; never hardcoded in the shell). Owner destructive actions (Suspend Access) render in red-600 (UI-SPEC §Color Destructive) with a confirmation dialog using the existing `ModalOverlay`/radix `AlertDialog` pattern from Phase 32 (UI-SPEC §Registry Safety).
- Pino observability: emit `workspace.scope.rendered` debug with `{ workspaceId, workspaceType, fieldsMissing }` ONLY — NEVER `permissions[]` array values or `grantedByName`/`propertyAddress` PII (T-122-09 mitigation). `fieldsMissing` is an array of field-name strings ("name","scope","delegator","expires"), NOT the PII values themselves.

Use UI-SPEC carry-forward design tokens — NO new hex values. Split into `PersonalScopePanel`/`ProviderScopePanel`/`PropertyScopePanel`/`OwnerScopePanel` sub-components if the main file exceeds 200 lines (AGENTS.md React max), each in its own file under `src/widgets/workspace/ui/` (update files_modified if so — but the barrel already exports `WorkspaceScopePanel` only; sub-panels are internal).

Run tests → GREEN → commit `feat(122-06): implement WorkspaceScopePanel (WS-06, D-13)`. REFACTOR if needed (extract a `PermissionsChips` helper); re-run → GREEN.
</action>
<verify>
<automated>
cd ../worktrees/phase-122-workspace-context &&
npx vitest run src/widgets/workspace/ui/**tests**/WorkspaceScopePanel.test.tsx 2>&1 | tail -15 &&
npx tsc --noEmit --pretty src/widgets/workspace/ui/WorkspaceScopePanel.tsx 2>&1 | head -20 &&
rg -n "role\s\*===|Role\." src/widgets/workspace/ui/WorkspaceScopePanel.tsx || echo "NO_ROLE_BRANCHING" &&
git diff dev..HEAD --name-only | grep -E 'prisma/schema.prisma|drizzle/schema.ts|src/db/schema.ts|supabase/migrations/' || echo "NO_SCHEMA_CHANGES"
</automated>
</verify>
<done>
All parametrized tests GREEN (4 types + missing-field + D-11 + observability PII guard). Component renders registry-driven fields; AUTOMATION never. No Role branching. No schema touched. Component ≤200 lines (or split with sub-panels).
</done>
<acceptance_criteria> - `npx vitest run WorkspaceScopePanel.test.tsx` → GREEN. - `NO_ROLE_BRANCHING` confirmed (C-04). - PII-guard test GREEN: `workspace.scope.rendered` never logs permissions[] or delegator PII. - "Delegated by" only for Provider/Property; "Expires: Never" when null; "Unnamed workspace"/"—No permissions—" with Pino warn on missing. - AUTOMATION never renders (D-11). - NO_SCHEMA_CHANGES confirmed.
</acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                                                                                                             |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| Panel ↔ delegation data | The panel renders delegator names + permissions from RLS-gated useDelegations — a tenant-A user only ever sees tenant-A delegation PII. |
| Panel ↔ Pino logs       | Observability surface; PII must not leak into structured logs.                                                                          |

## STRIDE Threat Register

| Threat ID | Category               | Component                             | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-122-17  | Information Disclosure | WorkspaceScopePanel cross-tenant PII  | medium   | mitigate    | Delegator name + address come from `useDelegations({ propertyId, status:'ACTIVE' })` → RLS-gated `GET /api/delegations` (T-122-04). Tenant isolation is enforced server-side; the panel only renders what the server already authorized. Cross-tenant PII leakage requires breaking RLS, which is out of scope for this frontend plan. Verified by the parametrized test mocking only the caller's own delegations.                                                      |
| T-122-18  | Information Disclosure | Pino `workspace.scope.rendered` event | medium   | mitigate    | The debug log emits `{ workspaceId, workspaceType, fieldsMissing[] }` only — `fieldsMissing` is an array of FIELD-NAME STRINGS ("name","scope","delegator","expires"), NEVER the PII values. The PII-guard test asserts the logged object never contains `permissions` array values, `grantedByName`, or `propertyAddress` (T-122-09 rule). This keeps delegator PII out of log aggregators.                                                                             |
| T-122-19  | Elevation of Privilege | Permissions chip list display         | low      | accept      | The panel READS `context.permissions[]` for display only — it never grants or modifies them. Permissions are server-authoritative (delegation.permissions) and already enforced by the gate layer. Displaying them to the user who already has them poses no escalation risk.                                                                                                                                                                                            |
| T-122-20  | Tampering              | D-14 Owner destructive actions menu   | medium   | mitigate    | "Suspend Access" renders in red-600 with a confirmation dialog (existing `ModalOverlay`/radix AlertDialog from Phase 32). The action triggers a server endpoint (existing/follow-up); the dialog's irreversibility copy names the affected entity per UI-SPEC §Copywriting Contract (Destructive — Owner suspends access). This plan ships the dialog SURFACE; the actual suspend server call is a P2 follow-up (out of P1a). The dialog alone cannot tamper with state. |
| T-122-SC  | Tampering              | npm/pip/cargo installs                | low      | accept      | Plan 06 adds NO new dependencies (reuses lucide-react, @entities/delegation, radix AlertDialog existing). No package-legitimacy gate needed this plan.                                                                                                                                                                                                                                                                                                                   |

</threat_model>

<verification>
- [ ] `npx vitest run src/widgets/workspace/ui/__tests__/WorkspaceScopePanel.test.tsx` → GREEN (4-type parametrized + missing-field + D-11 + PII-guard)
- [ ] `pnpm typecheck` scoped to the widget clean; component ≤200 lines (sub-panels extracted if needed)
- [ ] `rg "role\s*===|Role\." src/widgets/workspace/ui/WorkspaceScopePanel.tsx` → no match (NO_ROLE_BRANCHING — C-04)
- [ ] PII-guard: logged `workspace.scope.rendered` object never contains permissions[] values / grantedByName / propertyAddress (T-122-09)
- [ ] No new hex values beyond carry-forward palette (`rg "#[0-9A-Fa-f]{6}"` filtered)
- [ ] `git diff dev..HEAD --name-only | grep -E 'prisma/...|drizzle/...|src/db/schema|supabase/migrations/' || echo NO_SCHEMA_CHANGES` → frontend-only (schema-gate)
- [ ] WorkspaceScopePanel renders only the 6 allowed WorkspaceContext fields + RLS-gated delegation data (C-03 lightweight enforced — no domain data widgets)
</verification>

<success_criteria>

- WS-06 satisfied: scope panel is the first-class workspace object (scope/delegator/permissions/expiry), parametrized across PERSONAL/PROVIDER/PROPERTY/OWNER.
- D-11 honored: AUTOMATION never renders.
- D-13 honored: the workspace is a first-class visible object.
- D-14 honored: actions are registry-driven (def.actions[]), Owner destructive actions in red-600 with confirmation.
- C-02 honored: panel is the sole identity surface (URLs stay resource-only).
- C-03 honored: only the 6 allowed fields + RLS-gated delegation data; no domain widgets.
- C-04 honored: rendering branches on workspaceType (discriminator), not Role.
- T-122-09 honored: Pino never logs permissions[] or PII values.
  </success_criteria>

<output>
Create `.planning/phases/122-workspace-context-architecture/122-06-SUMMARY.md` when done.
</output>
