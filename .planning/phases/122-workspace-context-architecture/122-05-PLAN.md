---
phase: 122-workspace-context-architecture
plan: 05
type: execute
wave: 4
depends_on:
  - 122-02
  - 122-03
files_modified:
  - src/widgets/workspace/ui/EmptyWorkspaceState.tsx
  - src/widgets/workspace/ui/__tests__/EmptyWorkspaceState.test.tsx
autonomous: true
requirements:
  - WS-05
must_haves:
  truths:
    - "EmptyWorkspaceState renders the welcome surface with the EXACT copy from D-12 + UI-SPEC §Copywriting Contract — heading 'Welcome to Agent Workspace', body 'You don't currently manage any delegated properties.', and three verb-first CTAs ('Accept a delegation invitation', 'Browse the Marketplace', 'Learn how property delegation works')"
    - "The component shows ONLY when the user has zero delegations (UI-SPEC §Empty/Loading/Error Matrix 'Zero delegations' row)"
    - 'Decorative house/agent illustration is subtle (text-gray-300, 48px) — lowest visual weight (UI-SPEC §Surface 2 Priority 3)'
    - "NO identity language ('Working As', 'Logged in as', 'Become') — D-05 enforced via UI-SPEC Copywriting Rules"
    - "CTAs are verb-first imperative ('Accept', 'Browse', 'Learn') per UI-SPEC Copywriting Rule 3"
    - "The component does NOT branch on Role (C-04) — it consumes useWorkspaceContext() and useDelegations() to decide emptiness, never `role === 'AGENT'`"
  artifacts:
    - 'src/widgets/workspace/ui/EmptyWorkspaceState.tsx — full impl replacing the plan 122-03 stub'
    - 'src/widgets/workspace/ui/__tests__/EmptyWorkspaceState.test.tsx — copy-equality assertions for the 3 CTAs + heading + body'
  key_links:
    - 'EmptyWorkspaceState → useWorkspaceContext via @features/workspace (decide visibility)'
    - 'EmptyWorkspaceState → useDelegations via @entities/delegation (zero-delegation detection)'
    - 'EmptyWorkspaceState → lucide-react icons for the 3 CTA affordances + decorative illustration (string-key resolution OR direct import in widgets layer)'
---

<objective>
Build the welcome surface shown to zero-delegation users — replacing a "broken dashboard" with onboarding and discovery paths (D-12). This is the compassionate empty state that keeps the user oriented instead of dumping them into an inactive workspace shell.

Purpose: WS-05 ensures a user with no delegated properties lands on a guided path (accept invitation, browse marketplace, learn how delegation works), not a blank dashboard. It is autonomous:true — pure presentational widget; touches only `EmptyWorkspaceState.tsx` (replaces the plan 122-03 stub in place; zero files_modified overlap with plan 122-04 which touches only `WorkspaceSelector.tsx` — enables parallel execution in wave 4).
Output: The `EmptyWorkspaceState` component with locked copy and 3 verb-first CTAs.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/122-workspace-context-architecture/CONTEXT.md
@.planning/phases/122-workspace-context-architecture/122-UI-SPEC.md
@.planning/phases/122-workspace-context-architecture/122-03-SUMMARY.md

# In-repo analogs (READ FIRST)

@src/widgets/dashboard/ui/HomeLayer.tsx # existing dashboard landing pattern (zoned empty/active states)
@src/entities/delegation/api.ts # useDelegations(params?) — zero-delegation detection source
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write EmptyWorkspaceState copy + visibility tests</name>
  <read_first>
    122-UI-SPEC.md §Surface 2 (empty-state focal priorities), §Copywriting Contract (Global Vocabulary + Per-Surface Copy), §Empty/Loading/Error State Matrix, §Color/§Spacing/§Typography (carry-forward tokens — no new design tokens this phase). CONTEXT.md D-12 empty-state example.
  </read_first>
  <files>
    src/widgets/workspace/ui/__tests__/EmptyWorkspaceState.test.tsx
  </files>
  <behavior>
    - Renders heading exactly: "Welcome to Agent Workspace" (24px / 700 — UI-SPEC Typography Heading role).
    - Renders body exactly: "You don't currently manage any delegated properties." (text-sm / 400).
    - Renders three CTAs with EXACT labels (verb-first imperative): "Accept a delegation invitation", "Browse the Marketplace", "Learn how property delegation works".
    - Decorative illustration present with text-gray-300 (lowest weight, no animation).
    - NO forbidden identity language anywhere in the rendered output: assert `screen.queryByText(/Working As/i)`, `/Logged in as/i`, `/Become/i`, `/Act as/i` all return null.
    - Visibility guard: when delegations.length > 0 (mocked), the component renders nothing (null) OR is gated by the caller — assert the component's internal `if (delegations.length > 0) return null;` behaviour.
  </behavior>
  <action>
Write the failing tests FIRST (RED) using `@testing-library/react` `render` + `screen.getByText` (text-equality assertions, NOT snapshot — UI-SPEC copy is locked verbatim, equality guards drift better). Mock `@entities/delegation` `useDelegations` to return `{ data: [] }` (zero) and `{ data: [fake] }` (non-zero) cases. Mock `@features/workspace` `useWorkspaceContext` to return a PERSONAL context.

The component stub from plan 122-03 returns null → all text-equality assertions FAIL (RED). Commit `test(122-05): add failing EmptyWorkspaceState copy + visibility tests`.
</action>
<verify>
<automated>
cd ../worktrees/phase-122-workspace-context &&
npx vitest run src/widgets/workspace/ui/**tests**/EmptyWorkspaceState.test.tsx 2>&1 | tail -15
</automated>
</verify>
<done>
All copy-equality + visibility + no-identity-language tests encoded. Run RED (stub returns null → text not found).
</done>
<acceptance_criteria> - 3 CTA labels, heading, body asserted by EXACT text (equality, not regex). - forbidden-identity-language assertions present (Working As / Logged in as / Become / Act as). - zero-vs-non-zero visibilityBehaviour tested.
</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement EmptyWorkspaceState (replace stub, exact copy + 3 verb-first CTAs)</name>
  <read_first>
    122-UI-SPEC.md §Spacing Scale (3xl 64px hero offset for empty-state vertical padding), §Typography (Heading 24px/700, Body text-sm/400), §Color (Decorative illustration text-gray-300, Accent #4F46E5 for primary CTA only).
  </read_first>
  <files>
    src/widgets/workspace/ui/EmptyWorkspaceState.tsx
  </files>
  <behavior>
    All Task-1 tests pass (GREEN) + scoped typecheck clean.
  </behavior>
  <action>
Implement `src/widgets/workspace/ui/EmptyWorkspaceState.tsx` (`'use client'`) replacing the plan 122-03 stub in place. Build:

- Internal guard: `const { data: delegations } = useDelegations({ status: 'ACTIVE' }); if (delegations && delegations.length > 0) return null;` (only ACTIVE delegations count toward non-emptiness).
- Heading `<h2 className="text-2xl font-bold">` → "Welcome to Agent Workspace" (UI-SPEC Per-Surface "Empty state heading" — EXACT, locked copy D-12).
- Body `<p className="text-sm">` → "You don't currently manage any delegated properties." (locked copy D-12).
- Three CTAs stacked (UI-SPEC §Surface 2 Priority 2), each verb-first:
  1. "Accept a delegation invitation" — primary CTA (accent #4F46E5). Affordance can be a `<Link href="/delegations/invitations">` or button (route is existing/follow-up; use a benign href placeholder documenting the destination — DO NOT invent a new route this phase).
  2. "Browse the Marketplace" — `<Link href="/marketplace">` (route already exists per STATE Phase 50 — IF NOT, use a benign href with a TODO comment).
  3. "Learn how property delegation works" — `<Link href="/delegation-learn">` or external docs link (benign href + TODO if no route).
     The CTAs herd the user toward onboarding; the actual route wiring is a follow-up P2 concern (D-03 incremental ship) — this plan ships the SURFACE + STRUCTURE, exact hrefs to be verified against existing routes by the executor (grep existing routes first; never invent routes).
- Decorative illustration: a single lucide-react icon (e.g. `Home` or `Building2`) at 48px, `text-gray-300`, `aria-hidden="true"`, no animation (UI-SPEC §Surface 2 Priority 3).
- Padding: 3xl (64px) vertical offset — major empty-state hero spacing (UI-SPEC §Spacing Scale). Component container `bg-white rounded-lg` mirroring UI-SPEC §Color Dominant.

DO NOT branch on Role anywhere (C-04). The component consumes `useWorkspaceContext()` and `useDelegations()` only. Use the UI-SPEC carry-forward design tokens — NO new hex values, NO new Tailwind config entries.

Run tests → GREEN → commit `feat(122-05): implement EmptyWorkspaceState welcome surface (WS-05, D-12)`.
</action>
<verify>
<automated>
cd ../worktrees/phase-122-workspace-context &&
npx vitest run src/widgets/workspace/ui/**tests**/EmptyWorkspaceState.test.tsx 2>&1 | tail -15 &&
npx tsc --noEmit --pretty src/widgets/workspace/ui/EmptyWorkspaceState.tsx 2>&1 | head -20 &&
rg -n "role\s*===|role\s*==|Role\." src/widgets/workspace/ui/EmptyWorkspaceState.tsx || echo "NO_ROLE_BRANCHING" &&
git diff dev..HEAD --name-only | grep -E 'prisma/schema.prisma|drizzle/schema.ts|src/db/schema.ts|supabase/migrations/' || echo "NO_SCHEMA_CHANGES"
</automated>
</verify>
<done>
All copy tests GREEN with EXACT strings. Component returns null on non-zero delegations. No Role branching. No schema touched.
</done>
<acceptance_criteria> - `npx vitest run EmptyWorkspaceState.test.tsx` → GREEN. - `NO_ROLE_BRANCHING` confirmed (no role checks — C-04). - No new hex values beyond carry-forward palette. - Heading/body/3 CTAs match UI-SPEC copy verbatim. - NO_SCHEMA_CHANGES confirmed.
</acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                                                                                                              |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Zero-delegation detection ↔ useDelegations | The visibility decision is based on RLS-gated delegation rows (withTenant); a tenant-A user never sees a non-empty state driven by tenant-B delegations. |

## STRIDE Threat Register

| Threat ID | Category                      | Component                                     | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                              |
| --------- | ----------------------------- | --------------------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-122-15  | Information Disclosure        | EmptyWorkspaceState visibility / cross-tenant | low      | mitigate    | Visibility uses `useDelegations({ status: 'ACTIVE' })` → RLS-gated `GET /api/delegations` (T-122-04). A user on tenant A cannot see tenant-B delegations, so the empty/non-empty decision is accurate per tenant. Verified by the visibility test mocking only the caller's own delegations. |
| T-122-16  | Spoofing / Social Engineering | "Accept a delegation invitation" CTA          | low      | accept      | The CTA is an affordance pointing to a delegation accept flow (existing/follow-up route). No trust boundary is crossed by the surface alone — the actual accept action is gated server-side via the delegation accept endpoint (Phase 111 AGENT-14). Accept the low UX-level surface risk.   |
| T-122-SC  | Tampering                     | npm/pip/cargo installs                        | low      | accept      | Plan 05 adds NO new dependencies (reuses lucide-react, existing Link). No package-legitimacy gate needed this plan.                                                                                                                                                                          |

</threat_model>

<verification>
- [ ] `npx vitest run src/widgets/workspace/ui/__tests__/EmptyWorkspaceState.test.tsx` → GREEN (copy equality + visibility + no-identity-language)
- [ ] `pnpm typecheck` scoped to the widget clean
- [ ] `rg "role\s*===|Role\." src/widgets/workspace/ui/EmptyWorkspaceState.tsx` → no match (NO_ROLE_BRANCHING — C-04)
- [ ] No new hex values beyond carry-forward palette (`rg "#[0-9A-Fa-f]{6}"` filtered against the UI-SPEC carry-forward set)
- [ ] `git diff dev..HEAD --name-only | grep -E 'prisma/...|drizzle/...|src/db/schema|supabase/migrations/' || echo NO_SCHEMA_CHANGES` → frontend-only (schema-gate)
</verification>

<success_criteria>

- WS-05 satisfied: welcome surface with 3 verb-first CTAs for zero-delegation users (D-12).
- Locked copy per UI-SPEC §Copywriting Contract reproduced verbatim (drift caught by equality tests).
- C-04 honored: emptiness driven by WorkspaceContext + delegations, NOT Role.
- No new design tokens introduced (UI-SPEC carry-forward verification).
- Parallel-safe with plan 122-04 (disjoint files_modified — only EmptyWorkspaceState.tsx).
  </success_criteria>

<output>
Create `.planning/phases/122-workspace-context-architecture/122-05-SUMMARY.md` when done.
</output>
