---
phase: 122-workspace-context-architecture
plan: 07
type: execute
wave: 6
depends_on: []
files_modified:
  - src/widgets/dashboard/ui/SpaceChrome.tsx
autonomous: true
gap_closure: true
requirements:
  - WS-05
must_haves:
  truths:
    - 'EmptyWorkspaceState renders inside SpaceChrome when user navigates to /dashboard with zero ACTIVE delegations — the welcome surface ("Welcome to Agent Workspace", 3 CTAs) replaces the blank dashboard'
    - 'EmptyWorkspaceState returns null (dashboard renders normally) when user has 1+ ACTIVE delegations — no regression for users WITH delegations'
  artifacts:
    - 'src/widgets/dashboard/ui/SpaceChrome.tsx — imports and renders <EmptyWorkspaceState /> above {children} in the <main> element'
  key_links:
    - 'SpaceChrome → EmptyWorkspaceState via @widgets/workspace (import + render, no prop passing needed — component self-resolves visibility via useDelegations)'
---

<objective>
Mount the EmptyWorkspaceState component in the SpaceChrome dashboard shell so that zero-delegation users see the welcome surface (D-12) instead of a blank dashboard. The component is already fully built and tested (12 tests GREEN, plan 122-05) — this plan is pure integration: import it, render it, verify it shows/hides correctly.

Purpose: Closes the UAT gap where EmptyWorkspaceState exists in the codebase but is invisible to users because it's never mounted in the app shell.
Output: SpaceChrome renders EmptyWorkspaceState above page content; zero-delegation users see the welcome surface.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/122-workspace-context-architecture/CONTEXT.md
@.planning/phases/122-workspace-context-architecture/122-UI-SPEC.md
@.planning/phases/122-workspace-context-architecture/122-05-PLAN.md
@.planning/phases/122-workspace-context-architecture/122-VERIFICATION.md

# Source of truth — the already-built component

@src/widgets/workspace/ui/EmptyWorkspaceState.tsx

# Integration point — the dashboard shell

@src/widgets/dashboard/ui/SpaceChrome.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount EmptyWorkspaceState in SpaceChrome main area</name>
  <files>src/widgets/dashboard/ui/SpaceChrome.tsx</files>
  <action>
Import `EmptyWorkspaceState` from `@widgets/workspace` at the top of SpaceChrome.tsx alongside the existing imports. Render `<EmptyWorkspaceState />` inside the `<main>` element, ABOVE `{children}`, so the welcome surface appears before page-specific content.

    The component is self-contained: it internally calls `useDelegations({ status: 'ACTIVE' })` and returns `null` when the delegation count is non-zero. No conditional rendering, prop passing, or delegation-count logic is needed in SpaceChrome — just import and render.

    Placement: render as the first child of `<main>` so it sits at the top of the content area:

    ```tsx
    <main className="flex-1 min-w-0 md:pb-0" style={{ paddingBottom: 'calc(4rem + env(safe-area-inset-bottom, 0px))' }}>
      <EmptyWorkspaceState />
      {children}
    </main>
    ```

    The component renders a full-page welcome surface via `useDelegations`, so pages below it will scroll naturally. No layout changes to SpaceChrome's flex structure are necessary.

    This plan touches ONLY SpaceChrome.tsx — no other files. The EmptyWorkspaceState component was built, styled, and tested in plan 122-05. It already reads `useWorkspaceContext()` and `useDelegations()` to determine its visibility and copy. The SpaceChrome needs zero awareness of delegation state.

  </action>
  <verify>
    <automated>
cd ../worktrees/phase-122-workspace-context &&
pnpm typecheck 2>&1 | tail -5 &&
rg "EmptyWorkspaceState" src/widgets/dashboard/ui/SpaceChrome.tsx
    </automated>
  </verify>
  <done>
EmptyWorkspaceState imported and rendered above {children} in SpaceChrome's `<main>` element. TypeScript compiles without errors. Import line and JSX tag present in SpaceChrome.tsx.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify integration — no regressions for existing dashboard flows</name>
  <files>src/widgets/dashboard/ui/SpaceChrome.tsx</files>
  <action>
Run the full dashboard test suite and existing EmptyWorkspaceState tests to confirm the mount does not break anything:

    1. Run the existing EmptyWorkspaceState unit tests to ensure the component still passes (it should — we only added an import + render call, nothing internal changed).
    2. Run a build check to ensure SpaceChrome's type signature remains compatible with all 11 layout.tsx consumers (dashboard, admin, services, messages, community, home, etc.).
    3. Verify the component's own test coverage confirms it returns `null` when delegations exist (existing test: zero-delegation welcome surface, non-zero → null return). This proves mounting it unconditionally is safe.

    No new tests are created — the component already has 12 passing tests from plan 122-05. The gap is purely integration (component was un-mounted), not behavioral.

  </action>
  <verify>
    <automated>
cd ../worktrees/phase-122-workspace-context &&
npx vitest run src/widgets/workspace/ui/__tests__/EmptyWorkspaceState.test.tsx 2>&1 | tail -10 &&
pnpm typecheck 2>&1 | tail -5
    </automated>
  </verify>
  <done>
All 12 EmptyWorkspaceState tests GREEN. Full project typecheck passes. SpaceChrome imports and renders EmptyWorkspaceState without compilation errors.
  </done>
</task>

</tasks>

<verification>
- [ ] `rg "EmptyWorkspaceState" src/widgets/dashboard/ui/SpaceChrome.tsx` confirms import and JSX render
- [ ] `pnpm typecheck` passes with no new errors
- [ ] Existing EmptyWorkspaceState tests (12) all GREEN — component behavior unchanged
- [ ] No Role branching introduced in SpaceChrome (the import is unconditional — C-04 honored)
- [ ] No schema changes (`git diff dev..HEAD --name-only | grep -E 'prisma/|drizzle/' || echo NO_SCHEMA_CHANGES`)
</verification>

<success_criteria>
EmptyWorkspaceState is mounted in SpaceChrome. Zero-delegation users arriving at /dashboard see the welcome surface with 3 CTAs. Users with 1+ delegations see the normal dashboard (EmptyWorkspaceState returns null). No regressions in existing dashboard shell behavior.
</success_criteria>

<output>
Create `.planning/phases/122-workspace-context-architecture/122-07-SUMMARY.md` when done.
</output>
