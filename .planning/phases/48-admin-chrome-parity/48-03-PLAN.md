---
phase: 48-admin-chrome-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - src/app/(tenant)/admin/layout.tsx
autonomous: false
requirements:
  - ADMIN-CHROME-01
  - ADMIN-CHROME-02
  - ADMIN-CHROME-03
  - ADMIN-CHROME-04
must_haves:
  truths:
    - "Visiting /admin as ADMIN renders SpaceLauncher (desktop, md+) with 'Admin' highlighted in indigo"
    - "Visiting /admin on mobile (md-) renders MobileSpaceBar with 'Admin' highlighted in indigo"
    - "Visiting /admin/users, /admin/requests, /admin/surveys, /admin/announcements, /admin/categories, /admin/competitions, /admin/content, /admin/events, /admin/external-surveys, /admin/groups, /admin/households, /admin/resources, /admin/surveys/[id], /admin/surveys/[id]/edit, /admin/surveys/[id]/preview, /admin/surveys/new — all show SpaceLauncher with 'Admin' highlighted"
    - "AdminLayer at /admin renders inside the chrome's <main> with no double-padding (its own p-6 max-w-5xl mx-auto stays the only width constraint)"
    - "Sub-page wrappers (e.g., /admin/requests' max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8) do not double up with the chrome's <main> (no extra padding, no extra max-width)"
    - 'SurveyEditor at /admin/surveys/[id]/edit does not visually collide with the collapsed sidebar (w-16)'
    - "Survey preview at /admin/surveys/[id]/preview renders with chrome (acceptable — preview stays inside the admin experience; 'Back to editor' link is the primary exit)"
    - "Non-admin user visiting /admin (via direct URL) does NOT see 'Admin' in the SpaceLauncher or MobileSpaceBar (filtered by getVisibleSpaces)"
    - 'Sidebar collapse state persists across admin sub-route navigation (no remount)'
  artifacts:
    - path: 'src/app/(tenant)/admin/layout.tsx'
      provides: 'Thin client layout that mounts <SpaceChrome> for /admin/* routes'
      max_lines: 15
      contains: 'SpaceChrome'
  key_links:
    - from: 'src/app/(tenant)/admin/layout.tsx'
      to: 'src/widgets/dashboard/ui/SpaceChrome.tsx'
      via: 'import + render as the chrome host'
      pattern: 'import.*SpaceChrome.*widgets/dashboard'
    - from: 'src/app/(tenant)/admin/page.tsx'
      to: 'src/app/(tenant)/admin/layout.tsx'
      via: 'Next.js App Router automatic layout composition'
      pattern: 'no explicit import — automatic'
---

<objective>
Create `src/app/(tenant)/admin/layout.tsx` that mounts the same shared `SpaceChrome` component as `dashboard/layout.tsx`. After this plan ships, all 27 admin sub-pages automatically inherit the desktop sidebar and mobile bottom bar, the admin space stays highlighted across all admin routes, and AdminLayer's own `p-6 max-w-5xl mx-auto` continues to be the only width constraint (no duplicate chrome).

Purpose: This is the primary deliverable of Phase 48. ADMIN-CHROME-01 (SpaceLauncher on /admin/_) and ADMIN-CHROME-02 (MobileSpaceBar on /admin/_) are satisfied by the file creation alone; ADMIN-CHROME-03 (active-space from /admin/\*) is satisfied by the `getActiveSpaceId` helper from Plan 48-01; ADMIN-CHROME-04 (no duplicate chrome) is satisfied by the passthrough `<main>` design verified via manual visual check.

Output: 1 new file (admin/layout.tsx, ~6 lines) + 1 blocking-human checkpoint (visual verification on key admin routes) + final typecheck/build.

</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-admin-chrome-parity/48-RESEARCH.md
@src/app/(tenant)/dashboard/layout.tsx
@src/app/(tenant)/admin/page.tsx
@src/widgets/dashboard/ui/SpaceChrome.tsx
@src/widgets/dashboard/ui/AdminLayer.tsx
@AGENTS.md

<interfaces>
<!-- Interfaces and contracts the executor needs (extracted from Plan 48-01) -->
<!-- No codebase exploration needed — interfaces defined below -->

From `src/widgets/dashboard/ui/SpaceChrome.tsx` (created in Plan 48-01):

```typescript
'use client';
import { ReactNode } from 'react';

export interface SpaceChromeProps {
  children: ReactNode;
}

export function SpaceChrome({ children }: SpaceChromeProps): JSX.Element;
// Renders <ErrorBoundary><div className="flex min-h-screen bg-gray-50"> + SpaceLauncher
// + <main className="flex-1 min-w-0" style={{paddingBottom:'calc(4rem + env(safe-area-inset-bottom, 0px))'}}>
// + {children}</main></div><MobileSpaceBar /></ErrorBoundary>
```

Reference: `src/app/(tenant)/dashboard/layout.tsx` (refactored in Plan 48-02) is the structural template for the new admin layout — both files are 6-line client layouts that mount SpaceChrome.

Reference: `src/app/(tenant)/admin/page.tsx` (the existing root admin page) renders `<AdminLayer />` — AdminLayer is 196 lines, contains its own `p-6 max-w-5xl mx-auto space-y-6` wrapper at line 149, and renders the command bar + domain grid + activity stream. This becomes the first child of the new admin layout's `<main>`.

27 admin sub-pages (listed in research §"Duplicate-Chrome Risk") each own their own `max-w-* mx-auto` wrapper; none will double up with the chrome's passthrough `<main>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/app/(tenant)/admin/layout.tsx mounting SpaceChrome</name>
  <files>src/app/(tenant)/admin/layout.tsx</files>
  <read_first>
- src/app/(tenant)/dashboard/layout.tsx (refactored template from Plan 48-02 — both layouts are structurally identical thin client wrappers)
- src/app/(tenant)/admin/page.tsx (the existing root admin page; AdminLayer is rendered here — the new layout becomes its parent)
- src/widgets/dashboard/ui/SpaceChrome.tsx (the new shared component from Plan 48-01)
- src/widgets/dashboard/ui/AdminLayer.tsx (read line 149 — the `p-6 max-w-5xl mx-auto space-y-6` wrapper that becomes the "page width" inside the new chrome's passthrough `<main>`)
  </read_first>
  <action>
Create `src/app/(tenant)/admin/layout.tsx`:
1. File path: `/home/ubuntupunk/Projects/soralia-village/src/app/(tenant)/admin/layout.tsx`
2. File begins with `'use client';` (the layout is client because it instantiates the client-only SpaceChrome).
3. Single import: `import { SpaceChrome } from '@widgets/dashboard/ui/SpaceChrome';`
4. Export a default function: `export default function AdminLayout({ children }: { children: React.ReactNode }) {`
5. Body: `return <SpaceChrome>{children}</SpaceChrome>;`
6. Closing brace.
7. Final file shape (mirror of dashboard/layout.tsx):
   - Line 1: `'use client';`
   - Line 2: (blank)
   - Line 3: `import { SpaceChrome } from '@widgets/dashboard/ui/SpaceChrome';`
   - Line 4: (blank)
   - Line 5: `export default function AdminLayout({ children }: { children: React.ReactNode }) {`
   - Line 6: `  return <SpaceChrome>{children}</SpaceChrome>;`
   - Line 7: `}`
8. File should be ≤12 lines total.

Do NOT modify any of the 27 existing admin sub-pages. Do NOT modify `(tenant)/admin/page.tsx`. Do NOT modify the AdminLayer component. The new layout is purely additive — it sits between `(tenant)/layout.tsx` and the 27 admin sub-pages, transparently.
</action>
<verify>
<automated>ls -la 'src/app/(tenant)/admin/layout.tsx' | awk '{print $5, $NF}' && [ "$(wc -l &lt; 'src/app/(tenant)/admin/layout.tsx')" -le 12 ] && echo 'OK: layout.tsx is 12 lines or fewer' || echo 'FAIL: layout.tsx exceeds 12 lines'</automated>
</verify>
<done>

- `src/app/(tenant)/admin/layout.tsx` exists, is ≤12 lines, and contains exactly one import (SpaceChrome) and one default export (AdminLayout).
- The file body is a single line: `return <SpaceChrome>{children}</SpaceChrome>;` — no inline chrome logic, no local hooks.
- The file is marked `'use client';` at the top.
- No edits to any other file in this task.
- `pnpm typecheck` (deferred to Task 3) will confirm the layout compiles.
  </done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification on key admin routes (desktop + mobile + edge cases)</name>
  <files>none — verification only</files>
  <read_first>
- .planning/phases/48-admin-chrome-parity/48-RESEARCH.md (read the "Edge Cases" section starting at line 586 — covers the survey editor collision risk, the preview page decision, and the back-compat policy)
- src/app/(tenant)/admin/page.tsx (root admin — renders AdminLayer; visual check starts here)
- src/app/(tenant)/admin/requests/page.tsx (heaviest sub-page — table + drawer; visual check verifies no double-padding)
- src/app/(tenant)/admin/users/page.tsx (full-screen users list; verifies the `min-h-screen bg-gray-50` outer wrapper does not double up with the chrome)
- src/app/(tenant)/admin/surveys/[id]/edit/page.tsx (SurveyEditor — the only edge case flagged in research as warranting manual visual verification for collapsed-sidebar collision)
- src/app/(tenant)/admin/surveys/[id]/preview/page.tsx (full-screen survey preview — per research, keep chrome; "Back to editor" link is the primary exit)
  </read_first>
  <action>
Run a Next.js dev server locally (`pnpm dev` if not already running) and verify the chrome on the following routes. **Start the dev server before navigating**; this checkpoint cannot be automated — a human must confirm the visual behavior.

**DESKTOP viewport (≥md, default 1280×800) — log in as an ADMIN user (or board) first:**

1. Navigate to `/admin`. EXPECTED: SpaceLauncher (desktop sidebar) visible on the LEFT with 5 spaces; the "Admin" item is HIGHLIGHTED in indigo (active state). AdminLayer renders INSIDE the chrome's `<main>` — its `p-6 max-w-5xl mx-auto` wrapper produces a centered content block. The command bar, domain grid, and activity stream all render normally. NO double-padding visible.

2. Navigate to `/admin/users`. EXPECTED: SpaceLauncher still visible. "Admin" STILL highlighted. The `min-h-screen bg-gray-50` outer wrapper on this page should NOT visually duplicate the layout's `bg-gray-50` (they should look identical to before Phase 48). The users table renders normally inside `<main>`. NO extra `px-4` from the layout — there is none (the layout's `<main>` is a passthrough).

3. Navigate to `/admin/requests`. EXPECTED: Same chrome. "Admin" highlighted. Maintenance request cards render with their existing `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8` — no padding duplication.

4. Click the "Admin" entry in SpaceLauncher. EXPECTED: Navigates to `/admin`. "Admin" stays highlighted.

5. Click the "Home" entry in SpaceLauncher. EXPECTED: Navigates to `/dashboard` (NOT `/`). "Home" highlighted, "Admin" unhighlighted. The dashboard layout (from Plan 48-02) renders the same chrome.

6. Click the collapse/expand toggle in SpaceLauncher (bottom of sidebar). EXPECTED: Sidebar shrinks to 16px wide, content area expands. Navigate to `/admin/users` and back. EXPECTED: Collapse state persists (the layout is not unmounted, so `useState(collapsed)` is preserved).

7. Navigate to `/admin/surveys/[id]/edit` (replace `[id]` with any existing survey ID from the database). EXPECTED: SurveyEditor renders. **CRITICAL CHECK:** verify the editor's left edge does NOT visually collide with the collapsed sidebar (16px wide). TipTap toolbars should be top-anchored and start at the right edge of the sidebar. If any toolbar overflows INTO the sidebar's 16px region, file a BD issue and continue with the rest of the verification (do NOT block on this; it's a known risk documented in research §Edge Case 2).

8. Navigate to `/admin/surveys/[id]/preview` (same survey). EXPECTED: Survey preview renders with chrome. The "Back to editor" link at the top of the page is the primary exit; the sidebar is supplementary.

**MOBILE viewport (≤sm, default 375×667) — switch to mobile mode in browser dev tools:**

9. Navigate to `/admin`. EXPECTED: SpaceLauncher HIDDEN. MobileSpaceBar visible at the BOTTOM with 5 slots; "Admin" HIGHLIGHTED in indigo. AdminLayer renders above the bar with safe-area-aware bottom padding. No horizontal scrollbar.

10. Navigate to `/admin/users`. EXPECTED: Same MobileSpaceBar; "Admin" highlighted. The users table may be horizontally scrollable (its existing behavior — do not block on this).

11. Tap a MobileSpaceBar slot (e.g., "Home"). EXPECTED: Navigates to `/dashboard`; "Home" highlighted in the mobile bar.

**NON-ADMIN user (RESIDENT role) — log out, log back in as a RESIDENT, then:**

12. Navigate to `/admin/users` via direct URL (e.g., a stale bookmark). EXPECTED: SpaceLauncher visible but with only 4 spaces (NO "Admin" entry — filtered by `getVisibleSpaces`). MobileSpaceBar with 4 slots. The page itself may 403 or redirect per its own auth check (out of Phase 48 scope — not blocking).

13. Verify there is NO broken "Admin" link in the sidebar (the entry should not be rendered at all for non-admin roles).

**Cross-chrome sanity:**

14. From `/dashboard`, click the "Admin" nav item in SpaceLauncher. EXPECTED: Navigates to `/admin` (NOT `/dashboard/admin`). The link target is `SPACES.admin.href === '/admin'`.

15. From `/admin`, click "Home" in the sidebar. EXPECTED: Navigates to `/dashboard` (NOT `/`). The link target is `SPACES.home.href === '/dashboard'`.

**Resume signal:** Type "approved" if all 15 checks pass. If any check fails, describe which step and the observed behavior (e.g., "Step 7: SurveyEditor toolbar overlaps sidebar at 0-16px from viewport edge"). Do NOT mark as approved if a failure is observed — file a BD issue with the failure details, mark the corresponding step as failing, and continue.
</action>
<verify>
<human-check>15-step visual verification on /admin, /admin/users, /admin/requests, /admin/surveys/[id]/edit, /admin/surveys/[id]/preview (desktop + mobile + non-admin + cross-chrome) — described in detail in the task action above. Approver types "approved" or describes specific failures per step.</human-check>
</verify>
<done>

- All 15 visual checks performed by a human reviewer.
- If "approved": chrome works on /admin/\* desktop, mobile, and non-admin paths; active-space derivation correct; no duplicate padding on AdminLayer or any sub-page; cross-chrome navigation works (/dashboard ↔ /admin).
- If any step failed: BD issue filed with the specific step number + observed behavior; task marked as failed and routed back to the executor for fix.
  </done>
  </task>

<task type="auto">
  <name>Task 3: Final quality gate — pnpm typecheck and pnpm build</name>
  <files>none — quality gate only</files>
  <read_first>
- The completed `src/app/(tenant)/admin/layout.tsx` from Task 1
- `package.json` (find the `typecheck` and `build` scripts — typically `tsc --noEmit` and `next build`)
  </read_first>
  <action>
Run the project-wide quality gates to confirm Phase 48 ships clean:

1. Run `pnpm typecheck` from the worktree root. EXPECTED: exits 0. The new `admin/layout.tsx`, the refactored `SpaceChrome.tsx`, and the updated `SpaceLauncher.tsx` / `MobileSpaceBar.tsx` must all compile. The new `getActiveSpaceId` export must be reachable from `SpaceChrome`.

2. Run `pnpm build` from the worktree root. EXPECTED: exits 0. Next.js must successfully build the routes under both `/(tenant)/admin/*` (using the new `admin/layout.tsx`) and `/(tenant)/dashboard/*` (using the refactored `dashboard/layout.tsx`). No static analysis errors, no missing types, no broken imports.

3. If either command fails, DO NOT mark the task complete. Diagnose the error, fix the root cause (likely candidates: missing `SpaceChrome` import alias in vitest config, unhandled case in `getActiveSpaceId`, or an admin sub-page that imports `getActiveSpaceId` from the old path), and re-run.

4. If both pass, the phase is ready for summary.
   </action>
   <verify>
   <automated>pnpm typecheck &amp;&amp; pnpm build</automated>
   </verify>
   <done>

- `pnpm typecheck` exits 0 (TypeScript compilation passes with the new admin layout, the shared SpaceChrome component, and the refactored SpaceLauncher/MobileSpaceBar).
- `pnpm build` exits 0 (Next.js production build succeeds; both /admin/_ and /dashboard/_ routes are built with the shared chrome).
- No new warnings introduced beyond the existing baseline.
- No edits to any file in this task — it is a quality gate only.
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                                                                                                                                               |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Client → URL pathname          | The new admin layout mounts `SpaceChrome` for every /admin/\* route. The chrome does not add or remove auth checks — per-page auth is preserved.                          |
| Admin pages → Tenant isolation | The new layout sits at `(tenant)/admin/`, not at `(platform)/admin/platform/` (the latter is the platform admin surface, separate route group, out of scope per ROADMAP). |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                        | Disposition | Mitigation                                                                                                                                                                                                                                                                                        |
| ---------- | ---------------------- | ------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-48-03-SC | Tampering              | npm/pip/cargo installs                           | mitigate    | slopcheck — this plan creates 1 new file, no installs                                                                                                                                                                                                                                             |
| T-48-03-01 | Elevation of Privilege | New admin/layout.tsx exposes chrome to /admin/\* | accept      | The admin space is already filtered by `getVisibleSpaces(role, flags)` inside `SpaceChrome` — non-admin users do not see the admin entry. The page-level auth check on each admin sub-page is unchanged. A non-admin user reaching /admin/\* via direct URL still gets the existing 403/redirect. |
| T-48-03-02 | Tampering              | AdminLayer double-padding                        | mitigate    | The new layout's `<main>` is a passthrough (no padding, no max-width). AdminLayer's own `p-6 max-w-5xl mx-auto space-y-6` is the only width constraint. Verified by Task 2 visual check step 1.                                                                                                   |
| T-48-03-03 | Repudiation            | Sub-page wrapper duplication                     | mitigate    | Each admin sub-page owns its own `max-w-* mx-auto` wrapper (per research §Duplicate-Chrome Risk). The chrome adds none. Task 2 visual check steps 2-3 confirm no double-padding on `/admin/users` and `/admin/requests`.                                                                          |
| T-48-03-04 | Information Disclosure | SurveyEditor collision                           | mitigate    | The collapsed sidebar is 16px wide. TipTap toolbars in the SurveyEditor are top-anchored and start at the right edge of the sidebar's column. Task 2 visual check step 7 explicitly verifies this; if collision is observed, a BD issue is filed for follow-up (not blocking the phase).          |
| T-48-03-05 | Elevation of Privilege | Stale `/dashboard/admin` legacy route            | accept      | Preserved intentionally per Phase 37. The active-space derivation still resolves `/dashboard/admin` to `'admin'` (back-compat). No new attack surface — the route was already reachable.                                                                                                          |

**No new ASVS concerns introduced.** The admin layout is structurally identical to the dashboard layout (both mount `<SpaceChrome>`); security posture is the same as the dashboard's, which has been in production since Phase 30.
</threat_model>

<verification>
- [ ] `src/app/(tenant)/admin/layout.tsx` exists and is ≤12 lines (wc -l confirms)
- [ ] The new layout contains exactly one import: `SpaceChrome` from `@widgets/dashboard/ui/SpaceChrome`
- [ ] No modifications to any of the 27 admin sub-pages
- [ ] No modifications to `(tenant)/admin/page.tsx`
- [ ] No modifications to AdminLayer or any other widget
- [ ] `pnpm typecheck` exits 0 (Task 3)
- [ ] `pnpm build` exits 0 (Task 3)
- [ ] `pnpm vitest run src/widgets/dashboard/model/` exits 0 (Plan 48-01's tests pass; backstop for the active-space derivation used by the new admin chrome)
- [ ] Task 2 visual verification completed by a human reviewer — all 15 checks pass, OR a BD issue is filed for any failure
- [ ] Worktree workflow: execute in a fresh git worktree per AGENTS.md worktree isolation rule
</verification>

<success_criteria>

- `src/app/(tenant)/admin/layout.tsx` is created, ≤12 lines, and mounts `<SpaceChrome>`
- ADMIN-CHROME-01: SpaceLauncher is visible on all /admin/\* routes (desktop, md+) — verified by Task 2 step 1
- ADMIN-CHROME-02: MobileSpaceBar is visible on /admin/\* at mobile breakpoint — verified by Task 2 step 9
- ADMIN-CHROME-03: Active-space derivation correctly identifies "admin" as the active space on /admin/\* — verified by Task 2 steps 1, 2, 9 and backed by Plan 48-01's 16-case unit test
- ADMIN-CHROME-04: No duplicate chrome (no double-padding, no double-max-width) on AdminLayer or any admin sub-page — verified by Task 2 steps 1-3
- ADMIN-CHROME-05: /dashboard/\* chrome unchanged — verified by Plan 48-02's regression-free refactor
- `pnpm typecheck && pnpm build` exits 0
- No new packages, no auth changes, no file moves
  </success_criteria>

<output>
Create `.planning/phases/48-admin-chrome-parity/48-03-SUMMARY.md` when done
</output>
