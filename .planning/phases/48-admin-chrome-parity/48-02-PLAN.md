---
phase: 48-admin-chrome-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - src/app/(tenant)/dashboard/layout.tsx
autonomous: true
requirements:
  - ADMIN-CHROME-05
must_haves:
  truths:
    - 'Visiting /dashboard still shows SpaceLauncher + MobileSpaceBar chrome with identical behavior'
    - "Visiting /dashboard/services still highlights the 'services' space in the sidebar"
    - "Visiting /dashboard/admin (legacy route) still highlights the 'admin' space"
    - 'No double-padding or double-max-width on existing dashboard pages'
    - 'The dashboard layout file is now under 15 lines (was 67)'
  artifacts:
    - path: 'src/app/(tenant)/dashboard/layout.tsx'
      provides: 'Thin client layout that mounts <SpaceChrome> for /dashboard/* routes'
      max_lines: 15
      contains: 'SpaceChrome'
  key_links:
    - from: 'src/app/(tenant)/dashboard/layout.tsx'
      to: 'src/widgets/dashboard/ui/SpaceChrome.tsx'
      via: 'import + render as the chrome host'
      pattern: 'import.*SpaceChrome.*widgets/dashboard'
---

<objective>
Refactor `src/app/(tenant)/dashboard/layout.tsx` to consume the new shared `SpaceChrome` component, replacing the 67-line inline chrome with a thin one-liner layout. The behavior must be byte-equivalent to before — same hooks, same JSX structure, same active-space derivation (now via `getActiveSpaceId` from the model), same visible-spaces filtering.

Purpose: This is the consumption-side validation of Wave 1. If `SpaceChrome` is correctly extracted, the dashboard layout shrinks dramatically with zero behavior change. If anything regresses, it surfaces here before Plan 48-03 mounts the same chrome under `/admin/*`.

Output: 1 file modified (dashboard/layout.tsx shrinks from 67 lines to ≤12).

</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-admin-chrome-parity/48-RESEARCH.md
@src/app/(tenant)/dashboard/layout.tsx
@src/widgets/dashboard/ui/SpaceChrome.tsx
@src/widgets/dashboard/model/spaces.ts
@AGENTS.md

<interfaces>
<!-- Interfaces and contracts the executor needs (extracted from Plan 48-01) -->
<!-- No codebase exploration needed — interfaces defined below -->

From `src/widgets/dashboard/ui/SpaceChrome.tsx` (created in Plan 48-01):

```typescript
'use client';
import { ReactNode } from 'react';

export interface SpaceChromeProps {
  children: ReactNode;
}

export function SpaceChrome({ children }: SpaceChromeProps): JSX.Element;
// Renders <ErrorBoundary><div className="flex min-h-screen bg-gray-50"> + SpaceLauncher
// + <main className="flex-1 min-w-0" style={{paddingBottom:'calc(4rem + env(safe-area-inset-bottom, 0px))'}}>
// + {children}</main></div><MobileSpaceBar /></ErrorBoundary>
```

The component self-contains: `useState(collapsed)`, `usePathname()`, `authClient.useSession()`, `usePageFlags()`, `getActiveSpaceId(pathname)`, `getVisibleSpaces(role, flags)`. The layout file does NOT need to import any of these — `SpaceChrome` handles them internally.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor dashboard/layout.tsx to consume SpaceChrome</name>
  <files>src/app/(tenant)/dashboard/layout.tsx</files>
  <read_first>
- src/app/(tenant)/dashboard/layout.tsx (the file being modified — currently 67 lines with the inline chrome; the goal is to delete everything except the wrapper and a single import)
- src/widgets/dashboard/ui/SpaceChrome.tsx (the new shared component — verify the export signature and the hooks it internally manages so the layout file can drop them)
- src/widgets/dashboard/model/spaces.ts (no longer needs to be imported by the layout since SpaceChrome handles `getActiveSpaceId` and `getVisibleSpaces` internally — confirm before deleting the import)
  </read_first>
  <action>
Modify `src/app/(tenant)/dashboard/layout.tsx`:
1. Keep the `'use client';` directive at the top (the layout itself is still a client component because it instantiates the client-only `SpaceChrome`).
2. Delete ALL of the following: `useState` import, `usePathname` import, `authClient` import, `usePageFlags` import, `ErrorBoundary` import, `SpaceLauncher` import, `MobileSpaceBar` import, `getVisibleSpaces, resolveSpace` import. The layout no longer needs any of these because `SpaceChrome` encapsulates them.
3. Add a single import: `import { SpaceChrome } from '@widgets/dashboard/ui/SpaceChrome';`
4. Replace the body of `DashboardLayout` (currently a multi-line return with useState, usePathname, session, flags, role, activeSpaceId, visibleSpaces, handleNavigate, and the JSX with ErrorBoundary + flex div + SpaceLauncher + main + MobileSpaceBar) with a single return: `return <SpaceChrome>{children}</SpaceChrome>;`
5. Delete the trailing `function getActiveSpaceId(pathname: string): string { ... }` helper at the bottom of the file (this is now imported from `@widgets/dashboard/model/spaces` — only Plan 48-03 needs to consume it from there; the layout doesn't need it at all).
6. Final file shape: 6-line client layout:
   - Line 1: `'use client';`
   - Line 2: (blank)
   - Line 3: `import { SpaceChrome } from '@widgets/dashboard/ui/SpaceChrome';`
   - Line 4: (blank)
   - Line 5: `export default function DashboardLayout({ children }: { children: React.ReactNode }) {`
   - Line 6: `  return <SpaceChrome>{children}</SpaceChrome>;`
   - Line 7: `}`
7. Verify the final file is ≤12 lines (including blank lines and the closing brace).

After the file edit, run `pnpm typecheck` to confirm the layout compiles (it now only depends on the SpaceChrome export, which Plan 48-01 created).
</action>
<verify>
<automated>pnpm typecheck</automated>
</verify>
<done>

- `src/app/(tenant)/dashboard/layout.tsx` exists and is ≤12 lines total.
- The file imports only `SpaceChrome` from `@widgets/dashboard/ui/SpaceChrome` and `React` (implicitly via `React.ReactNode` type) — no other imports.
- The file contains exactly one `export default function DashboardLayout` returning `<SpaceChrome>{children}</SpaceChrome>`.
- The local `getActiveSpaceId` function is removed (it now lives in the model file).
- `pnpm typecheck` exits 0.
- `pnpm vitest run src/widgets/dashboard/model/` exits 0 (the new getActiveSpaceId tests from Plan 48-01 still pass — they back the active-space derivation that the layout now delegates to SpaceChrome).
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary               | Description                                                                                                                                               |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Client → `SpaceChrome` | The layout now mounts `SpaceChrome` as a black-box chrome host. The boundary shifts one level down: the layout owns no chrome logic; SpaceChrome owns it. |

## STRIDE Threat Register

| Threat ID  | Category               | Component              | Disposition | Mitigation                                                                                                                                                                                                                                  |
| ---------- | ---------------------- | ---------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-48-02-SC | Tampering              | npm/pip/cargo installs | mitigate    | slopcheck — this plan modifies 1 file, no installs                                                                                                                                                                                          |
| T-48-02-01 | Tampering              | dashboard/layout.tsx   | mitigate    | Refactor only — no new auth surface, no new data flow, no new URL handling. Behavior is byte-equivalent to before.                                                                                                                          |
| T-48-02-02 | Information Disclosure | Behavior regression    | mitigate    | Active-space derivation now lives in `getActiveSpaceId` exported from the model; Plan 48-01's 16-case unit test covers the full derivation surface. A regression in the layout would either be a TS compile error or a vitest test failure. |
| T-48-02-03 | Elevation of Privilege | Layout chrome          | accept      | No auth check changes. The SpaceLauncher sidebar continues to be filtered by `getVisibleSpaces(role, flags)` inside SpaceChrome, with the same `minimumRole: 'admin'` constraint on the admin space.                                        |

**No new ASVS concerns introduced.** The dashboard layout's security posture is unchanged — it delegates the same logic to `SpaceChrome` instead of inlining it.
</threat_model>

<verification>
- [ ] `src/app/(tenant)/dashboard/layout.tsx` is ≤12 lines (wc -l confirms)
- [ ] The file contains exactly one import: `SpaceChrome` from `@widgets/dashboard/ui/SpaceChrome`
- [ ] No local `getActiveSpaceId` helper remains in the file
- [ ] `pnpm typecheck` exits 0
- [ ] `pnpm vitest run src/widgets/dashboard/model/` exits 0 (Plan 48-01's tests pass)
- [ ] No edits to any file outside `src/app/(tenant)/dashboard/layout.tsx`
- [ ] Worktree workflow: execute in a fresh git worktree per AGENTS.md worktree isolation rule
- [ ] Visual smoke (informal, not gated): navigate to `/dashboard`, `/dashboard/services`, `/dashboard/admin` (legacy) and confirm sidebar visible, mobile bar visible at mobile viewport, active space highlights correctly
</verification>

<success_criteria>

- The dashboard layout file is reduced from 67 lines to ≤12 lines
- All existing dashboard pages (`/dashboard`, `/dashboard/[space]`, etc.) continue to render with the same chrome (SpaceLauncher + MobileSpaceBar)
- The legacy `/dashboard/admin` route still resolves to the 'admin' space in the active-state derivation
- No new files, no new packages, no auth changes
- `pnpm typecheck` passes; Plan 48-01's unit tests pass
  </success_criteria>

<output>
Create `.planning/phases/48-admin-chrome-parity/48-02-SUMMARY.md` when done
</output>
