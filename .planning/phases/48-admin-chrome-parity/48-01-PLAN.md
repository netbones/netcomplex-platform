---
phase: 48-admin-chrome-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/dashboard/model/spaces.ts
  - src/widgets/dashboard/model/active-space.test.ts
  - src/widgets/dashboard/model/spaces.test.ts
  - src/widgets/dashboard/ui/SpaceChrome.tsx
  - src/widgets/dashboard/ui/SpaceLauncher.tsx
  - src/widgets/dashboard/ui/MobileSpaceBar.tsx
autonomous: true
requirements:
  - ADMIN-CHROME-03
must_haves:
  truths:
    - "getActiveSpaceId('/admin') and getActiveSpaceId('/admin/...') both return 'admin'"
    - "getActiveSpaceId('/dashboard/admin') still returns 'admin' (back-compat for legacy route)"
    - "getActiveSpaceId('/adminusers') returns 'home' (prefix must be followed by '/' or end)"
    - 'Each SpaceDefinition entry in the SPACES registry has an `href` field'
    - "SpaceLauncher's sidebar links use space.href (no inline ternary)"
    - "MobileSpaceBar's href and isActive both use space.href as the base path"
    - 'SpaceChrome renders SpaceLauncher + passthrough <main> + MobileSpaceBar with the same logic the old dashboard/layout.tsx had'
  artifacts:
    - path: 'src/widgets/dashboard/ui/SpaceChrome.tsx'
      provides: 'Shared client component for SpaceLauncher + <main> + MobileSpaceBar chrome'
      exports: ['SpaceChrome']
      min_lines: 60
      max_lines: 120
    - path: 'src/widgets/dashboard/model/spaces.ts'
      provides: 'SpaceDefinition with required `href` field; exported `getActiveSpaceId(pathname)`'
      exports:
        [
          'SpaceDefinition',
          'SPACES',
          'SpaceId',
          'getActiveSpaceId',
          'getVisibleSpaces',
          'resolveSpace',
        ]
      contains: 'getActiveSpaceId'
    - path: 'src/widgets/dashboard/model/active-space.test.ts'
      provides: 'Vitest unit tests for getActiveSpaceId'
      min_lines: 40
    - path: 'src/widgets/dashboard/model/spaces.test.ts'
      provides: 'Vitest assertions for SPACES href registry'
      min_lines: 20
  key_links:
    - from: 'src/widgets/dashboard/ui/SpaceChrome.tsx'
      to: 'src/widgets/dashboard/model/spaces.ts'
      via: 'getActiveSpaceId(pathname) + getVisibleSpaces(role, flags)'
      pattern: 'getActiveSpaceId|getVisibleSpaces'
    - from: 'src/widgets/dashboard/ui/SpaceLauncher.tsx'
      to: 'src/widgets/dashboard/model/spaces.ts'
      via: 'space.href consumed in <Link href={space.href}>'
      pattern: "space\\.href"
    - from: 'src/widgets/dashboard/ui/MobileSpaceBar.tsx'
      to: 'src/widgets/dashboard/model/spaces.ts'
      via: 'space.href consumed in <Link href={space.href}> and isActive base path'
      pattern: "space\\.href"
---

<objective>
Extract the dashboard chrome into a reusable `SpaceChrome` client component, add an `href` field to `SpaceDefinition` (one source of truth for space URLs), and centralize the active-space derivation as an exported `getActiveSpaceId` helper that recognizes both `/dashboard/<slug>` and the canonical `/admin/*` prefix.

Purpose: This is the foundation for Wave 2 plans. After this plan ships, `dashboard/layout.tsx` shrinks to a one-liner, a new `admin/layout.tsx` can mount the same chrome, and the active-space derivation is unit-testable.

Output: 4 new files (SpaceChrome.tsx, 2 test files, 1 model file edit) + 2 component updates (SpaceLauncher, MobileSpaceBar).

</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-admin-chrome-parity/48-RESEARCH.md
@src/widgets/dashboard/model/spaces.ts
@src/widgets/dashboard/ui/SpaceLauncher.tsx
@src/widgets/dashboard/ui/MobileSpaceBar.tsx
@src/app/(tenant)/dashboard/layout.tsx
@src/entities/tenant/api/flags/platform-flags.test.ts
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `href` to SpaceDefinition + export `getActiveSpaceId` (TDD)</name>
  <files>src/widgets/dashboard/model/spaces.ts, src/widgets/dashboard/model/active-space.test.ts, src/widgets/dashboard/model/spaces.test.ts</files>
  <read_first>
- src/widgets/dashboard/model/spaces.ts (source of truth — the file being modified)
- src/app/(tenant)/dashboard/layout.tsx (current location of the inline `getActiveSpaceId` and its regex `/^\/dashboard\/([^/]+)/`)
- src/entities/tenant/api/flags/platform-flags.test.ts (test pattern reference: hoisted mocks, describe/it blocks, single-source-of-truth assertions)
  </read_first>
  <behavior>
- getActiveSpaceId('') returns 'home'
- getActiveSpaceId('/dashboard') returns 'home'
- getActiveSpaceId('/dashboard/') returns 'home'
- getActiveSpaceId('/dashboard/services') returns 'services'
- getActiveSpaceId('/dashboard/community') returns 'community'
- getActiveSpaceId('/dashboard/messages') returns 'messages'
- getActiveSpaceId('/dashboard/messages/announcements') returns 'messages'
- getActiveSpaceId('/dashboard/admin') returns 'admin' (legacy back-compat)
- getActiveSpaceId('/dashboard/admin/users') returns 'admin' (legacy back-compat)
- getActiveSpaceId('/admin') returns 'admin' (canonical)
- getActiveSpaceId('/admin/') returns 'admin'
- getActiveSpaceId('/admin/users') returns 'admin'
- getActiveSpaceId('/admin/surveys/abc-123/preview') returns 'admin'
- getActiveSpaceId('/adminusers') returns 'home' (prefix must be followed by '/' or end — not a false positive)
- getActiveSpaceId('/dashboard/unknown-slug') returns 'home'
- getActiveSpaceId('/something/else') returns 'home'
- SPACES.home.href === '/dashboard'
- SPACES.services.href === '/dashboard/services'
- SPACES.community.href === '/dashboard/community'
- SPACES.messages.href === '/dashboard/messages'
- SPACES.admin.href === '/admin'
  </behavior>
  <action>
Modify `src/widgets/dashboard/model/spaces.ts`:
1. Add `href: string` as a REQUIRED field to the `SpaceDefinition` interface (after `id: SpaceId`, before `labelKey`). All 5 SPACES entries must populate it.
2. Populate href on every SPACES entry: `home` → '/dashboard', `services` → '/dashboard/services', `community` → '/dashboard/community', `messages` → '/dashboard/messages', `admin` → '/admin'.
3. Add a new exported function `getActiveSpaceId(pathname: string): SpaceId | 'home'` with logic: (a) empty pathname → 'home'; (b) pathname === '/admin' OR pathname.startsWith('/admin/') → 'admin'; (c) match `/^\/dashboard\/([^/]+)/` and if `resolveSpace(slug)` is truthy, return slug; (d) otherwise → 'home'.
4. Remove the local `getActiveSpaceId` function from `src/app/(tenant)/dashboard/layout.tsx` ONLY in Plan 48-02 (out of scope for this plan — the dashboard layout's local function stays until Plan 48-02 refactors it; both files can co-exist during Wave 1 → Wave 2 transition).

Create `src/widgets/dashboard/model/active-space.test.ts`:

- Vitest file using `describe('getActiveSpaceId', ...)` with one `it` per behavior case above (16 cases).
- No mocks needed (pure function).
- Import: `import { getActiveSpaceId } from './spaces';`

Create `src/widgets/dashboard/model/spaces.test.ts`:

- Vitest file using `describe('SPACES href registry', ...)` with 5 assertions mapping each SpaceId to its expected href string.
- Import: `import { SPACES, type SpaceId } from './spaces';`

Run the test command to confirm all pass: `pnpm test -- spaces.test active-space` (or `pnpm vitest run src/widgets/dashboard/model/active-space.test.ts src/widgets/dashboard/model/spaces.test.ts` if the workspace pattern doesn't match).
</action>
<verify>
<automated>pnpm vitest run src/widgets/dashboard/model/active-space.test.ts src/widgets/dashboard/model/spaces.test.ts</automated>
</verify>
<done>

- `src/widgets/dashboard/model/spaces.ts` declares `href: string` on `SpaceDefinition`; all 5 SPACES entries have it; `getActiveSpaceId` is exported with the admin-prefix branch and `SpaceId | 'home'` return type.
- `src/widgets/dashboard/model/active-space.test.ts` exists and covers all 16 behavior cases (empty, dashboard root, dashboard slugs, admin prefix, legacy /dashboard/admin back-compat, /adminusers false-positive guard).
- `src/widgets/dashboard/model/spaces.test.ts` exists and asserts all 5 space hrefs.
- `pnpm vitest run` on the two new test files exits 0 with all tests passing.
- `pnpm typecheck` exits 0 (the new required `href` field compiles with all SPACES entries populated).
  </done>
  </task>

<task type="auto">
  <name>Task 2: Create SpaceChrome.tsx + update SpaceLauncher and MobileSpaceBar to use space.href</name>
  <files>src/widgets/dashboard/ui/SpaceChrome.tsx, src/widgets/dashboard/ui/SpaceLauncher.tsx, src/widgets/dashboard/ui/MobileSpaceBar.tsx</files>
  <read_first>
- src/app/(tenant)/dashboard/layout.tsx (source of truth for the chrome to extract — useState(collapsed), usePathname, authClient.useSession, usePageFlags, getVisibleSpaces, getActiveSpaceId, ErrorBoundary, SpaceLauncher + passthrough <main> with safe-area bottom padding + MobileSpaceBar)
- src/widgets/dashboard/ui/SpaceLauncher.tsx (file being modified at line 36 — replace inline ternary `const href = space.id === 'home' ? '/dashboard' : \`/dashboard/${space.id}\`` with `const href = space.href`)
- src/widgets/dashboard/ui/MobileSpaceBar.tsx (file being modified at lines 51-56 — the inline `isActive` helper that special-cases home; and at line 72 — the same inline href ternary; both should use `space.href` and the helper should derive base path from `space.href`)
- src/widgets/dashboard/ui/SpaceChrome.tsx (new file — design template at .planning/phases/48-admin-chrome-parity/48-RESEARCH.md lines 282-329 — Pattern 1: Extract client layout chrome to a component)
  </read_first>
  <action>
Create `src/widgets/dashboard/ui/SpaceChrome.tsx`:
1. File begins with `'use client';`
2. Import: `useState` from 'react', `usePathname` from 'next/navigation', `authClient` from '@shared/api/auth-client', `usePageFlags` from '@/shared/lib/hooks/usePageFlags', `ErrorBoundary` from '@shared/ui', `SpaceLauncher` from './SpaceLauncher', `MobileSpaceBar` from './MobileSpaceBar', `getVisibleSpaces` AND `getActiveSpaceId` from '../model/spaces' (not `resolveSpace` — that's only needed in the model's internal logic).
3. Export `SpaceChrome` as a named function taking `{ children: React.ReactNode }` props.
4. Internal hooks (in this exact order): `const [collapsed, setCollapsed] = useState(true);` `const pathname = usePathname();` `const { data: session } = authClient.useSession();` `const { flags } = usePageFlags();`
5. Compute `const role = session?.user?.role || 'RESIDENT';` then `const activeSpaceId = getActiveSpaceId(pathname);` then `const visibleSpaces = flags ? getVisibleSpaces(role, flags) : [];` (empty list when flags not loaded — matches the existing dashboard layout pattern).
6. Define a no-op `const handleNavigate = (_spaceId: string) => {};` (preserves the existing callback signature in case the SpaceLauncher's prop contract changes).
7. Return JSX: `<ErrorBoundary>` wrapping `<div className="flex min-h-screen bg-gray-50">` which contains `<SpaceLauncher spaces={visibleSpaces} activeSpaceId={activeSpaceId} collapsed={collapsed} onNavigate={handleNavigate} onToggleCollapse={() => setCollapsed(prev => !prev)} />` and `<main className="flex-1 min-w-0 md:pb-0" style={{ paddingBottom: 'calc(4rem + env(safe-area-inset-bottom, 0px))' }}>{children}</main>`, then `<MobileSpaceBar />` as a sibling of the flex div (preserving the existing structure where the mobile bar is outside the flex parent). Close `</ErrorBoundary>`.
8. Keep file under 90 lines (well under the AGENTS.md 200-line cap).

Update `src/widgets/dashboard/ui/SpaceLauncher.tsx`:

- Line 36: replace `const href = space.id === 'home' ? '/dashboard' : \`/dashboard/${space.id}\`;`with`const href = space.href;` (single source of truth, removes the home special case).
- No other changes — the surrounding JSX, classes, icon rendering, collapse toggle, and isActive logic all stay the same.

Update `src/widgets/dashboard/ui/MobileSpaceBar.tsx`:

- Line 51-56: replace the `isActive` helper with: `const isActive = (spaceId: SpaceId): boolean => { const base = SPACES[spaceId].href; return pathname === base || pathname.startsWith(base + '/'); };` (generalizes the home special case; uses space.href so admin works automatically).
- Line 72: replace `href={space.id === 'home' ? '/dashboard' : \`/dashboard/${space.id}\`}`with`href={space.href}`.
- Add `SPACES` to the existing import from `'../model/spaces'` (line 8 currently imports `getVisibleSpaces, type SpaceId` — extend to `getVisibleSpaces, SPACES, type SpaceId`).
- No other changes.

After all three file changes, run `pnpm typecheck` to confirm no compilation errors (the new required `href` field on SpaceDefinition propagates through the consumers).
</action>
<verify>
<automated>pnpm typecheck</automated>
</verify>
<done>

- `src/widgets/dashboard/ui/SpaceChrome.tsx` exists, is marked `'use client'`, exports `SpaceChrome`, and renders the exact JSX structure from the existing `dashboard/layout.tsx` (flex container, SpaceLauncher, passthrough main with safe-area padding, MobileSpaceBar) inside an ErrorBoundary.
- `src/widgets/dashboard/ui/SpaceLauncher.tsx` line 36 is now `const href = space.href;` (no inline ternary).
- `src/widgets/dashboard/ui/MobileSpaceBar.tsx` line 51-56's `isActive` uses `SPACES[spaceId].href`; line 72's href is `space.href`; the import from `../model/spaces` includes `SPACES`.
- `pnpm typecheck` exits 0 (the `href` field propagates correctly through all 3 modified files).
- `pnpm vitest run src/widgets/dashboard/model/` exits 0 (all existing tests still pass; new tests for `getActiveSpaceId` and `SPACES.href` pass).
  </done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary              | Description                                                                                                                              |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Client → URL pathname | The `usePathname()` hook returns the current route. The derivation only maps this to a space ID; no mutation, no DB call, no auth check. |
| Client → session role | `authClient.useSession()` returns the real role; `getVisibleSpaces(role, flags)` filters spaces at render time. Same boundary as today.  |

## STRIDE Threat Register

| Threat ID  | Category               | Component                      | Disposition | Mitigation                                                                                                                                           |
| ---------- | ---------------------- | ------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-48-01-SC | Tampering              | npm/pip/cargo installs         | mitigate    | slopcheck — this plan touches no new packages; all changes are local file edits                                                                      |
| T-48-01-01 | Tampering              | `getActiveSpaceId`             | accept      | Pure function; returns only registry IDs. A malicious pathname cannot affect state — it can only cause the wrong space to be highlighted (cosmetic). |
| T-48-01-02 | Elevation of Privilege | `SpaceChrome` admin visibility | accept      | No auth check is added or removed. The admin space continues to be filtered by `getVisibleSpaces(role, flags)` — same source of truth as today.      |
| T-48-01-03 | Repudiation            | `space.href` registry          | mitigate    | The href is now driven by a single registry field. Drift between SpaceLauncher's ternary and MobileSpaceBar's ternary is structurally impossible.    |
| T-48-01-04 | Information Disclosure | <main> passthrough             | accept      | The new component renders children verbatim. No new data exposure surface.                                                                           |

**No new ASVS concerns introduced.** This plan is a strict refactor of existing layout chrome; the security posture is unchanged from `src/app/(tenant)/dashboard/layout.tsx`.
</threat_model>

<verification>
- [ ] `pnpm typecheck` exits 0
- [ ] `pnpm vitest run src/widgets/dashboard/model/` exits 0 (new tests for `getActiveSpaceId` and `SPACES.href` pass; existing tests unchanged)
- [ ] All 5 SpaceDefinition entries in `SPACES` have an `href` field (grep confirms)
- [ ] `SpaceChrome.tsx` exists and exports `SpaceChrome` (grep for `export function SpaceChrome` returns 1 match)
- [ ] `SpaceLauncher.tsx` line 36 contains `space.href` (not the old ternary)
- [ ] `MobileSpaceBar.tsx` line 72 contains `space.href` (not the old ternary)
- [ ] No new package installations
- [ ] No edits to `(tenant)/layout.tsx` (tenant root layout stays chrome-free)
- [ ] Worktree workflow: execute in a fresh git worktree per AGENTS.md worktree isolation rule
</verification>

<success_criteria>

- `getActiveSpaceId` correctly handles all 16 documented test cases (including the `/adminusers` false-positive guard)
- `SpaceDefinition.href` is a required field with the admin space pointing to `/admin` and the other 4 spaces pointing to `/dashboard/<slug>`
- `SpaceChrome` is a self-contained client component that any layout can mount with `<SpaceChrome>{children}</SpaceChrome>`
- Both `SpaceLauncher` and `MobileSpaceBar` consume `space.href` (no inline ternaries remain for the admin special case)
- All unit tests pass; typecheck passes; no new packages installed
  </success_criteria>

<output>
Create `.planning/phases/48-admin-chrome-parity/48-01-SUMMARY.md` when done
</output>
