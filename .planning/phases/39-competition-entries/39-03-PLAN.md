---
phase: 39-competition-entries
plan: 03
type: execute
wave: 3
depends_on:
  - 39-02
files_modified:
  - src/app/competition/page.tsx # REWRITE — cards grid of active competitions
  - src/app/competition/[id]/page.tsx # NEW — competition detail page
  - src/shared/lib/navigation.ts # update href if needed
autonomous: false
requirements:
  - COMP-ENTRY-05

must_haves:
  truths:
    - User sees cards of all ACTIVE competitions on /competition
    - Each card shows title, description snippet, date range, type badge, participant avatars, entry count
    - Clicking a card navigates to /competition/[id]
    - /competition/[id] shows full competition details
    - /competition/[id] has type-specific action (Join/Submit Entry/View Scores)
    - /competition/[id] shows participant list with avatars
    - /competition/[id] has winners gallery for completed competitions
    - All pages handle loading, empty, and error states
  artifacts:
    - path: 'src/app/competition/page.tsx'
      provides: 'Cards grid listing active competitions'
      min_lines: 80
    - path: 'src/app/competition/[id]/page.tsx'
      provides: 'Competition detail with join/submit/participants/winners'
      min_lines: 200
  key_links:
    - from: 'src/app/competition/page.tsx'
      to: 'trpc.competitions.listPublicCompetitions'
      via: 'React Query useQuery'
    - from: 'src/app/competition/[id]/page.tsx'
      to: 'trpc.competitions.getCompetitionDetail'
      via: 'React Query useQuery'
    - from: 'src/app/competition/[id]/page.tsx'
      to: 'trpc.competitions.listWinners'
      via: 'React Query useQuery'
    - from: 'src/app/competition/[id]/page.tsx'
      to: 'trpc.competitions.joinCompetition or submitPhotoEntry'
      via: 'React Query useMutation'
---

<objective>
Create the public competition pages — cards grid listing all active competitions, and individual competition detail page with join/submit, participant list, and winners gallery.

Purpose: Users can discover and participate in competitions. Competitions are visible, joinable, and winners are celebrated publicly.
Output: Rewritten /competition page (cards grid) + new /competition/[id] detail page.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/39-competition-entries/39-CONTEXT.md
@.planning/phases/39-competition-entries/39-02-PLAN.md
@src/app/competition/page.tsx
@src/app/providers.tsx
@src/shared/api/trpc/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /competition page to show competition cards grid</name>
  <files>
    src/app/competition/page.tsx  # REWRITE
  </files>
  <action>
    Rewrite `src/app/competition/page.tsx` to show a cards grid of all ACTIVE competitions.

    Component requirements:
    - Use `'use client'` directive
    - Import `trpc` from `@api/trpc/client` (the tRPC React client)
    - Fetch competitions via `trpc.competitions.listPublicCompetitions.useQuery()` (React Query integration)
    - On mount, use the tRPC hook which handles loading/error states automatically
    - Track user session via the auth client to determine if the user can join

    Card design (each card):
    - Background: white rounded-lg shadow, hover:shadow-md transition, cursor pointer
    - Click → navigate to `/competition/[id]` via `next/navigation` `useRouter`
    - Top section: type badge (colored — RAFFLE=purple, PHOTO=blue, SCORE=green) + status badge (green)
    - Title in text-lg font-semibold
    - Description snippet (first 120 chars + "...")
    - Date range row: calendar icon + "May 30 – Jun 6, 2026" format
    - Bottom row: participant avatars (stacked circles, max 5, with "+N more" overflow) + entry count
    - Prize info if available: small trophy icon with "Prize: ..." text

    Grid layout:
    - Responsive: 1 col mobile, 2 cols tablet, 3 cols desktop
    - `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`

    State handling:
    - Loading: show skeleton cards (pulsing gray rectangles mimicking card layout)
    - Empty: "No Active Competitions" message with illustration/icon, "Check back soon"
    - Error: toast error + retry button
    - Empty results (no active comps): friendly message with trophy icon

    Page layout:
    - Header: "Competitions" title + "Join our community competitions!" subtitle
    - Cards grid below
    - Keep the `ErrorBoundary` wrapper from existing code

    Participant avatars:
    - Stacked horizontal circles: first avatar at z-10, next at z-0 with -ml-2, etc.
    - Use user.avatar from participant data
    - If less than 5 total, just show the count text
    - Show "0 participants" when count is 0

  </action>
  <verify>
    <automated>
      <sampling_rate>run after task commits</sampling_rate>
    </automated>
    <manual>
      1. Visit /competition with active competitions in DB → cards grid shows
      2. Each card shows title, description, date, type badge, avatars
      3. Click card → navigates to /competition/[id]
      4. If no active comps → friendly empty state
    </manual>
  </verify>
  <done>
    /competition page shows responsive cards grid of ACTIVE competitions with participant avatars. Loading, empty, and error states handled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /competition/[id] detail page</name>
  <files>
    src/app/competition/[id]/page.tsx  # NEW
  </files>
  <action>
    Create `src/app/competition/[id]/page.tsx` as a client component.

    Fetch competition detail via `trpc.competitions.getCompetitionDetail.useQuery({ id })`.

    **Page sections (top to bottom):**

    1. **Hero section:**
    - If image exists: full-width banner image with overlay gradient
    - Competition title (text-3xl font-bold)
    - Type badge (colored) + status badge
    - Description paragraph

    2. **Info grid** (2 columns on desktop):
    - Start date
    - End date
    - Entry count / participant count
    - Max participants (if set)
    - Prize info (if available)

    3. **Rules section** (if rules exist):
    - Collapsible "Rules & Guidelines" section with expand/collapse toggle

    4. **Action section** (type-specific behavior):
    - **RAFFLE**: "Join Competition" button (primary indigo). If user already joined, show "Joined ✓" disabled badge. Must be within date range.
    - **PHOTO**: "Submit Entry" button → opens inline form with URL input (submissionUrl) + optional description textarea. After submit, show "Entry Submitted ✓" badge.
    - **SCORE**: If user has entry, show their score. Otherwise "This competition is score-based — participants are evaluated by judges."
    - For ENDED/CANCELLED status: disable actions, show "This competition has ended" message.

    5. **Participants section:**
    - "Participants ({count})" heading
    - Grid of participant avatars + names
    - Each: avatar circle + name below
    - Show max 20 initially with "Show All" toggle

    6. **Winners gallery** (for COMPLETED status competitions):
    - Only show if competition status is ENDED and there are entries with status=WINNER
    - Fetch via `trpc.competitions.listWinners.useQuery({ competitionId: id })`
    - Trophy section header: "🏆 Winners" with celebratory styling
    - Winner cards: gold-accented card with user avatar, name, prize won, winner badge
    - Runner-up section if there are RUNNER_UP entries
    - Empty winners state: "Winners haven't been announced yet. Check back soon!"

    **State handling:**
    - Loading: full-page skeleton with header skeleton + content skeleton
    - Not found (404 from API): "Competition not found" with back button to /competition
    - Error: error message with retry button

    **Join/Submit mutations:**
    - Use React Query mutations (`useMutation`)
    - On success: toast.success + invalidate competition query to refresh data
    - On error: toast.error with error message

    Use `next/navigation` `useParams()` for the route param.

  </action>
  <verify>
    <automated>
      <sampling_rate>run after task commits</sampling_rate>
    </automated>
    <manual>
      1. Visit /competition/[id] for an active RAFFLE comp → see detail + join button
      2. Click Join → "Joined ✓" appears + toast
      3. Visit PHOTO comp → see Submit Entry form
      4. Visit ENDED comp with winners → see winners gallery
      5. Visit non-existent ID → "Not found" state
    </manual>
  </verify>
  <done>
    /competition/[id] page shows full competition details, type-specific actions, participant list, and winners gallery. Loading/empty/error states handled.
  </done>
</task>

</tasks>

<verification>
- Both pages compile without TypeScript errors
- tRPC hooks work correctly with the competition router
- React Query handles loading/error states
- Toast notifications on successful join/submit
- Router navigates correctly between /competition and /competition/[id]
- All states (loading, empty, error, data) render without crashing
</verification>

<success_criteria>

- User sees competitions as cards on /competition
- User can join a raffle competition
- User can submit a photo entry
- Winners gallery renders for completed competitions
- Participant avatars shown on cards and detail page
  </success_criteria>

<output>
After completion, create `.planning/phases/39-competition-entries/39-03-SUMMARY.md`
</output>
