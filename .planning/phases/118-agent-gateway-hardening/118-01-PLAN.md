---
phase: 118-agent-gateway-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widgets/dashboard/model/widgets.ts
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "DelegationWidget is registered in the widget registry with id 'delegations' and can be added to the home Space"
    - 'Phase 111 has a visible entry in ROADMAP.md under M5 with goal, status Complete, and 5 plans'
    - 'REQUIREMENTS.md exists with a Phase 111 section mapping must-have truths to requirement IDs'
  artifacts:
    - path: src/widgets/dashboard/model/widgets.ts
      provides: DelegationWidget registration (registry.register call)
      contains: "id: 'delegations'"
    - path: .planning/ROADMAP.md
      provides: Phase 111 entry under M5 milestone
      contains: '## Phase 111'
    - path: .planning/REQUIREMENTS.md
      provides: Requirement mapping for Phase 111
      contains: 'AGENT-01'
  key_links:
    - from: src/widgets/dashboard/model/widgets.ts
      to: src/widgets/delegation/index.ts
      via: "lazy(() => import('@widgets/delegation'))"
      pattern: '@widgets/delegation'
---

<objective>
Register the DelegationWidget in the widget registry and update project documentation to properly reflect Phase 111.

**Purpose:** Close two gaps from Phase 111 verification — the DelegationWidget was built but never registered, so it's unreachable from any dashboard Space. Also, Phase 111 has no ROADMAP.md entry or REQUIREMENTS.md mapping, making it invisible in project tracking.

**Output:** DelegationWidget registered as `delegations` in the widget registry, Phase 111 block added to ROADMAP.md, REQUIREMENTS.md created with Phase 111 requirement mapping.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/widgets/dashboard/model/widgets.ts
@src/widgets/dashboard/model/types.ts
@src/widgets/delegation/DelegationWidget.tsx
@src/widgets/delegation/index.ts
@.planning/phases/111-agent-gateway/111-VERIFICATION.md
@.planning/phases/111-agent-gateway/111-01-PLAN.md
@.planning/phases/111-agent-gateway/111-02-PLAN.md
@.planning/phases/111-agent-gateway/111-03-PLAN.md
@.planning/phases/111-agent-gateway/111-04-PLAN.md
@.planning/phases/111-agent-gateway/111-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register DelegationWidget in widget registry</name>
  <files>src/widgets/dashboard/model/widgets.ts</files>
  <read_first>
    src/widgets/dashboard/model/widgets.ts — study the existing registry.register() pattern (structure, imports, lazy loading). Follow the dWallet-summary pattern (id, version, name, description, author, category, icon, component, defaultSize, minSize, dragHandleClassName, spaces).
    src/widgets/dashboard/model/types.ts — WidgetManifest interface for field reference.
    src/widgets/delegation/index.ts — barrel export for the lazy import path.
  </read_first>
  <action>
    Per D-01 through D-06 (all locked decisions):

    1. Add `UserCheck` to the lucide-react import block at the top of `widgets.ts` (around line 39, near `Gavel`). Follow the existing import style: comma on each line, closing with `} from 'lucide-react';`. The icon exists — confirmed at `node_modules/lucide-react/dist/esm/icons/user-check.js`.

    2. Add a new `registry.register({...})` call in the CORE WIDGETS section (around line 167, after the properties widget block, before the COMMUNICATION WIDGETS divider). Use this exact configuration:

    - `id: 'delegations'` (per D-01)
    - `version: '1.0.0'`
    - `name: 'My Delegations'` (per D-01)
    - `description: 'Manage property delegations and agent permissions'`
    - `author: 'internal'` (per D-04 — core feature, not premium)
    - `category: 'core'` (per D-04)
    - `icon: UserCheck` (per D-03 — UserCheck from lucide-react)
    - No `featureFlag` field (per D-04 — core platform feature, no flag)
    - `component: lazy(() => import('@widgets/delegation').then(m => ({ default: m.DelegationWidget })))` — uses the existing barrel at `src/widgets/delegation/index.ts`
    - `defaultSize: { width: 3, height: 3 }` (per D-05)
    - `minSize: { width: 1, height: 2 }` (per D-05)
    - `dragHandleClassName: 'widget-drag-handle'` — matches all existing registrations
    - `spaces: ['home']` (per D-02 — resident dashboard where owners manage delegations)
    - No `permissions` field (per D-06 — visibility is scoped to authenticated users; widget handles authorization internally)

    Do NOT add a featureFlag — per D-04 this is a core platform feature.
    Do NOT add `premium: true` — per D-04 it's not premium.

  </action>
  <verify>
    <automated>
      grep -c "id: 'delegations'" src/widgets/dashboard/model/widgets.ts 2>/dev/null || echo "FAIL: delegations not found"
      grep -c "UserCheck" src/widgets/dashboard/model/widgets.ts 2>/dev/null || echo "FAIL: UserCheck not imported"
      grep -c "import('@widgets/delegation')" src/widgets/dashboard/model/widgets.ts 2>/dev/null || echo "FAIL: delegation import not found"
    </automated>
    <human-check>Manual: Open the dashboard home space in the browser → verify the "My Delegations" widget appears in the widget picker when in edit mode.</human-check>
  </verify>
  <acceptance_criteria>
    - `UserCheck` imported from `lucide-react` at the top of widgets.ts
    - `registry.register(...)` call added for id `delegations` in the CORE WIDGETS section
    - Widget is lazy-loaded from `@widgets/delegation` via existing barrel export
    - No feature flag assigned (core feature per D-04)
    - Assigned to `home` Space per D-02
    - `pnpm tsc --noEmit` passes on widgets.ts (no new type errors)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add Phase 111 to ROADMAP.md and create REQUIREMENTS.md</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md — study existing phase entry templates (e.g., Phase 110, Phase 50, Phase 104) for consistent formatting. Note the M5 milestone section structure.
    .planning/phases/111-agent-gateway/111-01-PLAN.md through 111-05-PLAN.md — extract plan names, objectives, and the 37 must-have truths from frontmatter.
  </read_first>
  <action>
    Per D-14, D-15, D-16:

    **Step 1 — Add Phase 111 entry to ROADMAP.md:**
    Insert a new phase block in the M5 (Anchor Tenant Launch) milestone section per D-16. It should go between Phase 110 (ends around line 1319) and Phase 120 (starts around line 1320). Use this exact template, following existing formatting conventions:

    ```
    ## Phase 111: Agent Gateway

    **Goal:** Build the Agent Gateway — a unified access and delegation layer that mediates ALL caller types (human users, non-human AI agents, cron/webhook processes, and delegated third-party providers) through a single authorization pipeline.

    **Status:** Complete

    **Milestone:** M5 — Anchor Tenant Launch

    **Depends on:** Phase 110

    **Source:** Platform architecture — enables Phase 110 extension point D-08/D-09

    **Plans:** 5 plans in 5 waves

    | Wave | Plan | Objective |
    | ---- | ---- | --------- |
    | 1 | [x] 111-01-PLAN.md | Agent Token data model & lifecycle — AgentToken, DelegationAction, DelegationStatus, scopes/tokens/revoke API |
    | 2 | [x] 111-02-PLAN.md | Delegation lifecycle API — owner delegates, provider accepts/rejects, owner revokes, delegation listing |
    | 3 | [x] 111-03-PLAN.md | Agent scope resolution pipeline — resolveAgentScope(), access endpoint integration, canAccess() agent dimension |
    | 4 | [x] 111-04-PLAN.md | Delegation domain entity & resident widget — types, hooks, block/audit API, DelegationWidget, DelegationAuditLog |
    | 5 | [x] 111-05-PLAN.md | Maintenance request routing — MaintenanceRouting enum, routingType/landlordId, ResidentDelegation API, routing indicator |
    ```

    Each plan must have `[x]` (completed per PHASE 111 VERIFICATION.md all 37/37 must-haves passed). Use consistent pipe-format alignment matching Phase 110 entry conventions.

    Make sure the insertion does not disrupt the Phase 120 entry or Phase 112 entry below it. If Phase 120 comes after Phase 110, insert Phase 111 between them.

    **Step 2 — Create REQUIREMENTS.md:**
    Since no REQUIREMENTS.md exists, create `.planning/REQUIREMENTS.md` with an initial Phase 111 section. Map the 37 must-have truths to derived requirement IDs. Use this structure:

    ```markdown
    # Requirements

    This document maps all phase requirements across the project. Each requirement ID is prefixed with the domain abbreviation.

    ## Phase 111: Agent Gateway

    | ID | Description | Covered By |
    |----|-------------|------------|
    | AGENT-01 | AgentToken model persists scoped, expirable, revocable JWT credentials | 111-01 |
    | AGENT-02 | AgentToken complements (not replaces) AgentAccess | 111-01 |
    | AGENT-03 | AgentAccess.status replaces isActive with DelegationStatus workflow | 111-01 |
    | AGENT-04 | DelegationAction records every delegation lifecycle event with actor id and metadata | 111-01 |
    | AGENT-05 | AgentPermission enum removed; permissions stored as String[] validated against AgentScopeRegistry | 111-01 |
    | AGENT-06 | AgentAccess.originalPermissions captures grant-time permissions; never mutated after creation | 111-01 |
    | AGENT-07 | ResidentDelegation model allows owners to grant renters scoped initiation rights | 111-01 |
    | AGENT-08 | DelegationAction has @@index([delegationId]) and @@index([tenantId]) | 111-01 |
    | AGENT-09 | User model has back-relations agentTokensAsAgent and agentTokensIssued for AgentToken | 111-01 |
    | AGENT-10 | AGENT_SCOPES and SCOPE_BUNDLES exported from scopes.ts | 111-01 |
    | AGENT-11 | POST /api/agent/tokens creates scoped tokens; PATCH revoke invalidates immediately | 111-01 |
    | AGENT-12 | X-Agent-Token header parsed and validated; unknown/expired/revoked tokens return 401 | 111-01 |
    | AGENT-13 | Owner can POST /api/properties/[id]/delegate to create a PENDING delegation | 111-02 |
    | AGENT-14 | Provider can POST /api/delegations/[id]/accept (issues AgentToken) | 111-02 |
    | AGENT-15 | Provider can POST /api/delegations/[id]/reject | 111-02 |
    | AGENT-16 | Owner can PATCH /api/delegations/[id]/revoke | 111-02 |
    | AGENT-17 | Every delegation status change writes a DelegationAction row | 111-02 |
    | AGENT-18 | Unverified providers cannot accept delegations | 111-02 |
    | AGENT-19 | Delegation acceptance auto-issues scoped AgentToken | 111-02 |
    | AGENT-20 | Delegation create validates scopes against AGENT_SCOPES registry | 111-02 |
    | AGENT-21 | AgentAccess.originalPermissions set at creation time | 111-02 |
    | AGENT-22 | Unknown scopes return 400 VALIDATION_ERROR with invalid scopes list | 111-02 |
    | AGENT-23 | Bundle presets expand server-side to canonical scope arrays | 111-02 |
    | AGENT-24 | resolveAgentScope() decodes token, looks up AgentAccess, resolves EffectiveScope | 111-03 |
    | AGENT-25 | Phase 110 resolveAgent() stub replaced with real resolveAgentScope() call | 111-03 |
    | AGENT-26 | GET /api/access?caller=agent&token=X returns actual agent scopes | 111-03 |
    | AGENT-27 | Scope intersection: agent scope ∩ tenant gating ∩ feature flags | 111-03 |
    | AGENT-28 | usePageAccess() hydrates non-null agent field when caller=agent | 111-03 |
    | AGENT-29 | Phase 41 canAccess() extended with agent scope dimension | 111-03 |
    | AGENT-30 | Delegation domain entity exports types, API client, and React hooks | 111-04 |
    | AGENT-31 | DelegationWidget shows active delegations with per-agent block toggle | 111-04 |
    | AGENT-32 | Block removes communication:contact_occupant from permissions | 111-04 |
    | AGENT-33 | DelegationAuditLog displays DelegationAction rows reverse-chronological | 111-04 |
    | AGENT-34 | Widget displays human-readable scope labels via SCOPE_LABELS | 111-04 |
    | AGENT-35 | Widget is consumable as standalone embeddable component | 111-04 |
    | AGENT-36 | MaintenanceRequest.routingType determines HOA vs LANDLORD routing | 111-05 |
    | AGENT-37 | Routing decision automatic based on OccupancyType | 111-05 |
    ```

    Note: These IDs (AGENT-01 through AGENT-37) serve as the Phase 111's requirement mapping. Future phases that add requirements append their own sections and IDs.

  </action>
  <verify>
    <automated>
      grep -c "Phase 111" .planning/ROADMAP.md || echo "FAIL: Phase 111 not found in ROADMAP.md"
      grep -c "111-01-PLAN" .planning/ROADMAP.md || echo "FAIL: Plan 111-01 reference not found in ROADMAP.md"
      grep -c "AGENT-37" .planning/REQUIREMENTS.md || echo "FAIL: REQUIREMENTS.md missing AGENT-37"
    </automated>
  </verify>
  <acceptance_criteria>
    - Phase 111 entry exists in ROADMAP.md under M5 milestone with goal, status Complete, 5 completed plans
    - Phase 111 entry is placed between Phase 110 and Phase 120 in the M5 section
    - .planning/REQUIREMENTS.md exists with 37 requirement IDs (AGENT-01 through AGENT-37)
    - Each requirement maps to its plan number
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                                                                                                                                           |
| ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Widget registry → dashboard render       | Registry entries are imported at build time; only registered widgets appear in the UI. A misconfigured registration (wrong import path, missing lazy() wrapper) causes a runtime load failure but cannot expose data. |
| ROADMAP.md / REQUIREMENTS.md → developer | Documentation files — no runtime impact.                                                                                                                                                                              |

## STRIDE Threat Register

| Threat ID | Category  | Component             | Disposition | Mitigation Plan                                                                                                                   |
| --------- | --------- | --------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------- |
| T-118-01  | Spoofing  | Widget registry entry | mitigate    | No featureFlag means widget shows for all authenticated users — intentional per D-04/D-06; widget internally checks authorization |
| T-118-02  | Tampering | npm/lucide-react      | accept      | UserCheck is a standard lucide-react icon (no sudo/gate install); no package change in this plan                                  |
| T-118-SC  | Tampering | pnpm install          | mitigate    | No new packages — purely documentation and config changes; slopcheck exempt                                                       |

</threat_model>

<verification>
- `pnpm tsc --noEmit` shows ≤ 14 errors (same as baseline, no new type errors from widgets.ts)
- `pnpm lint` shows 0 new ESLint errors
- `grep "id: 'delegations'" src/widgets/dashboard/model/widgets.ts` returns the registration
- `grep "Phase 111" .planning/ROADMAP.md` returns the phase entry
- `grep "AGENT-37" .planning/REQUIREMENTS.md` confirms the full requirement mapping
</verification>

<success_criteria>

- DelegationWidget registered in widget registry with id `delegations`, UserCheck icon, `home` Space, core category, 3x3 default size, no feature flag
- Phase 111 visible in ROADMAP.md under M5 with goal, status Complete, 5 checkmarked plans
- REQUIREMENTS.md created with 37 Phase 111 requirement IDs (AGENT-01 through AGENT-37)
- No new TypeScript or ESLint warnings
  </success_criteria>

<output>
Create `.planning/phases/118-agent-gateway-hardening/118-01-SUMMARY.md` when done
</output>
