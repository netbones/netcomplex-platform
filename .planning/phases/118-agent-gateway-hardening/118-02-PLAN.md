---
phase: 118-agent-gateway-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/migrations/*_add_maintenance_routing_fields/migration.sql
  - src/db/schema/* # Drizzle auto-generated schema files
  - src/app/api/properties/[id]/resident-delegation/__tests__/resident-delegation.test.ts
autonomous: true
decisions:
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12
  - D-13
requirements: []

must_haves:
  truths:
    - 'MaintenanceRouting enum + routingType/landlordId fields have a formal Prisma migration (not just db push)'
    - 'ResidentDelegation integration tests cover the full lifecycle: grant → accept → authorize → revoke → deny'
    - 'Test file TS errors are fixed (Request→NextRequest, residentDelegation→residentDelegations)'
  artifacts:
    - path: prisma/migrations/*_add_maintenance_routing_fields/migration.sql
      provides: Formal migration for MaintenanceRouting enum + routingType/landlordId
      contains: 'MaintenanceRouting'
    - path: src/app/api/properties/[id]/resident-delegation/__tests__/resident-delegation.test.ts
      provides: Full lifecycle integration tests
      min_lines: 200
  key_links:
    - from: route.ts
      to: __tests__/resident-delegation.test.ts
      via: "import { POST, GET, DELETE } from '../route'"
      pattern: "from '../route'"
---

<objective>
Apply a formal Prisma migration for the Phase 111 MaintenanceRouting schema changes and rewrite the ResidentDelegation integration tests with full lifecycle coverage.

**Purpose:** Phase 111 Plan 05 added MaintenanceRouting enum and routingType/landlordId fields to MaintenanceRequest via `prisma db push` but never created a formal migration (D-07). The resident-delegation test file contains only structural tests with Prisma-style mocks that don't match the Drizzle query patterns used in the actual route — the tests are not integration tests, have TS errors, and don't cover the full grant→accept→authorize→revoke→deny lifecycle (D-11).

**Output:** Formal Prisma migration directory `*_add_maintenance_routing_fields/`, regenerated Drizzle schema files, rewritten test file with full lifecycle integration tests using Drizzle-compatible mock patterns.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@prisma/schema.prisma
@prisma/migrations/
@src/app/api/properties/[id]/resident-delegation/route.ts
@src/app/api/properties/[id]/resident-delegation/__tests__/resident-delegation.test.ts
@src/entities/maintenance/model/routing.ts
@src/entities/maintenance/__tests__/maintenance-routing.test.ts
@.planning/phases/111-agent-gateway/111-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create formal Prisma migration for MaintenanceRouting fields</name>
  <files>prisma/migrations/*_add_maintenance_routing_fields/migration.sql, src/db/schema/*</files>
  <read_first>
    prisma/schema.prisma — verify the MaintenanceRouting enum (line ~2302) and routingType/landlordId fields on MaintenanceRequest model (lines ~936-953) already exist in schema.
    prisma/migrations/ — list existing migrations to confirm no prior migration for these fields exists.
  </read_first>
  <action>
    Per D-07 through D-10 (locked decisions):

    1. Run `npx prisma validate` first to confirm the schema is valid before attempting migration.

    2. Attempt `npx prisma migrate dev --name add_maintenance_routing_fields`. This creates a formal migration that captures:
       - New enum `MaintenanceRouting` with values `HOA` and `LANDLORD`
       - New field `routingType MaintenanceRouting @default(HOA)` on `MaintenanceRequest`
       - New field `landlordId String?` on `MaintenanceRequest`
       - New indexes `@@index([routingType])` and `@@index([landlordId])`
       - New relation `landlord user? @relation("MaintenanceRequest_landlord")`

    3. **If the shadow database issue blocks step 2** (the known pre-existing issue with migration `20260624000000_add_user_role_and_seat_lifecycle`, per D-08/D-09):
       a. Generate the migration SQL manually using `npx prisma migrate diff --from-url "$DATABASE_URL" --to-schema-datamodel prisma/schema.prisma --script > prisma/migrations/$(date +%Y%m%d%H%M%S)_add_maintenance_routing_fields/migration.sql`
       b. Create the migration directory manually matching the timestamp pattern
       c. Mark it applied: `npx prisma migrate resolve --applied <timestamp>_add_maintenance_routing_fields`
       d. Record which fallback was used and why in the SUMMARY

    4. Do NOT attempt to fix the pre-existing shadow DB issue (`20260624000000_add_user_role_and_seat_lifecycle`) — this is inherited technical debt, explicitly out of scope per D-09.

    5. After migration succeeds (either path), run `npx prisma generate` to refresh Drizzle schema sync per D-10. This regenerates `src/db/schema/*` files from the updated Prisma schema.

    6. Verify with `npx prisma validate` that the migration succeeded and the schema is consistent.

  </action>
  <verify>
    <automated>
      npx prisma validate 2>&1 | grep -c "Your database is up to date" || echo "FAIL: DB not up to date"
      ls -d prisma/migrations/*add_maintenance_routing_fields*/migration.sql 2>/dev/null || echo "FAIL: migration directory not found"
      grep -c "MaintenanceRouting" prisma/migrations/*add_maintenance_routing_fields*/migration.sql 2>/dev/null || echo "FAIL: migration SQL does not contain MaintenanceRouting enum"
    </automated>
  </verify>
  <done>
    - Migration directory exists: `prisma/migrations/*_add_maintenance_routing_fields/migration.sql`
    - Migration SQL contains `MaintenanceRouting` enum creation (`CREATE TYPE "MaintenanceRouting"` or `enum MaintenanceRouting`)
    - `npx prisma validate` reports "Your database is up to date" (success)
    - Drizzle schema files regenerated (no manual changes needed — `npx prisma generate` handles this)
    - The pre-existing shadow DB issue remains untouched (D-09 honored)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite ResidentDelegation integration tests with full lifecycle coverage</name>
  <files>src/app/api/properties/[id]/resident-delegation/__tests__/resident-delegation.test.ts</files>
  <read_first>
    src/app/api/properties/[id]/resident-delegation/route.ts — study the actual Drizzle query patterns used: `db.select().from(residentDelegations).where(...)`, `db.insert(residentDelegations).values(...)`, `db.update(residentDelegations).set(...)`. The test mocks must match these Drizzle patterns, not Prisma findFirst/findMany patterns.
    src/entities/agent/scopes.ts — understand RENTER_DELEGATABLE_SCOPES (the scopes owners can delegate, like maintenance:create).
    src/app/api/maintenance/route.ts — understand how maintenance creation checks renter authorization via ResidentDelegation (so the test can verify the authorized path).
  </read_first>
  <behavior>
    - Test 1 (Happy path): Owner grants maintenance:create scope → Renter receives scope → Renter creates maintenance request (authorized) → Returns 201
    - Test 2 (Revoke): Owner revokes delegation → Renter attempts maintenance creation → Returns 403 FORBIDDEN
    - Test 3 (Non-owner): Non-owner attempts to grant delegation → Returns 403 FORBIDDEN
    - Test 4 (Invalid scope): Grant with non-delegatable scope → Returns 400 VALIDATION_ERROR
    - Test 5 (GET active): GET returns active delegations for a property
    - Test 6 (DELETE): DELETE revokes an existing delegation
    - Test 7 (DELETE non-owner): DELETE by non-owner → Returns 403
    - Test 8 (Revoked renter): Renter's token expires/revokes → maintenance creation returns 403
  </behavior>
  <action>
    Per D-11 through D-13 (locked decisions):

    Rewrite the entire test file at `src/app/api/properties/[id]/resident-delegation/__tests__/resident-delegation.test.ts`. The current file uses Prisma-style mocks (`findFirst`, `findMany`, `create`, `update`, `updateMany`) that do not match the actual Drizzle query patterns used by the route handler. The route uses:
    - `db.select(...).from(residentDelegations).where(...)`
    - `db.insert(residentDelegations).values(...)`
    - `db.update(residentDelegations).set(...).where(...)`
    - `db.delete(residentDelegations).where(...)`

    **Mock approach:** Use `vi.hoisted()` pattern (same as `src/entities/delegation/__tests__/api.test.tsx` uses) to create mutable mock state that the mocked `db` object reads from. For Drizzle chain patterns, mock `db.select`, `db.insert`, `db.update`, `db.delete` as functions that return chainable objects with `.from()`, `.where()`, `.values()`, `.set()`, `.returning()`, `.limit()` methods.

    **Fixes per D-13:**
    1. The `createRequest` helper already uses `NextRequest` (line 3 import, line 78-86 use `new NextRequest(...)`) — ensure all tests consistently use `NextRequest`. The `Response` vs `NextResponse` issue in mock helpers also needs fixing: `apiSuccess` and `apiError` mocks should return `NextResponse` or `Response` types compatible with the route's actual return types.
    2. The mock table name must be `residentDelegations` (plural, matching the Drizzle table export at `@api/server`) — the current mock already uses `residentDelegations` for the table object, which is correct. Fix any remaining references to `residentDelegation` (singular).

    **Test scenarios (8 tests minimum):**

    1. **Owner grants maintenance:create scope** → `POST` with `{ profileId, scopes: ['maintenance:create'] }` → `201` with delegation record containing granted scopes. Verify `db.insert(residentDelegations).values(...)` was called with the correct scope array.

    2. **Renter creates maintenance request while authorized** → Mock that the renter has an active ResidentDelegation with `maintenance:create` scope → renter `POST /api/maintenance` → Verify the route authorizes the request (reaches the creation logic — confirm by checking `db.insert(maintenanceRequests).values(...)` was called or the route returns `201`). This test requires mocking the maintenance creation path to verify the delegation authorization check passes. **Simplify:** mock the maintenance route to verify it checks the delegation; the core assertion is that the authorization gate does not 403 when a valid delegation exists.

    3. **Owner revokes delegation** → `DELETE` with `{ delegationId }` → `200`. Then mock that `db.select(...)` returns no active delegations → renter attempts maintenance creation → `403 FORBIDDEN`.

    4. **Non-owner attempts grant** → Set property ownerId to differ from session userId → `POST` → `403 FORBIDDEN`.

    5. **Grant with non-delegatable scope** → `POST` with `{ profileId, scopes: ['financials:read'] }` → `400 VALIDATION_ERROR`. Verify error code contains `VALIDATION`.

    6. **GET returns active delegations** → Mock `db.select().from(residentDelegations)` to return a delegation record → `GET` → `200` with delegation list.

    7. **DELETE by non-owner** → Mock delegation's property ownerId to differ from session user → `DELETE` with `{ delegationId }` → `403 FORBIDDEN`.

    8. **Revoked delegation denies maintenance creation** → Grant → Revoke → Verify renter gets `403` when attempting maintenance creation. This validates the full lifecycle from D-11 explicitly.

    For the maintenance authorization verification (tests 2, 3, 8), these tests verify the _route handler's_ response by checking the route's authorization decision. Do NOT import maintenance route handlers — mock the authorization check at the level the route calls it, or verify the route handler returns a 403. The simplest approach: for tests involving the maintenance route authorization path, add a test that directly imports the authorization helper and verifies it returns the correct result given mocked delegation state.

    **Important:** The test file imports `{ POST, GET, DELETE } from '../route'`. Keep this import pattern. The mock for `@api/server` must return Drizzle-compatible chainable query builders.

    Use `vi.hoisted()` for mutable mock state (following the pattern from `src/entities/delegation/__tests__/api.test.tsx`). Use `vi.clearAllMocks()` in `beforeEach()`.

  </action>
  <verify>
    <automated>
      pnpm vitest run src/app/api/properties/[id]/resident-delegation/__tests__/resident-delegation.test.ts --reporter=verbose 2>&1 | tail -20
    </automated>
    <human-check>The test file rewritten to use Drizzle-compatible mocks with vi.hoisted() pattern, covering grant→accept→authorize→revoke→deny lifecycle.</human-check>
  </verify>
  <done>
    - Test file replaced with full Drizzle-compatible mock patterns (not Prisma findFirst/findMany)
    - At least 8 tests covering: grant happy path, authorized maintenance creation, revoke → deny, non-owner rejection, invalid scope rejection, GET listing, DELETE revocation, revoked denial
    - `Request` replaced with `NextRequest` (if any occur)
    - `residentDelegation` (singular) references replaced with `residentDelegations` (plural)
    - All tests pass with `pnpm vitest run`
    - `pnpm tsc --noEmit` shows ≤ 14 errors (same baseline)
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                             |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| Prisma migration → database  | Schema change adds enum + nullable fields. Existing data unaffected (routingType has default HOA, landlordId nullable). |
| Test runner → route handlers | Tests import route handlers directly with mocked DB — no real database involved.                                        |

## STRIDE Threat Register

| Threat ID | Category               | Component               | Disposition | Mitigation Plan                                                                                                                                          |
| --------- | ---------------------- | ----------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-118-03  | Tampering              | Prisma migration SQL    | mitigate    | Migration is generated by Prisma CLI from schema — not hand-written. Verify contents include enum creation, ALTER TABLE, and index creation.             |
| T-118-04  | Information Disclosure | Migration SQL file      | accept      | Contains only schema types/column definitions — no data. Checked into version control per project convention.                                            |
| T-118-05  | Denial of Service      | Shadow database failure | mitigate    | If shadow DB blocks, use manual migration + `prisma migrate resolve --applied` fallback per D-08. Do NOT attempt to fix the pre-existing issue per D-09. |
| T-118-SC  | Tampering              | pnpm/prisma install     | mitigate    | No new packages — command uses existing `prisma` CLI from devDependencies. Slopcheck exempt.                                                             |

</threat_model>

<verification>
- `npx prisma validate` reports database is up to date
- `ls prisma/migrations/*add_maintenance_routing_fields*/migration.sql` exists with content
- `pnpm vitest run src/app/api/properties/[id]/resident-delegation/__tests__/resident-delegation.test.ts` — all 8+ tests pass
- `pnpm tsc --noEmit` shows ≤ 14 errors (same pre-existing baseline)
- `pnpm lint` shows 0 new ESLint errors
</verification>

<success_criteria>

- Formal Prisma migration created for MaintenanceRouting enum + routingType/landlordId fields
- Drizzle schema regenerated via `npx prisma generate`
- Pre-existing shadow DB issue left untouched (D-09)
- ResidentDelegation test file rewritten with 8+ Drizzle-compatible integration tests
- Full lifecycle covered: grant → authorize → revoke → deny
- Test file TS errors fixed (Request→NextRequest, residentDelegation→residentDelegations)
- All verification gates pass
  </success_criteria>

<output>
Create `.planning/phases/118-agent-gateway-hardening/118-02-SUMMARY.md` when done
</output>
