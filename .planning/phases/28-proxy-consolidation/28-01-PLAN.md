---
phase: 28-proxy-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - proxy.ts
  - docs/architecture/DOMAINS-PROXY.md
autonomous: true
requirements:
  - PROXY-01
  - PROXY-02
must_haves:
  truths:
    - 'Build succeeds without Module Not Found errors'
    - 'Multi-tenant routing works identically after consolidation'
    - 'A single source file contains all proxy/middleware logic'
    - 'No stale proxy.ts remains at project root'
    - 'Architecture docs reflect the actual Next.js 15.5 convention'
  artifacts:
    - path: 'src/middleware.ts'
      provides: 'Single authoritative multi-tenant middleware (proxy logic)'
      exports: ['middleware', 'config']
    - path: 'docs/architecture/DOMAINS-PROXY.md'
      provides: 'Corrected architecture doc aligned with Next.js 15.5'
  key_links:
    - from: 'src/middleware.ts'
      to: 'Next.js build system'
      via: 'middleware export name (Next.js 15.5 requirement)'
      pattern: 'export.*middleware'
---

<objective>
Consolidate the split proxy.ts + middleware.ts into a single src/middleware.ts that builds cleanly on Next.js 15.5 (current deployment), while aligning architecture docs with reality.

Purpose: The current setup is broken — `src/middleware.ts` imports `./proxy` but `src/proxy.ts` doesn't exist; the actual logic lives in root `proxy.ts`. Next.js 15.5 only recognizes `middleware.ts` (not `proxy.ts`) — that's a Next.js 16+ convention. We need one authoritative file that builds and deploys correctly on Vercel today.

Output: Single `src/middleware.ts` with all multi-tenant routing logic, root `proxy.ts` deleted, docs corrected.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/architecture/DOMAINS.md
@docs/architecture/DOMAINS-PROXY.md
@proxy.ts
@src/middleware.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Merge proxy logic into src/middleware.ts and delete root proxy.ts</name>
<files>src/middleware.ts, proxy.ts</files>
<action>
1. Replace the entire content of `src/middleware.ts` with the full multi-tenant routing logic currently in root `proxy.ts`, making these changes:
   - Change `export async function proxy(request: NextRequest)` → `export async function middleware(request: NextRequest)` (Next.js 15.5 requires the export name `middleware`)
   - Keep the `export const config = { matcher: [...] }` block as-is (already correct)
   - Remove the misleading comment `// src/middleware.ts` on line 1 of the root proxy.ts (it's not actually in src/)
   - Remove the `console.log('[Middleware] Loaded...')` debug line at the bottom (production noise)
   - Everything else (hostname parsing, platform/tenant detection, header injection, route enforcement) stays identical

2. Delete root `proxy.ts` — it is the sole source of the proxy logic and after migration should not exist

3. Verify `src/middleware.ts` now contains:
   - `import { NextResponse, type NextRequest } from 'next/server'`
   - `export async function middleware(request: NextRequest)` function
   - `export const config` with matcher
   - All PLATFORM_DOMAIN, TENANT_DOMAINS, helper functions from current proxy.ts
   - NO import of `./proxy` (this was the broken line)
     </action>
     <verify>
     <automated>cd /home/ubuntupunk/Projects/soralia-village && pnpm build 2>&1 | grep -E "Module not found|proxy|Build error|✓ Compiled" | head -5</automated>
     <manual>Verify `test ! -f proxy.ts` succeeds (root proxy.ts deleted) and `grep 'export async function middleware' src/middleware.ts` matches</manual>
     <sampling_rate>run after this task commits, before next task begins</sampling_rate>
     </verify>
     <done>

- `src/middleware.ts` contains all multi-tenant routing logic with `export async function middleware`
- Root `proxy.ts` no longer exists
- `pnpm build` succeeds without "Module not found: Can't resolve './proxy'" error
  </done>
  </task>

<task type="auto">
<name>Task 2: Correct DOMAINS-PROXY.md to reflect Next.js 15.5 middleware.ts reality</name>
<files>docs/architecture/DOMAINS-PROXY.md</files>
<action>
Update `docs/architecture/DOMAINS-PROXY.md` to accurately reflect the current deployment constraint:

1. At the top, add a clear note:

   ```markdown
   > **Next.js version note:** As of Next.js 15.5 (current deployment), the file convention
   > is `middleware.ts` with `export function middleware()`. The `proxy.ts` convention
   > (with `export function proxy()`) is a Next.js 16+ feature. When upgrading to
   > Next.js 16+, run `npx @next/codemod@canary middleware-to-proxy .` to migrate.
   ```

2. Update the "Recommended Netcomplex Structure" section:
   - Change `src/proxy.ts` to `src/middleware.ts` (current convention)
   - Add a comment: `// proxy.ts when on Next.js 16+`

3. Update the skeleton code to use `export async function middleware(request: NextRequest)` instead of `export async function proxy(request: NextRequest)`

4. Update the "Replace all references" section to clarify:
   - Current (Next.js 15.5): `middleware` export name, `middleware.ts` filename
   - Future (Next.js 16+): `proxy` export name, `proxy.ts` filename
   - Codemod available: `npx @next/codemod@canary middleware-to-proxy .`

5. Update the config section — `skipMiddlewareUrlNormalize` is still correct for 15.5; note it becomes `skipProxyUrlNormalize` in Next.js 16+

6. Add a "Migration Path" section at the end:
   ```markdown
   ## Migration to Next.js 16 proxy.ts

   When upgrading to Next.js 16+:

   1. Run: `npx @next/codemod@canary middleware-to-proxy .`
   2. This renames middleware.ts → proxy.ts and `export function middleware` → `export function proxy`
   3. The proxy runtime is `nodejs` (edge runtime NOT supported in proxy.ts)
   4. Update next.config.js: `skipMiddlewareUrlNormalize` → `skipProxyUrlNormalize`
   ```
   </action>
   <verify>
   <automated>grep -c "Next.js 15.5\|middleware\.ts\|middleware-to-proxy\|Next.js 16" /home/ubuntupunk/Projects/soralia-village/docs/architecture/DOMAINS-PROXY.md</automated>
   <manual>Doc should mention both conventions clearly and explain when to use which</manual>
   <sampling_rate>run after this task commits</sampling_rate>
   </verify>
   <done>

- DOMAINS-PROXY.md correctly states current convention is middleware.ts for Next.js 15.5
- DOMAINS-PROXY.md documents the migration path to proxy.ts for Next.js 16+
- No misleading instructions that would cause someone to create a root proxy.ts again
  </done>
  </task>

</tasks>

<verification>
1. `pnpm build` succeeds with zero errors
2. `test ! -f proxy.ts` — root proxy.ts is gone
3. `grep 'export async function middleware' src/middleware.ts` — single authoritative middleware file
4. `grep -c "import.*from.*proxy" src/middleware.ts` returns 0 — no broken proxy import
5. DOMAINS-PROXY.md mentions both Next.js 15.5 (middleware.ts) and 16+ (proxy.ts) conventions
</verification>

<success_criteria>

- Build compiles cleanly on Next.js 15.5 (current Vercel deployment)
- Single source of truth: src/middleware.ts with all multi-tenant routing logic
- Root proxy.ts deleted
- Architecture docs reflect current reality and provide clear migration path for Next.js 16+
- All existing multi-tenant behavior (hostname parsing, platform/tenant detection, header injection, route enforcement) preserved identically
  </success_criteria>

<output>
After completion, create `.planning/phases/28-proxy-consolidation/28-01-SUMMARY.md`
</output>
