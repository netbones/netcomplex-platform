---
phase: 21-content-events
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(tenant)/admin/events/page.tsx
  - src/app/(tenant)/admin/events/new/page.tsx
  - src/app/(tenant)/admin/events/[id]/page.tsx
  - src/app/api/events/route.ts
  - src/app/api/events/[id]/route.ts
  - src/widgets/admin/ui/EventForm.tsx
  - src/widgets/admin/ui/EventList.tsx
autonomous: true
requirements: [EVENTS-01, EVENTS-02]

must_haves:
  truths:
    - 'Admin can view a list of all events with title, date, location'
    - 'Admin can create a new event with title, description, date, location, organizer'
    - 'Admin can edit an existing event'
    - 'Admin can delete an event'
  artifacts:
    - path: 'src/app/(tenant)/admin/events/page.tsx'
      provides: 'Events list page'
      contains: 'EventList'
    - path: 'src/app/(tenant)/admin/events/new/page.tsx'
      provides: 'New event form page'
      contains: 'EventForm'
    - path: 'src/app/api/events/route.ts'
      provides: 'Events CRUD API (GET, POST)'
      exports: ['GET', 'POST']
    - path: 'src/app/api/events/[id]/route.ts'
      provides: 'Single event CRUD API (GET, PATCH, DELETE)'
      exports: ['GET', 'PATCH', 'DELETE']
    - path: 'src/widgets/admin/ui/EventForm.tsx'
      provides: 'Event form component'
      contains: 'title|description|date|location|organizer'
  key_links:
    - from: 'src/widgets/admin/ui/EventForm.tsx'
      to: 'src/app/api/events/route.ts'
      via: 'POST /api/events'
      pattern: 'fetch.*api/events'
    - from: 'src/app/(tenant)/admin/events/page.tsx'
      to: 'src/app/api/events/route.ts'
      via: 'GET /api/events'
      pattern: 'fetch.*api/events'
---

<objective>
Build /admin/events CRUD: list page, create/edit form, and API routes for Event model.

Purpose: The Event model exists in the schema but has no admin UI or API routes. Tenant admins need to manage community events (title, description, date, location, organizer). Without this, events are managed manually outside the system.

Output: Events admin pages (list, new, edit), EventForm and EventList components, API routes for full CRUD.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DISCUSSION.md — Phase B specification (events CRUD)
@prisma/schema.prisma — Event model (id, tenantId, title, description, date, location, organizer, image, isPublic, createdAt, updatedAt)
@src/db/schema/events.ts — Drizzle schema for events table
@src/app/api/content/route.ts — Pattern reference for API route structure
@src/widgets/admin/ui/ContentForm.tsx — Pattern reference for form component
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Event API routes (GET, POST, GET by ID, PATCH, DELETE)</name>
  <files>src/app/api/events/route.ts, src/app/api/events/[id]/route.ts</files>
  <action>
    Create two API route files following the pattern from src/app/api/content/route.ts:

    **src/app/api/events/route.ts:**
    - GET: List all events for the tenant, ordered by date descending. Use withTenant() for tenant isolation. Include session check. Return JSON array of events.
    - POST: Create a new event. Validate required fields (title, description, date, location, organizer). Use withTenant() for tenant isolation. Generate UUID for id. Return 201 with created event.

    **src/app/api/events/[id]/route.ts:**
    - GET: Get single event by ID. Return 404 if not found.
    - PATCH: Update event by ID. Accept partial updates. Return updated event.
    - DELETE: Delete event by ID. Return 200 with success message.

    All routes must:
    - Check authentication via auth.api.getSession()
    - Use withTenant() for tenant isolation
    - Use Drizzle ORM (db, events from @api/db)
    - Include revalidateContent() call on mutations (POST, PATCH, DELETE)

    Import pattern: `import { db, events } from '@api/db'`
    Use `eq(events.id, id)` for ID lookups.
    Use `desc(events.date)` for ordering.

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify all 5 API endpoints exist with correct HTTP methods, tenant isolation, and auth checks</manual>
  </verify>
  <done>Events API has GET list, POST create, GET by ID, PATCH update, DELETE with tenant isolation and auth</done>
</task>

<task type="auto">
  <name>Task 2: Create EventForm and EventList components</name>
  <files>src/widgets/admin/ui/EventForm.tsx, src/widgets/admin/ui/EventList.tsx</files>
  <action>
    Create two components:

    **src/widgets/admin/ui/EventForm.tsx:**
    Form component for creating/editing events. Fields:
    - Title (text input, required)
    - Description (textarea, required)
    - Date (datetime-local input, required)
    - Location (text input, required)
    - Organizer (text input, required)
    - Image URL (text input, optional)
    - isPublic (checkbox, default true)

    Use React Hook Form with Zod validation. Follow the pattern from ContentForm but simpler (no Tiptap editor, no localization). Include loading state on submit. Accept optional `defaultValues` prop for edit mode. On submit, POST to /api/events (create) or PATCH /api/events/[id] (update).

    **src/widgets/admin/ui/EventList.tsx:**
    Table/list component showing events. Columns: Title, Date, Location, Organizer, Actions (Edit, Delete buttons). Fetch events from GET /api/events on mount. Show loading state while fetching. Show empty state if no events. Format dates using toLocaleDateString(). Include delete confirmation dialog.

    Use Tailwind classes consistent with existing admin components. Follow the pattern from ContentList if it exists.

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify EventForm has all required fields with validation, EventList fetches and displays events with edit/delete actions</manual>
  </verify>
  <done>EventForm creates/updates events via API, EventList displays events with edit/delete actions, both TypeScript compile</done>
</task>

<task type="auto">
  <name>Task 3: Create admin events pages (list, new, edit)</name>
  <files>src/app/(tenant)/admin/events/page.tsx, src/app/(tenant)/admin/events/new/page.tsx, src/app/(tenant)/admin/events/[id]/page.tsx</files>
  <action>
    Create three page components following the pattern from admin/content pages:

    **src/app/(tenant)/admin/events/page.tsx:**
    Server component that renders EventList. Include page header with title "Events" and "New Event" button linking to /admin/events/new.

    **src/app/(tenant)/admin/events/new/page.tsx:**
    Client component that renders EventForm. On successful creation, redirect to /admin/events. Include page header with title "New Event" and back button.

    **src/app/(tenant)/admin/events/[id]/page.tsx:**
    Server component that fetches the event by ID, passes data to EventForm in edit mode. On successful update, redirect to /admin/events. Include page header with title "Edit Event" and back button. Handle 404 if event not found.

    All pages should be in the (tenant) route group, meaning they require authentication. Follow the layout pattern from existing admin pages (header, back button, etc.).

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify all three pages exist, EventList page has "New Event" link, new/edit pages redirect after submission</manual>
  </verify>
  <done>Admin events section has list page with "New Event" button, create page redirects after success, edit page loads existing event and redirects after update</done>
</task>

</tasks>

<verification>
- Events API has all 5 CRUD endpoints with tenant isolation and auth
- EventForm creates and updates events with validation
- EventList displays events with edit/delete actions
- Admin events pages: list, new, edit all functional
- TypeScript type check passes
- Events follow same patterns as content (API structure, form validation, page layout)
</verification>

<success_criteria>

- Admin can navigate to /admin/events and see event list
- Admin can create new event via /admin/events/new
- Admin can edit existing event via /admin/events/[id]
- Admin can delete event with confirmation
- All events scoped to tenant via withTenant()
- TypeScript type check passes
  </success_criteria>

<output>
After completion, create `.planning/phases/21-content-events/21-02-SUMMARY.md`
</output>
