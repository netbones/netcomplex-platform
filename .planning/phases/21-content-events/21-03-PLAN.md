---
phase: 21-content-events
plan: 03
type: execute
wave: 2
depends_on: ['21-02']
files_modified:
  - src/entities/admin/model/admin-config.ts
  - src/widgets/dashboard/types.ts
  - src/widgets/admin/ui/EventsWidget.tsx
autonomous: true
requirements: [EVENTS-03]

must_haves:
  truths:
    - 'Dashboard has an Events tab showing upcoming events'
    - 'Events tab shows event title, date, location for next 5 events'
    - 'Events tab links to /admin/events for full management'
  artifacts:
    - path: 'src/entities/admin/model/admin-config.ts'
      provides: 'Events tab in ADMIN_TABS array'
      contains: 'events.*DashboardTab'
    - path: 'src/widgets/admin/ui/EventsWidget.tsx'
      provides: 'Dashboard widget showing upcoming events'
      contains: 'fetch.*api/events'
  key_links:
    - from: 'src/widgets/admin/ui/EventsWidget.tsx'
      to: 'src/app/api/events/route.ts'
      via: 'GET /api/events?limit=5'
      pattern: 'fetch.*api/events'
---

<objective>
Add Events tab to admin dashboard with widget showing upcoming events.

Purpose: The admin dashboard currently has tabs for Overview, Maintenance, Users, Content, System, and Settings. Events need their own tab so admins can quickly see upcoming events without navigating to the full events management page. The widget shows a preview with a link to full management.

Output: Events tab in admin dashboard config, EventsWidget component showing upcoming 5 events.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/entities/admin/model/admin-config.ts — Admin tabs configuration (add events tab here)
@src/widgets/dashboard/types.ts — DashboardTab type definition
@src/app/api/events/route.ts — Events API (created in Plan 02)
@src/widgets/admin/ui/ContentForm.tsx — Pattern reference for widget styling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Events tab to admin dashboard configuration</name>
  <files>src/entities/admin/model/admin-config.ts</files>
  <action>
    Edit src/entities/admin/model/admin-config.ts to add an Events tab:

    1. Add a new entry to ADMIN_TABS array after the 'content' tab:
    ```typescript
    {
      id: 'events',
      label: 'Events',
      icon: 'fa-calendar',
      defaultWidgets: ['admin-events', 'admin-quick-links'],
    },
    ```

    2. Add 'admin-events' to ALL_ADMIN_WIDGETS array:
    ```typescript
    { id: 'admin-events', label: 'Upcoming Events', icon: 'fa-calendar' },
    ```

    3. Add sizing for admin-events widget in getAdminWidgetSize():
    ```typescript
    case 'admin-events':
      return 'medium';
    ```

    Place the events tab after content and before system. Use fa-calendar icon (Font Awesome).

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify ADMIN_TABS includes events tab, ALL_ADMIN_WIDGETS includes admin-events widget</manual>
  </verify>
  <done>Admin dashboard config includes Events tab with icon and default widgets, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create EventsWidget component for dashboard</name>
  <files>src/widgets/admin/ui/EventsWidget.tsx</files>
  <action>
    Create src/widgets/admin/ui/EventsWidget.tsx:

    Client component that:
    1. Fetches upcoming events from GET /api/events on mount (use useEffect + useState, or TanStack Query if project uses it)
    2. Displays up to 5 upcoming events (filter events where date >= now, sort by date ascending, limit 5)
    3. For each event shows: title, formatted date, location
    4. Includes "View All Events" link to /admin/events
    5. Shows loading state while fetching
    6. Shows empty state with "No upcoming events" message and "Create Event" link to /admin/events/new
    7. Handles error state with retry button

    Styling: Use Tailwind classes consistent with other admin widgets. Card layout with event items as list. Each event item should be a row with title (bold), date (muted color), location (muted color).

    Do NOT include edit/delete actions in the widget — those are on the full events page. This is a read-only preview.

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify EventsWidget fetches events, shows up to 5 upcoming events, has loading/empty/error states, links to /admin/events</manual>
  </verify>
  <done>EventsWidget displays upcoming events preview with loading/empty/error states, links to full events management, TypeScript compiles</done>
</task>

</tasks>

<verification>
- Admin dashboard has Events tab in tab navigation
- Events tab shows EventsWidget with upcoming events
- EventsWidget fetches from API and displays up to 5 events
- Loading, empty, and error states handled
- Links to /admin/events and /admin/events/new present
- TypeScript type check passes
</verification>

<success_criteria>

- Admin dashboard shows Events tab alongside other tabs
- Events tab displays upcoming events preview
- Widget shows max 5 events sorted by date
- Empty state shows "Create Event" link
- TypeScript type check passes
  </success_criteria>

<output>
After completion, create `.planning/phases/21-content-events/21-03-SUMMARY.md`
</output>
