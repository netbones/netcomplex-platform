---
phase: 38-space-layers
plan: 04
type: execute
wave: 3
depends_on: ['38-02', '38-03']
files_modified:
  - src/app/(tenant)/dashboard/[space]/page.tsx
  - src/widgets/dashboard/model/spaces.ts
  - public/locales/en/services.json
  - public/locales/en/messages.json
autonomous: true
requirements:
  - LAYER-03
  - LAYER-04
must_haves:
  truths:
    - 'Navigating to /dashboard/services renders ServicesLayer (not SpaceLayout widget grid)'
    - 'Navigating to /dashboard/messages renders MessagesLayer (not SpaceLayout widget grid)'
    - 'Navigating to /dashboard/community still renders SpaceLayout (DnD widgets)'
    - 'Domain cards in ServicesLayer link to working /dashboard/services/[domain] routes'
    - 'Domain cards in MessagesLayer link to /dashboard/messages/announcements (existing) or show coming-soon for new domains'
  artifacts:
    - path: 'src/app/(tenant)/dashboard/[space]/page.tsx'
      provides: 'Routing switch for all 5 spaces'
      exports: ['default']
      min_lines: 30
    - path: 'src/widgets/dashboard/model/spaces.ts'
      provides: 'SERVICES_DOMAINS + MESSAGES_DOMAINS constants, updated MESSAGES_SUB_ROUTES'
      exports: ['SERVICES_DOMAINS', 'MESSAGES_DOMAINS', 'MESSAGES_SUB_ROUTES']
    - path: 'public/locales/en/services.json'
      provides: 'Domain label + description i18n keys for ServicesLayer'
      contains: 'domains'
    - path: 'public/locales/en/messages.json'
      provides: 'Domain label + description i18n keys for MessagesLayer'
      contains: 'domains'
  key_links:
    - from: '[space]/page.tsx'
      to: 'ServicesLayer'
      via: "conditional import when spaceId === 'services'"
      pattern: 'ServicesLayer'
    - from: '[space]/page.tsx'
      to: 'MessagesLayer'
      via: "conditional import when spaceId === 'messages'"
      pattern: 'MessagesLayer'
    - from: 'ServicesSubLauncher.tsx'
      to: 'public/locales/en/services.json'
      via: 'i18n labelKey/descriptionKey references'
      pattern: "services\\.domains\\."
    - from: 'MessagesSubLauncher.tsx'
      to: 'public/locales/en/messages.json'
      via: 'i18n labelKey/descriptionKey references'
      pattern: "messages\\.domains\\."
---

<objective>
Wire ServicesLayer and MessagesLayer into the routing system, add domain definitions to spaces.ts, and populate i18n keys for domain labels/descriptions.

Purpose: Plans 02 and 03 create the layer components but they're unreachable until this plan routes to them. This plan also ensures domain definitions are canonical (in spaces.ts, matching the admin pattern) and i18n keys exist so the layers render properly.

Output: Working navigation to /dashboard/services (ServicesLayer) and /dashboard/messages (MessagesLayer), with domain sub-routes defined and i18n populated.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-space-layers/38-02-SUMMARY.md
@.planning/phases/38-space-layers/38-03-SUMMARY.md
@src/app/(tenant)/dashboard/[space]/page.tsx
@src/widgets/dashboard/model/spaces.ts
@public/locales/en/services.json
@public/locales/en/messages.json
</context>

<tasks>
<task type="auto">
<name>Task 1: Add domain constants to spaces.ts + update routing in [space]/page.tsx</name>
<files>src/widgets/dashboard/model/spaces.ts, src/app/(tenant)/dashboard/[space]/page.tsx</files>
<action>
**Part A: Update spaces.ts**

Add services domain definitions (following ADMIN_DOMAINS pattern at line 250):

```ts
export const SERVICES_DOMAINS = [
  'maintenance',
  'bookings',
  'amenities',
  'my-services',
  'events',
] as const;

export type ServicesDomain = (typeof SERVICES_DOMAINS)[number];
```

Update MESSAGES_SUB_ROUTES to include all 3 message domains (currently only ['announcements']):

```ts
export const MESSAGES_SUB_ROUTES = ['conversations', 'announcements', 'notifications'] as const;
```

**Part B: Update [space]/page.tsx**

Current file routes 'admin' → AdminLayer, everything else → SpaceLayoutWithErrorBoundary.

Add two new branches BEFORE the SpaceLayout fallback:

```tsx
// Services space uses ServicesLayer (urgency zone + domain grid)
if (spaceId === 'services') {
  return <ServicesLayer />;
}

// Messages space uses MessagesLayer (urgency zone + domain grid)
if (spaceId === 'messages') {
  return <MessagesLayer />;
}
```

Add imports at top:

```tsx
import { ServicesLayer } from '@widgets/dashboard/ui/ServicesLayer';
import { MessagesLayer } from '@widgets/dashboard/ui/MessagesLayer';
```

The final routing order should be:

1. spaceId === 'admin' → AdminLayer
2. spaceId === 'services' → ServicesLayer
3. spaceId === 'messages' → MessagesLayer
4. everything else (home, community) → SpaceLayoutWithErrorBoundary

This preserves /dashboard/community as the ONLY true DnD widget space (SpaceLayout).
</action>
<verify>
<automated>npx tsc --noEmit --pretty 2>&1 | grep -i "page.tsx\|spaces.ts" | head -10 || echo "No TypeScript errors for routing files"</automated>
<sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
<done>/dashboard/services routes to ServicesLayer, /dashboard/messages routes to MessagesLayer, /dashboard/community still routes to SpaceLayout, SERVICES_DOMAINS and MESSAGES_SUB_ROUTES defined in spaces.ts</done>
</task>

<task type="auto">
<name>Task 2: Populate i18n keys for service and message domains</name>
<files>public/locales/en/services.json, public/locales/en/messages.json</files>
<action>
**Part A: Update services.json**

Add domain keys to the existing services.json (currently has 36 lines of service-request form keys). Append a "domains" section:

```json
{
  ...existing keys...,
  "domains": {
    "maintenance": "Maintenance",
    "bookings": "Bookings",
    "amenities": "Amenities",
    "myServices": "My Services",
    "events": "Events",
    "descriptions": {
      "maintenance": "Submit and track maintenance requests",
      "bookings": "Book community facilities and amenities",
      "amenities": "Community amenities and shared spaces",
      "myServices": "Community services you offer",
      "events": "Upcoming community events and activities"
    }
  }
}
```

Note: Use "myServices" (camelCase) not "my-services" because JSON keys don't support hyphens well with i18n libraries. The ServicesSubLauncher.tsx labelKey uses 'services.domains.myServices' — match exactly.

**Part B: Update messages.json**

The current messages.json is minimal (13 lines of generic toast messages). Add domain keys:

```json
{
  ...existing keys...,
  "domains": {
    "conversations": "Conversations",
    "announcements": "Announcements",
    "notifications": "Notifications",
    "descriptions": {
      "conversations": "Direct and group conversations",
      "announcements": "Community announcements and broadcasts",
      "notifications": "System notifications and alerts"
    }
  }
}
```

IMPORTANT: Do NOT remove or change any existing keys in either file. Only ADD the "domains" section.
</action>
<verify>
<automated>node -e "const s=require('./public/locales/en/services.json'); const m=require('./public/locales/en/messages.json'); console.assert(s.domains, 'services.json missing domains'); console.assert(m.domains, 'messages.json missing domains'); console.assert(s.domains.maintenance==='Maintenance', 'services domains.maintenance wrong'); console.assert(m.domains.conversations==='Conversations', 'messages domains.conversations wrong'); console.log('i18n OK')"</automated>
<sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
<done>services.json contains domains.maintenance/bookings/amenities/myServices/events + descriptions; messages.json contains domains.conversations/announcements/notifications + descriptions</done>
</task>
</tasks>

<verification>
- /dashboard/services renders ServicesLayer (not SpaceLayout)
- /dashboard/messages renders MessagesLayer (not SpaceLayout)
- /dashboard/community still renders SpaceLayout (DnD widget grid)
- /dashboard/admin still renders AdminLayer (unchanged)
- /dashboard/home still renders SpaceLayout (unchanged)
- Domain cards in ServicesLayer show translated labels from services.json
- Domain cards in MessagesLayer show translated labels from messages.json
- SERVICES_DOMAINS constant exists in spaces.ts with 5 domains
- MESSAGES_SUB_ROUTES updated to include conversations, announcements, notifications
</verification>

<success_criteria>

- Navigating /dashboard/services shows urgency zone + domain grid (not widget grid)
- Navigating /dashboard/messages shows urgency zone + domain grid (not widget grid)
- Navigating /dashboard/community still shows DnD widget grid
- All domain labels render from i18n keys
- No TypeScript compilation errors
  </success_criteria>

<output>
After completion, create `.planning/phases/38-space-layers/38-04-SUMMARY.md`
</output>
