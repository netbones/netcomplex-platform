---
phase: 38-space-layers
plan: 02
type: execute
wave: 2
depends_on: ['38-01']
files_modified:
  - src/widgets/dashboard/ui/ServicesLayer.tsx
  - src/widgets/dashboard/ui/ServicesCommandBar.tsx
  - src/widgets/dashboard/ui/ServicesSubLauncher.tsx
autonomous: true
requirements:
  - LAYER-01
  - LAYER-03
must_haves:
  truths:
    - 'User sees urgency chips (open maintenance, upcoming bookings) at top of Services space'
    - 'User sees domain cards (maintenance, bookings, amenities, my-services, events) in a responsive grid'
    - 'Each domain card shows an urgency badge if count > 0'
    - 'Domain cards link to /dashboard/services/[domain] sub-pages'
  artifacts:
    - path: 'src/widgets/dashboard/ui/ServicesLayer.tsx'
      provides: 'Main Services space layer component'
      exports: ['ServicesLayer']
      min_lines: 80
    - path: 'src/widgets/dashboard/ui/ServicesCommandBar.tsx'
      provides: 'Services urgency command bar with reactive CTAs + shortcuts'
      exports: ['ServicesCommandBar', 'ServicesCommandBarUrgency']
      min_lines: 60
    - path: 'src/widgets/dashboard/ui/ServicesSubLauncher.tsx'
      provides: 'Service domain definitions and domain card grid'
      exports: ['SERVICES_DOMAIN_DEFINITIONS', 'ServicesDomainDef']
      min_lines: 50
  key_links:
    - from: 'ServicesLayer.tsx'
      to: '/api/services/urgency'
      via: 'fetch in useEffect on mount'
      pattern: 'fetch.*api/services/urgency'
    - from: 'ServicesLayer.tsx'
      to: 'ServicesCommandBar'
      via: 'import + render as first section'
      pattern: 'ServicesCommandBar'
    - from: 'ServicesLayer.tsx'
      to: 'ServicesSubLauncher'
      via: 'import + render as domain grid section'
      pattern: 'SERVICES_DOMAIN_DEFINITIONS'
---

<objective>
Build the ServicesLayer component — a zoned space layer (urgency → domain grid) for the /dashboard/services space, following the AdminLayer pattern.

Purpose: Services currently falls through to SpaceLayout (widget grid), but it should have a purpose-built layer like Admin and Home. The ServicesLayer provides urgency-driven CTAs (open maintenance, upcoming bookings) and domain navigation cards (maintenance, bookings, amenities, etc.).

Output: Three new files — ServicesLayer.tsx, ServicesCommandBar.tsx, ServicesSubLauncher.tsx — ready for routing integration in Plan 04.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-space-layers/38-01-SUMMARY.md
@src/widgets/dashboard/ui/AdminLayer.tsx
@src/widgets/dashboard/ui/AdminCommandBar.tsx
@src/widgets/dashboard/ui/AdminSubLauncher.tsx
</context>

<tasks>
<task type="auto">
<name>Task 1: Create ServicesSubLauncher with domain definitions</name>
<files>src/widgets/dashboard/ui/ServicesSubLauncher.tsx</files>
<action>
Create src/widgets/dashboard/ui/ServicesSubLauncher.tsx following the EXACT pattern of AdminSubLauncher.tsx:

1. Export interface `ServicesDomainDef` — mirrors AdminDomainDef exactly: { id: string; labelKey: string; descriptionKey: string; icon: LucideIcon; description: string }
2. Export const `SERVICES_DOMAIN_DEFINITIONS: ServicesDomainDef[]` with 5 domains:
   - `{ id: 'maintenance', labelKey: 'services.domains.maintenance', descriptionKey: 'services.domains.descriptions.maintenance', icon: Wrench, description: 'Submit and track maintenance requests' }`
   - `{ id: 'bookings', labelKey: 'services.domains.bookings', descriptionKey: 'services.domains.descriptions.bookings', icon: Calendar, description: 'Book community facilities and amenities' }`
   - `{ id: 'amenities', labelKey: 'services.domains.amenities', descriptionKey: 'services.domains.descriptions.amenities', icon: Building, description: 'Community amenities and shared spaces' }` (import Building from lucide-react)
   - `{ id: 'my-services', labelKey: 'services.domains.myServices', descriptionKey: 'services.domains.descriptions.myServices', icon: Briefcase, description: 'Community services you offer' }`
   - `{ id: 'events', labelKey: 'services.domains.events', descriptionKey: 'services.domains.descriptions.events', icon: CalendarDays, description: 'Upcoming community events and activities' }` (import CalendarDays from lucide-react)

3. Do NOT export a standalone component function (AdminSubLauncher exports one but it's unused since AdminLayer renders its own DomainCard). Instead, just export the definitions — the ServicesLayer will render its own DomainCard inline (same as AdminLayer).

IMPORTANT: Use 'use client' directive. Import icons from lucide-react. Use i18n keys under 'services' namespace (matching existing public/locales/en/services.json pattern).
</action>
<verify>
<automated>npx tsc --noEmit --pretty 2>&1 | grep -i "ServicesSubLauncher" | head -5 || echo "No TypeScript errors for ServicesSubLauncher"</automated>
<manual>Verify file exports SERVICES_DOMAIN_DEFINITIONS with 5 domains, each having id/labelKey/descriptionKey/icon/description</manual>
<sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
<done>ServicesSubLauncher.tsx exports ServicesDomainDef type and SERVICES_DOMAIN_DEFINITIONS array with 5 service domains</done>
</task>

<task type="auto">
<name>Task 2: Create ServicesCommandBar + ServicesLayer</name>
<files>src/widgets/dashboard/ui/ServicesCommandBar.tsx, src/widgets/dashboard/ui/ServicesLayer.tsx</files>
<action>
Create TWO files:

**File 1: ServicesCommandBar.tsx**
Follow AdminCommandBar.tsx pattern but simplified for resident users:

1. Export interface `ServicesCommandBarUrgency { openMaintenance: number; upcomingBookings: number }`
2. Export component `ServicesCommandBar` with props: `{ urgency: ServicesCommandBarUrgency }`
3. Reactive CTAs (only shown if count > 0):
   - openMaintenance > 0 → UrgencyChip href="/dashboard/services/maintenance" icon=Wrench label="{count} open request(s)" colour="red"
   - upcomingBookings > 0 → UrgencyChip href="/dashboard/services/bookings" icon=Calendar label="{count} upcoming booking(s)" colour="blue"
4. Shortcuts row (always visible, resident-focused):
   - "New Request" → href="/dashboard/services/maintenance?action=new" icon=WrenchPlus (import from lucide-react)
   - "Book Facility" → href="/dashboard/services/bookings?action=new" icon=CalendarPlus
5. NO AddShortcutPopover (keep it simple for residents — not needed for MVP)
6. Use same UrgencyChip + ShortcutButton sub-components as AdminCommandBar (copy inline, don't import from admin)
7. Use 'use client' directive

**File 2: ServicesLayer.tsx**
Follow AdminLayer.tsx pattern exactly:

1. Import ServicesCommandBar + SERVICES_DOMAIN_DEFINITIONS
2. Define `UrgencyResponse { commandBar: ServicesCommandBarUrgency; domainBadges: Record<string, number> }`
3. Fetch /api/services/urgency on mount (useEffect + useCallback), unwrap apiSuccess envelope
4. Render skeleton while loading, error with retry on failure
5. Main render:
   - Section 1: `<ServicesCommandBar urgency={urgency.commandBar} />`
   - Section 2: Domain grid heading "Service Areas" (no i18n for heading yet — hardcode, i18n in Plan 04) + responsive grid of DomainCard components (same as AdminLayer's DomainCard but using ServicesDomainDef type)
   - DomainCard links to `/dashboard/services/${domain.id}` (no route overrides needed for services)
   - Badge from `urgency.domainBadges[domain.id] ?? 0`
6. NO activity stream (services doesn't need one for MVP — may add later)
7. Use 'use client' directive
8. Export both named and default: `export function ServicesLayer()` + `export default ServicesLayer`
   </action>
   <verify>
   <automated>npx tsc --noEmit --pretty 2>&1 | grep -i "ServicesLayer\|ServicesCommandBar" | head -5 || echo "No TypeScript errors for Services components"</automated>
   <manual>Navigate to /dashboard/services after Plan 04 routes it — should show command bar + domain grid</manual>
   <sampling_rate>run after this task commits, before next task begins</sampling_rate>
   </verify>
   <done>ServicesLayer renders urgency command bar + 5-card domain grid, fetching data from /api/services/urgency, following AdminLayer pattern</done>
   </task>
   </tasks>

<verification>
- ServicesSubLauncher exports SERVICES_DOMAIN_DEFINITIONS with 5 domains
- ServicesCommandBar renders urgency chips + shortcut buttons
- ServicesLayer fetches /api/services/urgency and renders command bar + domain grid
- Domain cards link to /dashboard/services/[domain]
- No TypeScript compilation errors
</verification>

<success_criteria>

- ServicesLayer.tsx compiles and follows AdminLayer pattern
- ServicesCommandBar shows reactive CTAs for open maintenance + upcoming bookings
- Domain grid shows 5 service domains with urgency badges
- All domain cards link to /dashboard/services/[id]
  </success_criteria>

<output>
After completion, create `.planning/phases/38-space-layers/38-02-SUMMARY.md`
</output>
