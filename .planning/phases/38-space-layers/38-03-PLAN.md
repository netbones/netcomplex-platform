---
phase: 38-space-layers
plan: 03
type: execute
wave: 2
depends_on: ['38-01']
files_modified:
  - src/widgets/dashboard/ui/MessagesLayer.tsx
  - src/widgets/dashboard/ui/MessagesCommandBar.tsx
  - src/widgets/dashboard/ui/MessagesSubLauncher.tsx
autonomous: true
requirements:
  - LAYER-02
  - LAYER-03
must_haves:
  truths:
    - 'User sees urgency chips (unread DMs, unread group messages, unread announcements) at top of Messages space'
    - 'User sees domain cards (conversations, announcements, notifications) in a responsive grid'
    - 'Each domain card shows an urgency badge if count > 0'
    - 'Domain cards link to /dashboard/messages/[domain] sub-pages'
  artifacts:
    - path: 'src/widgets/dashboard/ui/MessagesLayer.tsx'
      provides: 'Main Messages space layer component'
      exports: ['MessagesLayer']
      min_lines: 80
    - path: 'src/widgets/dashboard/ui/MessagesCommandBar.tsx'
      provides: 'Messages urgency command bar with reactive CTAs + shortcuts'
      exports: ['MessagesCommandBar', 'MessagesCommandBarUrgency']
      min_lines: 60
    - path: 'src/widgets/dashboard/ui/MessagesSubLauncher.tsx'
      provides: 'Message domain definitions and domain card grid'
      exports: ['MESSAGES_DOMAIN_DEFINITIONS', 'MessagesDomainDef']
      min_lines: 40
  key_links:
    - from: 'MessagesLayer.tsx'
      to: '/api/messages/urgency'
      via: 'fetch in useEffect on mount'
      pattern: 'fetch.*api/messages/urgency'
    - from: 'MessagesLayer.tsx'
      to: 'MessagesCommandBar'
      via: 'import + render as first section'
      pattern: 'MessagesCommandBar'
    - from: 'MessagesLayer.tsx'
      to: 'MessagesSubLauncher'
      via: 'import domain defs for DomainCard rendering'
      pattern: 'MESSAGES_DOMAIN_DEFINITIONS'
---

<objective>
Build the MessagesLayer component — a zoned space layer (urgency → domain grid) for the /dashboard/messages space, following the AdminLayer pattern.

Purpose: Messages currently falls through to SpaceLayout (widget grid), but it should have a purpose-built layer like Admin and Home. The MessagesLayer provides urgency-driven CTAs (unread DMs, group messages, announcements) and domain navigation cards (conversations, announcements, notifications).

Output: Three new files — MessagesLayer.tsx, MessagesCommandBar.tsx, MessagesSubLauncher.tsx — ready for routing integration in Plan 04.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-space-layers/38-01-SUMMARY.md
@src/widgets/dashboard/ui/AdminLayer.tsx
@src/widgets/dashboard/ui/AdminCommandBar.tsx
@src/widgets/dashboard/ui/AdminSubLauncher.tsx
</context>

<tasks>
<task type="auto">
<name>Task 1: Create MessagesSubLauncher with domain definitions</name>
<files>src/widgets/dashboard/ui/MessagesSubLauncher.tsx</files>
<action>
Create src/widgets/dashboard/ui/MessagesSubLauncher.tsx following the EXACT pattern of AdminSubLauncher.tsx:

1. Export interface `MessagesDomainDef` — mirrors AdminDomainDef: { id: string; labelKey: string; descriptionKey: string; icon: LucideIcon; description: string }
2. Export const `MESSAGES_DOMAIN_DEFINITIONS: MessagesDomainDef[]` with 3 domains:
   - `{ id: 'conversations', labelKey: 'messages.domains.conversations', descriptionKey: 'messages.domains.descriptions.conversations', icon: MessageSquare, description: 'Direct and group conversations' }`
   - `{ id: 'announcements', labelKey: 'messages.domains.announcements', descriptionKey: 'messages.domains.descriptions.announcements', icon: Megaphone, description: 'Community announcements and broadcasts' }`
   - `{ id: 'notifications', labelKey: 'messages.domains.notifications', descriptionKey: 'messages.domains.descriptions.notifications', icon: Bell, description: 'System notifications and alerts' }`

3. Only export definitions (no standalone component — MessagesLayer renders DomainCard inline like AdminLayer).

IMPORTANT: Use 'use client' directive. Import icons from lucide-react. Use i18n keys under 'messages' namespace.
</action>
<verify>
<automated>npx tsc --noEmit --pretty 2>&1 | grep -i "MessagesSubLauncher" | head -5 || echo "No TypeScript errors for MessagesSubLauncher"</automated>
<sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
<done>MessagesSubLauncher.tsx exports MessagesDomainDef type and MESSAGES_DOMAIN_DEFINITIONS array with 3 message domains</done>
</task>

<task type="auto">
<name>Task 2: Create MessagesCommandBar + MessagesLayer</name>
<files>src/widgets/dashboard/ui/MessagesCommandBar.tsx, src/widgets/dashboard/ui/MessagesLayer.tsx</files>
<action>
Create TWO files:

**File 1: MessagesCommandBar.tsx**
Follow AdminCommandBar.tsx pattern but simplified for all users:

1. Export interface `MessagesCommandBarUrgency { unreadDirect: number; unreadGroup: number; unreadAnnouncements: number }`
2. Export component `MessagesCommandBar` with props: `{ urgency: MessagesCommandBarUrgency }`
3. Reactive CTAs (only shown if count > 0):
   - unreadDirect > 0 → UrgencyChip href="/dashboard/messages/conversations" icon=MessageSquare label="{count} unread direct message(s)" colour="red"
   - unreadGroup > 0 → UrgencyChip href="/dashboard/messages/conversations" icon=Users icon=Users label="{count} unread group message(s)" colour="amber"
   - unreadAnnouncements > 0 → UrgencyChip href="/dashboard/messages/announcements" icon=Megaphone label="{count} new announcement(s)" colour="orange"
4. Shortcuts row (always visible):
   - "New Message" → href="/dashboard/messages/conversations?action=new" icon=Send (import from lucide-react)
5. NO AddShortcutPopover (keep simple for MVP)
6. Use same UrgencyChip + ShortcutButton sub-components as AdminCommandBar (copy inline)
7. Use 'use client' directive

**File 2: MessagesLayer.tsx**
Follow AdminLayer.tsx pattern exactly:

1. Import MessagesCommandBar + MESSAGES_DOMAIN_DEFINITIONS
2. Define `UrgencyResponse { commandBar: MessagesCommandBarUrgency; domainBadges: Record<string, number> }`
3. Fetch /api/messages/urgency on mount (useEffect + useCallback), unwrap apiSuccess envelope
4. Render skeleton while loading, error with retry on failure
5. Main render:
   - Section 1: `<MessagesCommandBar urgency={urgency.commandBar} />`
   - Section 2: Domain grid heading "Communication" (hardcode, i18n in Plan 04) + responsive grid of DomainCard components (same as AdminLayer's DomainCard but using MessagesDomainDef type)
   - DomainCard links to `/dashboard/messages/${domain.id}`
   - Badge from `urgency.domainBadges[domain.id] ?? 0`
6. NO activity stream (messages doesn't need one — the conversations list IS the activity)
7. Use 'use client' directive
8. Export both named and default: `export function MessagesLayer()` + `export default MessagesLayer`
   </action>
   <verify>
   <automated>npx tsc --noEmit --pretty 2>&1 | grep -i "MessagesLayer\|MessagesCommandBar" | head -5 || echo "No TypeScript errors for Messages components"</automated>
   <manual>Navigate to /dashboard/messages after Plan 04 routes it — should show command bar + domain grid</manual>
   <sampling_rate>run after this task commits, before next task begins</sampling_rate>
   </verify>
   <done>MessagesLayer renders urgency command bar + 3-card domain grid, fetching data from /api/messages/urgency, following AdminLayer pattern</done>
   </task>
   </tasks>

<verification>
- MessagesSubLauncher exports MESSAGES_DOMAIN_DEFINITIONS with 3 domains
- MessagesCommandBar renders urgency chips + shortcut buttons
- MessagesLayer fetches /api/messages/urgency and renders command bar + domain grid
- Domain cards link to /dashboard/messages/[domain]
- No TypeScript compilation errors
</verification>

<success_criteria>

- MessagesLayer.tsx compiles and follows AdminLayer pattern
- MessagesCommandBar shows reactive CTAs for unread DMs, groups, announcements
- Domain grid shows 3 message domains with urgency badges
- All domain cards link to /dashboard/messages/[id]
  </success_criteria>

<output>
After completion, create `.planning/phases/38-space-layers/38-03-SUMMARY.md`
</output>
