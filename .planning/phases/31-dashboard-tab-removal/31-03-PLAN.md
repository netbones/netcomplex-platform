---
phase: 31-dashboard-tab-removal
plan: 03
type: execute
wave: 2
depends_on: ['31-01']
files_modified:
  - src/entities/widget/model/default-layouts.ts
  - src/widgets/dashboard/ui/WidgetCard.tsx
  - src/widgets/dashboard/ui/DraggableWidget.tsx
  - src/widgets/dashboard/ui/SpaceLayout.tsx
autonomous: true
requirements:
  - TAB-REM-03
  - TAB-REM-06
must_haves:
  truths:
    - 'default-layouts.ts only exports space-keyed defaults (no tab-keyed constants or helpers)'
    - 'getDefaultLayout() is the only layout lookup function (no getSpaceDefaultLayout, no getDefaultLayout tab version)'
    - 'remapToSpaces and remapLayoutsToSpaces helper functions no longer exist'
    - 'WidgetCard and DraggableWidget use spaceId prop name instead of tabId'
    - 'SpaceLayout uses new store method names (addWidgetToSpace, removeWidgetFromSpace)'
  artifacts:
    - path: 'src/entities/widget/model/default-layouts.ts'
      provides: 'Only space-keyed defaults — SPACE_DEFAULT_USER_WIDGETS, SPACE_DEFAULT_LAYOUTS, getDefaultLayout (renamed from getSpaceDefaultLayout)'
      min_lines: 100
  key_links:
    - from: 'src/widgets/dashboard/ui/SpaceLayout.tsx'
      to: 'src/entities/widget/model/widget-store.ts'
      via: 'useWidgetStore — addWidgetToSpace, removeWidgetFromSpace, getSpaceDefaultLayout→getDefaultLayout'
      pattern: 'addWidgetToSpace|removeWidgetFromSpace'
---

<objective>
Strip all tab-keyed default layouts, remove migration helper functions, rename `getSpaceDefaultLayout()` → `getDefaultLayout()`, and rename `tabId`→`spaceId` prop in WidgetCard/DraggableWidget/SpaceLayout.

Purpose: After Plan 31-01 migrates data and Plan 31-02 deletes old tab files, the default-layouts file still carries ~210 lines of legacy tab-keyed defaults and migration helpers. This plan removes them and renames the canonical space-keyed defaults. Component props are also renamed for API consistency.

Output: Clean default-layouts.ts with only space-keyed exports, WidgetCard/DraggableWidget/SpaceLayout using `spaceId` prop
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/entities/widget/model/default-layouts.ts
@src/widgets/dashboard/ui/WidgetCard.tsx
@src/widgets/dashboard/ui/DraggableWidget.tsx
@src/widgets/dashboard/ui/SpaceLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip tab-keyed defaults and rename getSpaceDefaultLayout→getDefaultLayout</name>
  <files>src/entities/widget/model/default-layouts.ts</files>
  <action>
    In `src/entities/widget/model/default-layouts.ts`:

    **1. Remove the `TAB_TO_SPACE_MAP` import:**
    ```typescript
    // DELETE: import { TAB_TO_SPACE_MAP } from './tab-migration-map';
    ```
    (tab-migration-map.ts was deleted in Plan 31-02)

    **2. Remove the legacy tab-keyed defaults section** (lines ~28-209):
    Delete everything between `// LEGACY TAB-KEYED DEFAULTS` and the space-keyed section. This includes:
    - `RESIDENT_USER_WIDGETS` and `RESIDENT_LAYOUTS`
    - `BOARD_USER_WIDGETS` and `BOARD_LAYOUTS`
    - `ADMIN_USER_WIDGETS` and `ADMIN_LAYOUTS`
    - `DEFAULT_USER_WIDGETS`
    - `DEFAULT_LAYOUTS`
    - `getDefaultLayout()` function

    **3. Remove migration helper functions** (lines ~220-253):
    Remove:
    - `remapToSpaces()` function
    - `remapLayoutsToSpaces()` function
    - Any other references to `TAB_TO_SPACE_MAP` or remapping

    **4. Rename the space-keyed defaults:**
    - `SPACE_DEFAULT_USER_WIDGETS` → `DEFAULT_USER_WIDGETS` (the canonical name now)
    - `SPACE_DEFAULT_LAYOUTS` → `DEFAULT_LAYOUTS` (the canonical name now)
    - `getSpaceDefaultLayout()` → `getDefaultLayout()` (the canonical name now)
    - Update the function body — it references `SPACE_DEFAULT_USER_WIDGETS` and `SPACE_DEFAULT_LAYOUTS`, update those references to the renamed constants.

    The resulting file should contain:
    - Type imports (WidgetLayouts, UserWidgets)
    - The three space-keyed constants renamed to their canonical names:
      - `DEFAULT_USER_WIDGETS` (was `SPACE_DEFAULT_USER_WIDGETS`)
      - `DEFAULT_LAYOUTS` (was `SPACE_DEFAULT_LAYOUTS`)
      - `getDefaultLayout(role)` (was `getSpaceDefaultLayout(role)`)
    - The discovery comments at the top (keep them — they document the data shape)

    **5. Update the `entities/widget/index.ts` barrel:**
    Verify it re-exports from `default-layouts.ts`. It should still work since the renamed exports match their names.

    Check: `grep -n "default-layouts" src/entities/widget/index.ts` to confirm the barrel export exists. No change needed if it uses wildcard export.

    **6. Run typecheck:**
    ```bash
    npx tsc --noEmit 2>&1 | head -20
    ```
    Fix any errors. Expected: the renamed `getDefaultLayout()` should match imports elsewhere.

  </action>
  <verify>
    <automated>grep -c "SPACE_DEFAULT_USER_WIDGETS\|SPACE_DEFAULT_LAYOUTS\|getSpaceDefaultLayout\|remapToSpaces\|TAB_TO_SPACE_MAP" src/entities/widget/model/default-layouts.ts; grep -n "export function getDefaultLayout\|^const DEFAULT_USER_WIDGETS\|^const DEFAULT_LAYOUTS" src/entities/widget/model/default-layouts.ts</automated>
    <manual>Verify no references to SPACE_ prefix, remapToSpaces, or TAB_TO_SPACE_MAP remain</manual>
    <sampling_rate>run after commits</sampling_rate>
  </verify>
  <done>
    default-layouts.ts has only space-keyed defaults, canonical export names (DEFAULT_USER_WIDGETS, DEFAULT_LAYOUTS, getDefaultLayout), no migration code.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename tabId→spaceId props in WidgetCard, DraggableWidget, and SpaceLayout</name>
  <files>
    src/widgets/dashboard/ui/WidgetCard.tsx
    src/widgets/dashboard/ui/DraggableWidget.tsx
    src/widgets/dashboard/ui/SpaceLayout.tsx
  </files>
  <action>
    **1. In `src/widgets/dashboard/ui/WidgetCard.tsx`:**
    - Rename the `tabId` prop → `spaceId` in the interface definition
    - Rename the prop usage in the component body
    - Keep all other behavior identical

    **2. In `src/widgets/dashboard/ui/DraggableWidget.tsx`:**
    - Rename the `tabId` prop → `spaceId` in the interface definition
    - Rename the prop usage in the component body
    - Keep all other behavior identical

    **3. In `src/widgets/dashboard/ui/SpaceLayout.tsx`:**
    - Update imports from widget-store: `addWidgetToTab` → `addWidgetToSpace`, `removeWidgetFromTab` → `removeWidgetFromSpace`
    - Update the `getDefaultLayout` import if it was importing `getSpaceDefaultLayout` (it was renamed in Task 1)
    - Update calls: `addWidgetToTab(widgetId, spaceId)` is now `addWidgetToSpace(widgetId, spaceId)` — note parameter order stays the same (the first param in the store was already `tabId`/`spaceId`)
    - Update the `tabId` prop passed to `WidgetCard` and `DraggableWidget` → `spaceId`
    - Verify all references to `tabId` are replaced with `spaceId` in SpaceLayout.tsx

    **4. Run typecheck:**
    ```bash
    npx tsc --noEmit 2>&1
    ```
    There should be **zero type errors** after this task. If there are, fix them.

  </action>
  <verify>
    <automated>grep -c "tabId" src/widgets/dashboard/ui/WidgetCard.tsx src/widgets/dashboard/ui/DraggableWidget.tsx src/widgets/dashboard/ui/SpaceLayout.tsx; npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
    <manual>Verify no tabId references remain in these 3 files</manual>
    <sampling_rate>run after commits</sampling_rate>
  </verify>
  <done>
    Zero references to `tabId` in WidgetCard, DraggableWidget, SpaceLayout. Zero type errors in `npx tsc --noEmit`.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "tabId" src/widgets/dashboard/ui/WidgetCard.tsx` returns 0
2. `grep -c "tabId" src/widgets/dashboard/ui/DraggableWidget.tsx` returns 0
3. `grep -c "tabId" src/widgets/dashboard/ui/SpaceLayout.tsx` returns 0
4. `grep -c "SPACE_DEFAULT\|getSpaceDefaultLayout\|remapToSpaces\|remapLayoutsToSpaces" src/entities/widget/model/default-layouts.ts` returns 0
5. **`npx tsc --noEmit` passes with zero errors**
</verification>

<success_criteria>
Plan 03 complete when:

- [ ] default-layouts.ts has no tab-keyed defaults, no migration helpers, no TAB_TO_SPACE_MAP import
- [ ] Constants renamed: SPACE_DEFAULT_USER_WIDGETS→DEFAULT_USER_WIDGETS, SPACE_DEFAULT_LAYOUTS→DEFAULT_LAYOUTS
- [ ] Function renamed: getSpaceDefaultLayout→getDefaultLayout
- [ ] WidgetCard uses `spaceId` prop (no `tabId`)
- [ ] DraggableWidget uses `spaceId` prop (no `tabId`)
- [ ] SpaceLayout uses new store method names and passes `spaceId`
- [ ] `npx tsc --noEmit` passes with zero errors
      </success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-tab-removal/31-03-SUMMARY.md`
</output>
