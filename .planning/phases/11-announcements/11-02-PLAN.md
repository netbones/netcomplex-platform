---
phase: 11-announcements
plan: 02
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - src/features/announcements/ui/AnnouncementForm.tsx
  - src/features/announcements/ui/AnnouncementList.tsx
  - src/features/announcements/model/useAnnouncements.ts
  - src/app/(tenant)/admin/announcements/page.tsx
  - src/widgets/dashboard/ui/AnnouncementsStreamWidget.tsx
  - src/widgets/admin/ui/AdminAnnouncementsWidget.tsx
  - src/widgets/dashboard/model/widgets.ts
  - src/app/news/page.tsx
autonomous: true
requirements:
  - ANN-04
  - ANN-05
  - R1
  - R7
  - R8
must_haves:
  truths:
    - 'Board/admin can create/edit announcements with targetFilter, targetRoles, and optional document link'
    - 'Priority dropdown is role-gated — COMMITTEE cannot select urgent, MANAGER cannot select high'
    - 'Priority taxonomy definitions appear as helper text in the form (not just colour codes)'
    - 'Residents see a stream of announcements in the dashboard widget and on the /news page'
    - 'Admin dashboard has a compact announcement management widget'
    - 'NO new standalone page routes outside /admin/ — announcements embed in existing /news page'
    - 'NO new entries in navigation-config.ts — discovery through dashboard widget and notification feed only'
  artifacts:
    - path: 'src/features/announcements/ui/AnnouncementForm.tsx'
      provides: 'Announcement form with targeting fields, role-gated priority, resource link, taxonomy helper text'
      min_lines: 80
    - path: 'src/widgets/dashboard/ui/AnnouncementsStreamWidget.tsx'
      provides: 'Stream/log UI showing 5 most recent active announcements with taxonomy labels'
      min_lines: 40
    - path: 'src/widgets/admin/ui/AdminAnnouncementsWidget.tsx'
      provides: 'Admin dashboard compact list widget linking to /admin/announcements'
      min_lines: 30
    - path: 'src/app/news/page.tsx'
      provides: 'Existing /news page with embedded announcements stream section'
      contains: 'AnnouncementsStreamWidget'
  key_links:
    - from: 'src/features/announcements/ui/AnnouncementForm.tsx'
      to: 'priority-taxonomy.ts'
      via: 'import getAllowedPriorities and PRIORITY_TAXONOMY for role-gated dropdown + helper text'
      pattern: 'getAllowedPriorities|PRIORITY_TAXONOMY'
    - from: 'src/widgets/dashboard/ui/AnnouncementsStreamWidget.tsx'
      to: '/api/announcements'
      via: 'fetch with ?active=true&limit=5'
      pattern: 'fetch.*api/announcements'
    - from: 'src/widgets/dashboard/model/widgets.ts'
      to: 'AnnouncementsStreamWidget'
      via: "registry.register with id 'announcements-stream'"
      pattern: 'announcements-stream'
    - from: 'src/app/news/page.tsx'
      to: 'AnnouncementsStreamWidget'
      via: 'import and render as section'
      pattern: 'AnnouncementsStreamWidget'
---

<objective>
Build admin CRUD UI with role-gated priority and targeting fields, stream widget for dashboard + /news page, admin management widget.
Purpose: The problem definition (docs/architecture/11-ANNOUNCEMENTS-PROBLEM-DEFINITION.md) adds R1 (priority is structural, not cosmetic — taxonomy definitions must appear as helper text, dropdown must be role-gated) and R7 (document attachment via resourceId). The revised instructions add stream/log pattern and navigation governance. Combined, the admin form must: show taxonomy definitions per priority level, gate the priority dropdown by user role, include targetFilter/targetRoles, and optionally link to a Resource. The public view must be a stream with taxonomy-aware labels, not colour-only.
Output: Admin page with role-gated priority form + targeting + resource link, AnnouncementList, AnnouncementsStreamWidget (dashboard + /news embed), AdminAnnouncementsWidget (admin dashboard compact list), widget registry entries.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-announcements/11-01-SUMMARY.md
@src/app/news/page.tsx
@src/widgets/dashboard/model/widgets.ts
@src/widgets/admin/ui/SurveysWidget.tsx
@src/widgets/dashboard/ui/EventsWidget.tsx
@src/app/(tenant)/admin/events/page.tsx
@src/shared/lib/navigation-config.ts
@src/features/announcements/model/priority-taxonomy.ts
@11-ANNOUNCEMENTS-REVISED-INSTRUCTIONS.md
@docs/architecture/11-ANNOUNCEMENTS-PROBLEM-DEFINITION.md
</context>

<tasks>
<task type="auto">
<name>Task 1: Admin CRUD UI (form with role-gated priority + targeting + resource link + list + hook + page)</name>
<files>
  src/features/announcements/ui/AnnouncementForm.tsx
  src/features/announcements/ui/AnnouncementList.tsx
  src/features/announcements/model/useAnnouncements.ts
  src/app/(tenant)/admin/announcements/page.tsx
</files>
<action>
1. Create `src/features/announcements/model/useAnnouncements.ts`:
   - Fetch announcements from `/api/announcements`
   - CRUD operations (create, update, delete) calling the API
   - Loading/error state management
   - Follow the pattern from `src/features/maintenance/model/useMaintenanceForm.ts`

2. Create `src/features/announcements/ui/AnnouncementForm.tsx`:
   - 'use client' component
   - **Role-gated priority selector** (R1 — priority is structural):
     - Import `getAllowedPriorities` and `PRIORITY_TAXONOMY` from `../model/priority-taxonomy`
     - The component must receive the current user's role as a prop
     - Priority dropdown only shows options from `getAllowedPriorities(userRole)`
     - Each priority option shows the taxonomy label, NOT just the raw enum value:
       - `urgent` → "Legal / Urgent — Time-bound, legally consequential"
       - `high` → "Governance — Formal, action may be required"
       - `normal` → "Operational — Informational, no action required"
       - `low` → "Community — Social, awareness only"
     - Below the priority dropdown, show a **helper text block** with the full taxonomy from PRIORITY_TAXONOMY:
       ```
       Priority Definitions:
       • Legal / Urgent: Time-bound, legally consequential, requires immediate attention (e.g. High Court notice, interdict)
       • Governance: Formal, action may be required, statutory notice period applies (e.g. AGM notice, levy resolution)
       • Operational: Informational, temporary, no action required (e.g. Water outage, maintenance access)
       • Community: Social, contextual, awareness only (e.g. Pet reminder, noise request)
       ```
     - If the user's role restricts their max priority, show a note: "Your role permits a maximum priority of {label}."
     - Color-coded background on each dropdown option using PRIORITY_TAXONOMY[priority].color classes

   - **targetFilter select** (R2):
     - All Residents / Owners Only / Renters Only
     - Maps to ResidentFilter enum: ALL, OWNERS_ONLY, RENTERS_ONLY
     - Default: ALL
     - Human-readable labels: "All Residents", "Owners Only", "Renters Only"

   - **targetRoles multi-select** (R2):
     - Optional, defaults to empty (all roles)
     - Offer Role enum values with human-readable labels:
       RESIDENT → "Resident", GROUP_ADMIN → "Group Admin", COMMITTEE → "Committee", BOARD → "Board", ADMIN → "Admin", AGENT → "Agent", MANAGER → "Manager", ASSOCIATE → "Associate"
     - Show helper note: "Leave empty to send to all roles matching the audience filter above."
     - Use checkboxes or a multi-select dropdown

   - **resourceId select** (R7 — document attachment):
     - Optional dropdown to link to an existing Resource record
     - Fetch tenant resources from `/api/resources` to populate the dropdown
     - Show resource title + category in the dropdown
     - Label: "Attach Supporting Document"
     - Helper note: "Link a document (e.g. agenda, court document, financial statement) for governance or legal notices."
     - "None" option to clear the link

   - Other fields: title (input), content (textarea), author (input), expiresAt (datetime-local, optional)
   - Submit calls POST /api/announcements (create) or PATCH /api/announcements/[id] (edit)
   - If the API returns a `warning` field (priority was downgraded), show a toast with the warning message
   - Follow the simplified form pattern from Phase 21 (no Tiptap, no i18n)

3. Create `src/features/announcements/ui/AnnouncementList.tsx`:
   - 'use client' component
   - Display announcements as compact admin list items (NOT cards — admin needs density)
   - Show: title, priority badge using PRIORITY_TAXONOMY labels (not raw enum), target audience summary, linked resource indicator (if resourceId set), date
   - Priority badge uses PRIORITY_TAXONOMY[priority].color for styling
   - Target audience summary: "All Residents" or "Owners Only → Board, Committee"
   - Resource indicator: small paperclip icon + resource title if linked
   - Edit and delete actions for each item
   - Sort by priority (urgent first) then by date

4. Create `src/app/(tenant)/admin/announcements/page.tsx`:
   - Admin page at `/admin/announcements`
   - Uses AnnouncementList + AnnouncementForm
   - Permission check: only users with `canPublishAnnouncements(role)` — COMMITTEE, BOARD, ADMIN, MANAGER
   - Pass the current user's role to AnnouncementForm for priority gating
   - Follow the pattern from `src/app/(tenant)/admin/events/page.tsx` — same structure, tab-less single-page layout
   - Add "New Announcement" button that shows the form
   - Wrap in ErrorBoundary
     </action>
     <verify>
     <automated>cd /home/ubuntupunk/Projects/soralia-village && pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
     <manual>Admin /admin/announcements page renders with form showing role-gated priority dropdown with taxonomy helper text. COMMITTEE user cannot see 'urgent' option. resourceId dropdown shows tenant resources. List shows taxonomy labels and resource indicator.</manual>
     <sampling_rate>run after this task commits, before next task begins</sampling_rate>
     </verify>
     <done>Admin page at /admin/announcements allows full CRUD. Form has role-gated priority dropdown (using getAllowedPriorities + PRIORITY_TAXONOMY), targetFilter select, targetRoles multi-select with helper note, optional resourceId document link, and optional expiry. Priority taxonomy definitions shown as helper text in form. List shows compact items with taxonomy labels, target audience summary, resource indicator, and edit/delete actions.</done>
     </task>

<task type="auto">
<name>Task 2: AnnouncementsStreamWidget + AdminAnnouncementsWidget + /news embed + widget registry</name>
<files>
  src/widgets/dashboard/ui/AnnouncementsStreamWidget.tsx
  src/widgets/admin/ui/AdminAnnouncementsWidget.tsx
  src/widgets/dashboard/model/widgets.ts
  src/app/news/page.tsx
</files>
<action>
1. Create `src/widgets/dashboard/ui/AnnouncementsStreamWidget.tsx`:
   - 'use client' component
   - Import `PRIORITY_TAXONOMY` from `@features/announcements/model/priority-taxonomy` for taxonomy-aware labels
   - Fetch from `/api/announcements?active=true&limit=5` (only non-expired, 5 most recent)
   - Render as a **stream/log**, NOT a card grid. Stream item anatomy per revised instructions:

```
[PRIORITY LABEL] Title [date]
Content (full, not truncated)
— Author name · Expires [date if set] · [📎 Document] (if resourceId)
```

- Priority indicator uses PRIORITY_TAXONOMY:
  - Left-border colour accent from `PRIORITY_TAXONOMY[priority].borderColor`
  - Small pill badge with `PRIORITY_TAXONOMY[priority].label` (not raw enum) — e.g. "Legal / Urgent", "Governance", "Operational", "Community"
  - Badge styled with `PRIORITY_TAXONOMY[priority].color`
- If announcement has resourceId, show a small paperclip icon + "View Document" link (href to `/resources/{resourceId}`)
- Items stacked vertically with small gaps — no heavy card padding
- Empty state: "No active announcements"
- Wrap in ErrorBoundary
- Make the component exportable for embedding in /news page

2. Create `src/widgets/admin/ui/AdminAnnouncementsWidget.tsx`:
   - 'use client' admin dashboard widget
   - Import `PRIORITY_TAXONOMY` for taxonomy-aware labels
   - Fetch from `/api/announcements` (all, not just active, limit 5)
   - Show compact list items: title, priority badge with taxonomy label (small pill using PRIORITY_TAXONOMY color), target audience summary, date
   - Link to `/admin/announcements` for full management
   - Follow the pattern from `src/widgets/admin/ui/SurveysWidget.tsx`
   - Wrap in ErrorBoundary

3. Register BOTH widgets in `src/widgets/dashboard/model/widgets.ts`:

   **Stream widget (resident + admin dashboards):**

   ```ts
   registry.register({
     id: 'announcements-stream',
     version: '1.0.0',
     name: 'Announcements',
     description: 'Recent community announcements',
     author: 'internal',
     category: 'core',
     icon: Megaphone, // import from lucide-react
     component: lazy(() =>
       import('../ui/AnnouncementsStreamWidget').then(m => ({
         default: m.AnnouncementsStreamWidget,
       }))
     ),
     loader: () => import('../ui/AnnouncementsStreamWidget'),
     defaultSize: { width: 3, height: 2 },
     minSize: { width: 2, height: 1 },
     dragHandleClassName: 'widget-drag-handle',
   });
   ```

   **Admin management widget (admin dashboard only):**

   ```ts
   registry.register({
     id: 'admin-announcements',
     version: '1.0.0',
     name: 'Admin Announcements',
     description: 'Manage community announcements',
     author: 'internal',
     category: 'core',
     icon: Megaphone,
     permissions: ['admin'],
     component: lazy(() =>
       import('../../admin/ui/AdminAnnouncementsWidget').then(m => ({
         default: m.AdminAnnouncementsWidget,
       }))
     ),
     loader: () => import('../../admin/ui/AdminAnnouncementsWidget'),
     defaultSize: { width: 3, height: 2 },
     minSize: { width: 2, height: 1 },
     dragHandleClassName: 'widget-drag-handle',
   });
   ```

   Add `Megaphone` to the lucide-react imports at the top of the file.

4. Embed the announcements stream in the existing `/news` page (`src/app/news/page.tsx`):
   - Import `AnnouncementsStreamWidget` from `@widgets/dashboard`
   - Add it as a named section BELOW the existing content grid, with a heading "Announcements"
   - The section should be a `<section id="announcements">` — this anchors the `#announcement-{id}` fragment links from notifications
   - Use a visual separator (thin border or spacing) between the existing Content grid and the announcements stream
   - Do NOT remove or alter the existing Content category filter or grid — announcements are an ADDITIONAL section
   - The existing category filter includes "ANNOUNCEMENT" for Content items — that's fine, it refers to Content-based announcements, not the standalone Announcement model
   - DO NOT create `/news/announcements` as a new page route
   - DO NOT add any new entries to `src/shared/lib/navigation-config.ts`
     </action>
     <verify>
     <automated>cd /home/ubuntupunk/Projects/soralia-village && grep -c "announcements-stream" src/widgets/dashboard/model/widgets.ts && grep -c "AnnouncementsStreamWidget" src/app/news/page.tsx && grep -c "PRIORITY_TAXONOMY" src/widgets/dashboard/ui/AnnouncementsStreamWidget.tsx && grep -rc "/news/announcements" src/app/ 2>/dev/null | grep -v ":0" || echo "No standalone announcements page (correct)" && pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
     <manual>Stream widget shows 5 recent announcements with taxonomy labels (not just colour). Admin widget shows compact list with taxonomy labels. /news page has announcements section. NO /news/announcements route. NO new navigation config entries.</manual>
     <sampling_rate>run after this task commits, before next task begins</sampling_rate>
     </verify>
     <done>AnnouncementsStreamWidget renders stream/log items with priority taxonomy labels and left-border accents, available on resident and admin dashboards. AdminAnnouncementsWidget shows compact management list with taxonomy labels on admin dashboard. /news page embeds the stream as a section with #announcements anchor. Both widgets registered in registry. No new page routes. No new navigation items. Resource link shown with paperclip icon in stream.</done>
     </task>
     </tasks>

<verification>
- `/admin/announcements` renders with form including role-gated priority, targetFilter, targetRoles, and resourceId
- Priority dropdown shows taxonomy definitions as helper text, not just colour codes
- COMMITTEE user cannot select 'urgent' in priority dropdown
- Dashboard widget picker shows "Announcements" (stream widget)
- Admin dashboard widget picker shows "Admin Announcements"
- `/news` page has an announcements section at the bottom
- Stream items show taxonomy labels ("Legal / Urgent", "Governance", etc.) not just raw enum
- NO `/news/announcements` route exists
- NO new entries in `navigation-config.ts`
- TypeScript compiles with zero errors
</verification>

<success_criteria>

- Admin form has role-gated priority dropdown using getAllowedPriorities + PRIORITY_TAXONOMY
- Priority taxonomy definitions appear as helper text in the form (R1)
- Form includes targetFilter select, targetRoles multi-select, and optional resourceId document link (R2, R7)
- Stream widget shows announcements as a chronological log with taxonomy labels (not colour-only)
- Admin widget links to /admin/announcements and shows compact list with taxonomy labels
- /news page embeds announcements stream as a section (not a separate page) (R8)
- Navigation governance satisfied: no new nav items, discovery through widget + notification feed only
  </success_criteria>

<output>
After completion, create `.planning/phases/11-announcements/11-02-SUMMARY.md`
</output>
