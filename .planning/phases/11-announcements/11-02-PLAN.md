---
phase: 11-announcements
plan: 02
type: execute
wave: 2
depends_on: ['11-01']
files_modified:
  - src/app/news/announcements/page.tsx
  - src/widgets/admin/ui/AnnouncementsWidget.tsx
  - src/widgets/dashboard/model/widgets.ts
  - src/shared/lib/navigation-config.ts
autonomous: true
requirements:
  - ANN-04
  - ANN-05
must_haves:
  truths:
    - 'Residents can view active announcements on a public page'
    - 'Announcements widget available on admin dashboard'
    - 'Announcements link appears in navigation when page flag is enabled'
  artifacts:
    - path: 'src/app/news/announcements/page.tsx'
      provides: 'Public announcements listing page'
      min_lines: 40
    - path: 'src/widgets/admin/ui/AnnouncementsWidget.tsx'
      provides: 'Admin dashboard announcement summary widget'
      min_lines: 30
  key_links:
    - from: 'src/app/news/announcements/page.tsx'
      to: '/api/announcements'
      via: 'fetch with ?active=true'
      pattern: 'fetch.*api/announcements'
    - from: 'src/widgets/dashboard/model/widgets.ts'
      to: 'AnnouncementsWidget'
      via: 'registry.register'
      pattern: 'admin-announcements'
---

<objective>
Add public announcements page, admin dashboard widget, and navigation link.
Purpose: Residents need to see announcements; admins need quick access from dashboard; navigation needs announcements link.
Output: Public /news/announcements page, AnnouncementsWidget, widget registry entry, nav config update.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/app/news/page.tsx
@src/widgets/dashboard/model/widgets.ts
@src/widgets/admin/ui/SurveysWidget.tsx
@src/shared/lib/navigation-config.ts
</context>

<tasks>
<task type="auto">
<name>Task 1: Public announcements page + admin widget + nav integration</name>
<files>
  src/app/news/announcements/page.tsx
  src/widgets/admin/ui/AnnouncementsWidget.tsx
  src/widgets/dashboard/model/widgets.ts
  src/shared/lib/navigation-config.ts
</files>
<action>
1. Create `src/app/news/announcements/page.tsx`:
   - 'use client' component
   - Fetch from `/api/announcements?active=true` (only non-expired)
   - Display announcements as cards sorted by priority then date
   - Urgent announcements get a red left border, high get orange, normal blue, low gray
   - Show title, content (full, not truncated), author, date, and "EXPIRES [date]" if expiry set
   - Empty state: "No active announcements"
   - Wrap in ErrorBoundary
   - Add Breadcrumbs: Home > News & Updates > Announcements

2. Create `src/widgets/admin/ui/AnnouncementsWidget.tsx`:
   - 'use client' admin dashboard widget
   - Fetch from `/api/announcements` (all, not just active)
   - Show last 5 announcements as compact list items (title, priority badge, date)
   - Link to /admin/announcements for full management
   - Follow the pattern from `src/widgets/admin/ui/SurveysWidget.tsx`
   - Wrap in ErrorBoundary (use createErrorBoundary from shared)

3. Register widget in `src/widgets/dashboard/model/widgets.ts`:
   - Add `admin-announcements` widget following the `admin-surveys` pattern
   - id: 'admin-announcements', name: 'Admin Announcements'
   - category: 'core', icon: Megaphone (import from lucide-react)
   - permissions: ['admin']
   - lazy import from `../../admin/ui/AnnouncementsWidget`

4. Update `src/shared/lib/navigation-config.ts`:
   - Add announcements item to the COMMUNITY section of navigation items
   - href: '/news/announcements', labelKey: 'nav.announcements'
   - This will automatically appear in Header, MobileMenu, and Footer via getBurgerSections()
     </action>
     <verify>
     <automated>cd /home/ubuntupunk/Projects/soralia-village && pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
     <manual>Public /news/announcements page shows active announcements with priority styling. Admin dashboard widget picker includes Announcements. Navigation shows announcements link.</manual>
     <sampling_rate>run after this task commits, before next task begins</sampling_rate>
     </verify>
     <done>Public announcements page at /news/announcements renders active announcements with priority-based visual styling. Admin dashboard has AnnouncementsWidget registered. Navigation config includes announcements link in community section.</done>
     </task>
     </tasks>

<verification>
- `/news/announcements` renders without error
- Admin dashboard widget picker shows "Admin Announcements"
- Navigation config includes announcements item
- TypeScript compiles with zero errors
</verification>

<success_criteria>

- Public page shows active announcements with priority visual hierarchy
- Admin widget registered in dashboard widget registry
- Navigation link points to /news/announcements
- Full announcement feature end-to-end works
  </success_criteria>

<output>
After completion, create `.planning/phases/11-announcements/11-02-SUMMARY.md`
</output>
