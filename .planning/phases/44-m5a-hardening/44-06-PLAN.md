---
phase: 44-m5a-hardening
plan: 44-06
type: execute
title: 'Audit closure wave C — 1ei + 5u2 + brp + huo'
status: planning
depends_on: ['44-05']
wave: 2
autonomous: false
schema_push_required: true
requirements:
  - soralia-village-1ei
  - soralia-village-5u2
  - soralia-village-brp
  - soralia-village-huo
files_modified:
  - prisma/schema.prisma
  - prisma/seed.ts
  - src/db/schema/residency-type-enum.ts
  - src/db/schema/occupant-type-enum.ts
  - src/db/schema/household-role-enum.ts
  - src/db/schema/profiles.ts
  - src/db/schema/invitations.ts
  - src/db/schema/resident-type-enum.ts
  - src/entities/user/model/types.ts
  - src/entities/tenant/model/types.ts
  - src/entities/directory/model/types.ts
  - src/entities/directory/model/constants.ts
  - src/shared/api/dto/household.ts
  - src/shared/api/dto/invitation.ts
  - src/server/routers/identity.ts
  - src/app/api/invitations/route.ts
  - src/app/api/invitations/validate/route.ts
  - src/app/api/users/[id]/route.ts
  - src/app/api/households/[id]/route.ts
  - src/app/api/users/route.ts
  - src/app/api/announcements/route.ts
  - src/app/(tenant)/page.tsx
  - src/app/unit/[id]/member/[profileId]/page.tsx
  - src/app/unit/[id]/page.tsx
  - src/widgets/admin/ui/users/helpers/resolve-user-helpers.ts
  - src/widgets/admin/ui/users/InviteModal.tsx
  - src/features/onboarding/ui/steps/InviteStep.tsx
  - src/features/directory/model/useResidentFilter.ts
  - src/widgets/dashboard/ui/PremiumPortfolioWidget.tsx
  - scripts/seed-data/soralia-village.ts
  - scripts/seed-data/solaris-heights.ts
  - scripts/seed-data/types.ts
  - scripts/seed-drizzle.ts
  - scripts/migrate-renter-relationships.ts
  - src/test/api/invitations.test.ts
  - src/test/api/directory.test.ts
  - src/test/api/properties.test.ts
  - src/test/api/announcements.test.ts
  - src/widgets/dashboard/ui/DashboardStats.tsx
  - src/widgets/dashboard/ui/EventsWidget.tsx
  - src/widgets/dashboard/ui/MessagesLayer.tsx
  - src/widgets/dashboard/ui/ServicesLayer.tsx
  - src/widgets/dashboard/ui/HomeLayer.tsx
  - src/widgets/dashboard/ui/AdminLayer.tsx
  - src/widgets/admin/ui/AdminStatsWidget.tsx
  - src/widgets/admin/ui/CompetitionList.tsx
  - src/widgets/admin/ui/ResourceForm.tsx
  - src/widgets/admin/ui/EventForm.tsx
  - src/widgets/admin/ui/CompetitionForm.tsx
  - src/features/dashboard/model/useDashboardStats.ts
  - src/features/events/model/useUpcomingEvents.ts
  - src/features/chat/model/useUnreadUrgency.ts
  - src/features/service/model/useServicesUrgency.ts
  - src/features/admin/model/useAdminUrgency.ts
  - src/features/admin/model/useAdminStats.ts
  - src/features/admin/model/useCompetitionMutation.ts
  - src/features/admin/model/useResourceMutation.ts
  - src/features/admin/model/useEventMutation.ts
  - src/app/api/maintenance/route.ts
  - src/entities/maintenance/api/route.ts
  - src/entities/maintenance/services/index.ts
  - src/test/maintenance-transform.test.ts
  - docs/UBIQUITOUS_LANGUAGE.md
must_haves:
  truths:
    - "Profile table uses householdRole instead of occupantType (C6 closed)"
    - "Profile.residencyType and Invitation.residencyType share the same {FAMILY, RENTER, OWNER} enum (C5 closed)"
    - "DashboardStats, EventsWidget, MessagesLayer, ServicesLayer, HomeLayer, AdminLayer, and AdminStatsWidget read data via useQuery/useMutation, not useEffect+fetch (1ei closed)"
    - "src/app/api/maintenance/route.ts and src/entities/maintenance/api/route.ts share a single service-layer transform function (5u2 closed)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "ResidencyType {FAMILY, RENTER, OWNER}, HouseholdRole enum, invitation.residencyType, profile.householdRole"
    - path: "src/db/schema/household-role-enum.ts"
      provides: "Drizzle HouseholdRole enum replacing OccupantType"
    - path: "src/db/schema/residency-type-enum.ts"
      provides: "Drizzle ResidencyType {FAMILY, RENTER, OWNER}"
    - path: "src/features/dashboard/model/useDashboardStats.ts"
      provides: "useDashboardStats hook"
    - path: "src/features/events/model/useUpcomingEvents.ts"
      provides: "useUpcomingEvents hook"
    - path: "src/features/chat/model/useUnreadUrgency.ts"
      provides: "useUnreadUrgency hook"
    - path: "src/features/service/model/useServicesUrgency.ts"
      provides: "useServicesUrgency hook"
    - path: "src/features/admin/model/useAdminUrgency.ts"
      provides: "useAdminUrgency hook"
    - path: "src/features/admin/model/useAdminStats.ts"
      provides: "useAdminStats hook"
    - path: "src/entities/maintenance/services/index.ts"
      provides: "toMaintenanceRequestViewList transform function"
    - path: "src/test/maintenance-transform.test.ts"
      provides: "Unit tests for maintenance transform branches"
  key_links:
    - from: "prisma/schema.prisma"
      to: "dev database"
      via: "npx prisma migrate dev"
      pattern: "schema_push_required"
    - from: "src/widgets/dashboard/ui/*.tsx"
      to: "src/features/*/model/use*.ts"
      via: "import and call useQuery/useMutation"
      pattern: "useQuery|useMutation"
    - from: "src/app/api/maintenance/route.ts"
      to: "src/entities/maintenance/services/index.ts"
      via: "import toMaintenanceRequestViewList"
      pattern: "toMaintenanceRequestViewList"
---

<objective>
Close the four remaining M5a audit/conflict BD issues in a single wave:

- **soralia-village-1ei**: Convert widget `useEffect`+`fetch` data reads into feature-level TanStack Query hooks.
- **soralia-village-5u2**: Consolidate duplicated maintenance response transform logic into one entity service function.
- **soralia-village-brp**: Align `Profile.residencyType` and `Invitation.residentType` on a single `residencyType` field with enum `{FAMILY, RENTER, OWNER}` (drop `OWNER_RESIDENT`).
- **soralia-village-huo**: Rename `Profile.occupantType` to `householdRole` across Prisma, Drizzle, and all seed/script references.

Purpose: Remove the last wave of Chapter 6/8 architecture debt and close the C5/C6 naming conflicts before M5b launch work begins.
Output: Schema push completed, type-safe hooks, single maintenance transform source, and all four BD issues closed.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-m5a-hardening/44-CONTEXT.md
@.planning/phases/44-m5a-hardening/44-03-RESEARCH.md
@.planning/phases/44-m5a-hardening/44-03-PATTERNS.md
@.planning/phases/44-m5a-hardening/44-02-VERIFICATION.md
</context>

## Artifacts this phase produces

### New files

- `src/features/dashboard/model/useDashboardStats.ts` — TanStack Query hook for `/api/dashboard/stats`.
- `src/features/events/model/useUpcomingEvents.ts` — TanStack Query hook for `/api/events?limit=5&upcoming=true`.
- `src/features/chat/model/useUnreadUrgency.ts` — TanStack Query hook for `/api/messages/urgency` and `/api/messages/unread`.
- `src/features/service/model/useServicesUrgency.ts` — TanStack Query hook for `/api/services/urgency`.
- `src/features/admin/model/useAdminUrgency.ts` — TanStack Query hook for `/api/admin/urgency`.
- `src/features/admin/model/useAdminStats.ts` — TanStack Query hook that aggregates `/api/users`, `/api/maintenance`, `/api/groups`, `/api/content`.
- `src/db/schema/household-role-enum.ts` — Drizzle `HouseholdRole` enum (`OCCUPANT`, `MINOR`, `FAMILY`).
- `src/test/maintenance-transform.test.ts` — Unit tests for the consolidated maintenance transform.

### Modified files

- `prisma/schema.prisma` — `ResidencyType` enum, `HouseholdRole` enum, `profile.householdRole`, `invitation.residencyType`.
- `src/db/schema/residency-type-enum.ts` — Values `{FAMILY, RENTER, OWNER}`.
- `src/db/schema/occupant-type-enum.ts` — Renamed or removed in favor of `household-role-enum.ts`.
- `src/db/schema/profiles.ts` — `householdRole` column using `householdRoleEnum`.
- `src/db/schema/invitations.ts` — `residencyType` column using `residencyTypeEnum`.
- `src/db/schema/resident-type-enum.ts` — Removed if no longer referenced.
- TypeScript types, DTOs, routers, API routes, seed scripts, and tests that reference `occupantType`, `residencyType`, `residentType`, or `OwnerType`.
- Widget files listed in `files_modified` converted from `useEffect`+`fetch` to `useQuery`/`useMutation`.
- Maintenance routes refactored to share `toMaintenanceRequestViewList`.

## Tasks

<task type="auto">
  <name>Task 1: Align schemas and seeds for brp (C5) and huo (C6)</name>
  <files>
    prisma/schema.prisma
    prisma/seed.ts
    src/db/schema/residency-type-enum.ts
    src/db/schema/occupant-type-enum.ts
    src/db/schema/household-role-enum.ts
    src/db/schema/profiles.ts
    src/db/schema/invitations.ts
    src/db/schema/resident-type-enum.ts
    src/entities/user/model/types.ts
    src/entities/tenant/model/types.ts
    src/entities/directory/model/types.ts
    src/entities/directory/model/constants.ts
    src/shared/api/dto/household.ts
    src/shared/api/dto/invitation.ts
    src/server/routers/identity.ts
    src/app/api/invitations/route.ts
    src/app/api/invitations/validate/route.ts
    src/app/api/users/[id]/route.ts
    src/app/api/households/[id]/route.ts
    src/app/api/users/route.ts
    src/app/api/announcements/route.ts
    src/app/(tenant)/page.tsx
    src/app/unit/[id]/member/[profileId]/page.tsx
    src/app/unit/[id]/page.tsx
    src/widgets/admin/ui/users/helpers/resolve-user-helpers.ts
    src/widgets/admin/ui/users/InviteModal.tsx
    src/features/onboarding/ui/steps/InviteStep.tsx
    src/features/directory/model/useResidentFilter.ts
    src/widgets/dashboard/ui/PremiumPortfolioWidget.tsx
    scripts/seed-data/soralia-village.ts
    scripts/seed-data/solaris-heights.ts
    scripts/seed-data/types.ts
    scripts/seed-drizzle.ts
    scripts/migrate-renter-relationships.ts
    src/test/api/invitations.test.ts
    src/test/api/directory.test.ts
    src/test/api/properties.test.ts
    src/test/api/announcements.test.ts
    docs/UBIQUITOUS_LANGUAGE.md
  </files>
  <read_first>
    - `prisma/schema.prisma` lines 1241–1292 (`OccupantType`, `ResidencyType`, `ResidentType` enums) and lines 862–894 (`profile` model) and lines 754–774 (`invitation` model).
    - `prisma/seed.ts` (profile seed objects with `occupantType`/`residencyType`).
    - `src/db/schema/residency-type-enum.ts`, `src/db/schema/occupant-type-enum.ts`, `src/db/schema/resident-type-enum.ts`, `src/db/schema/profiles.ts`, `src/db/schema/invitations.ts`.
    - `src/entities/user/model/types.ts` (`InviteFormData`, `occupantType`, `residencyType`, `residentType`).
    - `src/entities/directory/model/constants.ts` (residentType filter map).
    - `src/server/routers/identity.ts` (Zod schemas for `occupantType` and `residencyType`).
    - `scripts/seed-data/soralia-village.ts` and `scripts/seed-data/solaris-heights.ts` (profile seed objects).
    - `scripts/seed-data/types.ts` (seed type definitions).
    - `scripts/seed-drizzle.ts` and `scripts/migrate-renter-relationships.ts` (script references to `occupantType`).
    - `docs/UBIQUITOUS_LANGUAGE.md` C5 and C6 conflict register entries.
  </read_first>
  <action>
    Apply the C5/C6 conflict resolutions **without behavior reduction**:

    1. In `prisma/schema.prisma`:
       - Rename `OccupantType` enum to `HouseholdRole` and update `profile.occupantType` to `profile.householdRole` with type `HouseholdRole`.
       - Change `ResidencyType` enum values to `FAMILY`, `RENTER`, `OWNER` (drop `OWNER_RESIDENT`).
       - Rename `invitation.residentType` to `invitation.residencyType` with type `ResidencyType`.
       - Remove the `ResidentType` enum if it becomes unused; if other references remain, convert them to `ResidencyType` as well.

    2. In Drizzle:
       - Create `src/db/schema/household-role-enum.ts` with `HouseholdRole` enum (`OCCUPANT`, `MINOR`, `FAMILY`).
       - Update `src/db/schema/residency-type-enum.ts` to `['FAMILY', 'RENTER', 'OWNER']`.
       - Update `src/db/schema/profiles.ts` to import `householdRoleEnum` and use `householdRole` column.
       - Update `src/db/schema/invitations.ts` to import `residencyTypeEnum` and use `residencyType` column.
       - Delete `src/db/schema/occupant-type-enum.ts` and `src/db/schema/resident-type-enum.ts` once no longer imported.

    3. Update all references found by `rg "occupantType|OccupantType|residentType|ResidentType|OWNER_RESIDENT" src/ scripts/ prisma/schema.prisma prisma/seed.ts`:
       - TypeScript interfaces and DTOs (`householdRole`, `residencyType`).
       - Zod schemas in `src/server/routers/identity.ts`.
       - API routes that read or write these fields.
       - UI components, directory filter constants, and seed scripts (including `prisma/seed.ts`, `scripts/seed-drizzle.ts`, `scripts/migrate-renter-relationships.ts`).
       - Test mocks and fixtures.

    4. Map old values where needed:
       - `OWNER_RESIDENT` → `OWNER` in seed data and any runtime comparisons.
       - Keep `OCCUPANT`, `MINOR`, `FAMILY` values unchanged under the new `HouseholdRole` enum name.
       - Invitation `residentType` default `OWNER` stays as `OWNER` under `residencyType`.

    5. Update `docs/UBIQUITOUS_LANGUAGE.md` C5 and C6 entries: mark them as closed with the current date and reference the migration commit hash.

    6. Run `pnpm typecheck` after edits and fix all errors before proceeding to the schema push task.

    **Scope guard:** Do not change unrelated field names (e.g., `street`/`unit`, `occupancyType`, `profileStatus`). Do not add migrations beyond the schema push task.
  </action>
  <acceptance_criteria>
    - `rg "occupantType" src/ scripts/ prisma/schema.prisma prisma/seed.ts` returns **0** matches.
    - `rg "OWNER_RESIDENT" src/ scripts/ prisma/schema.prisma prisma/seed.ts` returns **0** matches.
    - `rg "residentType" src/ scripts/ prisma/schema.prisma prisma/seed.ts` returns **0** matches.
    - `ResidencyType` enum values are exactly `FAMILY`, `RENTER`, `OWNER` in both Prisma and Drizzle.
    - `docs/UBIQUITOUS_LANGUAGE.md` C5 and C6 entries are marked as closed with the current date.
    - `pnpm typecheck` passes with zero errors.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(rg 'occupantType' src/ scripts/ prisma/schema.prisma prisma/seed.ts)" && test -z "$(rg 'OWNER_RESIDENT' src/ scripts/ prisma/schema.prisma prisma/seed.ts)" && test -z "$(rg 'residentType' src/ scripts/ prisma/schema.prisma prisma/seed.ts)" && pnpm typecheck</automated>
  </verify>
  <done>
    All Prisma/Drizzle/schema/type/DTO/seed references are renamed and aligned, and TypeScript compiles cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Push schema changes to the database</name>
  <files>
    prisma/schema.prisma
  </files>
  <read_first>
    - `prisma/schema.prisma` after Task 1 edits.
    - `pnpm prisma migrate status` or `npx prisma migrate status` output if available.
  </read_first>
  <action>
    Run the schema migration inside the worktree using the project-mandated formal migration path:

    1. Run `npx prisma migrate dev --name align_residency_household_role` (or equivalent name). Review the generated migration SQL before applying.
    2. If the CLI reports that the migration will be destructive to existing `OWNER_RESIDENT` or `OCCUPANT` values, stop and surface the exact affected rows to the user for a go/no-go decision. Only use `--create-only` + manual destructive acceptance after explicit user approval, or use `--accept-data-loss` only after explicit user approval.
    3. If the migration is non-destructive, proceed and then run `npx prisma generate`.
    4. After the push, verify the live schema matches by inspecting the generated Prisma client and, if needed, querying `information_schema.columns` for `profile.householdRole` and `invitation.residencyType`.
  </action>
  <acceptance_criteria>
    - `npx prisma migrate dev` completes without error (or with user-approved destructive acceptance).
    - A migration file exists under `prisma/migrations/YYYYMMDDHHMMSS_align_residency_household_role/`.
    - `npx prisma generate` regenerates the Drizzle/Prisma clients without errors.
    - `npx prisma migrate status` reports the new migration as applied.
  </acceptance_criteria>
  <verify>
    <automated>npx prisma migrate status && npx prisma generate && test -n "$(ls prisma/migrations/ | grep -E '[0-9]{14}_align_residency_household_role')"</automated>
  </verify>
  <done>
    Schema is live in the dev database and the generated client is up to date.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate admin widget writes to useMutation (1ei write-side)</name>
  <files>
    src/widgets/admin/ui/CompetitionList.tsx
    src/widgets/admin/ui/ResourceForm.tsx
    src/widgets/admin/ui/EventForm.tsx
    src/widgets/admin/ui/CompetitionForm.tsx
    src/features/admin/model/useCompetitionMutation.ts
    src/features/admin/model/useResourceMutation.ts
    src/features/admin/model/useEventMutation.ts
  </files>
  <read_first>
    - `src/widgets/admin/ui/CompetitionList.tsx` (already on tRPC — pattern reference for mutations).
    - `src/widgets/admin/ui/ResourceForm.tsx` (submit handler and inline fetch/tRPC call).
    - `src/widgets/admin/ui/EventForm.tsx` (submit handler and inline fetch/tRPC call).
    - `src/widgets/admin/ui/CompetitionForm.tsx` (submit handler and inline fetch/tRPC call).
    - Existing tRPC router setup under `src/server/routers/` and `src/app/providers.tsx`.
  </read_first>
  <action>
    Extract the write-side mutation logic from the four admin form/list components into feature-level `useMutation` hooks under `src/features/admin/model/`:

    1. `src/widgets/admin/ui/CompetitionList.tsx` → `src/features/admin/model/useCompetitionMutation.ts` (create/update/delete competition actions).
    2. `src/widgets/admin/ui/ResourceForm.tsx` → `src/features/admin/model/useResourceMutation.ts` (create/update resource).
    3. `src/widgets/admin/ui/EventForm.tsx` → `src/features/admin/model/useEventMutation.ts` (create/update event).
    4. `src/widgets/admin/ui/CompetitionForm.tsx` → reuse `useCompetitionMutation` from step 1 if it covers form submission, or extend it.

    Each hook must:
    - Return `{ mutate, mutateAsync, isPending, error, reset }` (or equivalent).
    - Wrap the existing tRPC mutation call (or REST call if tRPC is not yet available for that entity).
    - Accept the same payload shape currently used by the component's submit handler.
    - Surface errors via the existing toast/error UX; keep success callbacks unchanged.

    Update the four components to consume the hooks, removing inline `useState` mutation loading states and inline `fetch`/tRPC client calls from submit handlers. Preserve form validation and redirect behavior.
  </action>
  <acceptance_criteria>
    - `rg "useEffect\(\(\).*fetch\(" src/widgets/admin/ui/` returns **0** matches.
    - `CompetitionList`, `ResourceForm`, `EventForm`, and `CompetitionForm` import and use their mutation hooks for all create/update/delete operations.
    - Each mutation hook is exported from `src/features/admin/model/` and referenced by at least one component.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(rg 'useEffect\(\(\).*fetch\(' src/widgets/admin/ui/)" && pnpm typecheck</automated>
  </verify>
  <done>
    All listed admin widgets write data through TanStack Query `useMutation` hooks; no inline write fetches remain.
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate dashboard widgets from useEffect+fetch to useQuery (1ei read-side)</name>
  <files>
    src/widgets/dashboard/ui/DashboardStats.tsx
    src/widgets/dashboard/ui/EventsWidget.tsx
    src/widgets/dashboard/ui/MessagesLayer.tsx
    src/widgets/dashboard/ui/ServicesLayer.tsx
    src/widgets/dashboard/ui/HomeLayer.tsx
    src/widgets/dashboard/ui/AdminLayer.tsx
    src/widgets/admin/ui/AdminStatsWidget.tsx
    src/features/dashboard/model/useDashboardStats.ts
    src/features/events/model/useUpcomingEvents.ts
    src/features/chat/model/useUnreadUrgency.ts
    src/features/service/model/useServicesUrgency.ts
    src/features/admin/model/useAdminUrgency.ts
    src/features/admin/model/useAdminStats.ts
  </files>
  <read_first>
    - `src/widgets/dashboard/ui/DashboardStats.tsx` (lines 65–85).
    - `src/widgets/dashboard/ui/EventsWidget.tsx` (lines 40–72).
    - `src/widgets/dashboard/ui/MessagesLayer.tsx` (lines 90–116).
    - `src/widgets/dashboard/ui/ServicesLayer.tsx` (lines 102–126).
    - `src/widgets/dashboard/ui/HomeLayer.tsx` (lines 440–465).
    - `src/widgets/dashboard/ui/AdminLayer.tsx` (lines 106–138).
    - `src/widgets/admin/ui/AdminStatsWidget.tsx` (lines 60–113).
    - Existing TanStack Query / tRPC provider setup in `src/app/providers.tsx`.
  </read_first>
  <action>
    For each affected widget, extract the fetch logic into a feature-level TanStack Query hook and replace the inline `useState`/`useEffect`/`fetch` pattern:

    1. `src/widgets/dashboard/ui/DashboardStats.tsx` → `src/features/dashboard/model/useDashboardStats.ts` (useQuery for `/api/dashboard/stats`).
    2. `src/widgets/dashboard/ui/EventsWidget.tsx` → `src/features/events/model/useUpcomingEvents.ts` (useQuery for `/api/events?limit=5&upcoming=true`).
    3. `src/widgets/dashboard/ui/MessagesLayer.tsx` → `src/features/chat/model/useUnreadUrgency.ts` (useQuery for `/api/messages/urgency`).
    4. `src/widgets/dashboard/ui/ServicesLayer.tsx` → `src/features/service/model/useServicesUrgency.ts` (useQuery for `/api/services/urgency`).
    5. `src/widgets/dashboard/ui/HomeLayer.tsx` → reuse `useUnreadUrgency` from step 3 for unread messages; replace the `/api/messages/unread` inline fetch with the same hook or extend the hook to accept the `unread` endpoint.
    6. `src/widgets/dashboard/ui/AdminLayer.tsx` → `src/features/admin/model/useAdminUrgency.ts` (useQuery for `/api/admin/urgency`).
    7. `src/widgets/admin/ui/AdminStatsWidget.tsx` → `src/features/admin/model/useAdminStats.ts` (useQuery that aggregates the four endpoints with Promise.all or parallel `useQuery` calls).

    Each hook must:
    - Return `{ data, isLoading, error, refetch }` (or equivalent).
    - Handle the canonical `{success, data}` envelope with a fallback for raw JSON.
    - Keep the existing widget loading, error, and retry UX unchanged.
    - Use `useApiToast` only for ad-hoc retry/error cases; remove it for simple reads.

    Update the widget components to consume the hooks, removing local `useState` for data and the `useEffect` fetch blocks. Preserve the existing `ErrorBoundary` wrappers and skeleton/error components.
  </action>
  <acceptance_criteria>
    - `rg "useEffect\(\(\).*fetch\(" src/widgets/dashboard/ui/ src/widgets/admin/ui/` returns **0** matches for read-side data loading.
    - `DashboardStats`, `EventsWidget`, `MessagesLayer`, `ServicesLayer`, `HomeLayer`, `AdminLayer`, and `AdminStatsWidget` import and use their feature hooks.
    - Each hook is exported from its feature `model` directory and referenced by at least one widget.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(rg 'useEffect\(\(\).*fetch\(' src/widgets/dashboard/ui/ src/widgets/admin/ui/)" && pnpm typecheck</automated>
  </verify>
  <done>
    All listed widgets read data through TanStack Query hooks; no inline `useEffect`+`fetch` data loading remains.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: De-duplicate maintenance API transform logic (5u2)</name>
  <files>
    src/app/api/maintenance/route.ts
    src/entities/maintenance/api/route.ts
    src/entities/maintenance/services/index.ts
    src/test/maintenance-transform.test.ts
  </files>
  <read_first>
    - `src/app/api/maintenance/route.ts` lines 100–172 (DTO transform, community PII strip, search filter).
    - `src/entities/maintenance/api/route.ts` lines 17–71 (duplicate transform and search filter).
    - `src/entities/maintenance/services/index.ts` (existing service functions and `buildMaintenanceConditions`).
  </read_first>
  <behavior>
    - Test 1: `toMaintenanceRequestViewList(rows, 'all', search)` returns DTO-shaped objects with `user`, `assignedTeam`, and `assignedProvider` for admin scope.
    - Test 2: `toMaintenanceRequestViewList(rows, 'community')` strips PII and internal fields, returning only `id`, `ticketNumber`, `category`, `priority`, `status`, `createdAt`, `updatedAt`.
    - Test 3: `toMaintenanceRequestViewList(rows, 'all', 'leak')` filters by description, user name, email, address, category, and ticketNumber.
    - Test 4: `toMaintenanceRequestViewList(rows, 'mine')` returns the resident-scoped shape without requiring `canViewAll`.
    - Test 5: Both route entry points produce identical JSON for the same input rows and query parameters.
  </behavior>
  <action>
    Consolidate the duplicated transform into a single entity service function:

    1. In `src/entities/maintenance/services/index.ts`, add `toMaintenanceRequestViewList(rows, scope, search?)` that:
       - Accepts the joined row shape returned by `listMaintenanceRequests`.
       - Returns the DTO + user/team/provider object for `all`/`mine` scopes.
       - Returns the community-scoped PII-stripped object for `community` scope.
       - Applies the in-memory search filter only when `search` is provided and scope is not `community`.

    2. Update `src/app/api/maintenance/route.ts` to:
       - Call `toMaintenanceRequestViewList(transformed, scope, search)` after fetching raw rows from `listMaintenanceRequests`.
       - Remove the inline DTO mapping, community strip, and search filter logic.
       - Keep the query-param parsing and scope resolution.

    3. Update `src/entities/maintenance/api/route.ts` to:
       - Call the same `toMaintenanceRequestViewList` function with `params.scope` and `params.search`.
       - Remove its duplicated transform and search filter.
       - Consider whether this file should remain a thin re-export or be deleted; keep it only if it has non-route callers.

    4. Create `src/test/maintenance-transform.test.ts` with unit tests covering admin, resident, community, and search-filtered branches. Mock the DTO helper if needed.
  </action>
  <acceptance_criteria>
    - `src/entities/maintenance/services/index.ts` contains a single `toMaintenanceRequestViewList` function used by both routes.
    - Duplicated DTO mapping, community PII strip, and search filter logic are removed from both route files.
    - `pnpm test -- src/test/maintenance-transform.test.ts` passes.
    - `pnpm typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/test/maintenance-transform.test.ts && pnpm typecheck</automated>
  </verify>
  <done>
    One service function owns the maintenance response shape; both routes are thin wrappers; tests cover all scopes and search.
  </done>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Next.js API routes | Widget hooks cross from browser to server; auth header must be injected via `authClient.getSession()`. |
| API route → database | Maintenance transform receives raw DB rows; must not leak PII in community scope. |
| Schema push → production data | Enum value removal (`OWNER_RESIDENT`) and column rename (`occupantType` → `householdRole`) can destroy existing data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06-01 | Information Disclosure | `toMaintenanceRequestViewList` community scope | mitigate | PII strip enforced inside the single service function; unit test asserts only allowed fields are returned. |
| T-44-06-02 | Tampering | Widget hook auth headers | mitigate | Reuse existing `authClient.getSession()` token injection pattern; never store tokens in hook state. |
| T-44-06-03 | Data Loss | `npx prisma migrate dev` enum/column changes | mitigate | Blocking human checkpoint; destructive migration only after user approval; inspect existing `OWNER_RESIDENT`/`OCCUPANT` row counts first. |
| T-44-06-04 | Denial of Service | `AdminStatsWidget` parallel queries | accept | Four GET requests in parallel; existing pattern unchanged; add `maxDuration` only if route already supports it. |
| T-44-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; slopcheck not required. |
</threat_model>

<verification>
## Wave-level verification

1. **Schema alignment (Task 1)**
   - `pnpm typecheck` passes.
   - `rg "occupantType|OWNER_RESIDENT|residentType" src/ scripts/ prisma/schema.prisma prisma/seed.ts` returns 0 matches.
   - `docs/UBIQUITOUS_LANGUAGE.md` C5 and C6 marked as closed.

2. **Schema migration (Task 2)**
   - `npx prisma migrate dev` completes (with or without approved destructive migration).
   - `npx prisma generate` completes.
   - Migration file exists under `prisma/migrations/`.

3. **Widget writes (Task 3)**
   - `rg "useEffect\(\(\).*fetch\(" src/widgets/admin/ui/` returns 0 matches for write-side.
   - All four admin widgets import and use their mutation hooks.
   - `pnpm typecheck` passes.

4. **Widget reads (Task 4)**
   - `rg "useEffect\(\(\).*fetch\(" src/widgets/dashboard/ui/ src/widgets/admin/ui/` returns 0 matches.
   - Each new hook file is imported by at least one widget.
   - `pnpm typecheck` passes.

5. **Maintenance deduplication (Task 5)**
   - `pnpm test -- src/test/maintenance-transform.test.ts` passes.
   - Both `src/app/api/maintenance/route.ts` and `src/entities/maintenance/api/route.ts` import `toMaintenanceRequestViewList` from `src/entities/maintenance/services/index.ts`.
   - `pnpm typecheck` passes.

6. **Final gates**
   - `pnpm lint` passes.
   - `pnpm build` passes (or `pnpm build` is run after schema migration if needed).
   - Close BD issues `soralia-village-1ei`, `soralia-village-5u2`, `soralia-village-brp`, `soralia-village-huo` with the final commit hash.
</verification>

<success_criteria>
- All four BD issues are closed with a fix commit on the phase branch.
- `pnpm typecheck`, `pnpm lint`, and `pnpm build` pass.
- `npx prisma migrate dev` has been run and a formal migration file exists under `prisma/migrations/`.
- Zero `useEffect(() => fetch(...))` data-loading or write patterns remain in the listed widget files.
- A single `toMaintenanceRequestViewList` function is the source of truth for maintenance response shaping.
- `docs/UBIQUITOUS_LANGUAGE.md` C5 and C6 are marked as closed with the date.
</success_criteria>

<output>
Create `.planning/phases/44-m5a-hardening/44-06-SUMMARY.md` when done, summarizing closed BD issues, schema changes, hook inventory, and test results.
</output>
