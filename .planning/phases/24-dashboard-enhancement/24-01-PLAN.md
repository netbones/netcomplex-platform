---
phase: 24-dashboard-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/admin/model/admin-config.ts
  - src/widgets/admin/ui/AdminWidgetRenderer.tsx
  - src/widgets/admin/ui/SurveysWidget.tsx
  - src/app/api/surveys/[id]/responses/route.ts
  - src/app/(tenant)/admin/surveys/[id]/page.tsx
autonomous: true
requirements: [DASH-01, DASH-03]
user_setup: []

must_haves:
  truths:
    - 'Admin dashboard has a Surveys tab showing survey overview widget'
    - 'Admin can view survey results with response counts, charts, and answer breakdowns'
    - 'Survey results API returns aggregated data for a single survey'
  artifacts:
    - path: 'src/entities/admin/model/admin-config.ts'
      provides: 'Surveys tab added to ADMIN_TABS, surveys-widget added to ALL_ADMIN_WIDGETS'
      contains: 'surveys'
    - path: 'src/widgets/admin/ui/SurveysWidget.tsx'
      provides: 'Survey overview widget for dashboard tab'
      min_lines: 30
    - path: 'src/app/api/surveys/[id]/responses/route.ts'
      provides: 'GET survey responses with aggregation (counts per question, answer distribution)'
      exports: ['GET']
    - path: 'src/app/(tenant)/admin/surveys/[id]/page.tsx'
      provides: 'Survey results page with charts and answer breakdowns'
      min_lines: 40
  key_links:
    - from: 'src/widgets/admin/ui/SurveysWidget.tsx'
      to: '/api/surveys'
      via: 'fetch GET'
      pattern: 'fetch.*api/surveys'
    - from: 'src/app/(tenant)/admin/surveys/[id]/page.tsx'
      to: '/api/surveys/[id]/responses'
      via: 'fetch GET'
      pattern: 'fetch.*api/surveys.*responses'
    - from: 'src/app/api/surveys/[id]/responses/route.ts'
      to: 'drizzle responses + questions tables'
      via: 'database query with surveyId filter'
      pattern: 'responses.*surveyId|questions.*surveyId'
---

<objective>
Add Surveys tab to admin dashboard with SurveysWidget, create survey results API endpoint, and build survey results visualisation page.

Purpose: Surveys exist in the system (model, list page, create page) but have no dashboard presence and no results visualisation. Admins cannot see response data in a meaningful way.
Output: Surveys tab in dashboard, SurveysWidget, survey responses API with aggregation, survey results page with charts.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DISCUSSION.md
@src/entities/admin/model/admin-config.ts
@src/widgets/admin/ui/AdminWidgetRenderer.tsx
@src/widgets/admin/ui/EventsWidget.tsx
@src/app/api/surveys/route.ts
@src/app/(tenant)/admin/surveys/page.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Surveys tab to admin dashboard config and create SurveysWidget</name>
  <files>src/entities/admin/model/admin-config.ts, src/widgets/admin/ui/AdminWidgetRenderer.tsx, src/widgets/admin/ui/SurveysWidget.tsx</files>
  <action>
    1. Add surveys tab to ADMIN_TABS in src/entities/admin/model/admin-config.ts:
       {
         id: 'surveys',
         label: 'Surveys',
         icon: 'fa-poll',
         defaultWidgets: ['admin-surveys', 'admin-stats'],
       }

    2. Add surveys-widget to ALL_ADMIN_WIDGETS:
       { id: 'admin-surveys', label: 'Survey Overview', icon: 'fa-poll' }

    3. Add size mapping in getAdminWidgetSize: 'admin-surveys' → 'medium'

    4. Create src/widgets/admin/ui/SurveysWidget.tsx following EventsWidget pattern:
       - Fetches surveys from GET /api/surveys
       - Shows list of active surveys with title, status badge, question count, response count
       - Links each survey to /admin/surveys/[id] for results
       - Shows "No surveys yet" empty state with link to create
       - Uses ErrorBoundary, loading skeleton, retry on error
       - "View All Surveys →" link to /admin/surveys

    5. Wire SurveysWidget in AdminWidgetRenderer.tsx:
       - Import SurveysWidget
       - Add case 'admin-surveys': return <SurveysWidget />

  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <manual>Admin dashboard shows Surveys tab; clicking it displays SurveysWidget with survey list</manual>
    <sampling_rate>run before Task 2 begins</sampling_rate>
  </verify>
  <done>Surveys tab exists in dashboard with SurveysWidget showing active surveys, linked to results pages</done>
</task>

<task type="auto">
  <name>Task 2: Create survey responses API endpoint with aggregation</name>
  <files>src/app/api/surveys/[id]/responses/route.ts</files>
  <action>
    Create src/app/api/surveys/[id]/responses/route.ts:

    GET /api/surveys/[id]/responses:
    - Requires authentication (use existing getSessionAndRole pattern)
    - Requires content permission (hasPermission check)
    - Enforce tenant isolation via withTenant()
    - Verify the survey belongs to the current tenant
    - Fetch the survey with its questions
    - Fetch all responses for the survey
    - Return aggregated data:
      {
        survey: { id, title, status, type },
        totalResponses: number,
        questions: [
          {
            id, text, type, options,
            responses: {
              total: number,
              distribution: { [answer]: count }  // for SINGLE_CHOICE, MULTIPLE_CHOICE, YES_NO
              // for TEXT: array of response texts (limited to 50)
              // for RATING: { average: number, distribution: { [1-5]: count } }
            }
          }
        ]
      }
    - Use Drizzle ORM — import responses, questions from @api/db
    - Join responses with questions for aggregation
    - Handle surveys with no responses (return empty distribution)

    Follow the existing survey API auth pattern from src/app/api/surveys/route.ts.

  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <manual>curl /api/surveys/[id]/responses returns aggregated response data with question distributions</manual>
    <sampling_rate>run before Task 3 begins</sampling_rate>
  </verify>
  <done>Survey responses API returns aggregated data including total responses, per-question distributions, and rating averages</done>
</task>

<task type="auto">
  <name>Task 3: Build survey results page with response visualisation</name>
  <files>src/app/(tenant)/admin/surveys/[id]/page.tsx</files>
  <action>
    Create src/app/(tenant)/admin/surveys/[id]/page.tsx:

    - Client component ('use client')
    - Breadcrumbs: Admin > Surveys > [Survey Title]
    - Header with survey title, status badge, total response count
    - Fetch from GET /api/surveys/[id]/responses
    - For each question, render a results card:
      - Question text as heading
      - Question type badge
      - For SINGLE_CHOICE/MULTIPLE_CHOICE/YES_NO: horizontal bar chart showing answer distribution (use Tailwind width percentages, no external chart library)
      - For RATING: show average rating (e.g. "4.2 / 5") with star icons + distribution bars
      - For TEXT: show list of recent responses (max 20, with "show more" toggle)
    - Loading skeleton during fetch
    - Error state with retry button
    - "Back to Surveys" link
    - Use existing UI patterns (ErrorBoundary, card styling, Tailwind)
    - No external chart library — use simple CSS bar charts with Tailwind

    The page should handle the case where survey has no responses yet (friendly empty state).

  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
    <manual>Visit /admin/surveys/[id] — shows question-by-question results with bar charts, rating averages, and text responses</manual>
    <sampling_rate>run after Task 3 commits, before next plan begins</sampling_rate>
  </verify>
  <done>Survey results page displays response data with visual charts for each question type, handles empty state gracefully</done>
</task>

</tasks>

<verification>
- Surveys tab appears in admin dashboard with correct icon and label
- SurveysWidget displays active surveys with links to results
- Survey responses API returns properly aggregated data
- Survey results page renders visual charts for all question types
- All TypeScript type checks pass
- ESLint passes with no errors
</verification>

<success_criteria>

- Admin dashboard has a Surveys tab with survey overview widget
- Clicking a survey from the widget navigates to its results page
- Results page shows response counts, bar charts for choices, star ratings for rating questions, text responses for open-ended
- Survey with no responses shows friendly empty state
- All 3 tasks complete with passing typecheck and lint
  </success_criteria>

<output>
After completion, create `.planning/phases/24-dashboard-enhancement/24-01-SUMMARY.md`
</output>
