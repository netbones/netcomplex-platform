---
phase: 99-build-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/bookings/route.ts
  - src/app/api/maintenance/[id]/route.ts
  - src/app/api/maintenance/[id]/notify/route.ts
  - src/app/api/maintenance/[id]/notes/route.ts
  - src/app/api/maintenance/[id]/history/route.ts
  - src/app/api/admin/board-members/route.ts
autonomous: true
requirements:
  - BUILD-01

must_haves:
  truths:
    - 'All API routes import from @/lib/db'
    - 'Project builds without module errors'
  artifacts:
    - path: 'src/app/api/bookings/route.ts'
      provides: 'Fixed imports'
---

<objective>
Fix build errors by updating broken import paths in API routes.

Purpose: The multi-tenant refactor introduced schema changes that broke imports. Fix them to get the project building again.
Output: All API routes importing correctly from @/lib/db
</objective>

<context>
@src/lib/db.ts - Re-exports all tables

Files with broken imports:

- src/app/api/bookings/route.ts
- src/app/api/maintenance/[id]/route.ts
- src/app/api/maintenance/[id]/notify/route.ts
- src/app/api/maintenance/[id]/notes/route.ts
- src/app/api/maintenance/[id]/history/route.ts
- src/app/api/admin/board-members/route.ts
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bookings/route.ts imports</name>
  <files>src/app/api/bookings/route.ts</files>
  <action>
    Change:
    import { bookings } from '../../../../prisma/drizzle/bookings';
    import { users } from '../../../../prisma/drizzle/users';
    
    To:
    import { db, bookings, users } from '@/lib/db';
  </action>
  <verify>grep "from '@/lib/db'" src/app/api/bookings/route.ts</verify>
  <done>Imports fixed</done>
</task>

<task type="auto">
  <name>Task 2: Fix maintenance/[id]/route.ts imports</name>
  <files>src/app/api/maintenance/[id]/route.ts</files>
  <action>
    Change relative imports to @/lib/db. Note: requestHistories may not exist in db.ts - check and add if needed.
  </action>
  <verify>grep "from '@/lib/db'" src/app/api/maintenance/[id]/route.ts</verify>
  <done>Imports fixed</done>
</task>

<task type="auto">
  <name>Task 3: Fix maintenance/[id]/notify/route.ts imports</name>
  <files>src/app/api/maintenance/[id]/notify/route.ts</files>
  <action>
    Change relative imports to @/lib/db
  </action>
  <verify>grep "from '@/lib/db'" src/app/api/maintenance/[id]/notify/route.ts</verify>
  <done>Imports fixed</done>
</task>

<task type="auto">
  <name>Task 4: Fix maintenance/[id]/notes/route.ts imports</name>
  <files>src/app/api/maintenance/[id]/notes/route.ts</files>
  <action>
    Change relative imports to @/lib/db
  </action>
  <verify>grep "from '@/lib/db'" src/app/api/maintenance/[id]/notes/route.ts</verify>
  <done>Imports fixed</done>
</task>

<task type="auto">
  <name>Task 5: Fix maintenance/[id]/history/route.ts imports</name>
  <files>src/app/api/maintenance/[id]/history/route.ts</files>
  <action>
    Change relative imports to @/lib/db
  </action>
  <verify>grep "from '@/lib/db'" src/app/api/maintenance/[id]/history/route.ts</verify>
  <done>Imports fixed</done>
</task>

<task type="auto">
  <name>Task 6: Fix admin/board-members/route.ts imports</name>
  <files>src/app/api/admin/board-members/route.ts</files>
  <action>
    Change relative imports to @/lib/db
  </action>
  <verify>grep "from '@/lib/db'" src/app/api/admin/board-members/route.ts</verify>
  <done>Imports fixed</done>
</task>

<task type="auto">
  <name>Task 7: Verify build passes</name>
  <files>package.json</files>
  <action>
    Run npm run build and verify no module resolution errors
  </action>
  <verify>npm run build 2>&1 | tail -20</verify>
  <done>Build successful</done>
</task>

</tasks>

<verification>
- [ ] All 6 files have fixed imports
- [ ] npm run build passes
</verification>

<success_criteria>
Build passes with no module resolution errors
</success_criteria>

<output>
After completion, create `.planning/phases/99-build-fix/{phase}-{plan}-SUMMARY.md`
</output>
