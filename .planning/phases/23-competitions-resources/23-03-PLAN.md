---
phase: 23-competitions-resources
plan: 03
type: execute
wave: 2
depends_on: ['23-02']
files_modified:
  - src/app/resources/page.tsx
  - src/lib/migrations/migrate-resources.ts
  - prisma/schema.prisma
autonomous: true
requirements: [RES-04, RES-05]
user_setup: []

must_haves:
  truths:
    - 'Public /resources page displays resources from the Resource model with visibility filtering'
    - 'Existing Content records with category=RESOURCE are migrated to the Resource model'
    - 'RESOURCE is removed from ContentCategory enum after migration is confirmed clean'
  artifacts:
    - path: 'src/app/resources/page.tsx'
      provides: 'Public resources page querying Resource model with visibility filtering'
      min_lines: 40
    - path: 'src/lib/migrations/migrate-resources.ts'
      provides: 'Migration script to reclassify Content RESOURCE records into Resource model'
      contains: 'migrateResources'
    - path: 'prisma/schema.prisma'
      provides: 'ContentCategory enum without RESOURCE value'
      contains: 'enum ContentCategory'
  key_links:
    - from: 'src/app/resources/page.tsx'
      to: '/api/resources'
      via: 'fetch GET with visibility filtering'
      pattern: 'fetch.*api/resources'
    - from: 'src/lib/migrations/migrate-resources.ts'
      to: 'Content table'
      via: 'SELECT WHERE category=RESOURCE'
      pattern: 'category.*RESOURCE|RESOURCE.*category'
    - from: 'src/lib/migrations/migrate-resources.ts'
      to: 'Resource table'
      via: 'INSERT INTO Resource'
      pattern: 'resources.*insert|insert.*resources'
---

<objective>
Rewrite public /resources page to query the Resource model with visibility filtering, and migrate existing Content RESOURCE records to the new Resource model.

Purpose: The public resources page currently fetches from Content with category=RESOURCE. After Plan 02 creates the Resource model and API, the public page must switch to the new data source. Additionally, any existing Content records with RESOURCE category must be migrated to avoid data loss.
Output: Updated public resources page, migration script, RESOURCE removed from ContentCategory enum.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DISCUSSION.md
@src/app/resources/page.tsx
@src/app/api/resources/route.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite public /resources page to fetch from Resource API with visibility filtering</name>
  <files>src/app/resources/page.tsx</files>
  <action>
    Rewrite src/app/resources/page.tsx to:
    - Fetch resources from GET /api/resources (client-side, with 'use client')
    - Apply visibility filtering automatically based on user session (the API handles this)
    - Group resources by category (ARCHITECTURAL, ENGINEERING, GOVERNANCE, BOARD_REPORT, DIY, FINANCIAL, LEGAL, OTHER)
    - Display each resource card with:
      - Title, description
      - File type icon and file size (if fileUrl present)
      - Download button (links to fileUrl)
      - View button (for externalUrl or bodyContent preview)
      - Version badge (if version present)
    - Category filter tabs/dropdown at top
    - Loading state with usePageLoading hook
    - ErrorBoundary wrapper
    - Breadcrumbs: Home > Resources
    - Remove all hardcoded static data (documents, events, guidelines, contacts arrays)
    - Keep the visual design consistent with existing Tailwind patterns
    - Use i18n for translatable strings (t('resources:*'))

    The page should gracefully handle empty state (no resources) with a friendly message.

  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <manual>Visit /resources — shows resources from database grouped by category, with download/view buttons; shows empty state if no resources exist</manual>
    <sampling_rate>run before Task 2 begins</sampling_rate>
  </verify>
  <done>Public /resources page displays database-driven resources from Resource model with category grouping, file downloads, and visibility filtering</done>
</task>

<task type="auto">
  <name>Task 2: Write migration script to reclassify Content RESOURCE records into Resource model</name>
  <files>src/lib/migrations/migrate-resources.ts</files>
  <action>
    Create a migration script at src/lib/migrations/migrate-resources.ts that:

    1. Queries all Content records where category = 'RESOURCE'
    2. For each Content record:
       - Create a new Resource record with:
         - tenantId: from Content.tenantId
         - title: extract from Content.title JSON (use 'en' locale, or first available)
         - description: extract excerpt from Content.excerpt JSON if available
         - category: map to ResourceCategory.OTHER (no direct mapping — Content RESOURCE is generic)
         - bodyContent: from Content.content JSON (preserve rich text)
         - authorId: from Content.authorId
         - publishedAt: from Content.publishedAt if published=true, else null
         - visibility: ResourceVisibility.ALL_RESIDENTS (default — was tenant-wide published)
       - Log the migration (old Content ID -> new Resource ID)
    3. After all records are migrated, output a summary:
       - Total Content RESOURCE records found
       - Total Resource records created
       - Any errors encountered
    4. Do NOT delete the original Content records — leave them for manual verification

    The script should be runnable as a Node.js script (use tsx or ts-node).
    Add console output for progress tracking.
    Wrap in a transaction so failures roll back cleanly.

  </action>
  <verify>
    <automated>tsx src/lib/migrations/migrate-resources.ts</automated>
    <manual>Run migration script, verify output shows migrated count, check Resource table has new records matching Content RESOURCE count</manual>
    <sampling_rate>run after Task 2 commits, before Task 3 begins</sampling_rate>
  </verify>
  <done>Migration script successfully converts all Content RESOURCE records to Resource model records with summary output</done>
</task>

<task type="auto">
  <name>Task 3: Remove RESOURCE from ContentCategory enum after migration confirmed clean</name>
  <files>prisma/schema.prisma, src/db/schema/index.ts</files>
  <action>
    After migration script confirms all Content RESOURCE records are migrated:

    1. Edit prisma/schema.prisma: Remove RESOURCES from the ContentCategory enum.
       Current: ANNOUNCEMENT, NEWS, EVENT, BLOG, CONSERVATION, SERVICES, RESOURCES, CAMPAIGN
       New: ANNOUNCEMENT, NEWS, EVENT, BLOG, CONSERVATION, SERVICES, CAMPAIGN

    2. Run `npx prisma generate` to regenerate Drizzle schema.
    3. Run `npx prisma db push` to apply the enum change.
    4. Search the codebase for any remaining references to ContentCategory.RESOURCES or 'RESOURCES' as a content category filter and update them:
       - Check API routes that filter by category=RESOURCES
       - Check any UI components that reference RESOURCE category
       - Update or remove as appropriate

    If any Content records still have category=RESOURCES after migration, the db push will fail — in that case, manually update remaining records before proceeding.

  </action>
  <verify>
    <automated>npx prisma generate && npx prisma db push --accept-data-loss && npm run typecheck</automated>
    <manual>Search codebase for "RESOURCES" — no references to ContentCategory.RESOURCES should remain</manual>
    <sampling_rate>run after Task 3 commits, end of plan</sampling_rate>
  </verify>
  <done>RESOURCE value removed from ContentCategory enum, all references updated, schema and Drizzle types regenerated</done>
</task>

</tasks>

<verification>
- Public /resources page fetches from Resource API, not Content API
- Resources are grouped by category with file download/view functionality
- Migration script converts all Content RESOURCE records to Resource model
- RESOURCE removed from ContentCategory enum with no remaining references
- All TypeScript type checks pass
- ESLint passes with no errors
</verification>

<success_criteria>

- Public /resources page shows resources from Resource model with category grouping
- Migration script runs successfully and outputs summary of migrated records
- ContentCategory enum no longer contains RESOURCE value
- No code references ContentCategory.RESOURCES after cleanup
- All 3 tasks complete with passing typecheck and lint
  </success_criteria>

<output>
After completion, create `.planning/phases/23-competitions-resources/23-03-SUMMARY.md`
</output>
