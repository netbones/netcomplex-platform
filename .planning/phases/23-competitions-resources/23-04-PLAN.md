---
phase: 23-competitions-resources
plan: 04
type: gap_closure
wave: 3
depends_on: ['23-01', '23-02', '23-03']
files_modified:
  - src/app/api/competitions/route.ts
  - src/widgets/admin/ui/CompetitionForm.tsx
  - src/app/(tenant)/admin/resources/[id]/page.tsx
autonomous: true
gap_closure: true
requirements: [COMP-01, COMP-02, RES-01, RES-02, RES-03, RES-04, RES-05]
user_setup: []

must_haves:
  truths:
    - 'Public /competition page displays active competition data from the database without requiring authentication'
    - 'Admin can change competition status from DRAFT to ACTIVE through the UI'
    - 'Resource edit page can access BOARD_ONLY and COMMITTEE_ONLY resources without 404'
  artifacts:
    - path: 'src/app/api/competitions/route.ts'
      provides: 'Unauthenticated access to upcoming competitions with ACTIVE status filter'
      exports: ['GET', 'POST']
    - path: 'src/widgets/admin/ui/CompetitionForm.tsx'
      provides: 'Status selector field for editing competitions'
      min_lines: 300
    - path: 'src/app/(tenant)/admin/resources/[id]/page.tsx'
      provides: 'Server component that forwards auth cookies or fetches directly from Drizzle'
      min_lines: 39
  key_links:
    - from: 'src/app/competition/page.tsx'
      to: '/api/competitions?upcoming=true'
      via: 'fetch GET without auth requirement'
      pattern: 'fetch.*api/competitions.*upcoming'
    - from: 'src/widgets/admin/ui/CompetitionForm.tsx'
      to: '/api/competitions/[id]'
      via: 'PATCH with status field'
      pattern: 'status.*select|select.*status'
    - from: 'src/app/(tenant)/admin/resources/[id]/page.tsx'
      to: 'drizzle resources table'
      via: 'direct Drizzle query or API with auth cookies'
      pattern: 'cookies|headers|db\.select'
---

<objective>
Close 3 verification gaps identified in 23-VERIFICATION.md:

1. **Competition status management missing from UI** — CompetitionForm lacks a status selector field, preventing admins from activating competitions (DRAFT → ACTIVE).

2. **Public competition page blocked by auth requirement** — Competition API GET returns 401 for unauthenticated users, blocking the public /competition page. Additionally, the `upcoming=true` filter doesn't exclude DRAFT competitions.

3. **Resource edit page auth forwarding** — Resource edit page fetches from API without forwarding auth cookies, causing 404 for BOARD_ONLY/COMMITTEE_ONLY resources.

Output: Updated Competition API with unauthenticated upcoming access, CompetitionForm with status selector, Resource edit page with proper auth handling.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-competitions-resources/23-VERIFICATION.md
@src/app/api/competitions/route.ts
@src/widgets/admin/ui/CompetitionForm.tsx
@src/app/(tenant)/admin/resources/[id]/page.tsx
@src/app/api/resources/[id]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Competition API to allow unauthenticated access for upcoming filter and add ACTIVE status filter</name>
  <files>src/app/api/competitions/route.ts</files>
  <action>
    Modify src/app/api/competitions/route.ts GET handler:

    1. **Allow unauthenticated access for upcoming filter only:**
       - If `upcoming=true` query param is present, allow the request without authentication
       - Apply visibility filter: only return competitions with status=ACTIVE
       - This enables the public /competition page to work without login

    2. **Add ACTIVE status filter to upcoming query:**
       - When `upcoming=true`, add `eq(competitions.status, 'ACTIVE')` to the WHERE clause
       - This prevents DRAFT competitions from appearing on the public page

    3. **Keep authenticated behavior unchanged:**
       - If user is authenticated and NOT using upcoming filter, return all competitions per their role
       - Existing status filter and other query params continue to work as before

    Current pattern: API calls getSessionAndRole() and returns 401 if null.
    New pattern: Check for upcoming=true first — if present, skip auth and apply ACTIVE filter.
    Otherwise, proceed with existing auth flow.

    Ensure tenant scoping via withTenant() is still applied in both paths.

  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <manual>curl -X GET /api/competitions?upcoming=true returns 200 without auth, only ACTIVE competitions; curl -X GET /api/competitions without auth returns 401</manual>
    <sampling_rate>run before Task 2 begins</sampling_rate>
  </verify>
  <done>Competition API allows unauthenticated access to upcoming=true with ACTIVE status filter, authenticated access unchanged for other queries</done>
</task>

<task type="auto">
  <name>Task 2: Add status selector field to CompetitionForm for editing mode</name>
  <files>src/widgets/admin/ui/CompetitionForm.tsx</files>
  <action>
    Modify src/widgets/admin/ui/CompetitionForm.tsx:

    1. **Add status field to Zod schema:**
       - Add `status: z.enum(['DRAFT', 'ACTIVE', 'ENDED', 'CANCELLED']).default('DRAFT')`
       - Only required/visible in edit mode (not needed for new competitions — they default to DRAFT)

    2. **Add status select field to form UI:**
       - Add a select dropdown with options: DRAFT, ACTIVE, ENDED, CANCELLED
       - Only show when editing an existing competition (initialData is present)
       - Label: "Status"
       - Default value: competition's current status

    3. **Include status in form submission:**
       - Add status to the onSubmit payload for PATCH requests
       - Ensure the field is included in the initialData passed to reset()

    4. **Visual design:**
       - Follow existing form field patterns (same styling as other selects)
       - Place near the top of the form (after title, before dates)
       - Use same Tailwind classes as other form fields

    Reference the existing form structure — it uses react-hook-form with zodResolver, has fields for title, description, rules, prizeInfo, startDate, endDate, image.

  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
    <manual>Navigate to /admin/competitions/[id], see status dropdown, change from DRAFT to ACTIVE, save — competition status updates</manual>
    <sampling_rate>run before Task 3 begins</sampling_rate>
  </verify>
  <done>CompetitionForm displays status selector in edit mode, allows changing competition status, submits status in PATCH request</done>
</task>

<task type="auto">
  <name>Task 3: Fix Resource edit page to forward auth cookies or fetch directly from Drizzle</name>
  <files>src/app/(tenant)/admin/resources/[id]/page.tsx</files>
  <action>
    Modify src/app/(tenant)/admin/resources/[id]/page.tsx:

    **Option A (preferred): Fetch directly from Drizzle in server component**
    - Import db and resources table from @api/db
    - Import withTenant() helper
    - Import auth helper to get session role
    - Query resource directly: `db.select().from(resources).where(eq(resources.id, id))`
    - Apply tenant scoping: verify resource.tenantId matches current tenant
    - No visibility filtering needed — admins should see all resources in edit mode
    - This avoids the API entirely and eliminates the auth forwarding problem

    **Option B (alternative): Forward auth cookies to API**
    - Import `cookies()` from next/headers
    - Forward cookie header in fetch: `headers: { cookie: cookies().toString() }`
    - This is less preferred because it adds complexity and depends on cookie format

    Use Option A — direct Drizzle query is simpler, more reliable, and follows the pattern of server components having direct database access.

    Current code: Server component fetches from /api/resources/[id] without auth headers.
    New code: Server component queries Drizzle directly with tenant scoping.

    Keep the rest of the page unchanged — it renders ResourceForm with the fetched data.

  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <manual>Navigate to /admin/resources/[id] for a BOARD_ONLY resource — page loads without 404</manual>
    <sampling_rate>run after Task 3 commits, end of plan</sampling_rate>
  </verify>
  <done>Resource edit page fetches directly from Drizzle with tenant scoping, can access all resources regardless of visibility setting</done>
</task>

</tasks>

<verification>
- Competition API GET with upcoming=true returns 200 without authentication
- Competition API upcoming filter only returns ACTIVE status competitions
- CompetitionForm shows status selector in edit mode with DRAFT/ACTIVE/ENDED/CANCELLED options
- Competition status can be changed from DRAFT to ACTIVE via the form
- Resource edit page loads BOARD_ONLY and COMMITTEE_ONLY resources without 404
- All TypeScript type checks pass
- ESLint passes with no errors
</verification>

<success_criteria>

- Public /competition page works without login and displays only ACTIVE competitions
- Admin can activate competitions via the status selector in CompetitionForm
- Resource edit page loads all resources regardless of visibility setting
- All 3 gaps from 23-VERIFICATION.md are resolved
- All tasks complete with passing typecheck and lint
  </success_criteria>

<output>
After completion, create `.planning/phases/23-competitions-resources/23-04-SUMMARY.md`
</output>
