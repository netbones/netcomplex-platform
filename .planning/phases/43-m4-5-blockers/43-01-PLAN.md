---
phase: 43-m4-5-blockers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/seed.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - 'Running `npx prisma db seed` against a test DB exits 0 with no schema type errors'
    - 'Running `pnpm db:seed` (which invokes scripts/seed-drizzle.ts) still produces the same seed data as before the change'
    - "Prisma's `schema.prisma` `seed` block (if present) still resolves to a file that runs successfully"
  artifacts:
    - path: 'prisma/seed.ts'
      provides: 'Re-export shim that delegates to scripts/seed-drizzle.ts seed() function'
      max_lines: 40
      contains: 'import.*seed-drizzle'
  key_links:
    - from: 'prisma/seed.ts'
      to: 'scripts/seed-drizzle.ts'
      via: 'import { seed } and call'
      pattern: "await seed\\(\\)"
    - from: 'package.json db:seed script'
      to: 'scripts/seed-drizzle.ts'
      via: 'pnpm exec tsx scripts/seed-drizzle.ts (unchanged)'
      pattern: "tsx scripts/seed-drizzle\\.ts"
---

<objective>
Replace the broken 1630-line `prisma/seed.ts` with a thin re-export shim that delegates to the working `scripts/seed-drizzle.ts`. This satisfies BD issue `tc4` (P1) — "prisma/seed.ts schema type errors" — and lets `npx prisma db seed` work end-to-end without duplicating seed data.

Purpose: `tc4` blocks the soak because `npx prisma db seed` (the Prisma default command) is the operator's expected seed entry point. Replacing the file with a shim is the minimum-risk fix — no schema migration, no data duplication, no package.json change.

Output: A `prisma/seed.ts` ≤ 40 lines that imports the `seed()` function from `scripts/seed-drizzle.ts` and calls it. `npx prisma db seed` and `pnpm db:seed` both produce identical seed data.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-m4-5-blockers/43-CONTEXT.md
@.planning/phases/43-m4-5-blockers/43-RESEARCH.md

# Source-of-truth files

@.planning/ROADMAP.md (Phase 43 section)
@prisma/seed.ts (the 1630-line file to replace)
@scripts/seed-drizzle.ts (the working seed; re-exports `seed()` at line 2997)
@package.json (db:seed already points to scripts/seed-drizzle.ts via pnpm exec tsx)
</context>

<interfaces>
<!-- Contract the shim must satisfy -->
<!-- prisma/seed.ts is the file Prisma runs when `npx prisma db seed` is invoked -->
<!-- scripts/seed-drizzle.ts currently has `async function seed()` at line 2997 (NOT exported) -->
<!-- and ends with `seed().catch(e => ...).finally(() => process.exit(0))` at lines 3206-3211 -->

From scripts/seed-drizzle.ts (current state, top of file):

- `const pool = new Pool({ connectionString, ... })` — already configured
- `const db = drizzle(pool)` — Drizzle instance
- All schema imports use `@schema/*` aliases (e.g., `@schema/users`, `@schema/tenants`)
- The file uses top-level invocation: `seed().catch(...).finally(() => process.exit(0))` at the bottom

From scripts/seed-drizzle.ts (line 2997):

- `async function seed()` is declared but not exported
- Internally calls `console.log('🌱 Seeding...')` and inserts/uperts all data
- Returns `Promise<void>`

From package.json (line 21):

- `"db:seed": "pnpm exec tsx scripts/seed-drizzle.ts"` — the working entry point
- The shim should NOT change package.json (the existing script continues to work)
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert seed-drizzle.ts to export seed() and replace prisma/seed.ts with re-export shim</name>
  <files>prisma/seed.ts, scripts/seed-drizzle.ts</files>
  <read_first>
    - prisma/seed.ts (the 1630-line broken file to replace)
    - scripts/seed-drizzle.ts (line 2997 has the `async function seed()` declaration; lines 3206-3211 have the top-level invocation)
  </read_first>
  <acceptance_criteria>
    - The shim at `prisma/seed.ts` is ≤ 40 lines total
    - `prisma/seed.ts` contains exactly one import (`import { seed } from '../scripts/seed-drizzle';`) and one call site (`seed().then(...).catch(...).finally(() => process.exit(...))`)
    - `scripts/seed-drizzle.ts` line 2997 reads `export async function seed()` (the `export` keyword was added; the function body is unchanged)
    - `scripts/seed-drizzle.ts` lines 3206-3211 still contain the original top-level invocation (unchanged)
    - `package.json` is NOT modified
    - `pnpm typecheck | grep "prisma/seed.ts"` produces ZERO matches (pre-existing errors in `scripts/seed-drizzle.ts` are out of scope)
  </acceptance_criteria>
  <action>
    In `scripts/seed-drizzle.ts`:
    1. Add the `export` keyword to line 2997 — change `async function seed()` to `export async function seed()`.
    2. The top-level invocation block at lines 3206-3211 (`seed().catch(...).finally(...)`) MUST remain exactly as-is so that `pnpm db:seed` keeps working. Do not move or wrap it.

    Replace the entire contents of `prisma/seed.ts` (currently 1630 lines) with a thin shim containing ONLY:
    - A single import: `import { seed } from '../scripts/seed-drizzle';` (the `@schema/*` aliases used inside seed-drizzle.ts are not resolvable from prisma/seed.ts, so use the relative path; tsx resolves the import the same way)
    - A `main()` wrapper that calls `await seed()` inside a try/catch:
      - On success, log "✅ Seed complete" and `process.exit(0)`
      - On error, log the error and `process.exit(1)`
    - Call `main()` at the bottom

    Do NOT add any business logic, type definitions, or data to the shim. It must remain ≤ 40 lines.

    Do NOT modify `package.json`. The `db:seed` script (`pnpm exec tsx scripts/seed-drizzle.ts`) continues to work via top-level invocation. The new shim is the entry point for `npx prisma db seed` (which Prisma calls without args).

  </action>
  <verify>
    <automated>wc -l /home/ubuntupunk/Projects/soralia-village/prisma/seed.ts && grep -c "import.*seed-drizzle" /home/ubuntupunk/Projects/soralia-village/prisma/seed.ts && grep -c "export async function seed" /home/ubuntupunk/Projects/soralia-village/scripts/seed-drizzle.ts && pnpm typecheck 2>&1 | grep -E "prisma/seed\.ts" | wc -l</automated>
  </verify>
  <done>
    - `prisma/seed.ts` is ≤ 40 lines and contains `import { seed } from '../scripts/seed-drizzle';`
    - `scripts/seed-drizzle.ts` line 2997 reads `export async function seed()`
    - `pnpm typecheck | grep "prisma/seed.ts"` produces ZERO matches (the new shim has no type errors; pre-existing errors in `scripts/seed-drizzle.ts` are out of scope for tc4 and accepted)
    - The top-level `seed().catch(...).finally(...)` block at lines 3206-3211 of `scripts/seed-drizzle.ts` is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: End-to-end validation — run both seed entry points and confirm identical data shape</name>
  <files>prisma/seed.ts, scripts/seed-drizzle.ts</files>
  <read_first>
    - scripts/seed-drizzle.ts (lines 3203-3211: the success message and process.exit)
    - package.json (line 21: `db:seed` script)
    - .env or .env.local (DATABASE_URL must be set; do not commit)
  </read_first>
  <acceptance_criteria>
    - `pnpm db:seed` exits 0 and prints "🌱 Seeding Soralia Village..." → "✅ Seed complete!" in under 30 seconds
    - `npx prisma db seed` exits 0 (this was the broken case before the shim) and prints the same seed sequence
    - `/tmp/seed-drizzle-run.log` and `/tmp/prisma-seed-run.log` match (line-by-line `diff -u` produces no material differences)
    - A `SELECT COUNT(*) FROM users` query returns the same non-zero count after both runs (proves both entry points produce identical data)
    - Neither log contains ERROR or FAIL strings
  </acceptance_criteria>
  <action>
    1. Run `pnpm db:seed` against the configured test database. This invokes `scripts/seed-drizzle.ts` directly. Expect the script to print "🌱 Seeding Soralia Village..." and finish with "✅ Seed complete!" in under 30 seconds. Capture the full output to `/tmp/seed-drizzle-run.log`.
    2. Run `npx prisma db seed` against the same database. This invokes `prisma/seed.ts` (the new shim). Expect the same sequence of console output, ending with "✅ Seed complete!" from the shim's success branch. Capture the full output to `/tmp/prisma-seed-run.log`.
    3. Compare `/tmp/seed-drizzle-run.log` and `/tmp/prisma-seed-run.log` line-by-line (use `diff -u`). They MUST be identical OR differ only in the wrapper log lines (e.g., the shim's "✅ Seed complete" vs seed-drizzle.ts's "✅ Seed complete!" — both are valid). If they differ materially (e.g., one reports a different tenant UUID or a different row count), fix the shim and re-run.
    4. Confirm no new ERROR or FAIL lines appear in either log. If the run fails with a database connection error, do NOT debug the database — abort and report.
    5. After both runs complete, run a SELECT count on the `users` table: `pnpm exec tsx -e "import { Pool } from 'pg'; import { drizzle } from 'drizzle-orm/node-postgres'; const p = new Pool({connectionString: process.env.DATABASE_URL?.replace('sslmode=require','sslmode=no-verify'), connectionTimeoutMillis: 5000}); const db = drizzle(p); db.execute('SELECT COUNT(*) FROM users').then(r => { console.log('users_count:', r.rows[0].count); process.exit(0); }).catch(e => { console.error(e); process.exit(1); });"` — expect a non-zero number matching the previous baseline (e.g., 6+ users).
  </action>
  <verify>
    <automated>pnpm db:seed 2>&1 | tee /tmp/seed-drizzle-run.log && echo "---" && npx prisma db seed 2>&1 | tee /tmp/prisma-seed-run.log && diff -u /tmp/seed-drizzle-run.log /tmp/prisma-seed-run.log | head -30 && echo "OK: both seeds completed"</automated>
  </verify>
  <done>
    - `pnpm db:seed` exits 0 and produces the same row counts as the previous baseline
    - `npx prisma db seed` exits 0 (this was the broken case before the shim)
    - `/tmp/seed-drizzle-run.log` and `/tmp/prisma-seed-run.log` match (or differ only in wrapper log lines)
    - No `ERROR` or `FAIL` strings in either log
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                                                       |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Operator CLI → seed script | The seed script runs `INSERT`/`UPDATE` directly against the production-shaped DB schema. A malformed or partial seed could leave the DB in an inconsistent state. |

## STRIDE Threat Register

| Threat ID  | Category  | Component                  | Disposition | Mitigation Plan                                                                                                                                                                                |
| ---------- | --------- | -------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-43-01-SC | Tampering | pnpm install / npm install | mitigate    | The shim does not introduce new packages; verify `package.json` is unchanged before commit.                                                                                                    |
| T-43-01-T1 | Tampering | Seed data divergence       | mitigate    | The shim delegates to the existing `seed()` function — no data is duplicated or transformed.                                                                                                   |
| T-43-01-E1 | Elevation | Process exit codes         | mitigate    | Both the shim and `scripts/seed-drizzle.ts` exit with 0/1 codes; the shim explicitly calls `process.exit(0)` on success and `process.exit(1)` on error to satisfy Prisma's exit-code contract. |

</threat_model>

<verification>
- `prisma/seed.ts` is ≤ 40 lines and contains exactly one import (`from '../scripts/seed-drizzle'`) and one `main()` call
- `scripts/seed-drizzle.ts` line 2997 reads `export async function seed()`
- `pnpm typecheck` exits 0
- `pnpm db:seed` exits 0
- `npx prisma db seed` exits 0
- Row counts in seeded tables (users, properties, households, profiles, etc.) match the previous baseline
</verification>

<success_criteria>

- BD issue `tc4` is closed: `npx prisma db seed` no longer fails with schema type errors
- `prisma/seed.ts` is a shim ≤ 40 lines, no data duplication
- Both `pnpm db:seed` and `npx prisma db seed` produce identical seeded data
- `pnpm typecheck` passes
- No regressions to existing `pnpm db:seed` behavior
  </success_criteria>

<output>
Create `.planning/phases/43-m4-5-blockers/43-01-SUMMARY.md` when done
</output>
