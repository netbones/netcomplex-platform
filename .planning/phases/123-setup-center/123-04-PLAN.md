---
phase: 123-setup-center
plan: 123-04
type: execute
title: Launch & Populate Sections — identity config + people management
status: planning
created: 2026-07-07
updated: 2026-07-07
depends_on: ["123-03"]
must_haves:
  truths:
    - "Launch section shows all 6 required identity missions with inline editing"
    - "Populate section supports board member invitation, resident invitation, and role management"
    - "Both sections auto-save on blur — no explicit Save button needed"
    - "Required missions are visually marked; completion updates in real-time"
    - "`pnpm lint` produces zero new lint warnings or errors on section component files"
  artifacts:
    - "src/features/setup/ui/sections/LaunchSection.tsx"
    - "src/features/setup/ui/sections/PopulateSection.tsx"
    - "src/features/setup/model/useAutoSaveSetting.ts"
  key_links:
    - "LaunchSection → PATCH /api/platform/setup/settings for each identity field"
    - "PopulateSection → POST /api/invitations for sending invites"
    - "PopulateSection → GET /api/platform/setup/missions for invitation tracking"
    - "useAutoSaveSetting → debounce → SetupSetting upsert"
---

# Plan 123-04: Launch & Populate Sections

## Goal

Build the two highest-priority Setup Center sections. Launch captures all required tenant identity configuration. Populate handles team and member onboarding.

## Pre-flight

This phase executes inside `phase-123-setup-center` worktree. Verify:

```bash
wt list | grep phase-123-setup-center  # must show active worktree
[ -f .env ] || cp ../soralia-village/.env .  # .env must exist
```

## Tasks

### Task 1: Launch section

<files>src/features/setup/ui/sections/LaunchSection.tsx</files>
<action>

Create `LaunchSection.tsx` ("use client"):

Six missions, all required (100% needed before public launch):

- **Community Name** — inline text edit, validates uniqueness, saves to SetupSetting `launch.name`
- **Branding** — reuse branding form from existing `BrandingStep` (logo upload, color picker, font selector). Saves to SetupSetting `launch.branding`. Adapt existing `BrandingStep.tsx` as basis, don't duplicate code
- **Contact Details** — email + phone inline edits. Saves to SetupSetting `launch.contact`
- **Domain** — subdomain inline edit (e.g., `soralia.netbones.co.za`). Saves to SetupSetting `launch.domain`
- **Timezone** — dropdown selector. Saves to SetupSetting `launch.timezone`
- **Address** — inline text fields (line1, city, province, postalCode). Saves to SetupSetting `launch.address`

Each field auto-saves on blur using `useAutoSaveSetting(tenantId, key, value)`. Mission is marked complete when the corresponding setting value exists and is valid.

</action>
<verify>All 6 missions render, edit, and auto-save. Completion checkmarks update. F5 refresh preserves all data.</verify>
<done>Launch section with 6 required missions and auto-save</done>

### Task 2: Populate section

<files>src/features/setup/ui/sections/PopulateSection.tsx</files>
<action>

Create `PopulateSection.tsx` ("use client"):

Missions (all optional):

- **Invite Board Members** — reuse invite form from existing `InviteStep.tsx`. Email input + role dropdown (BOARD). Sends real invitations via `POST /api/invitations`. Adapt don't duplicate
- **Invite Residents** — batch email input (comma or newline separated). Role defaults to RESIDENT. Sends invitations
- **Import Members** — CSV upload with preview table. Parse name, email, role columns. Bulk create invitations
- **Assign Roles** — existing user list filtered by role. Role dropdown to reassign. Uses existing user management API
- **Create Service Accounts** — form for provider/contractor accounts. Links to existing provider registration (Phase 46)

Track completion: mission is "complete" when at least one invitation has been sent in that category. This is tracked via SetupMission.isCompleted (manually toggled, or inferred from invitation count).

</action>
<verify>Board + resident invitations send and track correctly. CSV import parses and previews. Role assigner works on existing users.</verify>
<done>Populate section with 5 optional missions</done>

### Task 3: Auto-save hook

<files>src/features/setup/model/useAutoSaveSetting.ts</files>
<action>

Create `useAutoSaveSetting(tenantId)` hook:

- `saveSetting(key: string, value: unknown)` — debounced PATCH to `/api/platform/setup/settings`
- Returns `{ saveSetting, isSaving, lastSaved, error }`
- Uses 500ms debounce (matching existing widget auto-save pattern from Phase 27)
- Optimistic local state update with rollback on error
- Shows saving indicator after successful save ("Saved" → fades after 2s)

</action>
<verify>Saving a setting debounces, shows "Saving..." then "Saved", persists across page refresh</verify>
<done>Debounced auto-save hook with optimistic updates</done>

### Task 4: Section component tests

<files>src/features/setup/__tests__/LaunchSection.test.tsx</files>
<files>src/features/setup/__tests__/PopulateSection.test.tsx</files>
<action>

Write component tests:

- `LaunchSection` renders all 6 mission fields
- Editing a field triggers auto-save
- `PopulateSection` renders invitation forms
- CSV import parses valid CSV content

</action>
<verify>`pnpm vitest run src/features/setup/__tests__/` passes all tests</verify>
<done>Section component tests passing</done>

## Acceptance

- Launch section: 6 required missions, all auto-save, progress tracked
- Populate section: 5 missions, invitations actually send
- Auto-save hook works with debounce and optimistic updates
- Component tests pass

## Out of scope

- Configure section (Plan 123-05)
- Grow section / recommendation engine (Plan 123-05)
