---
phase: 03-second-tenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/auth.ts
  - src/app/admin/platform/[id]/edit/page.tsx
autonomous: true
requirements:
  - MULTI-07
  - MULTI-08

must_haves:
  truths:
    - 'Second tenant can be onboarded without data leakage'
    - 'RLS policies prevent cross-tenant data access at DB level'
    - 'Better-Auth organization integrated with tenant resolution'
  artifacts:
    - path: 'prisma/migrations/*rls*.sql'
      provides: 'RLS policies for tenant-scoped tables'
    - path: 'src/lib/auth.ts'
      provides: 'Organization-tenant mapping'
---

<objective>
Phase 3: Enable second tenant onboarding with full security guarantees. This includes RLS policies on database tables and verifying Better-Auth organization integration.

Purpose: Complete the security foundation before onboarding a second tenant. Data leakage between tenants would be a critical incident.
Output: RLS enabled on all tenant-scoped tables, organization-tenant mapping verified.
</objective>

<context>
@docs/netcomplex_migration_planv1.md - Phase 3 requirements

# Current State

- Better-Auth organization plugin already configured in src/lib/auth.ts
- organizations table exists in prisma/drizzle/schema.ts
- tenantQueries exist in src/lib/tenant.ts
- All API routes use withTenant() for enforcement

# What Needs Work

- RLS policies not yet enabled on tenant-scoped tables
- Organization-tenant mapping needs verification
- Second tenant onboarding flow needs testing
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Audit tenant-scoped tables for RLS</name>
  <files>prisma/schema.prisma</files>
  <action>
    Identify all tables that need RLS:
    1. List all tables with tenantId column
    2. Verify all tenant-scoped tables are identified
    
    Expected tables: users, households, bookings, maintenanceRequests, notifications, events, conversations, messages, surveys, announcements, groups, albums, contents, invitations, communityServiceListings, communityServiceInquiries, communityServiceReviews, propertyListings, agentProfiles, agentAccesses, profiles, settings, platformSuspensions, externalSurveys, standardSeats, premiumSeats, soloSeats, members, userGroups, groupMembershipRequests, conversationParticipants, questions, responses
  </action>
  <verify>grep -c "tenantId" prisma/schema.prisma</verify>
  <done>All tenant-scoped tables identified</done>
</task>

<task type="auto">
  <name>Task 2: Enable RLS on tenant-scoped tables</name>
  <files>prisma/schema.prisma</files>
  <action>
    Add RLS policies to each tenant-scoped table:
    
    ```prisma
    // Example pattern for each table
    model User {
      id        String   @id
      tenantId  String
      
      // Add RLS
      @@rls
    }
    
    // Then create policies via migration:
    -- For each table: CREATE POLICY "tenant_isolation" ON "User" 
    -- USING (tenantId = current_setting('app.current_tenant_id')::text)
    ```
    
    Create a migration file with RLS policies for all tenant-scoped tables.
    Use current_setting('app.current_tenant_id') pattern for dynamic tenant context.
  </action>
  <verify>ls prisma/migrations/*rls*.sql 2>/dev/null || echo "No RLS migration yet"</verify>
  <done>RLS migration created</done>
</task>

<task type="auto">
  <name>Task 3: Verify Better-Auth organization integration</name>
  <files>src/lib/auth.ts</files>
  <action>
    Verify the organization plugin is properly integrated:
    1. Check organization plugin configuration
    2. Verify organizationId is stored on user records
    3. Check that tenant-organization mapping exists
    
    The auth.ts already has organization() plugin. Verify:
    - Users have organizationId field
    - Organization is linked to tenant (tenantId on organization or vice versa)
    - API routes can derive tenant from organization membership
  </action>
<verify>grep -n "organization" src/lib/auth.ts | head -5</verify>
  <done>Organization integration verified</done>
</task>

<task type="auto">
  <name>Task 4: Create second tenant onboarding test</name>
  <files>src/app/admin/platform/new/page.tsx</files>
  <action>
    Create a test scenario to verify tenant isolation:
    1. Add ability to create new tenant via /admin/platform/new
    2. Verify tenant data is isolated (Soralia data not visible to new tenant)
    3. Test custom domain resolution for new tenant
    
    This is a dry-run/test - create the capability but don't activate a real second tenant yet.
  </action>
  <verify>ls -la src/app/admin/platform/new/page.tsx</verify>
  <done>Second tenant onboarding capability exists</done>
</task>

<task type="auto">
  <name>Task 5: Test tenant isolation</name>
  <files>src/lib/tenant.ts</files>
  <action>
    Create a verification test:
    1. Query for tenant A data with tenant B's tenantId
    2. Verify the query returns no results (RLS blocks it)
    3. Or verify that withTenant() throws for unauthorized access
    
    Document the isolation test results.
  </action>
  <verify>echo "Isolation test - manual verification required in staging"</verify>
  <done>Isolation test documented</done>
</task>

</tasks>

<verification>
- [ ] All tenant-scoped tables identified
- [ ] RLS migration created
- [ ] Organization integration verified
- [ ] Second tenant onboarding capability exists
- [ ] Isolation test documented
</verification>

<success_criteria>
Phase 3 complete when:

1. RLS policies enabled on all tenant-scoped tables
2. Organization-tenant mapping verified working
3. Second tenant can be created via admin UI
4. Tenant isolation verified (no data leakage)
   </success_criteria>

<output>
After completion, create `.planning/phases/03-second-tenant/{phase}-{plan}-SUMMARY.md`
</output>
