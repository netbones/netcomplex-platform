---
phase: 106-dispute-api-routes-intake-screen
plan: 02
type: execute
wave: 2
depends_on: [106-01]
files_modified:
  - src/app/api/disputes/[id]/messages/route.ts
  - src/app/api/disputes/[id]/evidence/route.ts
  - src/app/api/disputes/[id]/assign/route.ts
  - src/app/api/disputes/[id]/ruling/route.ts
  - src/app/api/disputes/__tests__/specialized-routes.test.ts
  - src/entities/dispute/index.server.ts
autonomous: true
requirements:
  - DISPUTE-04
user_setup: []

must_haves:
  truths:
    - 'POST /api/disputes/[id]/messages creates a mediation message with isInternal flag, returns 403 when non-moderator posts internal note'
    - 'GET /api/disputes/[id]/messages returns only non-internal messages for parties; returns all messages (including internal) for moderators'
    - 'POST /api/disputes/[id]/evidence uploads a file via uploadImage(), inserts a DisputeEvidence row, logs EVIDENCE_ADDED event'
    - 'POST /api/disputes/[id]/assign sets assignedModeratorId (BOARD/ADMIN only), logs ASSIGNED event'
    - 'POST /api/disputes/[id]/ruling sets rulingDescription and rulingIssuedAt (BOARD/ADMIN only), logs RULING_ISSUED event'
  artifacts:
    - path: 'src/app/api/disputes/[id]/messages/route.ts'
      provides: 'GET list messages with visibility filtering, POST create message with isInternal enforcement'
      exports: ['GET', 'POST']
    - path: 'src/app/api/disputes/[id]/evidence/route.ts'
      provides: 'POST upload evidence file with rate limiting'
      exports: ['POST']
    - path: 'src/app/api/disputes/[id]/assign/route.ts'
      provides: 'POST assign moderator (BOARD/ADMIN)'
      exports: ['POST']
    - path: 'src/app/api/disputes/[id]/ruling/route.ts'
      provides: 'POST issue formal ruling (BOARD/ADMIN)'
      exports: ['POST']
  key_links:
    - from: 'src/app/api/disputes/[id]/messages/route.ts'
      to: 'Supabase Realtime channel dispute:{id}'
      via: 'supabase.channel(...).send(...)'
      pattern: 'new-mediation-message'
    - from: 'src/app/api/disputes/[id]/evidence/route.ts'
      to: 'src/shared/api/storage.ts'
      via: 'import { uploadImage }'
      pattern: "uploadImage\\(file"
---

<objective>
Implement the four specialized dispute API routes: mediation thread (messages with visibility rules and Supabase Realtime broadcast), evidence file upload (S3 via uploadImage), moderator assignment (BOARD/ADMIN only), and formal ruling issuance (BOARD/ADMIN only). Each route follows the established guard pattern and logs audit events.

Purpose: These routes provide the operational core of dispute resolution — communication between parties and moderators, evidence collection, moderator assignment, and formal rulings.
Output: 4 new route handler files, 1 test file covering all specialized routes.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-dispute-api-routes-intake-screen/106-CONTEXT.md
@.planning/phases/106-dispute-api-routes-intake-screen/106-RESEARCH.md
@.planning/phases/106-dispute-api-routes-intake-screen/106-PATTERNS.md
@src/app/api/disputes/route.ts (created in 106-01 — GET/POST pattern)
@src/app/api/disputes/[id]/route.ts (created in 106-01 — withErrorHandler + access control pattern)
@src/app/api/messages/route.ts (analog for Supabase Realtime broadcast)
@src/shared/api/storage.ts (uploadImage function)
@src/entities/dispute/model/schemas.ts (created in 106-01 — Zod schemas for message, assign, ruling)
@src/shared/lib/sanitize/server.ts (sanitizeHtml for message content)
@.planning/phases/106-dispute-api-routes-intake-screen/106-PATTERNS.md lines 261-517 — messages, evidence, assign, ruling patterns
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mediation Thread Routes — Messages GET + POST with Visibility Rules</name>
  <files>src/app/api/disputes/[id]/messages/route.ts</files>
  <read_first>
src/app/api/messages/route.ts lines 1-282 — exact Supabase Realtime broadcast pattern to replicate
src/app/api/disputes/[id]/route.ts (created in 106-01) — withErrorHandler wrapper, getSessionAndRole inline pattern
src/entities/dispute/model/schemas.ts — disputeMessageCreateSchema from Task 1
src/shared/lib/sanitize/server.ts — sanitizeHtml function signature
@.planning/phases/106-dispute-api-routes-intake-screen/106-PATTERNS.md lines 261-351 — mediation thread patterns
@.planning/phases/106-dispute-api-routes-intake-screen/106-RESEARCH.md lines 486-523 — visibility rules
  </read_first>
  <behavior>
- Test 1: POST /api/disputes/[id]/messages returns 401 without auth
- Test 2: POST /api/disputes/[id]/messages returns 429 when rate limited (30/min)
- Test 3: POST /api/disputes/[id]/messages creates message with sanitized content (sanitizeHtml applied)
- Test 4: POST /api/disputes/[id]/messages returns 403 when non-moderator sets isInternal: true
- Test 5: POST /api/disputes/[id]/messages broadcasts via Supabase Realtime channel dispute:{id}
- Test 6: GET /api/disputes/[id]/messages returns only isInternal:false messages for party user
- Test 7: GET /api/disputes/[id]/messages returns all messages for BOARD user
- Test 8: GET /api/disputes/[id]/messages returns access-denied for non-party non-moderator
  </behavior>
  <action>
Create src/app/api/disputes/[id]/messages/route.ts. Wrap both handlers with withErrorHandler. Set export const maxDuration = 8. Initialize Supabase client at module level: `const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!)`. Inline getSessionAndRole helper.

GET handler: await params.id, withTenant(), getSessionAndRole(). Fetch dispute to verify user is party or moderator. Determine isParty (complainantId === userId || respondentId === userId) and isModerator (hasPermission(authData.role, 'admin') || role === 'BOARD' || role === 'COMMITTEE'). If neither, return apiForbidden(). Build Drizzle query with where(and(eq(disputeMessages.disputeId, id), eq(disputeMessages.tenantId, tenantId), isNull(disputeMessages.deletedAt))). Append visibility filter: if isParty, add eq(disputeMessages.isInternal, false); if isModerator, no filter (sees all). Order by asc(disputeMessages.createdAt). Return apiSuccess(messages).

POST handler: await params.id, withTenant(), getSessionAndRole(). Apply rate limit: `const rateLimit = await rateLimitByUser(authData.userId, { windowMs: 60_000, maxRequests: 30 }); if (rateLimit) return rateLimit;`. Validate body with disputeMessageCreateSchema.safeParse(). Verify user is party or moderator (same logic as GET). Enforce isInternal: if body.isInternal is true AND user is not moderator (isModerator is false), return apiForbidden('Only moderators can post internal notes'). Sanitize content with sanitizeHtml(content). Insert message: db.insert(disputeMessages).values({ id: crypto.randomUUID(), tenantId, disputeId: id, senderId: authData.userId, content: sanitized, isInternal: body.isInternal ?? false, createdAt: now() }).returning(). Broadcast via Supabase: supabase.channel(`dispute:${id}`).send({ type: 'broadcast', event: 'new-mediation-message', payload: newMessage }). Return apiCreated(newMessage).

Write tests in src/app/api/disputes/**tests**/specialized-routes.test.ts (shared test file with Task 2). Mock Supabase client with vi.mock.
</action>
<verify>
<automated>npx vitest run src/app/api/disputes/**tests**/specialized-routes.test.ts -t "message"</automated>
</verify>
<done>POST creates messages with sanitizeHtml, enforces isInternal role check, broadcasts via Supabase. GET returns messages with correct visibility filtering (parties see non-internal only; moderators see all). All 8 test behaviors pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evidence Upload + Assign Moderator + Issue Ruling Routes</name>
  <files>src/app/api/disputes/[id]/evidence/route.ts, src/app/api/disputes/[id]/assign/route.ts, src/app/api/disputes/[id]/ruling/route.ts</files>
  <read_first>
src/shared/api/storage.ts — uploadImage(file: File, userId: string): Promise<UploadResult> exact signature and ALLOWED_TYPES/MAX_FILE_SIZE values
src/app/api/disputes/[id]/route.ts (created in 106-01) — PATCH handler with db.transaction() pattern for status+event
src/entities/dispute/model/schemas.ts — disputeAssignSchema (moderatorId: z.string().uuid()), disputeRulingSchema (rulingDescription: z.string().min(10).max(5000))
src/entities/dispute/model/lifecycle.ts — canTransition() for ruling (transition to FORMAL_RULING must be valid)
@.planning/phases/106-dispute-api-routes-intake-screen/106-PATTERNS.md lines 354-457 — evidence, assign, ruling patterns
@.planning/phases/106-dispute-api-routes-intake-screen/106-RESEARCH.md §12 Access Control — BOARD/ADMIN only for assign and ruling
  </read_first>
  <behavior>
- Test 1: POST /api/disputes/[id]/evidence returns 401 without auth
- Test 2: POST /api/disputes/[id]/evidence returns 429 when rate limited (10/min)
- Test 3: POST /api/disputes/[id]/evidence uploads file via uploadImage, inserts DisputeEvidence, logs EVIDENCE_ADDED
- Test 4: POST /api/disputes/[id]/evidence returns 422 when no file provided
- Test 5: POST /api/disputes/[id]/assign returns 403 for RESIDENT role
- Test 6: POST /api/disputes/[id]/assign returns 422 for invalid moderatorId (not a UUID)
- Test 7: POST /api/disputes/[id]/assign sets assignedModeratorId and logs ASSIGNED event (BOARD role)
- Test 8: POST /api/disputes/[id]/ruling returns 403 for RESIDENT role
- Test 9: POST /api/disputes/[id]/ruling returns 409 when canTransition() returns false
- Test 10: POST /api/disputes/[id]/ruling sets ruling fields and logs RULING_ISSUED event (BOARD role)
  </behavior>
  <action>
Create three route handler files, all following the same auth/tenant guard pattern from Task 1.

### Evidence Upload Route — src/app/api/disputes/[id]/evidence/route.ts

Wrap with withErrorHandler. Set export const maxDuration = 8. Inline getSessionAndRole helper.

POST handler: await params.id, withTenant(), getSessionAndRole(). Apply rateLimitByUser(authData.userId, { windowMs: 60_000, maxRequests: 10 }). Verify dispute exists and user is party or moderator (same isParty/isModerator check as messages route). Use request.formData() to get file: `const formData = await request.formData(); const file = formData.get('file') as File | null`. If no file: return apiValidationError([{ message: 'File is required' }]). Call uploadImage(file, authData.userId) from @api/server. If uploadResult.error: return apiError('UPLOAD_FAILED', uploadResult.error, 400). Insert DisputeEvidence row: db.insert(disputeEvidences).values({ id: crypto.randomUUID(), tenantId, disputeId: id, uploadedBy: authData.userId, fileUrl: uploadResult.url, fileType: file.type, fileName: file.name, createdAt: now() }).returning(). Log EVIDENCE_ADDED event: db.insert(disputeEvents).values({ id: crypto.randomUUID(), tenantId, disputeId: id, actorId: authData.userId, eventType: 'EVIDENCE_ADDED', createdAt: now() }). Return apiCreated(evidence).

### Assign Moderator Route — src/app/api/disputes/[id]/assign/route.ts

Wrap with withErrorHandler. Set export const maxDuration = 8. Inline getSessionAndRole helper.

POST handler: await params.id, withTenant(), getSessionAndRole(). Guard: only BOARD/ADMIN — `if (authData.role !== 'BOARD' && !hasPermission(authData.role, 'admin')) return apiForbidden()`. Validate body with disputeAssignSchema.safeParse(). Extract moderatorId from body. Fetch dispute to verify exists and tenant-scoped. Wrap in db.transaction(): tx.update(disputeCases).set({ assignedModeratorId: moderatorId, updatedAt: now() }).where(and(eq(id), eq(tenantId))), then tx.insert(disputeEvents).values({ id: crypto.randomUUID(), tenantId, disputeId: id, actorId: authData.userId, eventType: 'ASSIGNED', metadata: { assignedModeratorId: moderatorId }, createdAt: now() }). Return apiSuccess({ success: true, assignedModeratorId: moderatorId }).

### Issue Ruling Route — src/app/api/disputes/[id]/ruling/route.ts

Wrap with withErrorHandler. Set export const maxDuration = 8. Inline getSessionAndRole helper.

POST handler: await params.id, withTenant(), getSessionAndRole(). Guard: only BOARD/ADMIN (same as assign). Validate body with disputeRulingSchema.safeParse(). Extract rulingDescription from body. Fetch dispute with where(and(eq(id), eq(tenantId))). Return apiNotFound if missing. Validate status transition: `if (!canTransition(dispute.status, 'FORMAL_RULING')) return apiConflict('Cannot issue ruling from current status')`. Wrap in db.transaction(): tx.update(disputeCases).set({ status: 'FORMAL_RULING', rulingDescription, rulingIssuedAt: now(), updatedAt: now() }).where(and(eq(id), eq(tenantId))), then tx.insert(disputeEvents).values({ id: crypto.randomUUID(), tenantId, disputeId: id, actorId: authData.userId, eventType: 'RULING_ISSUED', fromStatus: dispute.status, toStatus: 'FORMAL_RULING', note: rulingDescription, createdAt: now() }). Return apiSuccess({ success: true }).

Write all tests in src/app/api/disputes/**tests**/specialized-routes.test.ts (appending to Task 1's test file). Cover all 10 test behaviors above.
</action>
<verify>
<automated>npx vitest run src/app/api/disputes/**tests**/specialized-routes.test.ts</automated>
</verify>
<done>Evidence upload uses uploadImage(), inserts DisputeEvidence, logs event. Assign sets moderator (BOARD/ADMIN only). Ruling validates canTransition(), sets status to FORMAL_RULING, logs RULING_ISSUED. All 10 specialized route tests pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                            |
| -------------------------- | ---------------------------------------------------------------------- |
| Client → Mediation API     | isInternal flag enforced server-side — client cannot bypass role check |
| Client → Evidence API      | File validation server-side via uploadImage (MIME, size); rate limited |
| Client → Assign/Ruling API | BOARD/ADMIN role enforced server-side — RESIDENT/COMMITTEE blocked     |

## STRIDE Threat Register

| Threat ID | Category               | Component                                     | Disposition | Mitigation Plan                                                                                                                              |
| --------- | ---------------------- | --------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| T-106-09  | Information Disclosure | GET /api/disputes/[id]/messages               | mitigate    | Visibility filter: parties see `isInternal: false` only; moderators see all. Server-side role check on every request.                        |
| T-106-10  | Elevation of Privilege | POST /api/disputes/[id]/messages (isInternal) | mitigate    | `isInternal: true` rejected with 403 unless user has BOARD/ADMIN/COMMITTEE role. Enforced BEFORE DB insert.                                  |
| T-106-11  | Tampering              | POST /api/disputes/[id]/messages (content)    | mitigate    | `sanitizeHtml()` applied to all message content before DB insert. Prevents XSS in mediation thread rendering.                                |
| T-106-12  | Denial of Service      | POST /api/disputes/[id]/messages              | mitigate    | `rateLimitByUser` (30 req/min) prevents message flood. Rate limit checked before any DB work.                                                |
| T-106-13  | Tampering              | POST /api/disputes/[id]/evidence              | mitigate    | `uploadImage()` validates MIME type (image/jpeg, image/png, image/gif, image/webp) and 2MB size limit server-side. Client cannot bypass.     |
| T-106-14  | Denial of Service      | POST /api/disputes/[id]/evidence              | mitigate    | `rateLimitByUser` (10 req/min) prevents upload abuse.                                                                                        |
| T-106-15  | Elevation of Privilege | POST /api/disputes/[id]/assign                | mitigate    | Role guard: `authData.role !== 'BOARD' && !hasPermission(authData.role, 'admin')` returns 403. RESIDENT/COMMITTEE blocked.                   |
| T-106-16  | Elevation of Privilege | POST /api/disputes/[id]/ruling                | mitigate    | Dual guard: role check (BOARD/ADMIN only) + `canTransition()` status validation. Cannot issue ruling from invalid status (e.g., DRAFT).      |
| T-106-17  | Repudiation            | All three routes                              | mitigate    | Every action logs a `DisputeEvent` (EVIDENCE_ADDED, ASSIGNED, RULING_ISSUED) with actorId, timestamp, and metadata. Append-only audit trail. |
| T-106-SC  | Tampering              | npm/pnpm installs                             | mitigate    | No new packages installed. slopcheck: N/A.                                                                                                   |

</threat_model>

<verification>
Run specialized route tests:
```bash
npx vitest run src/app/api/disputes/__tests__/specialized-routes.test.ts
```
All 18 test cases (8 messages + 10 evidence/assign/ruling) must pass green.
</verification>

<success_criteria>

- [ ] `src/app/api/disputes/[id]/messages/route.ts` — GET with visibility filtering, POST with isInternal enforcement, sanitizeHtml, Supabase Realtime broadcast
- [ ] `src/app/api/disputes/[id]/evidence/route.ts` — POST with formData file extraction, uploadImage(), DisputeEvidence insert, rate limiting
- [ ] `src/app/api/disputes/[id]/assign/route.ts` — POST with BOARD/ADMIN guard, assignedModeratorId update, ASSIGNED event in transaction
- [ ] `src/app/api/disputes/[id]/ruling/route.ts` — POST with BOARD/ADMIN guard, canTransition() validation, ruling fields, RULING_ISSUED event in transaction
- [ ] All routes follow: withTenant() → getSessionAndRole() → assertModuleEnabled → business logic → apiSuccess/apiError
- [ ] All DB queries include eq(tenantId) and isNull(deletedAt)
- [ ] 18 Vitest tests pass
      </success_criteria>

<output>
Create `.planning/phases/106-dispute-api-routes-intake-screen/106-02-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

### Route Handlers

- `GET /api/disputes/[id]/messages` — list mediation messages with visibility filtering (exported handler)
- `POST /api/disputes/[id]/messages` — create mediation message with isInternal enforcement, sanitizeHtml, Supabase Realtime broadcast (exported handler)
- `POST /api/disputes/[id]/evidence` — upload evidence file via S3 uploadImage, inserts DisputeEvidence, logs EVIDENCE_ADDED (exported handler)
- `POST /api/disputes/[id]/assign` — assign moderator (BOARD/ADMIN only), sets assignedModeratorId, logs ASSIGNED (exported handler)
- `POST /api/disputes/[id]/ruling` — issue formal ruling (BOARD/ADMIN only), sets rulingDescription + rulingIssuedAt, transitions to FORMAL_RULING, logs RULING_ISSUED (exported handler)

### Files Created

- `src/app/api/disputes/[id]/messages/route.ts`
- `src/app/api/disputes/[id]/evidence/route.ts`
- `src/app/api/disputes/[id]/assign/route.ts`
- `src/app/api/disputes/[id]/ruling/route.ts`
- `src/app/api/disputes/__tests__/specialized-routes.test.ts` (shared with 106-01 tests)
