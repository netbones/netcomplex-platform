---
phase: 106-dispute-api-routes-intake-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/dispute/model/schemas.ts
  - src/entities/dispute/lib/pii-sanitizer.ts
  - src/entities/dispute/index.server.ts
  - src/app/api/disputes/route.ts
  - src/app/api/disputes/[id]/route.ts
  - src/app/api/disputes/[id]/submit/route.ts
  - src/app/api/disputes/__tests__/helpers.ts
  - src/app/api/disputes/__tests__/core-crud.test.ts
  - src/app/api/disputes/__tests__/submit-cooling-off.test.ts
autonomous: true
requirements:
  - DISPUTE-03
user_setup: []

must_haves:
  truths:
    - 'POST /api/disputes creates a DRAFT with a DSP-YYYY-NNNN reference number, coolingOffEndsAt set to now + 24h, and logs a CREATED DisputeEvent'
    - 'GET /api/disputes lists disputes scoped to the authenticated user (or all disputes for BOARD/ADMIN), filtered by tenant, excluding soft-deleted, with pagination'
    - 'GET /api/disputes/[id] returns a single dispute with access control (party or moderator only), returning 401 for unauthenticated and 404 for mismatched tenant'
    - 'PATCH /api/disputes/[id] updates dispute fields with canTransition() validation for status changes, logging STATUS_CHANGED events atomically'
    - 'POST /api/disputes/[id]/submit rejects with 423 and remaining seconds when coolingOffEndsAt is in the future, transitions DRAFT→SUBMITTED atomically'
    - 'PII sanitizer strips surnames, unit numbers, phones, emails, and vehicle plates from dispute descriptions'
  artifacts:
    - path: 'src/entities/dispute/model/schemas.ts'
      provides: 'Zod validation schemas for all dispute API requests'
      exports:
        [
          'disputeCreateSchema',
          'disputeUpdateSchema',
          'disputeSubmitSchema',
          'disputeMessageCreateSchema',
          'disputeEvidenceSchema',
          'disputeAssignSchema',
          'disputeRulingSchema',
          'intakeScreenRequestSchema',
          'intakeScreenOutputSchema',
        ]
    - path: 'src/entities/dispute/lib/pii-sanitizer.ts'
      provides: 'POPIA-compliant PII stripping before AI calls'
      exports: ['sanitizeDescriptionForAi']
    - path: 'src/app/api/disputes/route.ts'
      provides: 'GET list disputes, POST create DRAFT dispute'
      exports: ['GET', 'POST']
    - path: 'src/app/api/disputes/[id]/route.ts'
      provides: 'GET single dispute, PATCH update dispute'
      exports: ['GET', 'PATCH']
    - path: 'src/app/api/disputes/[id]/submit/route.ts'
      provides: 'POST submit dispute with cooling-off validation'
      exports: ['POST']
  key_links:
    - from: 'src/app/api/disputes/route.ts'
      to: 'src/entities/dispute/model/schemas.ts'
      via: 'import { disputeCreateSchema }'
      pattern: 'safeParse'
    - from: 'src/app/api/disputes/[id]/route.ts'
      to: 'src/entities/dispute/model/lifecycle.ts'
      via: 'import { canTransition }'
      pattern: "canTransition\\(.*status"
    - from: 'src/app/api/disputes/intake-screen/route.ts'
      to: 'src/entities/dispute/lib/pii-sanitizer.ts'
      via: 'import { sanitizeDescriptionForAi }'
      pattern: "sanitizeDescriptionForAi\\("
---

<objective>
Establish the foundation for all dispute API routes: create Zod request validation schemas, the PII sanitization utility, core CRUD routes (create DRAFT, list, get single, update), and the submit route with server-side cooling-off enforcement. Also scaffolds the test fixture infrastructure.

Purpose: All subsequent dispute routes depend on the same schemas, auth/tenant patterns, and test helpers created here.
Output: 6 new source files, 3 new test files, all 5 core CRUD+submit routes operational with tenant isolation, auth guards, and status lifecycle validation.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-dispute-api-routes-intake-screen/106-CONTEXT.md
@.planning/phases/106-dispute-api-routes-intake-screen/106-RESEARCH.md
@.planning/phases/106-dispute-api-routes-intake-screen/106-PATTERNS.md
@src/entities/dispute/model/types.ts
@src/entities/dispute/model/constants.ts
@src/entities/dispute/model/lifecycle.ts
@src/entities/dispute/api/reference.ts
@src/entities/dispute/index.server.ts
@src/app/api/maintenance/route.ts
@src/app/api/maintenance/[id]/route.ts
@src/shared/api/server/index.ts (for barrel exports: db, disputeCases, disputeEvents, apiSuccess, apiCreated, apiUnauthorized, apiForbidden, apiNotFound, apiError, apiValidationError, apiInternalError, apiConflict, withErrorHandler, auth, now, rateLimitByUser, getSessionAndRole, revalidateDashboard)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod Validation Schemas + PII Sanitizer</name>
  <files>src/entities/dispute/model/schemas.ts, src/entities/dispute/lib/pii-sanitizer.ts, src/entities/dispute/index.server.ts</files>
  <read_first>
src/entities/dispute/model/types.ts — all DisputeStatus, DisputeCategory, DisputeSeverity, DisputeRespondent types
src/entities/dispute/model/constants.ts — ALL_DISPUTE_STATUSES, ALL_DISPUTE_CATEGORIES const arrays for Zod enum validation
src/shared/lib/sanitize/server.ts — existing sanitizeHtml pattern
@.planning/phases/106-dispute-api-routes-intake-screen/106-PATTERNS.md lines 641-678 — pii-sanitizer pattern spec
  </read_first>
  <behavior>
- Test 1: disputeCreateSchema rejects missing title (returns ZodError with path: ['title'])
- Test 2: disputeCreateSchema accepts valid category value 'NOISE'
- Test 3: disputeCreateSchema rejects invalid category 'INVALID' (Zod enum error)
- Test 4: disputeUpdateSchema allows partial update (description only, title omitted)
- Test 5: disputeUpdateSchema rejects invalid status transition that canTransition() would block
- Test 6: sanitizeDescriptionForAi strips "Unit 42" → "[ADDRESS]"
- Test 7: sanitizeDescriptionForAi strips "Mr. van der Merwe" → "[NAME]"
- Test 8: sanitizeDescriptionForAi strips "083 123 4567" → "[PHONE]"
- Test 9: sanitizeDescriptionForAi strips "test@example.com" → "[EMAIL]"
- Test 10: intakeScreenRequestSchema rejects description shorter than 10 characters
  </behavior>
  <action>
Create src/entities/dispute/model/schemas.ts with Zod schemas per the Phase 105 entity types. Define these schemas as named exports:

1. disputeCreateSchema — validates POST /api/disputes body: category (z.enum(ALL_DISPUTE_CATEGORIES)), title (z.string().min(5).max(200)), description (z.string().min(10).max(5000)), respondentId (z.string().uuid().optional()), respondentType (z.enum(['RESIDENT','HOA','BOARD_MEMBER','TENANT_PROVIDER']).default('RESIDENT')), desiredOutcome (z.string().max(2000).optional()), severity (z.enum(['MINOR','MODERATE','SERIOUS','URGENT']).default('MODERATE')). Use .strip() to reject unknown fields.

2. disputeUpdateSchema — validates PATCH /api/disputes/[id] body: title (z.string().min(5).max(200).optional()), description (z.string().min(10).max(5000).optional()), category (z.enum(ALL_DISPUTE_CATEGORIES).optional()), desiredOutcome (z.string().max(2000).optional()), severity (z.enum(['MINOR','MODERATE','SERIOUS','URGENT']).optional()), status (z.enum(ALL_DISPUTE_STATUSES).optional()). Use .strip().

3. intakeScreenRequestSchema — validates POST /api/disputes/intake-screen body: description (z.string().min(10).max(5000)), disputeId (z.string().uuid().optional()). Use .strip().

4. intakeScreenOutputSchema — validates AI response shape: toneScore (z.number().int().min(0).max(10).default(0)), issueClarity (z.number().int().min(0).max(10).default(5)), likelyFrivolous (z.boolean().default(false)), suggestedCategory (z.string().default('OTHER')), deEscalationTip (z.string().nullable().default(null)). Export the inferred type IntakeScreenOutput.

5. disputeMessageCreateSchema — validates POST /api/disputes/[id]/messages body: content (z.string().min(1).max(5000)), isInternal (z.boolean().default(false)). Use .strip().

6. disputeEvidenceSchema — no body validation needed (formData), but export a const for max file size: MAX_EVIDENCE_FILE_SIZE = 2 _ 1024 _ 1024 (2MB), ALLOWED_EVIDENCE_TYPES = ['image/jpeg','image/png','image/gif','image/webp','application/pdf'].

7. disputeAssignSchema — validates POST /api/disputes/[id]/assign body: moderatorId (z.string().uuid()). Use .strip().

8. disputeRulingSchema — validates POST /api/disputes/[id]/ruling body: rulingDescription (z.string().min(10).max(5000)). Use .strip().

Create src/entities/dispute/lib/pii-sanitizer.ts with a single export function sanitizeDescriptionForAi(description: string): string. Apply four regex replacement passes: (1) unit/house numbers (Unit|House|Apartment|Suite|Apt|Flat|# followed by alphanum → '[ADDRESS]'), (2) prefixed names (Mr|Mrs|Ms|Miss|Dr|Prof optionally dot, space, capitalised word → '[NAME]'), (3) South African phone numbers (+27|0 followed by 9 digits → '[PHONE]'), (4) email addresses (standard email regex → '[EMAIL]').

Update src/entities/dispute/index.server.ts: add exports for schemas (from ./model/schemas) and pii-sanitizer (from ./lib/pii-sanitizer).

Write tests in src/entities/dispute/model/**tests**/schemas.test.ts covering all 8 schemas with valid and invalid inputs. Write tests in src/entities/dispute/lib/**tests**/pii-sanitizer.test.ts covering all 4 PII patterns.
</action>
<verify>
<automated>npx vitest run src/entities/dispute/model/**tests**/schemas.test.ts src/entities/dispute/lib/**tests**/pii-sanitizer.test.ts</automated>
</verify>
<done>All 8 Zod schemas pass validation tests (>10 test cases). PII sanitizer strips all 4 PII patterns. Both test files pass green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core CRUD Routes — POST, GET List, GET [id], PATCH [id]</name>
  <files>src/app/api/disputes/route.ts, src/app/api/disputes/[id]/route.ts, src/app/api/disputes/__tests__/helpers.ts, src/app/api/disputes/__tests__/core-crud.test.ts</files>
  <read_first>
src/app/api/maintenance/route.ts lines 1-177 — exact pattern to replicate for GET/POST flat routes
src/app/api/maintenance/[id]/route.ts lines 1-80 (GET with withErrorHandler), lines 175-388 (PATCH with status transitions)
src/entities/dispute/model/schemas.ts — Zod schemas created in Task 1
src/entities/dispute/model/lifecycle.ts — canTransition() for status validation
src/entities/dispute/api/reference.ts — generateDisputeReference() for reference numbers
src/shared/api/server/index.ts — barrel for apiSuccess/apiCreated/apiUnauthorized/etc., db, disputeCases, disputeEvents
@.planning/phases/106-dispute-api-routes-intake-screen/106-PATTERNS.md lines 29-209 — exact import and handler patterns
  </read_first>
  <behavior>
- Test 1: GET /api/disputes returns 401 when no auth session
- Test 2: GET /api/disputes returns empty array for user with no disputes
- Test 3: GET /api/disputes returns only user's own disputes for RESIDENT role
- Test 4: GET /api/disputes returns all disputes for ADMIN role
- Test 5: POST /api/disputes returns 422 for invalid body (missing title)
- Test 6: POST /api/disputes creates DRAFT with DSP-YYYY-NNNN reference and 24h coolingOffEndsAt
- Test 7: GET /api/disputes/[id] returns 401 without auth
- Test 8: GET /api/disputes/[id] returns 404 for mismatched tenant
- Test 9: GET /api/disputes/[id] returns 403 for non-party non-moderator
- Test 10: PATCH /api/disputes/[id] returns 409 for invalid status transition
- Test 11: PATCH /api/disputes/[id] updates title and logs STATUS_CHANGED event
  </behavior>
  <action>
Create the test fixture file src/app/api/disputes/__tests__/helpers.ts first. Follow the exact pattern from 106-PATTERNS.md lines 682-757. Include: createMockRequest(options), makeSelectChain(result), makeInsertChain(result), makeUpdateChain(result). Add dispute-specific fixtures: MOCK_DRAFT_DISPUTE (status 'DRAFT', complainantId 'user-resident', tenantId 'test-tenant-id'), MOCK_USERS with resident/board/admin/committee roles.

Then create src/app/api/disputes/route.ts — flat route (no withErrorHandler for flat routes — follow maintenance/route.ts pattern exactly). Define an inline getSessionAndRole() helper (copy the pattern from maintenance/route.ts lines 35-52). Set export const maxDuration = 8.

GET handler: extract searchParams (status, category, page, limit). Build Drizzle query with where(and(eq(tenantId), isNull(deletedAt), ...filters)). For RESIDENT: add eq(complainantId, userId) filter. For BOARD/ADMIN: add no complainant filter. Order by desc(createdAt), limit 20, offset from page param. Return apiSuccess(results).

POST handler: parse body with request.json(), validate with disputeCreateSchema.safeParse(). Compute coolingOffEndsAt = Date.now() + 24 \* 3600_000 (default 24h; document TODO for tenant-configurable hours). Insert into disputeCases with crypto.randomUUID(), generateDisputeReference(tenantId), all required fields. In same try block, insert DisputeEvent (eventType 'CREATED', fromStatus null, toStatus 'DRAFT'). Return apiCreated(dispute). Wrap in try/catch with apiLogger.error and apiInternalError.

Create src/app/api/disputes/[id]/route.ts — dynamic param route, WRAP with withErrorHandler (import from @api/server). Same inline getSessionAndRole helper. Set export const maxDuration = 8.

GET handler: await params.id, withTenant(), getSessionAndRole(). Fetch dispute with where(and(eq(id), eq(tenantId))). Return apiNotFound if missing. Check isParty (complainantId === userId || respondentId === userId) and isModerator (hasPermission(authData.role, 'admin') || role === 'BOARD' || role === 'COMMITTEE'). Return apiForbidden if neither. Return apiSuccess(dispute).

PATCH handler: same auth + tenant flow. Fetch existing dispute. Must be party or moderator. Validate body with disputeUpdateSchema.safeParse(). If body.status differs from existing.status: call canTransition(existing.status, body.status) from @entities/dispute; if false, return apiConflict(). Wrap update + event insert in db.transaction(): tx.update(disputeCases).set({...updates, updatedAt: now()}).where(and(eq(id), eq(tenantId))).returning(), then tx.insert(disputeEvents).values({eventType: 'STATUS_CHANGED', fromStatus: existing.status, toStatus: body.status, ...}). Return apiSuccess(updated).

Write test file src/app/api/disputes/**tests**/core-crud.test.ts with Vitest. Use vi.mock for @api/server and @entities/tenant/server (follow 106-PATTERNS.md lines 765-838). Cover all 11 test behaviors above.
</action>
<verify>
<automated>npx vitest run src/app/api/disputes/**tests**/core-crud.test.ts</automated>
</verify>
<done>All 4 route handlers operational. Tests pass: 401 for no auth, 422 for invalid input, 404 for mismatched tenant, 403 for no access, 409 for invalid transition. DRAFT created with reference number and cooling-off timer. Status change logged as DisputeEvent in transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Submit Route with Cooling-Off Enforcement</name>
  <files>src/app/api/disputes/[id]/submit/route.ts, src/app/api/disputes/__tests__/submit-cooling-off.test.ts</files>
  <read_first>
src/app/api/disputes/[id]/route.ts — PATCH handler pattern for status transitions (created in Task 2)
src/entities/dispute/model/lifecycle.ts — canTransition() validates DRAFT→SUBMITTED
@.planning/phases/106-dispute-api-routes-intake-screen/106-RESEARCH.md lines 443-482 — cooling-off validation spec
@.planning/phases/106-dispute-api-routes-intake-screen/106-PATTERNS.md lines 222-259 — submit route pattern
  </read_first>
  <behavior>
- Test 1: POST /api/disputes/[id]/submit returns 401 without auth
- Test 2: POST /api/disputes/[id]/submit returns 423 with remaining seconds when coolingOffEndsAt is in the future
- Test 3: POST /api/disputes/[id]/submit returns 409 when dispute is not DRAFT status
- Test 4: POST /api/disputes/[id]/submit returns 403 when user is not the complainant
- Test 5: POST /api/disputes/[id]/submit transitions DRAFT→SUBMITTED, sets submittedAt, logs SUBMITTED event
  </behavior>
  <action>
Create src/app/api/disputes/[id]/submit/route.ts — single POST handler wrapped with withErrorHandler. Inline getSessionAndRole helper. Set export const maxDuration = 8.

POST handler: await params.id, withTenant(), getSessionAndRole(). Fetch dispute with where(and(eq(id), eq(tenantId), isNull(deletedAt))). Return apiNotFound if missing.

Validate status is DRAFT: return apiConflict('Dispute is not in draft status') if dispute.status !== 'DRAFT'.

Validate complainant: return apiForbidden() if dispute.complainantId !== authData.userId.

Validate cooling-off: let now = new Date(); if dispute.coolingOffEndsAt exists AND coolingOffEndsAt > now, compute remainingMs = coolingOffEndsAt.getTime() - now.getTime(), remainingSeconds = Math.ceil(remainingMs / 1000). Return apiError('COOLING_OFF_ACTIVE', `Cooling-off period has not elapsed. ${remainingSeconds}s remaining.`, 423).

Transition atomically in db.transaction(): tx.update(disputeCases).set({status: 'SUBMITTED', submittedAt: now, updatedAt: now}).where(and(eq(id), eq(tenantId))), then tx.insert(disputeEvents).values({id: crypto.randomUUID(), tenantId, disputeId: id, actorId: userId, eventType: 'SUBMITTED', fromStatus: 'DRAFT', toStatus: 'SUBMITTED', createdAt: now}).

Return apiSuccess({ message: 'Dispute submitted successfully', submittedAt: now.toISOString() }).

Write test file src/app/api/disputes/**tests**/submit-cooling-off.test.ts. Mock scenarios: (a) DRAFT with coolingOffEndsAt in the future → 423, (b) DRAFT with coolingOffEndsAt in the past → 200, (c) non-DRAFT status → 409, (d) wrong complainant → 403, (e) unauthenticated → 401.
</action>
<verify>
<automated>npx vitest run src/app/api/disputes/**tests**/submit-cooling-off.test.ts</automated>
</verify>
<done>Submit route enforces server-side cooling-off with 423. DRAFT→SUBMITTED transition is atomic (update + event in transaction). All 5 test scenarios pass.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                   | Description                                                                                      |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| Client → API routes                                        | Untrusted HTTP requests cross here — all auth, validation, tenant isolation enforced server-side |
| API routes → Database                                      | Drizzle parameterized queries prevent SQL injection; tenantId filter on every query              |
| API routes → AI Provider (intake screen, Task 3 in 106-03) | Sanitized description only; PII stripped before leaving server                                   |

## STRIDE Threat Register

| Threat ID | Category               | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                         |
| --------- | ---------------------- | ----------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-106-01  | Spoofing               | All dispute routes                        | mitigate    | `getSessionAndRole()` on every route returns 401 for unauthenticated. Better Auth session validated via `auth.api.getSession()`.                                                        |
| T-106-02  | Tampering              | POST /api/disputes/[id]/submit            | mitigate    | Cooling-off timestamp server-side only — `coolingOffEndsAt` set at creation, validated at submit. Client cannot influence. 423 returned with remaining seconds.                         |
| T-106-03  | Information Disclosure | GET /api/disputes, GET /api/disputes/[id] | mitigate    | `eq(disputeCases.tenantId, tenantId)` in every query. For RESIDENT: filter `complainantId === userId`. Non-party non-moderator returns 403. 404 masks existence for mismatched tenants. |
| T-106-04  | Elevation of Privilege | PATCH /api/disputes/[id] (status change)  | mitigate    | `canTransition()` validates from-status before allowing status change. Non-party non-moderator blocked by 403. Party users cannot transition beyond DRAFT→SUBMITTED/WITHDRAWN.          |
| T-106-05  | Tampering              | POST /api/disputes (creation)             | mitigate    | Zod `disputeCreateSchema` validates all fields. `withTenant()` derives tenantId from request context — client cannot forge. Reference number auto-generated server-side.                |
| T-106-06  | Repudiation            | All status-changing routes                | mitigate    | Every status change logged as `DisputeEvent` in the same `db.transaction()`. Append-only — events never updated or deleted. Creates auditable timeline for CSOS compliance.             |
| T-106-07  | Information Disclosure | PATCH /api/disputes/[id]                  | mitigate    | Zod `.strip()` rejects unknown fields. No field injection possible. Update set is whitelisted (only fields present in schema).                                                          |
| T-106-08  | Denial of Service      | POST /api/disputes                        | accept      | No rate limit on dispute creation (low expected volume, 5-20/month per community). Accept risk — monitor and add if abused.                                                             |
| T-106-SC  | Tampering              | npm/pnpm installs                         | mitigate    | No new packages installed this plan. All dependencies already in package.json and verified. slopcheck: N/A.                                                                             |

</threat_model>

<verification>
Run the full dispute test suite after Task 3:
```bash
npx vitest run src/entities/dispute/ src/app/api/disputes/__tests__/
```
All 20+ tests must pass green. No type errors: `npx tsc --noEmit --pretty src/app/api/disputes/ 2>&1 | head -30`
</verification>

<success_criteria>

- [ ] `src/entities/dispute/model/schemas.ts` exists with 8 named Zod schema exports
- [ ] `src/entities/dispute/lib/pii-sanitizer.ts` exists with `sanitizeDescriptionForAi` export
- [ ] `src/app/api/disputes/route.ts` handles GET list and POST create with tenant isolation
- [ ] `src/app/api/disputes/[id]/route.ts` handles GET single and PATCH update with access control
- [ ] `src/app/api/disputes/[id]/submit/route.ts` enforces server-side cooling-off with 423
- [ ] All routes follow pattern: withTenant() → getSessionAndRole() → assertModuleEnabled → business logic → apiSuccess/apiError
- [ ] All Drizzle queries include eq(table.tenantId, tenantId) and isNull(deletedAt)
- [ ] Status changes wrapped in db.transaction() with DisputeEvent insert
- [ ] 20+ Vitest tests pass across 3 test files
      </success_criteria>

<output>
Create `.planning/phases/106-dispute-api-routes-intake-screen/106-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

### Functions

- `sanitizeDescriptionForAi(description: string): string` — strips PII from dispute descriptions before AI calls

### Types / Interfaces

- `IntakeScreenOutput` — inferred type from `intakeScreenOutputSchema`

### Constants / Variables

- `disputeCreateSchema` — ZodObject for POST /api/disputes
- `disputeUpdateSchema` — ZodObject for PATCH /api/disputes/[id]
- `intakeScreenRequestSchema` — ZodObject for POST /api/disputes/intake-screen
- `intakeScreenOutputSchema` — ZodObject for AI response validation
- `disputeMessageCreateSchema` — ZodObject for POST /api/disputes/[id]/messages
- `disputeEvidenceSchema` — constants: MAX_EVIDENCE_FILE_SIZE, ALLOWED_EVIDENCE_TYPES
- `disputeAssignSchema` — ZodObject for POST /api/disputes/[id]/assign
- `disputeRulingSchema` — ZodObject for POST /api/disputes/[id]/ruling

### Route Handlers

- `GET /api/disputes` — list disputes (exported handler)
- `POST /api/disputes` — create DRAFT dispute (exported handler)
- `GET /api/disputes/[id]` — get single dispute (exported handler)
- `PATCH /api/disputes/[id]` — update dispute fields (exported handler)
- `POST /api/disputes/[id]/submit` — submit dispute with cooling-off (exported handler)

### Files Created

- `src/entities/dispute/model/schemas.ts`
- `src/entities/dispute/lib/pii-sanitizer.ts`
- `src/app/api/disputes/route.ts`
- `src/app/api/disputes/[id]/route.ts`
- `src/app/api/disputes/[id]/submit/route.ts`
- `src/app/api/disputes/__tests__/helpers.ts`
- `src/app/api/disputes/__tests__/core-crud.test.ts`
- `src/app/api/disputes/__tests__/submit-cooling-off.test.ts`
- `src/entities/dispute/model/__tests__/schemas.test.ts`
- `src/entities/dispute/lib/__tests__/pii-sanitizer.test.ts`

### Files Modified

- `src/entities/dispute/index.server.ts` — added schema and pii-sanitizer exports
