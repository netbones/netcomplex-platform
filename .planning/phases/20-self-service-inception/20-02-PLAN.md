---
phase: 20-self-service-inception
plan: 02
type: execute
wave: 2
depends_on: ['20-01']
files_modified:
  - src/app/(platform)/onboarding/[tenantId]/page.tsx
  - src/app/(platform)/onboarding/layout.tsx
  - src/features/onboarding/model/useOnboarding.ts
  - src/features/onboarding/ui/OnboardingWizard.tsx
  - src/features/onboarding/ui/OnboardingStep.tsx
  - src/features/onboarding/ui/steps/BrandingStep.tsx
  - src/features/onboarding/ui/steps/ModulesStep.tsx
  - src/features/onboarding/ui/steps/PagesStep.tsx
  - src/features/onboarding/ui/steps/InviteStep.tsx
  - src/features/onboarding/ui/steps/LaunchStep.tsx
  - src/app/api/platform/onboarding/route.ts
autonomous: true
requirements: [INCEPT-03, INCEPT-04]

must_haves:
  truths:
    - 'User can complete 5-step onboarding wizard after signup'
    - 'Branding step saves logo, colors, font to tenant settings'
    - 'Modules step shows STANDARD tier modules with toggle switches'
    - 'Pages step shows page visibility toggles with sensible defaults'
    - 'Invite step accepts email addresses and sends invitations'
    - 'Launch step confirms site is live with links to public URL and admin panel'
  artifacts:
    - path: 'src/app/(platform)/onboarding/[tenantId]/page.tsx'
      provides: 'Onboarding wizard page with 5 steps'
    - path: 'src/features/onboarding/model/useOnboarding.ts'
      provides: 'Onboarding state management and step navigation'
    - path: 'src/features/onboarding/ui/steps/BrandingStep.tsx'
      provides: 'Logo upload, color picker, font selector'
    - path: 'src/features/onboarding/ui/steps/ModulesStep.tsx'
      provides: 'Module toggles for STANDARD tier'
    - path: 'src/features/onboarding/ui/steps/PagesStep.tsx'
      provides: 'Page visibility toggles'
    - path: 'src/features/onboarding/ui/steps/InviteStep.tsx'
      provides: 'Email input for co-admin invites'
    - path: 'src/features/onboarding/ui/steps/LaunchStep.tsx'
      provides: 'Launch confirmation with links'
    - path: 'src/app/api/platform/onboarding/route.ts'
      provides: 'API for saving onboarding progress'
  key_links:
    - from: 'src/features/onboarding/model/useOnboarding.ts'
      to: 'src/app/api/platform/onboarding/route.ts'
      via: 'fetch POST to save step data'
      pattern: 'fetch.*api/platform/onboarding'
    - from: 'src/app/(platform)/onboarding/[tenantId]/page.tsx'
      to: 'src/features/onboarding/ui/OnboardingWizard.tsx'
      via: 'import and render wizard'
      pattern: 'import.*OnboardingWizard'
---

<objective>
Build the 5-step onboarding wizard at /platform/onboarding/[tenantId] — branding, modules, pages, invites, launch.

Purpose: After self-service signup, new tenant admins need guided setup to configure their community site. The wizard reuses existing components (BrandingForm, PageSettingsWidget logic) where possible and provides sensible defaults. Without this, users are dropped into the raw admin panel with no guidance.

Output: Complete onboarding wizard with 5 steps, API for saving progress, and redirect to admin panel on completion.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DISCUSSION.md — Section 5, Journey 1 (Onboarding Wizard)
@src/app/(platform)/admin/platform/[id]/edit/page.tsx — existing branding UI to reference
@src/widgets/admin/ui/PageSettingsWidget.tsx — page visibility toggles to reuse logic
@src/entities/tenant/api/flags/platform-flags.ts — page flags system
@src/entities/tenant/api/features/registry.ts — module/tier configuration
@src/app/api/admin/settings/page-flags/route.ts — existing page flags API
@src/features/auth/ui/ — existing signup UI components for style reference
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create onboarding wizard shell and API route</name>
  <files>src/app/(platform)/onboarding/[tenantId]/page.tsx, src/app/(platform)/onboarding/layout.tsx, src/features/onboarding/model/useOnboarding.ts, src/features/onboarding/ui/OnboardingWizard.tsx, src/features/onboarding/ui/OnboardingStep.tsx, src/app/api/platform/onboarding/route.ts</files>
  <action>
    Create the onboarding wizard infrastructure:

    1. Create src/app/(platform)/onboarding/layout.tsx:
       - Simple layout with PageLayout background="fieldstone" and PlatformFooter
       - No auth guard needed — user is authenticated from signup

    2. Create src/app/(platform)/onboarding/[tenantId]/page.tsx:
       - Extract tenantId from params
       - Verify user owns this tenant (session.user.id === tenant.ownerId)
       - Render OnboardingWizard component with tenantId prop

    3. Create src/features/onboarding/model/useOnboarding.ts:
       - State: currentStep (1-5), loading, error, formData
       - Functions: handleNext, handleBack, handleSkip, saveStep, complete
       - saveStep: POST to /api/platform/onboarding with { tenantId, step, data }
       - complete: marks onboarding as complete, redirects to /admin

    4. Create src/features/onboarding/ui/OnboardingWizard.tsx:
       - Progress bar showing 5 steps
       - Renders current step component based on step state
       - Navigation buttons (Back, Skip, Next) — Skip available on steps 2, 4
       - Step labels: "Branding", "Modules", "Pages", "Invite Team", "Launch"

    5. Create src/features/onboarding/ui/OnboardingStep.tsx:
       - Reusable step wrapper with title, description, children
       - Consistent styling matching signup form

    6. Create src/app/api/platform/onboarding/route.ts:
       - POST handler: saves onboarding step data to tenant settings
       - Validates tenantId matches authenticated user's tenant
       - Returns 200 on success

    Keep this plan focused on infrastructure — step components come in Task 2.

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify all files created, wizard renders with 5 steps, API route accepts POST with tenantId+step+data</manual>
  </verify>
  <done>Onboarding wizard shell exists with 5-step navigation, API route saves progress, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Build all 5 onboarding step components</name>
  <files>src/features/onboarding/ui/steps/BrandingStep.tsx, src/features/onboarding/ui/steps/ModulesStep.tsx, src/features/onboarding/ui/steps/PagesStep.tsx, src/features/onboarding/ui/steps/InviteStep.tsx, src/features/onboarding/ui/steps/LaunchStep.tsx</files>
  <action>
    Create all 5 step components:

    **Step 1: BrandingStep.tsx**
    - Logo upload input (accept image/png, image/jpeg, max 2MB)
    - Primary color picker (default: #4F46E5)
    - Accent color picker (optional)
    - Font selector dropdown (default: system fonts)
    - Reuse BrandingForm patterns from existing admin UI
    - Save to: PATCH /api/admin/platform/tenants/[tenantId]

    **Step 2: ModulesStep.tsx**
    - Fetch available modules from /api/platform/modules
    - Show modules available at STANDARD tier
    - Toggle switches for each module (all enabled by default)
    - Upsell callout for PREMIUM features (bookings, premium seats)
    - Save to: POST /api/platform/onboarding with step=2

    **Step 3: PagesStep.tsx**
    - Show page toggles: News (ON), Directory (ON), Events (ON), Campaign (ON), Chat (OFF), Conservation (default)
    - Reuse PageSettingsWidget toggle pattern
    - Sensible defaults for new communities
    - Save to: POST /api/admin/settings/page-flags (existing API)

    **Step 4: InviteStep.tsx**
    - Email input with "Add" button for multiple invites
    - List of pending invitees with remove button
    - Role selector per invite (ADMIN, MANAGER, BOARD)
    - "Skip for now" button
    - Save to: POST /api/invitations (existing invite API)

    **Step 5: LaunchStep.tsx**
    - "Your community site is live!" heading
    - Link to public URL: /{slug}
    - Link to admin panel: /admin
    - "Complete Setup" button → redirects to /admin
    - Option: "Go back to make changes"

    Each step should:
    - Call saveStep() from useOnboarding on "Next"
    - Show loading state during save
    - Show error state if save fails
    - Be skippable where noted (steps 2, 4)

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify all 5 step components exist, each has save functionality, LaunchStep redirects to /admin</manual>
  </verify>
  <done>All 5 step components built with save functionality, LaunchStep redirects to /admin on completion</done>
</task>

</tasks>

<verification>
- Onboarding wizard accessible at /platform/onboarding/[tenantId]
- All 5 steps render with correct content and save functionality
- Branding step saves to tenant record
- Modules step saves module enablement
- Pages step saves page flags via existing API
- Invite step creates invitations via existing API
- Launch step redirects to /admin
- TypeScript compiles without errors
</verification>

<success_criteria>

- User can navigate through all 5 steps after signup
- Each step saves its data to the appropriate API
- Skip buttons work on steps 2 and 4
- Launch step confirms completion and redirects to admin panel
- Progress is preserved if user navigates away and returns
  </success_criteria>

<output>
After completion, create `.planning/phases/20-self-service-inception/20-02-SUMMARY.md`
</output>
