---
phase: 20-self-service-inception
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/platform/tenants/route.ts
  - src/shared/api/auth.ts
  - src/features/auth/model/useSignupForm.ts
autonomous: true
requirements: [INCEPT-01, INCEPT-02]

must_haves:
  truths:
    - "POST /api/platform/tenants creates tenant with ownerId set to the new admin user's id"
    - 'New admin user has password set via Better Auth (not raw DB insert)'
    - 'After successful signup, user is redirected to /platform/onboarding/[tenantId]'
    - 'Tenant is created with tier=STANDARD, role=ADMIN for founding user'
  artifacts:
    - path: 'src/app/api/platform/tenants/route.ts'
      provides: 'Atomic tenant + user creation with ownerId and Better Auth password'
      exports: ['POST']
    - path: 'src/features/auth/model/useSignupForm.ts'
      provides: 'Signup form redirecting to onboarding wizard'
      contains: 'onboarding'
  key_links:
    - from: 'src/app/api/platform/tenants/route.ts'
      to: 'src/shared/api/auth.ts'
      via: 'Better Auth createUser or signup call'
      pattern: 'auth.*createUser|auth.*signup'
    - from: 'src/features/auth/model/useSignupForm.ts'
      to: 'src/app/api/platform/tenants/route.ts'
      via: 'fetch POST /api/platform/tenants'
      pattern: 'fetch.*api/platform/tenants'
---

<objective>
Fix the self-service signup API to properly create tenants with ownerId, set passwords via Better Auth, and redirect to the onboarding wizard.

Purpose: The current signup API creates a tenant and user record but doesn't set the password (user can't log in), doesn't set ownerId on the Tenant (added in Phase 19), and redirects to a tenant sign-in page instead of the onboarding wizard. This plan fixes all three issues to complete the self-service signup flow.

Output: Working signup API that creates tenant with ownerId, sets password via Better Auth, and redirects to onboarding.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DISCUSSION.md — Sections 1 (self-service signup), 5 (Journey 0)
@.planning/phases/19-schema-corrections/19-01-SUMMARY.md — ownerId and isPlatformAdmin fields added
@src/app/api/platform/tenants/route.ts — current signup API (creates tenant + user, no password)
@src/features/auth/model/useSignupForm.ts — signup form hook (redirects to sign-in, not onboarding)
@src/shared/api/auth.ts — Better Auth configuration
@src/shared/api/schemas.ts — signupSchema validation
@prisma/schema.prisma — Tenant model with ownerId, user model with isPlatformAdmin
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix signup API to set ownerId and use Better Auth for password</name>
  <files>src/app/api/platform/tenants/route.ts</files>
  <action>
    Edit src/app/api/platform/tenants/route.ts to fix the signup flow:

    1. Keep the existing validation (slug uniqueness, email uniqueness, tier check)

    2. When creating the tenant, add ownerId:
       - The current code creates the tenant first, then the user
       - Change to: create tenant with ownerId=null first, then create user, then update tenant with ownerId=user.id
       - OR: use a transaction that creates user first, then tenant with ownerId=user.id
       - Use a database transaction (db.transaction()) to ensure atomicity

    3. Replace the raw user insert with Better Auth's createUser API:
       - Import auth from '@api/auth'
       - Use auth.api.createUser() or the Better Auth equivalent to create the user with password
       - This ensures the password is properly hashed via Better Auth's hashing mechanism
       - Set role='ADMIN', tenantId=tenant.id, isPlatformAdmin=false
       - The Better Auth createUser API handles password hashing, email verification token, etc.

    4. After successful creation, return the tenantId in the response:
       - Add tenantId to the JSON response so the frontend can redirect to onboarding
       - Response should include: { tenantId, tenant: {...}, user: {...} }

    The key changes:
    - Transaction wrapping for atomicity
    - ownerId set on Tenant after user creation
    - Better Auth used for password hashing instead of raw DB insert
    - tenantId included in response

    Do NOT change the validation logic or error handling — those are correct.

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify route.ts uses db.transaction(), calls Better Auth for user creation, sets ownerId on Tenant, includes tenantId in response</manual>
  </verify>
  <done>Signup API creates tenant with ownerId, uses Better Auth for password, returns tenantId in response, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Update signup form to redirect to onboarding wizard</name>
  <files>src/features/auth/model/useSignupForm.ts</files>
  <action>
    Edit src/features/auth/model/useSignupForm.ts:

    1. In the handleSubmit function, after the successful API response:
       - Extract tenantId from the response: `const { tenantId } = await res.json();`
       - Redirect to onboarding wizard: `router.push(\`/platform/onboarding/${tenantId}\`);`
       - Remove the current redirect to `https://${subdomain}.netbones.co.za/sign-in`

    2. The rest of the form logic (validation, step navigation, error handling) should remain unchanged.

    The change is minimal — just the redirect URL after successful signup.

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify useSignupForm.ts redirects to /platform/onboarding/${tenantId} after successful signup</manual>
  </verify>
  <done>Signup form redirects to onboarding wizard with tenantId after successful creation</done>
</task>

</tasks>

<verification>
- POST /api/platform/tenants creates tenant with ownerId set to admin user's id
- Password is hashed via Better Auth (not raw DB insert)
- Response includes tenantId
- Signup form redirects to /platform/onboarding/[tenantId]
- TypeScript compiles without errors
- Atomic transaction ensures tenant + user created together or not at all
</verification>

<success_criteria>

- Signup API returns tenantId in response
- Tenant record has ownerId pointing to the founding admin user
- User password is properly hashed via Better Auth
- Signup form redirects to onboarding wizard, not sign-in page
- Transaction ensures atomicity — no orphaned tenants or users
  </success_criteria>

<output>
After completion, create `.planning/phases/20-self-service-inception/20-01-SUMMARY.md`
</output>
