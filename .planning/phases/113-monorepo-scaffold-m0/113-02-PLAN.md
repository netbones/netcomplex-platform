---
phase: 113-monorepo-scaffold-m0
plan: 02
type: execute
wave: 2
depends_on: ['113-01']
files_modified:
  - packages/shared/package.json
  - packages/shared/src/index.ts
  - packages/db/package.json
  - packages/db/src/index.ts
  - packages/auth/package.json
  - packages/auth/src/index.ts
  - packages/api/package.json
  - packages/api/src/index.ts
  - packages/ui/package.json
  - packages/ui/src/index.ts
  - packages/server/package.json
  - packages/server/src/index.ts
  - packages/analytics/package.json
  - packages/analytics/src/index.ts
  - apps/web/package.json
  - apps/web/src/index.ts
  - apps/expo/package.json
  - apps/expo/src/index.ts
  - .gitignore
  - .prettierignore
  - tsconfig.json
  - eslint.config.mjs
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - '7 packages/* stub package.json files exist with @soralia/* names'
    - '2 apps/* stub package.json files exist with @soralia/* names'
    - 'All 9 stubs have src/index.ts entry point files'
    - 'pnpm ls -r lists all 10 workspace packages (9 stubs + root)'
    - '.gitignore excludes .turbo/ cache directories'
    - '.prettierignore excludes packages/, apps/, tooling/ from root formatting'
    - 'tsconfig.json excludes packages/, apps/, tooling/ from typecheck'
    - 'eslint.config.mjs ignores packages/**, apps/**, tooling/** from linting'
    - 'pnpm build passes (web app unaffected)'
    - 'pnpm lint passes (no new warnings)'
    - 'pnpm typecheck passes (no new errors)'
  artifacts:
    - path: 'packages/*/package.json'
      provides: '7 shared package stubs'
      min_count: 7
    - path: 'apps/*/package.json'
      provides: '2 app package stubs'
      min_count: 2
    - path: '.gitignore'
      provides: 'Monorepo-aware ignore patterns'
      contains: '**/.turbo/'
    - path: 'tsconfig.json'
      provides: 'Workspace directory exclusion'
      exclude: ['packages', 'apps', 'tooling']
    - path: 'eslint.config.mjs'
      provides: 'Workspace directory ignores'
      contains: 'packages/**'
  key_links:
    - from: 'pnpm-workspace.yaml'
      to: 'packages/*/package.json'
      via: 'workspace glob resolution'
    - from: 'pnpm-workspace.yaml'
      to: 'apps/*/package.json'
      via: 'workspace glob resolution'
    - from: 'eslint.config.mjs'
      to: 'packages/**'
      via: 'ignores pattern'
    - from: 'tsconfig.json'
      to: 'packages, apps, tooling'
      via: 'exclude array'
---

<objective>
Create all 9 stub workspace packages and update root config files for monorepo awareness. Then run full verification to confirm everything works.

Purpose: Populate the workspace with stubs that declare package names and entry points, and prevent existing tools (tsc, ESLint, Prettier) from scanning workspace directories.
Output: 18 new files across packages/ and apps/, 4 modified config files.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/113-monorepo-scaffold-m0/113-RESEARCH.md
@docs/MOBILE_MONOREPO.md

# Configs being modified

@.gitignore
@.prettierignore
@tsconfig.json
@eslint.config.mjs

# Root package for dependency reference

@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create 9 stub workspace packages (7 packages/* + 2 apps/*)</name>
  <files>
    packages/shared/package.json,
    packages/shared/src/index.ts,
    packages/db/package.json,
    packages/db/src/index.ts,
    packages/auth/package.json,
    packages/auth/src/index.ts,
    packages/api/package.json,
    packages/api/src/index.ts,
    packages/ui/package.json,
    packages/ui/src/index.ts,
    packages/server/package.json,
    packages/server/src/index.ts,
    packages/analytics/package.json,
    packages/analytics/src/index.ts,
    apps/web/package.json,
    apps/web/src/index.ts,
    apps/expo/package.json,
    apps/expo/src/index.ts
  </files>

<read_first>
@package.json — root package.json to confirm packageManager and existing patterns
@docs/MOBILE_MONOREPO.md — §Target Architecture for the full package tree and §Package Dependency Graph for package names and dependency direction
@.planning/phases/113-monorepo-scaffold-m0/113-RESEARCH.md — §Stub Package Package.json Pattern for the exact template
</read_first>

  <action>
    Create the following directories (use `mkdir -p` for each):
    - packages/shared/src/
    - packages/db/src/
    - packages/auth/src/
    - packages/api/src/
    - packages/ui/src/
    - packages/server/src/
    - packages/analytics/src/
    - apps/web/src/
    - apps/expo/src/

    Create a stub `package.json` and `src/index.ts` for each, following this exact pattern:

    **package.json template:**
    ```json
    {
      "name": "@soralia/{name}",
      "version": "0.0.0",
      "private": true,
      "main": "./src/index.ts",
      "types": "./src/index.ts",
      "scripts": {
        "typecheck": "tsc --noEmit"
      }
    }
    ```

    **src/index.ts template:**
    ```typescript
    // @soralia/{name}
    export {};
    ```

    The `{name}` values for each package are:

    | Directory | @soralia/{name} | Notes |
    |-----------|----------------|-------|
    | packages/shared | @soralia/shared | Pure types/utils/schemas — no deps |
    | packages/db | @soralia/db | Drizzle schema definitions — depends on shared in M1 |
    | packages/auth | @soralia/auth | Better Auth config — depends on db/shared in M1 |
    | packages/api | @soralia/api | tRPC router definitions — depends on auth/db/shared in M1 |
    | packages/ui | @soralia/ui | Component library — depends on shared in M1 |
    | packages/server | @soralia/server | Server-only (db client/email) — depends on db/shared in M1 |
    | packages/analytics | @soralia/analytics | Analytics abstraction — depends on shared in M1 |
    | apps/web | @soralia/web | Web app stub — declares dependency; real code at root until M2 |
    | apps/expo | @soralia/expo | Expo app stub — scaffolded in M3 |

    IMPORTANT: Do NOT add `dependencies` or `devDependencies` fields to stub package.json files. These will be populated in M1 when code is moved. Stubs are declarations only — they register the workspace package name and entry point.

    IMPORTANT: Do NOT create package.json files for the tooling/. TypeScript, ESLint, and Prettier presets are consumed via relative filesystem path in M1+ and do not need to be workspace packages.

  </action>

<acceptance_criteria> - All 9 directories exist: `ls -d packages/shared packages/db packages/auth packages/api packages/ui packages/server packages/analytics apps/web apps/expo` - All 9 package.json files exist: `ls packages/*/package.json apps/*/package.json` - All 9 src/index.ts files exist: `ls packages/*/src/index.ts apps/*/src/index.ts` - Each package.json has the correct name: `grep -q '@soralia/' packages/shared/package.json` - `pnpm ls -r --depth -1` lists all 9 workspace packages plus the root (10 total)
</acceptance_criteria>

  <verification>
    <automated>
      count=$(pnpm ls -r --depth -1 2>/dev/null | grep -c '@soralia') && [ "$count" -eq 9 ] && echo "OK: 9 workspace packages found" || echo "FAIL: expected 9, found $count"
    </automated>
  </verification>

  <done>
    9 stub workspace packages created. `pnpm ls -r --depth -1` shows @soralia/{shared,db,auth,api,ui,server,analytics,web,expo}. Each has package.json with name, version, private, main, types, and typecheck script.
  </done>

<effort>M</effort>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update root config files for monorepo awareness</name>
  <files>
    .gitignore,
    .prettierignore,
    tsconfig.json,
    eslint.config.mjs
  </files>

<read_first>
@.gitignore — current gitignore patterns (need recursive turbo cache entry)
@.prettierignore — current prettier ignore entries (need workspace directories)
@tsconfig.json — current tsconfig with `include: ["**/*.ts", "**/*.tsx"]` (needs workspace exclude)
@eslint.config.mjs — current eslint config with `ignores` array (needs workspace ignores)
</read_first>

  <action>
    Apply the following edits to each config file. Make surgical edits — do not reformat unrelated lines.

    ---
    **`.gitignore`** — Add after the existing `node_modules/` line:
    ```
    # Turborepo
    .turbo/
    **/.turbo/
    ```
    Insert after the `node_modules/` block. This ensures .turbo cache directories at any level are ignored.

    ---
    **`.prettierignore`** — Add to the existing file (preserve existing content):
    ```
    # Workspace packages — each package formats independently
    packages/
    apps/
    tooling/
    ```

    ---
    **`tsconfig.json`** — Add `"packages"`, `"apps"`, `"tooling"` to the `exclude` array:
    Current: `"exclude": ["node_modules", "src/**/*.html", "migrate-*.ts", "docs", "scripts"]`
    New:    `"exclude": ["node_modules", "src/**/*.html", "migrate-*.ts", "docs", "scripts", "packages", "apps", "tooling"]`
    This prevents `tsc --noEmit` from typechecking workspace stubs (which have no tsconfig at this stage).

    ---
    **`eslint.config.mjs`** — Add to the `ignores` array in the first config object:
    Current: `ignores: ['node_modules/**', 'dist/**', 'build/**', '.next/**']`
    New:    `ignores: ['node_modules/**', 'dist/**', 'build/**', '.next/**', 'packages/**', 'apps/**', 'tooling/**']`
    This prevents ESLint from linting stub workspace files.

  </action>

<acceptance_criteria> - `.gitignore` contains `**/.turbo/`: `grep -q '\.turbo/' .gitignore` - `.prettierignore` contains `packages/`: `grep -q 'packages/' .prettierignore` - `.prettierignore` contains `apps/`: `grep -q 'apps/' .prettierignore` - `.prettierignore` contains `tooling/`: `grep -q 'tooling/' .prettierignore` - `tsconfig.json` has `"packages"` in exclude array: `grep -q '"packages"' tsconfig.json` - `tsconfig.json` has `"apps"` in exclude array: `grep -q '"apps"' tsconfig.json` - `eslint.config.mjs` has `'packages/**'` in ignores: `grep -q "packages" eslint.config.mjs`
</acceptance_criteria>

  <verification>
    <automated>
      grep -q '\.turbo/' .gitignore && grep -q 'packages/' .prettierignore && grep -q '"packages"' tsconfig.json && grep -q "packages" eslint.config.mjs && echo "Configs OK"
    </automated>
  </verification>

  <done>
    All 4 config files updated: .gitignore excludes .turbo/, .prettierignore excludes workspace dirs, tsconfig.json excludes workspace dirs from typecheck, eslint.config.mjs ignores workspace dirs from linting.
  </done>

<effort>XS</effort>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full verification — pnpm install, build, lint, typecheck</name>
  <files>
    (no file changes — verification pass)
  </files>

<read_first>
@.planning/phases/113-monorepo-scaffold-m0/113-RESEARCH.md — §Validation Architecture for the verification matrix
</read_first>

  <action>
    Run the full verification suite to confirm the monorepo skeleton is functional and the existing web app is unaffected:

    ```bash
    # 1. Workspace resolution
    pnpm install
    echo "=== Workspace packages ==="
    pnpm ls -r --depth -1 2>/dev/null

    # 2. Build (web app at root)
    pnpm build

    # 3. Lint (no new warnings from workspace stubs)
    pnpm lint 2>&1 | tail -20

    # 4. Typecheck (no new errors from workspace stubs)
    pnpm typecheck 2>&1 | tail -20

    # 5. Verify all 9 stubs resolved
    pkgs=$(pnpm ls -r --depth -1 2>/dev/null | grep -c '@soralia')
    echo "Workspace packages found: $pkgs (expected 9)"

    # 6. Turbo dry-run (verify task graph)
    pnpm turbo run build lint typecheck --dry-run 2>&1 | head -20
    ```

    If any command fails:
    - For `pnpm build` failure: check that the root `next build` still works (no workspace package has a `build` script yet, so turbo skips them — but root scripts are still direct commands per Plan 01 design, so `pnpm build` runs `next build` as before)
    - For `pnpm lint` failure: verify that `eslint.config.mjs` ignores correctly exclude workspace directories
    - For `pnpm typecheck` failure: verify that `tsconfig.json` exclude array includes `packages`, `apps`, `tooling`
    - For `pnpm install` failure: check for workspace glob conflicts with existing overrides

    The expected state: all 4 quality gate commands exit 0 with no warnings or errors related to workspace stubs.

  </action>

<acceptance_criteria> - `pnpm install` exits 0 - `pnpm build` exits 0 (web app builds successfully) - `pnpm lint` exits 0 (no new warnings; stub files ignored) - `pnpm typecheck` exits 0 (no new errors; stub files excluded) - `pnpm ls -r --depth -1 | grep -c '@soralia'` returns exactly 9 - `pnpm turbo run build --dry-run 2>&1 | grep -q 'Packages in scope'` (turbo task graph resolves)
</acceptance_criteria>

  <verification>
    <automated>
      pnpm install && pnpm build && pnpm lint && pnpm typecheck
    </automated>
  </verification>

  <done>
    All quality gates pass with the monorepo skeleton in place. 9 workspace packages resolved. Existing web app builds, lints, and typechecks without regression. Turbo task graph resolves correctly.
  </done>

<effort>S</effort>
</task>

</tasks>

<artifacts_produced>
| File | Purpose | Status |
|------|---------|--------|
| `packages/shared/package.json` | @soralia/shared stub declaration | NEW |
| `packages/shared/src/index.ts` | Stub entry point | NEW |
| `packages/db/package.json` | @soralia/db stub declaration | NEW |
| `packages/db/src/index.ts` | Stub entry point | NEW |
| `packages/auth/package.json` | @soralia/auth stub declaration | NEW |
| `packages/auth/src/index.ts` | Stub entry point | NEW |
| `packages/api/package.json` | @soralia/api stub declaration | NEW |
| `packages/api/src/index.ts` | Stub entry point | NEW |
| `packages/ui/package.json` | @soralia/ui stub declaration | NEW |
| `packages/ui/src/index.ts` | Stub entry point | NEW |
| `packages/server/package.json` | @soralia/server stub declaration | NEW |
| `packages/server/src/index.ts` | Stub entry point | NEW |
| `packages/analytics/package.json` | @soralia/analytics stub declaration | NEW |
| `packages/analytics/src/index.ts` | Stub entry point | NEW |
| `apps/web/package.json` | @soralia/web stub declaration | NEW |
| `apps/web/src/index.ts` | Stub entry point | NEW |
| `apps/expo/package.json` | @soralia/expo stub declaration | NEW |
| `apps/expo/src/index.ts` | Stub entry point | NEW |
| `.gitignore` | Recursive .turbo/ cache ignore added | MODIFIED |
| `.prettierignore` | Workspace directory ignores added | MODIFIED |
| `tsconfig.json` | Workspace directories added to exclude | MODIFIED |
| `eslint.config.mjs` | Workspace directories added to ignores | MODIFIED |
</artifacts_produced>

<phase_artifacts_summary>

## All Artifacts This Phase Creates (Cumulative)

### New files

| #   | File                              | Plan | Purpose                                 |
| --- | --------------------------------- | ---- | --------------------------------------- |
| 1   | `turbo.json`                      | 01   | Turborepo task graph (5 baseline tasks) |
| 2   | `tooling/typescript/base.json`    | 01   | Shared strict TypeScript base config    |
| 3   | `tooling/typescript/nextjs.json`  | 01   | Next.js tsconfig preset                 |
| 4   | `tooling/typescript/expo.json`    | 01   | Expo tsconfig preset stub               |
| 5   | `tooling/eslint/base.js`          | 01   | Shared ESLint flat config               |
| 6   | `tooling/eslint/nextjs.js`        | 01   | Next.js ESLint preset                   |
| 7   | `tooling/eslint/expo.js`          | 01   | Expo ESLint preset stub                 |
| 8   | `tooling/prettier/index.js`       | 01   | Shared Prettier config                  |
| 9   | `packages/shared/package.json`    | 02   | @soralia/shared stub                    |
| 10  | `packages/shared/src/index.ts`    | 02   | Stub entry point                        |
| 11  | `packages/db/package.json`        | 02   | @soralia/db stub                        |
| 12  | `packages/db/src/index.ts`        | 02   | Stub entry point                        |
| 13  | `packages/auth/package.json`      | 02   | @soralia/auth stub                      |
| 14  | `packages/auth/src/index.ts`      | 02   | Stub entry point                        |
| 15  | `packages/api/package.json`       | 02   | @soralia/api stub                       |
| 16  | `packages/api/src/index.ts`       | 02   | Stub entry point                        |
| 17  | `packages/ui/package.json`        | 02   | @soralia/ui stub                        |
| 18  | `packages/ui/src/index.ts`        | 02   | Stub entry point                        |
| 19  | `packages/server/package.json`    | 02   | @soralia/server stub                    |
| 20  | `packages/server/src/index.ts`    | 02   | Stub entry point                        |
| 21  | `packages/analytics/package.json` | 02   | @soralia/analytics stub                 |
| 22  | `packages/analytics/src/index.ts` | 02   | Stub entry point                        |
| 23  | `apps/web/package.json`           | 02   | @soralia/web stub                       |
| 24  | `apps/web/src/index.ts`           | 02   | Stub entry point                        |
| 25  | `apps/expo/package.json`          | 02   | @soralia/expo stub                      |
| 26  | `apps/expo/src/index.ts`          | 02   | Stub entry point                        |

### Modified files

| #   | File                  | Plan | Change                                                        |
| --- | --------------------- | ---- | ------------------------------------------------------------- |
| 1   | `package.json`        | 01   | Added `turbo` devDependency + `ci` script                     |
| 2   | `pnpm-workspace.yaml` | 01   | Workspace globs: `['apps/*', 'packages/*', 'tooling/*']`      |
| 3   | `.gitignore`          | 02   | Added `.turbo/` and `**/.turbo/`                              |
| 4   | `.prettierignore`     | 02   | Added `packages/`, `apps/`, `tooling/`                        |
| 5   | `tsconfig.json`       | 02   | Added `packages`, `apps`, `tooling` to exclude                |
| 6   | `eslint.config.mjs`   | 02   | Added `'packages/**'`, `'apps/**'`, `'tooling/**'` to ignores |

### New directories created

- `tooling/typescript/`
- `tooling/eslint/`
- `tooling/prettier/`
- `packages/shared/`, `packages/db/`, `packages/auth/`, `packages/api/`, `packages/ui/`, `packages/server/`, `packages/analytics/`
- `apps/web/`, `apps/expo/`

### NativeWind v5 / Expo SDK 53 Incompatibility Flag

The research identified a confirmed conflict: NativeWind v5 (preview) requires React Native 0.81+, which ships with Expo SDK 54+, not SDK 53 (locked by D-12). This does NOT block M0 (no Expo code), but MUST be resolved before M3 (Phase 116). See 113-RESEARCH.md §Open Question 1 and 112-CONTEXT.md D-15/ADVISORY-019 GATE G-3. Recommended resolution: hold a `checkpoint:decision` before M3 to choose between upgrading to Expo SDK 54 or downgrading to NativeWind 4.x.
</phase_artifacts_summary>

<threat_model>
**ASVS Level:** 1
**Phase type:** Tooling/infrastructure scaffold — no application logic, no user data, no auth.

| Threat ID | Category               | Component                     | Disposition | Mitigation                                                                              |
| --------- | ---------------------- | ----------------------------- | ----------- | --------------------------------------------------------------------------------------- |
| T-113-01  | Tampering              | turbo npm package             | mitigate    | Package legitimacy verified in RESEARCH.md (Vercel-maintained, 10M+ weekly downloads)   |
| T-113-02  | Information Disclosure | tsconfig include patterns     | mitigate    | Workspace directories excluded from root typecheck in Plan 02 Task 2                    |
| T-113-03  | Denial of Service      | pnpm workspace re-resolution  | mitigate    | `pnpm install` after each workspace change; lockfile update expected                    |
| T-113-04  | Tampering              | stub package.json names       | mitigate    | Names verified against MOBILE_MONOREPO.md spec: all 9 follow @soralia/{name} convention |
| T-113-SC  | Tampering              | pnpm-workspace.yaml overrides | mitigate    | Existing overrides/patchedDependencies preserved verbatim; only packages: glob changed  |

</threat_model>

<verification>
<h3>Full Phase Verification</h3>

Run this after Plan 02 completes to confirm the entire M0 scaffold:

```bash
# 1. Workspace resolution
pnpm install && echo "install: OK"

# 2. Quality gates
pnpm build && echo "build: OK"
pnpm lint && echo "lint: OK"
pnpm typecheck && echo "typecheck: OK"

# 3. Package count
count=$(pnpm ls -r --depth -1 2>/dev/null | grep -c '@soralia')
[ "$count" -eq 9 ] && echo "packages: $count OK"

# 4. Turbo task graph
pnpm turbo run build --dry-run 2>&1 | grep -q 'Packages in scope' && echo "turbo: OK"

# 5. Check no regression
git diff --stat
```

</verification>

<success_criteria>

- [ ] All 4 quality gates pass: pnpm install, pnpm build, pnpm lint, pnpm typecheck
- [ ] `pnpm ls -r --depth -1` shows exactly 9 @soralia/\* workspace packages
- [ ] `turbo --version` returns 2.x
- [ ] `pnpm turbo run build --dry-run` resolves packages in scope
- [ ] `.gitignore` excludes `.turbo/`
- [ ] `tsconfig.json` excludes `packages`, `apps`, `tooling`
- [ ] `eslint.config.mjs` ignores `packages/**`, `apps/**`, `tooling/**`
- [ ] No existing web app functionality is affected (verified by quality gates)
      </success_criteria>

<output>
Create `.planning/phases/113-monorepo-scaffold-m0/113-02-SUMMARY.md` when done
</output>
