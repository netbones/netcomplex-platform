---
phase: 125
plan: 08
type: execute
wave: 4
depends_on:
  - 125-07
files_modified:
  - src/features/proxy-vote/ui/ProxyUploadForm.tsx
  - src/features/proxy-vote/ui/ProxyStatusCard.tsx
  - src/features/proxy-vote/ui/ProxyAcceptanceCard.tsx
requirements:
  - PV-03
autonomous: true
---

# Plan 125-08: UI — Remaining Wizard Steps + Widget Integration

<objective>
Build the three remaining wizard step components: ProxyUploadForm (document upload drop zone), ProxyStatusCard (status display after submission), and ProxyAcceptanceCard (notification-linked accept/decline card for proxy nominees). Integrate all components into the ProxyFlowWizard step mapping.
</objective>

<tasks>

<task id="125-08-01" type="execute">

**ProxyUploadForm + ProxyStatusCard components**

<read_first>
- .planning/phases/125-proxy-vote-module/125-UI-SPEC.md §Component Inventory (ProxyUploadForm: dashed border upload zone, drag-drop + click, PDF/JPG/PNG, max 10MB, filename display after upload; ProxyStatusCard: owner/proxy names, checkmarks rows, status badge, QR when Approved, withdraw button)
- .planning/phases/125-proxy-vote-module/125-UI-SPEC.md §Copywriting Contract (upload label "Upload signed proxy form (PDF, JPG, or PNG — max 10MB)", checkmark labels "✓ Owner signed" / "✓ Proxy signed" / "✓ Form uploaded")
- src/shared/api/storage.ts (uploadDocument() from plan 125-06 — call this for file upload)
</read_first>

<action>
1. src/features/proxy-vote/ui/ProxyUploadForm.tsx:
   - Client component
   - **Wrap upload action in try/catch with user-friendly error toast** — file uploads can fail (network, MIME mismatch, size limit exceeded) per AGENTS.md §Error Handling
   - Props: { onUploadComplete: (documentId: string, fileUrl: string) => void; proxyId: string }
   - Upload zone: `border-2 border-dashed border-gray-300 bg-gray-50 rounded-lg p-8` (UI-SPEC upload zone surface)
   - Inner: upload icon (lucide Upload, gray-400, w-8 h-8) + label text "Upload signed proxy form (PDF, JPG, or PNG — max 10MB)" (text-sm, gray-600)
   - Supports drag-and-drop + click-to-browse (hidden <input type="file" accept=".pdf,.jpg,.jpeg,.png">)
   - After file selected: validate type (ALLOWED_DOCUMENT_TYPES from storage.ts) + size (MAX_DOCUMENT_SIZE)
   - Validation error: red border (border-red-500) + error copy "Upload failed — try again or use a PDF, JPG, or PNG under 10MB." per UI-SPEC
   - On valid upload: call uploadDocument(file, tenantId, 'proxy-forms') from plan 125-06
   - Show uploaded filename + size after success (text-sm, green-600, checkmark icon)
   - Auto-advance to Step 4 (Proxy Acceptance) after upload complete
   - Upload button: "Submit Proxy Appointment" CTA per UI-SPEC copywriting
   - **Loading state visible during upload** — show spinner on Submit button while uploadDocument() awaits per AGENTS.md §Async Operations

2. src/features/proxy-vote/ui/ProxyStatusCard.tsx:
   - Client component
   - Props: { proxy: MeetingProxyDTO }
   - Card with bg-white shadow-sm rounded-lg p-5
   - Header: owner name (600 weight, text-lg) + proxy name (400 weight, text-base, gray-600)
   - Checkmark rows (3 rows, inline-flex with gap-2):
     - "✓ Owner signed" — green-600 if ownerSignedAt not null, gray-400 otherwise
     - "✓ Proxy signed" — green-600 if proxySignedAt not null, gray-400 otherwise
     - "✓ Form uploaded" — green-600 if formDocumentId not null, gray-400 otherwise
   - Status badge: per STATUS_META from constants (plan 125-02) — uses 7-color palette
   - When Approved: QR reference section — <ProxyQRCode referenceCode={proxy.referenceCode} />
   - When Draft/WaitingForUpload/WaitingForProxy: withdraw button "Withdraw Proxy" (red-600 destructive) with confirmation dialog
   - Status copy: "Submitted to HOA. Pending verification." for PendingHoaReview per UI-SPEC copywriting
</action>

<acceptance_criteria>
- ProxyUploadForm shows dashed border upload zone with drag-drop support
- File type validation rejects non-PDF/JPG/PNG — error message per UI-SPEC copywriting
- File size validation rejects >10MB
- Successful upload shows filename + size in green
- ProxyStatusCard shows 3 checkmarks with correct colors (green if signed, gray if not)
- Status badge renders with correct color per STATUS_META (Approved=accent, Rejected=red, Waiting=amber)
- QR reference displayed when Approved status
- Withdraw button visible in Draft/WaitingForUpload/WaitingForProxy states with confirmation dialog
- npx tsc --noEmit --pretty src/features/proxy-vote/ui/ProxyUploadForm.tsx exits 0
- npx tsc --noEmit --pretty src/features/proxy-vote/ui/ProxyStatusCard.tsx exits 0
</acceptance_criteria>

</task>

<task id="125-08-02" type="execute">

**ProxyAcceptanceCard + final wizard integration**

<read_first>
- .planning/phases/125-proxy-vote-module/125-UI-SPEC.md §Component Inventory (ProxyAcceptanceCard: notification-embedded, owner name, meeting title, View Form link, Accept/Decline buttons)
- .planning/phases/125-proxy-vote-module/125-UI-SPEC.md §Copywriting Contract (accept "Accept Proxy Appointment", decline "Decline Proxy Appointment" with confirmation)
- src/features/proxy-vote/server/proxy-notify.service.ts (notification dispatch functions from plan 125-05)
</read_first>

<action>
1. src/features/proxy-vote/ui/ProxyAcceptanceCard.tsx:
   - Client component
   - Props: { proxy: MeetingProxyDTO; meetingTitle: string; onAccept: () => void; onDecline: () => void }
   - Embedded in notification detail view or email link
   - Card layout: bg-white shadow-sm rounded-lg p-5
   - Content: "You have been nominated as a proxy for {ownerName} — {meetingTitle}" (text-base, 400 weight)
   - "View Form" link button (text-sm, gray-600, opens formDocumentId in new tab)
   - Buttons: "Accept Proxy Appointment" (soralia-primary bg, 44px min) + "Decline Proxy Appointment" (red-600 border, destructive, 44px min)
   - Accept → calls onAccept (triggers signature step navigation)
   - Decline → confirmation dialog "Decline Proxy Appointment: Are you sure? This action cannot be undone." per UI-SPEC → calls onDecline (triggers notification + owner returns to Step 2)

2. Final wizard integration in ProxyFlowWizard.tsx:
   - Map all 6 steps to their components:
     - Step 1: MeetingAttendancePrompt
     - Step 2: ProxySearchBox
     - Step 3: ProxyUploadForm
     - Step 4: ProxyAcceptanceCard (idle — "Notification sent to proxy. Waiting for acceptance.")
     - Step 5: SignatureCanvas (only after proxy accepts)
     - Step 6: ProxyStatusCard
   - Component switching based on proxy.status + step state machine per plan 125-07 task 01
   - Status transitions flow: Draft → Step2 → upload → Step3 → notify → Step4 → proxy accepts → Step5 → sign → Step6

3. Export all components from src/features/proxy-vote/ui/index.ts barrel:
   - ProxyFlowWizard, SignatureCanvas, ProxyQRCode, ProxySearchBox, MeetingAttendancePrompt, ProxyUploadForm, ProxyStatusCard, ProxyAcceptanceCard
</action>

<acceptance_criteria>
- ProxyAcceptanceCard shows owner name + meeting title with View Form link
- Accept button triggers signature step (Step 5 appears after accept)
- Decline button shows confirmation dialog per UI-SPEC copywriting contract
- ProxyFlowWizard maps all 6 steps to correct components
- Status transitions flow correctly: Draft→Step2, WaitingForUpload→Step3, WaitingForProxy→Step4(idle), proxyAccepted→Step5, signed→Step6, Approved/Rejected→Step6(terminal)
- All 8 components barrel-exported from ui/index.ts
- npx tsc --noEmit --pretty src/features/proxy-vote/ui/index.ts exits 0
</acceptance_criteria>

</task>

</tasks>

<verification>
- Full 6-step wizard renders with component switching based on proxy status
- ProxyAcceptanceCard notification flow works end-to-end
- Upload form validates file type and size client-side
- Status card reflects current proxy state with correct badge color and checkmarks
- Component barrel exports all 8 UI components
- TypeScript compilation clean across all UI files
</verification>

<must_haves>
truths:
  - "ProxyFlowWizard maps 6 steps to correct components based on proxy.status"
  - "ProxyUploadForm validates PDF/JPG/PNG types + 10MB size limit client-side"
  - "ProxyStatusCard renders status badge with correct color per STATUS_META"
  - "ProxyAcceptanceCard has Accept/Decline buttons with confirmation dialog on decline"
  - "All 8 UI components barrel-exported from ui/index.ts"
</must_haves>

<threat_model>
ASVS Level: 1
Block on: high

| Threat | Severity | Mitigation | Verification |
|--------|----------|------------|--------------|
| File upload bypass (client-side validation circumvented via browser devtools) | MEDIUM | Server-side type validation in storage.ts uploadDocument(); Supabase Storage bucket policy rejects untrusted extensions | Manual smoke: attempt upload via curl with text/html MIME type — server rejects |
| XSS via uploaded filename display | LOW | Filename rendered as text content (not innerHTML); tenant-scoped storage key prevents path traversal | Manual: upload file named "<script>alert(1)</script>.pdf" — displayed as escaped text |
</threat_model>

<artifacts_this_phase_produces>
- src/features/proxy-vote/ui/ProxyUploadForm.tsx — document upload component
- src/features/proxy-vote/ui/ProxyStatusCard.tsx — status display component
- src/features/proxy-vote/ui/ProxyAcceptanceCard.tsx — acceptance notification component
- src/features/proxy-vote/ui/index.ts — UI barrel (8 component exports)
- Updated: src/features/proxy-vote/ui/ProxyFlowWizard.tsx — final 6-step mapping
</artifacts_this_phase_produces>