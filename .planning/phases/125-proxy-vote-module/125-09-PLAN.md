---
phase: 125
plan: 09
type: execute
wave: 5
depends_on:
  - 125-07
  - 125-08
files_modified:
  - src/widgets/proxy-vote/ui/HoaProxyWidget.tsx
  - src/widgets/proxy-vote/ui/ProxyWidget.tsx
  - src/widgets/proxy-vote/index.ts
  - src/widgets/dashboard/widgets.ts (WidgetManifest registration)
  - src/features/proxy-vote/__tests__/notifications.test.ts
  - src/features/proxy-vote/__tests__/qr.test.ts
  - src/features/proxy-vote/__tests__/__mocks__/signature-canvas.ts
requirements:
  - PV-08
autonomous: true
---

# Plan 125-09: Widgets + Wave 0 Test Stubs + Quality Gates

<objective>
Create the HoaProxyWidget (admin approval widget) and ProxyWidget (resident dashboard widget). Register both in the WidgetManifest registry with featureFlag='proxyVote'. Complete all remaining Nyquist Wave 0 test stubs: notifications dispatch tests, QR rendering tests, and canvas mock. Run quality gates: pnpm lint, pnpm fsd:check, pnpm build, full test suite.
</objective>

<tasks>

<task id="125-09-01" type="execute">

**HoaProxyWidget + ProxyWidget + WidgetManifest registration**

<read_first>
- .planning/phases/125-proxy-vote-module/125-UI-SPEC.md §Component Inventory (HoaProxyWidget: widget heading "Proxy Votes", per-proxy row with owner/proxy names, checkmarks, status badge, approve/reject buttons, QR reference with copy; ProxyWidget: resident dashboard widget showing user's active proxies)
- src/widgets/dashboard/widgets.ts (existing WidgetManifest entries — follow the pattern for new widget registration)
- src/widgets/admin/dashboard-config.ts (admin widget configuration — HoaProxyWidget goes in admin space)
</read_first>

<action>
1. src/widgets/proxy-vote/ui/HoaProxyWidget.tsx:
   - Client component, dashboard widget for admin space
   - Heading: "Proxy Votes" (text-lg font-semibold, per UI-SPEC copywriting)
   - Fetch: tRPC proxyVoteRouter.getAllByTenant (or reuse getProxiesByMeeting — batch across meetings)
   - Per-proxy row layout (flex flex-col space-y-4):
     - Owner name (600 weight) + proxy name (400 weight, gray-600)
     - Checkmark icons inline: ✓ Owner Signed (green if ownerSignedAt; gray otherwise), ✓ Proxy Signed, ✓ Form Uploaded
     - Status badge per STATUS_META (7-color palette)
     - When status=PendingHoaReview: "Approve Proxy" button (soralia-primary bg) + "Reject Proxy Appointment" button (red-600 border), both 44px min
     - When status=Approved: QR reference PV-YYYY-NNNN in bg-soralia-primary/5 box with copy-to-clipboard icon + "Copy" text label per UI-SPEC
   - Approve: confirmation → tRPC proxyVoteRouter.approve mutation
   - Reject: modal with notes textarea (required) → tRPC proxyVoteRouter.reject mutation
   - Empty state: "No proxy has been submitted for this meeting yet." per UI-SPEC copywriting

2. src/widgets/proxy-vote/ui/ProxyWidget.tsx:
   - Client component, dashboard widget for resident space (MyHomeSpace or Home space)
   - Fetch: tRPC proxyVoteRouter.getByOwner (proxies where ownerUserId = current user)
   - List: user's active proxies, each showing:
     - Meeting title + date (text-sm, gray-600)
     - Proxy status badge (7-color palette)
     - Link: "View Proxy" → /dashboard/proxy/[meetingId]
   - Empty state (if no proxies): no widget shown — hide via widget visibility or render empty "You have no active proxy submissions" (text-sm, gray-500)

3. Widget registry in src/widgets/dashboard/widgets.ts:
   - Add manifest for HoaProxyWidget:
     - name: 'HoaProxyWidget'
     - component: lazy(() => import('@/widgets/proxy-vote/ui/HoaProxyWidget'))
     - spaces: ['admin']
     - permissions: ['admin', 'board']
     - featureKey: 'proxy-vote'
     - description: 'Admin approval widget for proxy vote submissions'
   - Add manifest for ProxyWidget:
     - name: 'ProxyWidget'
     - component: lazy(() => import('@/widgets/proxy-vote/ui/ProxyWidget'))
     - spaces: ['home']
     - permissions: ['resident', 'board']
     - featureKey: 'proxy-vote'

4. Barrel: src/widgets/proxy-vote/index.ts — re-export both widgets
</action>

<acceptance_criteria>
- HoaProxyWidget renders "Proxy Votes" heading in admin space
- HoaProxyWidget shows per-proxy rows with owner/proxy names, checkmarks, and status badges
- Approve/Reject buttons visible only for PendingHoaReview status
- Approve button calls tRPC approveProxy mutation with confirmation
- Reject button opens notes modal (required textarea) before mutation
- Approved proxies display QR reference PV-YYYY-NNNN with Copy text label
- ProxyWidget shows user's active proxies with meeting title, status badge, and View link
- Both widgets registered in src/widgets/dashboard/widgets.ts with lazy imports and featureKey='proxy-vote'
- pnpm build exits 0 (widget lazy imports resolve)
- Empty state copy matches UI-SPEC contract
</acceptance_criteria>

</task>

<task id="125-09-02" type="execute">

**Wave 0 test stubs + canvas mock + quality gates**

<read_first>
- .planning/phases/125-proxy-vote-module/125-VALIDATION.md §Wave 0 Requirements (5 test stubs to complete)
- vitest.shared.ts (test config)
- src/widgets/dashboard/widgets.ts (widget manifest — verify new registrations)
</read_first>

<action>
1. Complete all 5 Wave 0 test stubs from VALIDATION.md:
   - src/features/proxy-vote/__tests__/schema.test.ts (already GREEN from plan 125-03) — add test for Drizzle schema import when entity Drizzle file exists
   - src/features/proxy-vote/__tests__/signature-adapter.test.ts (already GREEN from plan 125-04) — verify all 9 tests pass
   - src/features/proxy-vote/__tests__/notifications.test.ts — test proxy-notify functions:
     - notifyProxyNominated inserts notification for proxyUserId with type='proxy_vote'
     - notifyHoaNewProxy inserts notification for HOA admins
     - All notification inserts use db.insert(notifications).values() pattern
   - src/features/proxy-vote/__tests__/upload.test.ts (already GREEN from plan 125-06) — verify file type validation tests pass
   - src/features/proxy-vote/__tests__/qr.test.ts — test ProxyQRCode rendering:
     - <ProxyQRCode referenceCode="PV-2026-0043" /> renders with QRCodeSVG
     - Copy button fires clipboard.writeText with correct value

2. Create src/features/proxy-vote/__tests__/__mocks__/signature-canvas.ts:
   - Mock react-signature-canvas for vitest (canvas not available in jsdom)
   - Mock SignaturePad component: renders div with test-id, exposes mock toDataURL() returning 'data:image/png;base64,test-canvas-data'
   - Export mock for use across test files

3. Run full quality gates:
   - npx vitest run --config vitest.shared.ts src/features/proxy-vote/ — all tests GREEN
   - pnpm lint — no new ESLint errors in proxy-vote files
   - pnpm fsd:check — baseline violations unchanged (no new FSD violations from proxy-vote slice)
   - pnpm build — Next.js build passes with new proxy page + widgets

4. Update 125-VALIDATION.md frontmatter:
   - Set nyquist_compliant: true
   - Set wave_0_complete: true
   - Mark all Wave 0 checkboxes
</action>

<acceptance_criteria>
- All 5 Wave 0 test files exist and pass:
  - src/features/proxy-vote/__tests__/schema.test.ts (GREEN)
  - src/features/proxy-vote/__tests__/signature-adapter.test.ts (GREEN)
  - src/features/proxy-vote/__tests__/notifications.test.ts (GREEN — tests notification insert pattern)
  - src/features/proxy-vote/__tests__/upload.test.ts (GREEN)
  - src/features/proxy-vote/__tests__/qr.test.ts (GREEN — tests QR rendering + copy)
- src/features/proxy-vote/__tests__/__mocks__/signature-canvas.ts exists (jsdom-safe canvas mock)
- pnpm lint exits 0 (no new lint errors)
- pnpm fsd:check exits 0 or same baseline count (no new FSD violations)
- pnpm build exits 0 (build succeeds with proxy vote module)
- 125-VALIDATION.md frontmatter updated: nyquist_compliant=true, wave_0_complete=true
- All VALIDATION.md checkboxes marked [x]
</acceptance_criteria>

</task>

</tasks>

<verification>
- Both widgets registered and importable via widget store
- Admin widget shows approval queue with functional approve/reject mutations
- Resident widget shows user's active proxy submissions
- All 5 Wave 0 test stubs GREEN
- All quality gates pass (lint, FSD, build, test)
- No new FSD violations
- Nyquist validation frontmatter updated to compliant
</verification>

<must_haves>
truths:
  - "HoaProxyWidget registered in widget manifests with featureKey='proxy-vote' and spaces=['admin']"
  - "ProxyWidget registered with featureKey='proxy-vote' and spaces=['home']"
  - "All 5 Wave 0 test stubs exist and pass GREEN on vitest"
  - "Canvas mock provides jsdom-safe SignaturePad replacement"
  - "pnpm build passes without proxy-vote-related errors"
  - "125-VALIDATION.md nyquist_compliant set to true"
prohibitions:
  - statement: "Do NOT create new pages for proxy vote — wizard is a single page at /dashboard/proxy/[meetingId] per plan 125-07"
    status: resolved
    verification: ls src/app/(dashboard)/dashboard/proxy/ — only [meetingId]/page.tsx exists; no additional routes created
</must_haves>

<threat_model>
ASVS Level: 1
Block on: high

| Threat | Severity | Mitigation | Verification |
|--------|----------|------------|--------------|
| HOA admin approving without auth | HIGH | HoaProxyWidget dispatches tRPC proxyVoteRouter.approve — protectedProcedure() enforces session + role; requireTenantRLS() scopes to tenant | Manual smoke: try approve mutation without ADMIN session → 401 |
| Resident viewing other users' proxies via ProxyWidget | MEDIUM | ProxyWidget fetches tRPC proxyVoteRouter.getByOwner — server-side session.userId filter; no client-side userId parameter exposed | Verify getByOwner query includes WHERE ownerUserId = ctx.session.userId |
</threat_model>

<artifacts_this_phase_produces>
- src/widgets/proxy-vote/ui/HoaProxyWidget.tsx — admin approval widget
- src/widgets/proxy-vote/ui/ProxyWidget.tsx — resident proxy widget
- src/widgets/proxy-vote/index.ts — widget barrel
- Updated: src/widgets/dashboard/widgets.ts — 2 new WidgetManifest entries
- src/features/proxy-vote/__tests__/notifications.test.ts — notification dispatch tests
- src/features/proxy-vote/__tests__/qr.test.ts — QR rendering tests
- src/features/proxy-vote/__tests__/__mocks__/signature-canvas.ts — jsdom-safe canvas mock
- Updated: 125-VALIDATION.md — nyquist_compliant=true, wave_0_complete=true
</artifacts_this_phase_produces>