---
phase: 125
plan: 04
type: tdd
wave: 2
depends_on:
  - 125-03
files_modified:
  - src/features/proxy-vote/server/signature/internal-adapter.ts
  - src/features/proxy-vote/server/signature/provider-adapter.ts
  - src/features/proxy-vote/server/signature/registry.ts
  - src/features/proxy-vote/server/signature/index.ts
  - src/features/proxy-vote/__tests__/signature-adapter.test.ts
requirements:
  - PV-02
  - PV-07
autonomous: true
---

# Plan 125-04: TDD — InternalSignatureAdapter + SignatureService + Registry

<objective>
Build the INTERNAL-only signature adapter following RED/GREEN/REFACTOR. Create the SignatureProviderAdapter interface, the signatureProviders registry Map, and the SignatureService wrapper. Phase 125 ships only INTERNAL — draw canvas → PNG data URL, or typed name + checkbox. External providers (Lightning, Nostr, DocuSign, etc.) are schema-reserved but code-deferred.
</objective>

<tasks>

<task id="125-04-01" type="tdd">

**TDD: InternalSignatureAdapter**

<read_first>
- .planning/phases/125-proxy-vote-module/125-CONTEXT.md §Signature Service Architecture (SignatureEvidence shape for INTERNAL, sign/verify contracts)
- .planning/phases/125-proxy-vote-module/125-RESEARCH.md §Signature (react-signature-canvas export pipeline — canvas.toDataURL → PNG base64)
- src/features/proxy-vote/model/proxy-vote.zod.ts (signatureEvidenceSchema from plan 125-03 — adapter output must pass Zod validation)
</read_first>

<action>
RED: Write failing test at src/features/proxy-vote/__tests__/signature-adapter.test.ts:
- Test sign() with draw mode returns evidence where provider='INTERNAL', mode='draw', signatureDataUrl is non-empty base64 string, timestamp is ISO 8601
- Test sign() with type mode returns evidence where provider='INTERNAL', mode='type', typedName='John Smith', signatureDataUrl is undefined
- Test sign() with draw mode rejects empty canvas (signatureDataUrl is empty string or null → throws SignatureError)
- Test sign() with type mode rejects empty typedName (throws SignatureError with message 'typedName required for type mode')
- Test verify() with valid draw evidence returns true
- Test verify() with valid type evidence returns true
- Test verify() with mismatched mode returns false
- Test verify() with null signatureEvidence returns false

GREEN: Write src/features/proxy-vote/server/signature/internal-adapter.ts:
- Class InternalSignatureAdapter:
  - `sign(mode: 'draw' | 'type', data: SignInput): Promise<SignatureEvidence>`
  - SignInput = { signatureDataUrl?: string; typedName?: string }
  - Draw mode: requires non-empty signatureDataUrl (base64 PNG from react-signature-canvas)
  - Type mode: requires non-empty typedName
  - Returns SignatureEvidence with provider='INTERNAL', timestamp=new Date().toISOString()
  - Throws SignatureError for empty/invalid input
  - `verify(evidence: SignatureEvidence): Promise<boolean>` — minimal MVP: non-null evidence + valid mode + non-empty data → true; null/empty → false
  - `getProvider(): SignatureProvider` — returns 'INTERNAL'

- SignatureError class: extends Error, constructor(message: string)
- SignatureEvidence interface imported from model/types.ts (plan 125-03)

REFACTOR: Ensure Zod-safe — evidence output passes signatureEvidenceSchema.parse()
</action>

<acceptance_criteria>
- src/features/proxy-vote/__tests__/signature-adapter.test.ts with 9+ test cases
- InternalSignatureAdapter.sign('draw', { signatureDataUrl: 'data:image/png;base64,iVBOR...' }) returns evidence with provider='INTERNAL', mode='draw', non-null signatureDataUrl, ISO 8601 timestamp
- InternalSignatureAdapter.sign('type', { typedName: 'John Smith' }) returns evidence with provider='INTERNAL', mode='type', typedName='John Smith', signatureDataUrl undefined
- InternalSignatureAdapter.sign('draw', { signatureDataUrl: '' }) throws SignatureError
- InternalSignatureAdapter.sign('type', { typedName: '' }) throws SignatureError with message containing 'typedName required'
- InternalSignatureAdapter.verify(validDrawEvidence) → true
- InternalSignatureAdapter.verify(nullEvidence) → false
- Evidence output passes signatureEvidenceSchema.parse() (Zod validation)
- npx vitest run --config vitest.shared.ts src/features/proxy-vote/__tests__/signature-adapter.test.ts exits 0 (all GREEN)
- Separate RED/GREEN commits
</acceptance_criteria>

</task>

<task id="125-04-02" type="execute">

**SignatureProviderAdapter interface + registry Map + SignatureService**

<read_first>
- .planning/phases/125-proxy-vote-module/125-CONTEXT.md §Signature Service Architecture (interface signatures, registry pattern, four-layer model)
- src/features/proxy-vote/server/signature/internal-adapter.ts (from task 01 — implement the adapter interface)
</read_first>

<action>
1. src/features/proxy-vote/server/signature/provider-adapter.ts:
   - SignatureProviderAdapter interface:
     - `sign(input: SignInput): Promise<SignatureEvidence>`
     - `verify(evidence: SignatureEvidence): Promise<boolean>`
     - `getProvider(): SignatureProvider`
   - SignInput type (from task 01)

2. src/features/proxy-vote/server/signature/registry.ts:
   - Const `signatureProviders: Map<SignatureProvider, SignatureProviderAdapter>` = new Map()
   - Seed with: `signatureProviders.set('INTERNAL', new InternalSignatureAdapter())`
   - All other enum values (LIGHTNING, NOSTR, DOCUSIGN, ADOBE_SIGN, PASSKEY, PGP, GOV_EID) NOT registered — deferred
   - Comment above each deferred value: `// TODO: Phase-TBD — implement LIGHTNING adapter (LNbits message-signing extension)`
   - `getSignatureAdapter(provider: SignatureProvider): SignatureProviderAdapter` — registry lookup, throws if provider not registered with message: "Signature provider not available: {provider}"

3. src/features/proxy-vote/server/signature/index.ts barrel:
   - Re-export: InternalSignatureAdapter, SignatureProviderAdapter, signatureProviders, getSignatureAdapter, SignatureError, SignInput

4. Update src/features/proxy-vote/index.ts:
   - Add server/signature barrel re-export
</action>

<acceptance_criteria>
- SignatureProviderAdapter interface defined with sign()/verify()/getProvider() method signatures
- signatureProviders Map seeded with INTERNAL adapter
- getSignatureAdapter('INTERNAL') returns InternalSignatureAdapter instance
- getSignatureAdapter('LIGHTNING') throws with message containing "not available: LIGHTNING"
- All 7 non-INTERNAL enum values have TODO comments in registry
- src/features/proxy-vote/server/signature/index.ts barrel exports all symbols
- src/features/proxy-vote/index.ts updated with signature barrel
- npx tsc --noEmit --pretty src/features/proxy-vote/server/signature/index.ts exits 0
</acceptance_criteria>

</task>

</tasks>

<verification>
- InternalSignatureAdapter passes all TDD tests (9+ cases GREEN)
- SignatureEvidence output validated against Zod schema
- SignatureProviderAdapter interface is clean abstraction — any future provider implements same contract
- Registry dispatches correctly (INTERNAL works, external throws meaningful error)
- Feature barrel updated — proxy-vote/server module accessible from feature root
</verification>

<must_haves>
truths:
  - "InternalSignatureAdapter.sign() produces valid SignatureEvidence that passes Zod validation"
  - "InternalSignatureAdapter.sign() rejects empty canvas and empty typedName"
  - "SignatureProviderAdapter interface has sign()/verify()/getProvider() contract"
  - "signatureProviders Map dispatches INTERNAL adapter; throws for unregistered providers"
  - "All 7 external providers have deferred TODO comments in registry"
</must_haves>

<threat_model>
ASVS Level: 1
Block on: high

| Threat | Severity | Mitigation | Verification |
|--------|----------|------------|--------------|
| Signature forgery via empty canvas submission | HIGH | InternalSignatureAdapter.sign() rejects empty signatureDataUrl and empty typedName | vitest: test empty draw → SignatureError, empty type → SignatureError |
| Malformed signatureEvidence bypassing validation | MEDIUM | signatureEvidenceSchema (Zod) validates before DB insert; adapter output must pass schema.parse() | vitest: test adapter output → schema.parse() success |
| Unregistered provider accessed via registry bypass | MEDIUM | getSignatureAdapter() throws with descriptive message for unregistered providers; only INTERNAL registered in Phase 125 | vitest: test getSignatureAdapter('LIGHTNING') → throws |
</threat_model>

<artifacts_this_phase_produces>
- src/features/proxy-vote/server/signature/internal-adapter.ts — InternalSignatureAdapter class
- src/features/proxy-vote/server/signature/provider-adapter.ts — SignatureProviderAdapter interface + SignInput type
- src/features/proxy-vote/server/signature/registry.ts — signatureProviders Map + getSignatureAdapter()
- src/features/proxy-vote/server/signature/index.ts — signature barrel
- src/features/proxy-vote/__tests__/signature-adapter.test.ts — TDD tests
- SignatureError class (extends Error)
</artifacts_this_phase_produces>