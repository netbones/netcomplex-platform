---
phase: 125
plan: 03
type: execute
wave: 2
depends_on:
  - 125-01
files_modified:
  - src/features/proxy-vote/model/types.ts
  - src/features/proxy-vote/model/proxy-vote.zod.ts
  - src/features/proxy-vote/model/proxy-vote.dto.ts
  - src/features/proxy-vote/model/index.ts (barrel)
  - src/features/proxy-vote/index.ts (feature barrel)
requirements:
  - PV-01
  - PV-05
autonomous: true
---

# Plan 125-03: Entity Layer — Types, Zod Schemas, DTOs, Barrel Exports

<objective>
Create the proxy-vote entity layer following FSD conventions: TypeScript types/interfaces, Zod validation schemas, DTOs for API contracts, and barrel exports from both model/ and feature root. Types must mirror the MeetingProxy Prisma model exactly plus client-side status meta types from plan 125-02.
</objective>

<tasks>

<task id="125-03-01" type="execute">

**Types, Zod schemas, and DTOs**

<read_first>
- .planning/phases/125-proxy-vote-module/125-CONTEXT.md §Database Model (full field list, nullable markers, enum values)
- .planning/phases/125-proxy-vote-module/125-CONTEXT.md §Signature Service Architecture (SignatureEvidence shape for INTERNAL provider)
- src/entities/maintenance/model/types.ts (existing entity pattern — follow the interface structure)
- src/entities/maintenance/model/maintenance.zod.ts (existing Zod pattern — follow the schema conventions)
- src/shared/api/dto/ directory (existing DTO patterns — follow the type export conventions)
</read_first>

<action>
Create FSD entity layer files under src/features/proxy-vote/model/:

1. src/features/proxy-vote/model/types.ts:
- `ProxyStatus` enum (mirrors Prisma ProxyStatus — 7 values)
- `SignatureProvider` enum (mirrors Prisma — 8 values, INTERNAL default)
- `MeetingProxy` interface — all 18 fields from CONTEXT.md DB model, matching Prisma types exactly
- `ProxyStatusMeta` interface (from plan 125-02 constants contract): { label: string; color: string; description: string }
- `SignatureEvidence` interface — discriminated union:
  - INTERNAL: { provider: 'INTERNAL'; mode: 'draw' | 'type'; signatureDataUrl?: string; typedName?: string; timestamp: string }
- `SignatureEvidence` tagged union shape from CONTEXT.md §SignatureEvidence section

2. src/features/proxy-vote/model/proxy-vote.zod.ts:
- `proxyStatusSchema` = Zod nativeEnum (7 values)
- `signatureProviderSchema` = Zod nativeEnum (8 values)
- `signatureEvidenceSchema` — discriminated union on provider field:
  - z.discriminatedUnion('provider', [
    z.object({ provider: z.literal('INTERNAL'), mode: z.enum(['draw', 'type']), signatureDataUrl: z.string().optional(), typedName: z.string().optional(), timestamp: z.string() })
  ])
  - DEFERRED: other providers validated by shape but not implemented — mark with TODO: Phase-N for Lightning/Nostr/etc.
- `createProxySchema` — Zod object for POST create proxy:
  - meetingId: z.string().min(1)
  - proxyUserId: z.string().optional()
  - proxyName: z.string().optional()
  - proxyEmail: z.string().email().optional()
  - proxyPhone: z.string().optional()
  - refine: either proxyUserId OR (proxyName + proxyEmail + proxyPhone) must be provided — cannot have both empty
- `updateProxySchema` — Zod object for PATCH: partial fields only (status, notes, proxyUserId, proxyName, proxyEmail, proxyPhone)
- `approveProxySchema` — Zod object: notes: z.string().optional()
- `rejectProxySchema` — Zod object: notes: z.string().min(1, 'Rejection reason required')
- `signProxySchema` — Zod object: signatureEvidence (must match INTERNAL shape)

3. src/features/proxy-vote/model/proxy-vote.dto.ts:
- `MeetingProxyDTO` — meetingProxy fields serialized to JSON (dates as ISO strings, per existing DTO pattern)
- `createProxyDTO()` — maps createProxySchema → MeetingProxy shape (adds status: Draft, createdAt, etc.)
- `proxyResponseDTO()` — maps MeetingProxy DB row → MeetingProxyDTO (ISO date conversion)
- Re-export from src/shared/api/dto/ (or keep in feature — follow existing pattern)
</action>

<acceptance_criteria>
- src/features/proxy-vote/model/types.ts exports ProxyStatus enum (7 values), SignatureProvider enum (8 values), MeetingProxy interface (18 fields)
- src/features/proxy-vote/model/proxy-vote.zod.ts exports createProxySchema, updateProxySchema, signProxySchema, approveProxySchema, rejectProxySchema
- createProxySchema.refine() enforces proxyUserId OR (proxyName + proxyEmail + proxyPhone) — rejecting both-empty passes
- rejectProxySchema requires notes min 1
- signatureEvidenceSchema validates INTERNAL shape with discriminatedUnion
- proxy-vote.dto.ts exports MeetingProxyDTO, createProxyDTO(), proxyResponseDTO()
- npx vitest run --config vitest.shared.ts src/features/proxy-vote/__tests__/schema.test.ts exits 0 (Wave 0 schema test from 125-01 now GREEN — entity layer types exist)
</acceptance_criteria>

</task>

<task id="125-03-02" type="execute">

**Barrel exports and entity layer registration**

<read_first>
- src/features/maintenance/index.ts (existing feature barrel pattern)
- src/features/maintenance/model/index.ts (existing model barrel pattern)
</read_first>

<action>
1. src/features/proxy-vote/model/index.ts:
   - Re-export everything from types.ts, proxy-vote.zod.ts, proxy-vote.dto.ts
   - Export ALLOWED_EVENT_CATEGORIES, isProxyEligible, STATUS_META from ../lib/constants.ts — makes constants accessible from model layer
   - Export transition() from ../lib/status-transitions.ts — makes transition machine accessible from model layer

2. src/features/proxy-vote/index.ts:
   - Re-export model/index.ts (entity layer)
   - Re-export lib/constants.ts and lib/status-transitions.ts (service layer — when created in 125-04/125-05)
   - Placeholder comment: "// Service exports added in plan 125-04 (signature) and 125-05 (tRPC router)"
</action>

<acceptance_criteria>
- src/features/proxy-vote/model/index.ts exists with re-exports of: ProxyStatus, SignatureProvider, MeetingProxy, MeetingProxyDTO, createProxySchema, updateProxySchema, signProxySchema, statusTransitionMap, ALLOWED_EVENT_CATEGORIES, isProxyEligible, STATUS_META, REFERENCE_CODE_FORMAT
- src/features/proxy-vote/index.ts exists with re-exports of model/index.ts
- FSD check: `pnpm fsd:check` — no new violations introduced by proxy-vote feature slice (baseline + any new violations acceptable — Phase 125 does not fix pre-existing FSD debt)
- TypeScript: `npx tsc --noEmit --pretty src/features/proxy-vote/index.ts 2>&1 | head -30` — no errors on proxy-vote barrel
</acceptance_criteria>

</task>

</tasks>

<verification>
- All types match CONTEXT.md DB model field-by-field (18 fields, nullable markers correct)
- All Zod schemas validate against CONTEXT.md constraints (proxyUserId OR non-resident fields required)
- createProxySchema.refine() rejects both-empty proxy nomination
- signatureEvidenceSchema validated with discriminatedUnion for INTERNAL
- Barrel exports accessible from src/features/proxy-vote (fsd:check + tsc)
- Wave 0 schema test (from 125-01-03) now GREEN — schema types exist
</verification>

<must_haves>
truths:
  - "MeetingProxy interface has all 18 fields matching Prisma schema"
  - "ProxyStatus and SignatureProvider TypeScript enums mirror Prisma enums exactly"
  - "createProxySchema.refine() enforces proxyUserId OR non-resident-name — rejects simultaneous null"
  - "signatureEvidenceSchema validates INTERNAL shape via discriminatedUnion"
  - "Barrel exports (model/index.ts, proxy-vote/index.ts) provide single import surface"
</must_haves>

<threat_model>
ASVS Level: 1
Block on: high

| Threat | Severity | Mitigation | Verification |
|--------|----------|------------|--------------|
| Zod schema bypass allowing proxy without nominee | MEDIUM | createProxySchema.refine() enforces at least one proxy identifier; server-side validation via safeParse before DB insert | npx vitest — test that createProxySchema.parse({ meetingId: 'x' }) throws ZodError |
| signatureEvidence injection (malformed JSON via Zod bypass) | MEDIUM | signatureEvidenceSchema.discriminatedUnion validates INTERNAL shape; future providers each get their own Zod schema in separate adapters | npx vitest — test INTERNAL shape validation, test Lightning shape rejected (currently) |
</threat_model>

<artifacts_this_phase_produces>
- src/features/proxy-vote/model/types.ts — ProxyStatus, SignatureProvider, MeetingProxy, ProxyStatusMeta, SignatureEvidence
- src/features/proxy-vote/model/proxy-vote.zod.ts — 6 Zod schemas (proxyStatus, signatureProvider, signatureEvidence, createProxy, updateProxy, approveProxy, rejectProxy, signProxy)
- src/features/proxy-vote/model/proxy-vote.dto.ts — MeetingProxyDTO, createProxyDTO(), proxyResponseDTO()
- src/features/proxy-vote/model/index.ts — model barrel
- src/features/proxy-vote/index.ts — feature barrel
</artifacts_this_phase_produces>