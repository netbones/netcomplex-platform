---
phase: 125
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - package.json
  - drizzle/*.sql (auto-generated by drizzle-kit generate)
requirements:
  - PV-01
autonomous: true
---

# Plan 125-01: Schema Migration + Foundation

<objective>
Add the MeetingProxy model, ProxyStatus enum (7 values), and SignatureProvider enum (8 values) to the Prisma schema. Install react-signature-canvas and qrcode.react npm dependencies. Run database push to apply the migration. Generate Drizzle schema sync. Create a Wave 0 schema compilation test stub.
</objective>

<tasks>

<task id="125-01-01" type="execute">

**Schema migration — MeetingProxy model + enums**

<read_first>
- prisma/schema.prisma (existing models — follow enum and model conventions)
- .planning/phases/125-proxy-vote-module/125-CONTEXT.md §Database Model (field list, enum values)
- .planning/ROADMAP.md §Phase 125 — confirm Phase 30 (Focus Spaces), Phase 10 (Notifications), Phase 21 (Events) all show ✅ Complete (verified 2026-07-23 — Focus Spaces shipped in Phase 30, Notifications in Phase 10, Events in Phase 21)
- src/server/routers/maintenance.ts (existing Event relations in Prisma schema — confirm Event model exposes `category` field for proxy-eligibility validation)
- src/shared/api/db.ts (Drizzle schema location — verify src/db/schema/ directory exists for sync export)
</read_first>

<action>
Add to prisma/schema.prisma:

1. ProxyStatus enum:
```
enum ProxyStatus {
  Draft
  WaitingForUpload
  WaitingForProxy
  PendingHoaReview
  Approved
  Rejected
  Withdrawn
}
```

2. SignatureProvider enum:
```
enum SignatureProvider {
  INTERNAL
  LIGHTNING
  NOSTR
  DOCUSIGN
  ADOBE_SIGN
  PASSKEY
  PGP
  GOV_EID
}
```

3. MeetingProxy model with fields:
- id: String @id @default(cuid())
- meetingId: String (FK to Event, no cascade — MeetingProxy should outlive deleted events for audit)
- ownerUserId: String (FK to User)
- ownerHouseholdId: String (FK to Household)
- proxyUserId: String? (nullable — FK to User if resident proxy)
- proxyName: String? (non-resident proxy name)
- proxyEmail: String? (non-resident proxy email)
- proxyPhone: String? (non-resident proxy phone)
- formDocumentId: String? (nullable — FK to Document; null until Step 3 upload complete)
- ownerSignedAt: DateTime? (timestamp when owner completes upload)
- proxySignedAt: DateTime? (timestamp when proxy completes signature)
- approvedBy: String? (FK to User — HOA admin who approved)
- approvedAt: DateTime? (timestamp of approval)
- status: ProxyStatus @default(Draft)
- notes: String? @db.Text
- signatureProvider: SignatureProvider @default(INTERNAL)
- signatureEvidence: Json @default("{}") @db.JsonB
- createdAt: DateTime @default(now()) @map("created_at")
- updatedAt: DateTime @updatedAt @map("updated_at")
- referenceCode: String? @unique (PV-YYYY-NNNN, set only on approval)
- @@map("meeting_proxies")

4. Use named relations for all FK references on user model to avoid ambiguity with 54 existing relations:
- owner: meetingProxyOwner
- proxy: meetingProxyProxy
- approver: meetingProxyApprover

Follow the existing pattern from CommunityMerit relations: named relations with descriptive names on both sides. Map toUser→ownerUser, proxyUser, approvedBy fields.

5. Relation to Event model: add `proxies MeetingProxy[]` reverse field on Event, named relation `eventProxy`.
</action>

<acceptance_criteria>
- prisma/schema.prisma contains `enum ProxyStatus` with 7 values exactly: Draft, WaitingForUpload, WaitingForProxy, PendingHoaReview, Approved, Rejected, Withdrawn
- prisma/schema.prisma contains `enum SignatureProvider` with 8 values: INTERNAL, LIGHTNING, NOSTR, DOCUSIGN, ADOBE_SIGN, PASSKEY, PGP, GOV_EID
- prisma/schema.prisma contains `model MeetingProxy` with `@@map("meeting_proxies")`
- meetingId, ownerUserId, ownerHouseholdId fields have `String` type (FK to existing models)
- proxyUserId is `String?` (nullable)
- signatureEvidence field has type `Json @default("{}") @db.JsonB`
- status field has type `ProxyStatus @default(Draft)`
- signatureProvider field has type `SignatureProvider @default(INTERNAL)`
- referenceCode field has `@unique` constraint
- All user-facing FK relations use named relation descriptors (not auto-generated names)
- npx prisma validate exits 0 (schema is valid)
</acceptance_criteria>

</task>

<task id="125-01-02" type="execute">

**Install npm dependencies**

<read_first>
- package.json (current dependencies, note existing signature-related packages if any)
</read_first>

<action>
Install two new packages:
1. `pnpm add react-signature-canvas` — canvas-based signature capture, wraps signature_pad v5.1.3
2. `pnpm add qrcode.react` — Meta-maintained QR code React component v4.2.0
</action>

<acceptance_criteria>
- package.json dependencies contains `"react-signature-canvas":` entry
- package.json dependencies contains `"qrcode.react":` entry
- pnpm-lock.yaml updated with both packages
- `npx vitest run --config vitest.shared.ts` exits 0 (no regression from new deps)
</acceptance_criteria>

</task>

<task id="125-01-03" type="execute" autonomous="false">

**[BLOCKING] Database schema push + Drizzle generate + Wave 0 test stub**

<read_first>
- prisma/schema.prisma (the updated schema from task 01)
- drizzle/meta/_journal.json (existing Drizzle journal — do NOT rename files)
- vitest.shared.ts (shared test config)
</read_first>

<action>
1. Run database push: `npx prisma db push`
   - If push requires interactive confirmation, use `npx prisma db push --accept-data-loss` (new table + enums only — no existing data affected)
2. Run Drizzle generate: `npx drizzle-kit generate`
   - This creates a new migration file in drizzle/ directory
   - DO NOT rename the generated file
3. Verify the push:
   - `npx prisma db pull --print` confirms MeetingProxy model exists in remote
   - `npx drizzle-kit check` confirms no drift
4. Create Wave 0 schema compilation test stub:
   - Create file: src/features/proxy-vote/__tests__/schema.test.ts
   - Content: import MeetingProxy from Drizzle schema, verify model compiles with TypeScript
   - Test: `import { meetingProxies } from '@/db/schema/proxy-vote'; expect(meetingProxies).toBeDefined()`
   - Run: `npx vitest run --config vitest.shared.ts src/features/proxy-vote/__tests__/schema.test.ts` — expect FAIL (stub, no schema file yet — confirms Wave 0 infrastructure)
</action>

<acceptance_criteria>
- `npx prisma db push` exits 0 (migration applied to remote DB)
- `npx prisma db pull --print` output contains `MeetingProxy` model with all fields
- `npx drizzle-kit generate` exits 0 (new drizzle/*.sql file created in journal)
- `npx drizzle-kit check` reports no drift
- `npx prisma generate` exits 0 (Prisma client regenerated with new enums/types)
- src/features/proxy-vote/__tests__/schema.test.ts exists (Wave 0 stub)
- Test stub FAILS on run (expected — schema Drizzle file not yet created in entity layer plan 125-03)
</acceptance_criteria>

</task>

</tasks>

<verification>
- MeetingProxy table exists in database (verify via `npx prisma db pull --print`)
- Both enums (ProxyStatus, SignatureProvider) exist in database
- Both npm packages installed and importable
- No data loss on existing tables
- No Prisma/drizzle migration drift
- Wave 0 test stub file exists at correct path
</verification>

<must_haves>
truths:
  - "MeetingProxy table with all 18 fields exists in database"
  - "ProxyStatus enum has exactly 7 values: Draft, WaitingForUpload, WaitingForProxy, PendingHoaReview, Approved, Rejected, Withdrawn"
  - "SignatureProvider enum has exactly 8 values: INTERNAL, LIGHTNING, NOSTR, DOCUSIGN, ADOBE_SIGN, PASSKEY, PGP, GOV_EID"
  - "Database push applied without data loss to existing tables"
  - "react-signature-canvas and qrcode.react are installed dependencies"
  - "Wave 0 schema test stub exists at src/features/proxy-vote/__tests__/schema.test.ts"
prohibitions:
  - statement: "Do NOT rename generated drizzle/*.sql files — Drizzle journal tracks filenames"
    status: resolved
    verification: Check drizzle/meta/_journal.json after generation; no manual rename of any new drizzle/*.sql file
</must_haves>

<threat_model>
ASVS Level: 1
Block on: high severity

| Threat | Severity | Mitigation | Verification |
|--------|----------|------------|--------------|
| MeetingProxy created without auth | HIGH | All proxy endpoints use getSessionAndRole() + requireTenantRLS() + assertModuleEnabled('proxyVote') | 125-05: tRPC router includes requireTenantRLS() and session guard on all write operations |
| signatureEvidence injection (malicious JSON) | MEDIUM | signatureEvidence JSONB column validated server-side against provider-specific shape before insert/update; Zod schema enforces shape per provider | 125-03: Zod schemas validate signatureEvidence shape for INTERNAL provider |
| DB push destroys unrelated tables | LOW | New model + enums only — no existing table modifications; --accept-data-loss applies only to new items | Manual: verify existing tables (User, Event, Document, Notification) unchanged after push |
| npm supply chain (react-signature-canvas, qrcode.react) | MEDIUM | Both packages passed package-legitimacy check; pnpm lockfile pins versions | 125-01-02: pnpm-lock.yaml updated with pinned versions |
</threat_model>

<artifacts_this_phase_produces>
- ProxyStatus enum (Prisma)
- SignatureProvider enum (Prisma)
- MeetingProxy model (Prisma + @map("meeting_proxies"))
- Named relations: meetingProxyOwner, meetingProxyProxy, meetingProxyApprover, eventProxy
- npm dependency: react-signature-canvas (version pinned in pnpm-lock.yaml)
- npm dependency: qrcode.react (version pinned in pnpm-lock.yaml)
- drizzle/*.sql migration file (auto-generated, not renamed)
- src/features/proxy-vote/__tests__/schema.test.ts (Wave 0 stub)
</artifacts_this_phase_produces>