---
phase: 125
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/features/proxy-vote/lib/status-transitions.ts
  - src/features/proxy-vote/lib/constants.ts
  - src/features/proxy-vote/__tests__/status-transitions.test.ts
  - src/features/proxy-vote/__tests__/constants.test.ts
requirements:
  - PV-09
autonomous: true
---

# Plan 125-02: TDD — Status Transitions + Constants

<objective>
Build a pure-function status transition machine that encodes the 7-status lifecycle (Draft → WaitingForUpload → WaitingForProxy → PendingHoaReview → Approved/Rejected → Withdrawn). Write ts-pattern-style pure function with exhaustive type narrowing. Create ALLOWED_EVENT_CATEGORIES and STATUS_META constants. Both features follow RED/GREEN/REFACTOR — write failing test first, implement to green, then refactor.
</objective>

<tasks>

<task id="125-02-01" type="tdd">

**TDD: Status transition machine (pure function)**

<read_first>
- .planning/phases/125-proxy-vote-module/125-CONTEXT.md §Status Lifecycle (7-state flowchart with arrows)
- src/features/maintenance/lib/status-transitions.ts (if exists — check for existing transition pattern in codebase; note that the 7-status maintenance workflow transition pattern may exist as a reference)
</read_first>

<action>
RED: Write failing test at src/features/proxy-vote/__tests__/status-transitions.test.ts:
- Test `transition('Draft', 'upload')` → expects `'WaitingForUpload'`
- Test `transition('WaitingForUpload', 'uploadComplete')` → expects `'WaitingForProxy'`
- Test `transition('WaitingForProxy', 'proxyAccepted')` → expects `'PendingHoaReview'`
- Test `transition('PendingHoaReview', 'approve')` → expects `'Approved'`
- Test `transition('PendingHoaReview', 'reject')` → expects `'Rejected'`
- Test `transition('WaitingForProxy', 'withdraw')` → expects `'Withdrawn'`
- Test invalid transitions throw: `transition('Draft', 'approve')` throws ProxyStatusError

GREEN: Write `src/features/proxy-vote/lib/status-transitions.ts`:
- Pure function `transition(current: ProxyStatus, event: ProxyStatusEvent): ProxyStatus`
- ProxyStatusEvent union type: `'upload' | 'uploadComplete' | 'proxyAccepted' | 'proxyDeclined' | 'approve' | 'reject' | 'withdraw'`
- Use switch or map-based lookup with ProxyStatus narrowing
- Invalid transitions throw `ProxyStatusError extends Error` with message: "Invalid transition: {current} → {event}"
- All 7 valid transitions from CONTEXT.md status lifecycle diagram

REFACTOR: Ensure TypeScript exhaustiveness — every ProxyStatus variant has a case branch.
</action>

<acceptance_criteria>
- src/features/proxy-vote/__tests__/status-transitions.test.ts exists with 8+ test cases
- src/features/proxy-vote/lib/status-transitions.ts exports `transition(current: ProxyStatus, event: ProxyStatusEvent): ProxyStatus`
- `transition('Draft', 'upload')` returns `'WaitingForUpload'`
- `transition('PendingHoaReview', 'approve')` returns `'Approved'`
- `transition('PendingHoaReview', 'reject')` returns `'Rejected'`
- `transition('WaitingForProxy', 'withdraw')` returns `'Withdrawn'`
- `transition('Draft', 'approve')` throws `ProxyStatusError` with message containing "Invalid transition: Draft → approve"
- `transition('Approved', 'upload')` throws (terminal state — no transitions from Approved)
- `npx vitest run --config vitest.shared.ts src/features/proxy-vote/__tests__/status-transitions.test.ts` exits 0 (all tests GREEN)
- Separate RED and GREEN commits — RED commit has only test file, GREEN adds implementation + passing tests
</acceptance_criteria>

</task>

<task id="125-02-02" type="tdd">

**TDD: Constants module (ALLOWED_EVENT_CATEGORIES + STATUS_META)**

<read_first>
- .planning/phases/125-proxy-vote-module/125-CONTEXT.md §Module Scope (event categories list)
- .planning/phases/125-proxy-vote-module/125-UI-SPEC.md §Status Badge Palette (7 colors)
</read_first>

<action>
RED: Write failing test at src/features/proxy-vote/__tests__/constants.test.ts:
- Test `ALLOWED_EVENT_CATEGORIES` contains exactly: ['AGM', 'SGM', 'SPECIAL_RESOLUTION', 'TRUSTEE_ELECTION']
- Test `isProxyEligible(eventCategory)` returns true for AGM, false for 'COMMUNITY_EVENT'
- Test `STATUS_META.Draft.label` → 'Draft', STATUS_META.Approved.color → specific badge color
- Test every ProxyStatus value has a STATUS_META entry (exhaustive)

GREEN: Write `src/features/proxy-vote/lib/constants.ts`:
- Const array `ALLOWED_EVENT_CATEGORIES = ['AGM', 'SGM', 'SPECIAL_RESOLUTION', 'TRUSTEE_ELECTION'] as const`
- `isProxyEligible(category: string): boolean` — returns category ∈ ALLOWED_EVENT_CATEGORIES
- `ProxyStatusMeta` interface: `{ label: string; color: StatusBadgeColor; description: string }`
- `StatusBadgeColor` type: `'gray' | 'amber' | 'green' | 'red'` (maps to Tailwind classes per UI-SPEC badge palette)
- `STATUS_META: Record<ProxyStatus, ProxyStatusMeta>` — exhaustive record, every status has label/color/description
- Colors: Draft=gray, WaitingForUpload=amber, WaitingForProxy=amber, PendingHoaReview=amber, Approved=green, Rejected=red, Withdrawn=gray
- Labels: per UI-SPEC copywriting contract §Status Badge Palette (e.g., Approved → "Approved" with accent bg)
- REFERENCE_CODE_FORMAT = `PV-{YYYY}-{NNNN}` constant string pattern
</action>

<acceptance_criteria>
- src/features/proxy-vote/__tests__/constants.test.ts exists with 7+ test cases
- src/features/proxy-vote/lib/constants.ts exports ALLOWED_EVENT_CATEGORIES, isProxyEligible, STATUS_META
- `isProxyEligible('AGM')` returns `true`
- `isProxyEligible('COMMUNITY_EVENT')` returns `false`
- `STATUS_META.Approved.color` is `'green'`
- `STATUS_META.WaitingForProxy.color` is `'amber'`
- TypeScript compile: `STATUS_META` is exhaustive — removing any ProxyStatus value breaks compilation
- REFERENCE_CODE_FORMAT constant exported
- `npx vitest run --config vitest.shared.ts src/features/proxy-vote/__tests__/constants.test.ts` exits 0 (all GREEN)
- Separate RED/GREEN commits
</acceptance_criteria>

</task>

</tasks>

<verification>
- Both features follow RED → GREEN → REFACTOR with separate test/feat commits
- Status transition machine covers all 7 valid transitions from CONTEXT.md lifecycle
- Status transition machine rejects all invalid transitions with specific error messages
- CONSTANTS module has exhaustive STATUS_META (TypeScript compile error if status added without META entry)
- ALLOWED_EVENT_CATEGORIES const-tuple prevents accidental category additions
- Both test files GREEN on vitest
</verification>

<must_haves>
truths:
  - "transition() function covers all 7 valid ProxyStatus transitions"
  - "transition() throws ProxyStatusError for every disallowed transition"
  - "STATUS_META is exhaustive — every ProxyStatus variant has a metadata entry"
  - "isProxyEligible() correctly gates AGM/SGM/SPECIAL_RESOLUTION/TRUSTEE_ELECTION"
  - "Both features have separate RED (test-only) and GREEN (test+implementation) commits"
prohibitions:
  - statement: "Do NOT encode status transitions in database triggers or stored procedures — pure TypeScript function, unit-testable"
    status: resolved
    verification: grep 'CREATE TRIGGER' prisma/migrations/ — no migration created by plan 125-02
</must_haves>

<threat_model>
ASVS Level: 1
Block on: high

| Threat | Severity | Mitigation | Verification |
|--------|----------|------------|--------------|
| Invalid status transition bypassing proxy workflow | HIGH | transition() function is the single source of truth for status changes; all status writes in tRPC router (125-05) MUST call transition() before DB update | npx vitest run src/features/proxy-vote/__tests__/status-transitions.test.ts — verify all invalid transitions throw |
| Malicious event category triggering proxy on non-governance events | MEDIUM | isProxyEligible() gated on const-tuple; tRPC createProxy procedure (125-05) calls isProxyEligible() before creating proxy | npx vitest run src/features/proxy-vote/__tests__/constants.test.ts — verify COMMUNITY_EVENT returns false |
</threat_model>

<artifacts_this_phase_produces>
- src/features/proxy-vote/lib/status-transitions.ts — transition() pure function
- src/features/proxy-vote/__tests__/status-transitions.test.ts — TDD test (RED→GREEN)
- ProxyStatusError class (extends Error)
- ProxyStatusEvent union type
- src/features/proxy-vote/lib/constants.ts — ALLOWED_EVENT_CATEGORIES, isProxyEligible, STATUS_META, REFERENCE_CODE_FORMAT
- src/features/proxy-vote/__tests__/constants.test.ts — TDD test (RED→GREEN)
- StatusBadgeColor type ('gray'|'amber'|'green'|'red')
- ProxyStatusMeta interface
</artifacts_this_phase_produces>