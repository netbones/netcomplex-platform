---
phase: 37-admin-route-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.mjs
  - src/app/(tenant)/admin/page.tsx
  - src/widgets/dashboard/ui/AdminLayer.tsx
  - src/widgets/dashboard/ui/AdminSubLauncher.tsx
  - src/widgets/dashboard/ui/AdminCommandBar.tsx
  - src/widgets/admin/ui/AdminStatsWidget.tsx
  - src/widgets/admin/ui/AdminQuickLinksWidget.tsx
  - src/widgets/admin/ui/SurveysWidget.tsx
  - src/widgets/admin/ui/EventsWidget.tsx
  - src/widgets/admin/ui/AdminContentWidget.tsx
  - src/widgets/admin/ui/AdminAnnouncementsWidget.tsx
  - src/widgets/maintenance/ui/MaintenanceRequestsWidget.tsx
  - src/widgets/dashboard/ui/UserContentWidget.tsx
  - src/widgets/dashboard/ui/QuickActionsWidget.tsx
  - src/shared/lib/navigation.ts
  - src/app/(tenant)/admin/events/[id]/page.tsx
  - src/app/(tenant)/admin/events/new/page.tsx
  - src/app/(tenant)/admin/resources/page.tsx
  - src/app/(tenant)/admin/surveys/page.tsx
  - src/app/(tenant)/admin/surveys/[id]/page.tsx
  - src/features/onboarding/ui/steps/LaunchStep.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "/admin renders the AdminLayer with command bar, domain grid, and activity stream"
    - "All dashboard/admin/* links redirect to /admin/* equivalents"
    - "The dashboard SpaceLauncher sidebar admin space navigates to /admin"
    - "No broken redirect exists — users land on /admin/* pages directly"
    - "Old admin sub-pages (groups, households, categories, etc.) are reachable at /admin/* without redirection"
  artifacts:
    - path: "src/app/(tenant)/admin/page.tsx"
      provides: "AdminLayer rendering at /admin"
      min_lines: 5
    - path: "next.config.mjs"
      provides: "Redirect block removed"
      contains: "async redirects"
      excludes: "source: '/admin/:path*'"
  key_links:
    - from: "src/app/(tenant)/admin/page.tsx"
      to: "src/widgets/dashboard/ui/AdminLayer.tsx"
      via: "import AdminLayer"
      pattern: "AdminLayer"
    - from: "src/widgets/dashboard/ui/AdminLayer.tsx"
      to: "/admin/\${domain.id}"
      via: "DomainCard href"
      pattern: "/admin/"
    - from: "src/widgets/dashboard/ui/AdminCommandBar.tsx"
      to: "/admin/*"
      via: "Shortcut hrefs and UrgencyChip hrefs"
      pattern: "/admin/"
---

<objective>
**Consolidate all admin routes to `/admin` — remove the broken redirect, install the AdminLayer as the new admin landing page, and normalize all links.**

Purpose: Eliminate the dual-route confusion between `(tenant)/admin/` (full CRUD pages) and `(dashboard)/admin/` (widget overviews). Make `/admin` the single canonical admin route — with the AdminLayer (command bar + domain grid + activity stream) at `/admin` and full CRUD pages at `/admin/{feature}`.

Output: A clean, single admin route tree where `/admin` shows the AdminLayer and all feature links point to `/admin/{feature}`. The redirect is gone. All links are normalized. 22+ components updated for consistency.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

## Key context for this plan

- Phase 31 (dashboard-tab-removal) was planned but NEVER executed
- The redirect in next.config.mjs was added prematurely as setup for Phase 31
- Redirect is BROKEN: `/admin/*` → `/dashboard/admin/*` only handles 9 domain slugs, remaining routes 404
- The AdminLayer at `/dashboard/admin` (rendered via `[space]/page.tsx` when space === 'admin') has the exact UI the user wants at `/admin`

## Route mapping for domain cards that don't match old admin page names

Maintenance domain → links to /admin/requests (full page exists at /admin/requests)
System domain → links to /admin/categories (closest existing admin config page)

## All AdminLayer-related code lives in widget layer, imported into app layer

</context>

<tasks>

<task type="auto">
  <name>Task 1: Move AdminLayer to /admin + remove redirect + add href mapping</name>
  <files>
    next.config.mjs
    src/app/(tenant)/admin/page.tsx
    src/widgets/dashboard/ui/AdminLayer.tsx
    src/widgets/dashboard/ui/AdminSubLauncher.tsx
    src/widgets/dashboard/model/spaces.ts
  </files>
  <action>
    1. **Remove the redirect from next.config.mjs** — Delete the entire `async redirects()` block (lines 78-86). This redirect was added prematurely for Phase 31 which was never executed. It breaks all admin routes that don't match ADMIN_DOMAINS (groups, households, categories, external-surveys, requests, all detail pages).

    2. **Replace (tenant)/admin/page.tsx** — The current file is an old widget-tab admin dashboard. Replace the entire content with a page that imports and renders the `<AdminLayer />` component. Use the exact import path `@widgets/dashboard/ui/AdminLayer`. The new page should be minimal:

    ```tsx
    'use client';
    import { AdminLayer } from '@widgets/dashboard/ui/AdminLayer';
    export default function AdminPage() {
      return <AdminLayer />;
    }
    ```

    - The old admin page's widget-tab content (ADMIN_TABS, DraggableWidget, AddWidgetModal, AdminWidgetRenderer, widget store logic) — DELETE entirely. That UI is superseded by AdminLayer.
    - No need for ErrorBoundary wrapper — AdminLayer already has its own error handling.
    - No need for Breadcrumbs — AdminLayer doesn't use them.

    3. **Update AdminLayer.tsx DomainCard href** — Change the Link href from `/dashboard/admin/${domain.id}` to `/admin/${domain.id}`.

    However, `maintenance` domain maps to `/admin/requests` (not `/admin/maintenance`) because the full CRUD page exists at `/admin/requests`. And `system` domain maps to `/admin/categories` (the closest config-type page).

    Add a routeOverride map at the top of AdminLayer.tsx (after imports, before types):

    ```tsx
    // Domain ID → actual admin route override for domains whose page name differs
    const ADMIN_ROUTE_OVERRIDES: Record<string, string> = {
      maintenance: '/admin/requests',
      system: '/admin/categories',
    };
    ```

    Then in the DomainCard component, change the href from:
    ```tsx
    href={`/dashboard/admin/${domain.id}`}
    ```
    to:
    ```tsx
    href={ADMIN_ROUTE_OVERRIDES[domain.id] ?? `/admin/${domain.id}`}
    ```

    4. **Update AdminSubLauncher.tsx links** — Change the hardcoded `/dashboard/admin/${domain.id}` pattern in the `AdminSubLauncher` component's Link href to `/admin/${domain.id}`. Apply the same route override map:

    ```tsx
    import Link from 'next/link';
    // ...add route overrides...
    href={ADMIN_ROUTE_OVERRIDES[domain.id] ?? `/admin/${domain.id}`}
    ```

    Note: The AdminSubLauncher has a special case for 'users' (uses a button onClick instead of Link) — leave that as-is.

  </action>
  <verify>
    <automated>
      grep -c 'AdminLayer' src/app/\(tenant\)/admin/page.tsx && \
      grep -c "redirects()" next.config.mjs && \
      grep -c '/admin/' src/widgets/dashboard/ui/AdminLayer.tsx && \
      grep -c 'ADMIN_ROUTE_OVERRIDES' src/widgets/dashboard/ui/AdminLayer.tsx && \
      grep -c '/admin/' src/widgets/dashboard/ui/AdminSubLauncher.tsx
    </automated>
    <manual>
      grep 'redirects' next.config.mjs should show NO match
      grep '/dashboard/admin/' AdminLayer.tsx should show NO match
      AdminLayer.tsx DomainCard href should be "/admin/${domain.id}"  
      Restart dev server: navigate to /admin should show AdminLayer UI (command bar + domains + activity)
      Navigate to /admin/users, /admin/requests, /admin/groups — should show full CRUD pages, NOT 404
    </manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - next.config.mjs has NO redirects block
    - /admin renders AdminLayer (command bar + domain grid + activity stream)
    - Domain cards link to /admin/{domain} (with overrides for maintenance→requests, system→categories)
    - AdminSubLauncher links are updated
    - AdminLayer has a clean ADMIN_ROUTE_OVERRIDES map for domain→route mapping
  </done>
</task>

<task type="auto">
  <name>Task 2: Normalize AdminCommandBar and all widget links</name>
  <files>
    src/widgets/dashboard/ui/AdminCommandBar.tsx
    src/widgets/admin/ui/AdminStatsWidget.tsx
    src/widgets/admin/ui/AdminQuickLinksWidget.tsx
    src/widgets/admin/ui/SurveysWidget.tsx
    src/widgets/admin/ui/EventsWidget.tsx
    src/widgets/admin/ui/AdminContentWidget.tsx
    src/widgets/admin/ui/AdminAnnouncementsWidget.tsx
    src/widgets/maintenance/ui/MaintenanceRequestsWidget.tsx
    src/widgets/dashboard/ui/UserContentWidget.tsx
    src/widgets/dashboard/ui/QuickActionsWidget.tsx
  </files>
  <action>
    For each file, replace all `/dashboard/admin/*` link hrefs with `/admin/*`. Do NOT change links that are unrelated (e.g., /admin/surveys/${survey.id} is FINE as-is since it already uses /admin/).

    **AdminCommandBar.tsx** — 20+ link changes:
    - DEFAULT_SHORTCUTS (lines 58-95): 6 hrefs → change `/dashboard/admin/` to `/admin/`
    - EXTENDED_SHORTCUTS (lines 97-137): 6 hrefs → change `/dashboard/admin/` to `/admin/`
    - UrgencyChips (lines 307-337): 4 hrefs → change `/dashboard/admin/` to `/admin/`

    After updating, the file should have NO remaining `/dashboard/admin/` references.

    **AdminStatsWidget.tsx**: Lines 118, 126, 134, 142 — change `/dashboard/admin/` to `/admin/` for:
    - users, requests, groups, content

    **AdminQuickLinksWidget.tsx**: Lines 33, 40 — change `/dashboard/admin/content/new` to `/admin/content/new` and `/dashboard/admin/groups/new` to `/admin/groups/new`

    **SurveysWidget.tsx**: Lines 111, 166 — change `/dashboard/admin/surveys/new` to `/admin/surveys/new` and `/dashboard/admin/surveys` to `/admin/surveys`
    Note: Line 155 (`/admin/surveys/${survey.id}`) is already correct — leave it.

    **EventsWidget.tsx**: Lines 119, 158 — change `/dashboard/admin/events/new` to `/admin/events/new` and `/dashboard/admin/events` to `/admin/events`

    **AdminContentWidget.tsx**: Lines 116, 122 — change `/dashboard/admin/content` to `/admin/content` and `/dashboard/admin/content/new` to `/admin/content/new`

    **AdminAnnouncementsWidget.tsx**: Lines 131, 192 — change `/dashboard/admin/announcements` to `/admin/announcements`
    Note: Line 180 (`/admin/announcements`) is already correct — leave it.

    **MaintenanceRequestsWidget.tsx**: Lines 99, 108 — change `/dashboard/admin/requests` to `/admin/requests`

    **UserContentWidget.tsx**: Line 76 — change `/dashboard/admin/content/new` to `/admin/content/new`

    **QuickActionsWidget.tsx**: Line 30 — change `/dashboard/admin/content/new` to `/admin/content/new`

  </action>
  <verify>
    <automated>
      grep -c '/dashboard/admin/' src/widgets/dashboard/ui/AdminCommandBar.tsx && \
      grep -c '/dashboard/admin/' src/widgets/admin/ui/AdminStatsWidget.tsx && \
      grep -c '/dashboard/admin/' src/widgets/admin/ui/AdminQuickLinksWidget.tsx && \
      grep -c '/dashboard/admin/' src/widgets/admin/ui/SurveysWidget.tsx && \
      grep -c '/dashboard/admin/' src/widgets/admin/ui/AdminAnnouncementsWidget.tsx
    </automated>
    <manual>
      All grep commands above should return "0" — zero remaining /dashboard/admin/ references in these widget files
    </manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    Zero remaining `/dashboard/admin/` links in all widget files listed
  </done>
</task>

<task type="auto">
  <name>Task 3: Normalize old admin page links + dashboard space navigation + remaining references</name>
  <files>
    src/app/(tenant)/admin/events/[id]/page.tsx
    src/app/(tenant)/admin/events/new/page.tsx
    src/app/(tenant)/admin/resources/page.tsx
    src/app/(tenant)/admin/surveys/page.tsx
    src/app/(tenant)/admin/surveys/[id]/page.tsx
    src/shared/lib/navigation.ts
    src/features/onboarding/ui/steps/LaunchStep.tsx
  </files>
  <action>
    **1. Old admin event pages** — These files have mixed links (some /admin/events, some /dashboard/admin/events):
    
    - `admin/events/[id]/page.tsx`:
      - Line 58: `/dashboard/admin/events` → `/admin/events`
      - Lines 82-83: `/admin/...` breadcrumbs are already correct — leave them
      - Line 90: `/admin/events` is already correct — leave it
    
    - `admin/events/new/page.tsx`:
      - Lines 12-13: `/admin/...` breadcrumbs correct — leave them
      - Line 20: `/dashboard/admin/events` → `/admin/events`

    **2. Old admin resources page**:
    - `admin/resources/page.tsx`:
      - Line 10: Breadcrumb `/dashboard/admin` → `/admin`
      - Line 14: `/dashboard/admin/resources/new` → `/admin/resources/new`

    **3. Old admin surveys pages**:
    - `admin/surveys/page.tsx`:
      - Line 43: Breadcrumb `/admin` is correct — leave it
      - Line 51: `/dashboard/admin/surveys/new` → `/admin/surveys/new`
      - Line 63: `/dashboard/admin/surveys/new` → `/admin/surveys/new`
      - Line 109: `/admin/surveys/${survey.id}` is correct — leave it

    - `admin/surveys/[id]/page.tsx`:
      - Lines 269-270: `/admin/...` breadcrumbs correct — leave them
      - Line 292: `/dashboard/admin/surveys` → `/admin/surveys`

    **4. Update dashboard admin space navigation**:
    - `shared/lib/navigation.ts` line 69: Change the `dashboard-admin` nav item href from `/dashboard/admin` to `/admin`. This is the entry the SpaceLauncher sidebar uses to navigate to the admin space.

    **5. Onboarding launch step**:
    - `features/onboarding/ui/steps/LaunchStep.tsx` line 53: `/dashboard/admin` → `/admin`

    **⚠️ Do NOT touch** these `/dashboard/admin` links in these files (they're in the platform admin route group which is separate):
    - `features/admin/ui/BrandingForm.tsx:305`
    - `page-modules/admin/ui/TenantFeaturePage.tsx:50`
    - `page-modules/admin/ui/TenantBrandingPage.tsx:22`
    - `page-modules/admin/ui/PlatformAdminPage.tsx:130`
    - `app/(platform)/admin/platform/[id]/edit/components.tsx:341`

    These are tenant→platform cross-links that link to the separate platform admin. They're intentionally pointing to `/dashboard/admin/platform` which routes through the platform route group. These are a separate concern not in scope of this plan.

  </action>
  <verify>
    <automated>
      grep -c '/dashboard/admin/' src/app/\(tenant\)/admin/events/\[id\]/page.tsx && \
      grep -c '/dashboard/admin/' src/app/\(tenant\)/admin/surveys/page.tsx && \
      grep -c '/dashboard/admin/' src/app/\(tenant\)/admin/resources/page.tsx && \
      grep -c '/admin' src/shared/lib/navigation.ts | head -5
    </automated>
    <manual>
      All grep commands above should return "0" for /dashboard/admin/ references in tenant admin files
      navigation.ts dashboard-admin entry should have href: '/admin'
    </manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    Zero remaining `/dashboard/admin/` links in all tenant admin page files
    navigation.ts dashboard-admin href is `/admin`
    LaunchStep.tsx links to `/admin`
    Tenant→platform cross-links are preserved (separate concern, not changed)
  </done>
</task>

</tasks>

<verification>
1. **TypeScript check**: `npm run typecheck` must pass — all Link hrefs must match existing routes
2. **Build check**: `npm run build` must pass — no broken imports or references  
3. **Route check**: No `async redirects()` block remains in next.config.mjs
4. **Link audit**: Run `grep -r '/dashboard/admin/' src/app/\(tenant\)/` — should return ZERO matches
5. **Link audit**: Run `grep -r '/dashboard/admin/' src/widgets/` — should return ZERO matches (widgets that link to tenant admin)
6. **Runtime check**: /admin renders AdminLayer, /admin/groups renders groups page, /admin/requests renders requests page
</verification>

<success_criteria>

- [ ] `/admin` renders the AdminLayer (command bar with urgency chips + creation shortcuts, 9-domain grid, activity stream)
- [ ] `next.config.mjs` has NO redirects block
- [ ] All widget components link to `/admin/*` not `/dashboard/admin/*`
- [ ] Old admin pages (groups, households, categories, requests, etc.) are reachable at `/admin/*` without redirect
- [ ] Dashboard SpaceLauncher "Admin" space navigates to `/admin`
- [ ] `npm run typecheck` passes
- [ ] `npm run build` passes
- [ ] grep for `/dashboard/admin/` in `src/app/(tenant)/` and `src/widgets/` returns zero matches
      </success_criteria>

<output>
After completion, create `.planning/phases/37-admin-route-consolidation/37-01-SUMMARY.md`
</output>
