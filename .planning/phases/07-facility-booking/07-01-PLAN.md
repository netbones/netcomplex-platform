---
phase: 07-facility-booking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/bookings/page.tsx
  - src/app/api/bookings/route.ts
  - src/features/bookings/ui/BookingCalendar.tsx
  - src/features/bookings/ui/BookingForm.tsx
autonomous: true
requirements:
  - BOOKING-01
  - BOOKING-02
  - BOOKING-03

must_haves:
  truths:
    - Booking module hidden unless FACILITY_BOOKING feature flag enabled
    - Tenants configure their facilities
    - Users see calendar with availability
  artifacts:
    - path: src/app/bookings/page.tsx
      provides: Facility booking page with feature gate
      min_lines: 30
    - path: src/features/bookings/ui/BookingCalendar.tsx
      provides: Calendar view with availability
      min_lines: 50
    - path: src/features/bookings/ui/BookingForm.tsx
      provides: Booking form with facility/time selection
      min_lines: 50
  key_links:
    - from: src/app/bookings/page.tsx
      to: FeatureGate
      via: feature flag check
      pattern: FACILITY_BOOKING|FeatureGate
---

<objective>
**Implement facility booking as NetComplex module** — Feature flag gated, tenant-configurable, with calendar view and external API hooks. Build as installable module, not Soralia-specific.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-facility-booking/07-CONTEXT.md
@src/app/api/bookings/route.ts
@src/app/bookings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feature flag and tenant facility config</name>
  <files>src/app/bookings/page.tsx</files>
  <action>
Add module scaffolding:

1. Add FACILITY_BOOKING to FeatureGate:
   - Check src/entities/features/registry or similar
   - Add if not exists: `FACILITY_BOOKING = 'facility-booking'`

2. Wrap bookings page with FeatureGate:

   ```tsx
   <FeatureGate feature="FACILITY_BOOKING" fallback={null}>
     <BookingModule />
   </FeatureGate>
   ```

3. Add tenant facility config:
   - Check existing tenant settings structure
   - Add facilities[] array to tenant config
   - Each facility: { id, name, type, externalApiUrl? }

4. Update bookings page to read tenant facilities config
   - If tenant has no facilities configured, module stays hidden
     </action>
     <verify>grep -n "FACILITY_BOOKING\|FeatureGate" src/app/bookings/page.tsx</verify>
     <done>Booking module hidden unless feature flag enabled and tenant has configured facilities</done>
     </task>

<task type="auto">
  <name>Task 2: Create BookingCalendar component</name>
  <files>src/features/bookings/ui/BookingCalendar.tsx</files>
  <action>
Create calendar view component:

1. Create directory: src/features/bookings/ui/

2. Create BookingCalendar.tsx:
   - Use existing calendar pattern (check src/widgets for similar)
   - Show month view with booking indicators
   - Color-code: available (green), booked (red), maintenance (yellow)
   - Click date to see time slots

3. Fetch existing bookings from GET /api/bookings?date={month}
   - Filter by selected facility
   - Show on calendar

4. Create BookingCalendar.css or use tailwind classes
   </action>
   <verify>ls -la src/features/bookings/ui/BookingCalendar.tsx 2>/dev/null</verify>
   <done>Calendar shows bookings with availability color-coding</done>
   </task>

<task type="auto">
  <name>Task 3: Create BookingForm component</name>
  <files>src/features/bookings/ui/BookingForm.tsx</files>
  <action>
Create booking form component:

1. Create BookingForm.tsx:
   - Facility dropdown (from tenant config)
   - Date picker (from calendar selection)
   - Time slot picker (1-hour slots, filter booked)
   - Purpose textarea (optional)
   - Submit button

2. Submit to POST /api/bookings with:

   ```typescript
   {
     facility: selectedFacility,
     date: selectedDate,
     startTime: selectedSlot,
     endTime: calculateEnd(selectedSlot, duration),
     purpose: purposeText
   }
   ```

3. Show confirmation on success
   - Redirect to My Bookings or refresh calendar

4. Handle errors inline
   </action>
   <verify>grep -n "BookingForm" src/features/bookings/ui/ 2>/dev/null</verify>
   <done>Form submits booking, shows confirmation</done>
   </task>

</tasks>

<verification>
- [ ] Booking page hidden for Soralia (no facilities configured)
- [ ] Booking page visible when tenant enables module + configures facilities
- [ ] Calendar shows existing bookings
- [ ] Form creates new booking
- [ ] Availability check prevents double-booking
</verification>

<success_criteria>

- Facility booking module ready for tenants to enable
- Calendar view with availability
- External API hooks in place for San Marina, The Zone Gym integration
  </success_criteria>

<output>
After completion, create `.planning/phases/07-facility-booking/07-01-SUMMARY.md`
</output>
