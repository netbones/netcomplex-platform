---
phase: 27-tenant-config-and-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/settings/[key]/route.ts
  - src/features/onboarding/ui/steps/ModulesStep.tsx
  - src/features/onboarding/ui/steps/FacilitiesStep.tsx
  - src/features/onboarding/ui/steps/MaintenanceStep.tsx
  - src/features/onboarding/model/useOnboarding.ts
  - src/entities/booking/model/constants.ts
  - src/entities/booking/model/types.ts
  - src/entities/maintenance/model/constants.ts
  - src/shared/api/schemas.ts
  - src/app/api/bookings/route.ts
  - src/app/(tenant)/admin/bookings/page.tsx
  - prisma/schema.prisma
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
must_haves:
  truths:
    - 'Each tenant can define its own list of bookable facilities'
    - 'Each tenant can define its own list of maintenance request categories'
    - 'Onboarding wizard includes steps for configuring facilities and maintenance categories'
    - "Booking form dropdown shows only the tenant's configured facilities"
  artifacts:
    - path: 'src/features/onboarding/ui/steps/FacilitiesStep.tsx'
      provides: 'Onboarding step for tenant facility configuration'
      min_lines: 50
    - path: 'src/features/onboarding/ui/steps/MaintenanceStep.tsx'
      provides: 'Onboarding step for tenant maintenance category configuration'
      min_lines: 50
    - path: 'src/entities/booking/model/constants.ts'
      provides: 'Updated with preset facility options + tenant override loading'
      contains: 'PRESET_FACILITIES'
  key_links:
    - from: 'src/features/onboarding/ui/steps/FacilitiesStep.tsx'
      to: '/api/settings'
      via: 'saveStep with booking_facilities key'
      pattern: 'saveStep.*facilities'
    - from: 'src/app/api/bookings/route.ts'
      to: 'settings API'
      via: 'fetch tenant facility config from settings'
      pattern: 'booking_facilities|getSetting'
---

<objective>
Make booking facilities and maintenance categories tenant-configurable instead of hardcoded.
Purpose: Soralia Village only has "Meeting Room" and "Gardening Tools" for bookings and "water emergencies" for maintenance — the current hardcoded lists are wrong for them and will be wrong for every tenant. Each tenant needs to configure their own options during onboarding and edit them later in admin settings.
Output: Preset option catalogs, tenant settings for facilities/categories, onboarding steps, booking form reads tenant config, maintenance form reads tenant config.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/entities/booking/model/constants.ts
@src/entities/booking/model/types.ts
@src/features/maintenance/model/useMaintenanceForm.ts
@src/features/onboarding/ui/steps/ModulesStep.tsx
@src/features/onboarding/model/useOnboarding.ts
@src/shared/api/schemas.ts
@src/app/api/bookings/route.ts
@src/db/schema/settings.ts
</context>

<tasks>
<task type="auto">
<name>Task 1: Preset catalogs + tenant settings API for facilities and categories</name>
<files>
  src/entities/booking/model/constants.ts
  src/entities/booking/model/types.ts
  src/entities/maintenance/model/constants.ts
  src/features/maintenance/model/useMaintenanceForm.ts
  src/app/api/settings/[key]/route.ts
  src/shared/api/schemas.ts
</files>
<action>
1. Update `src/entities/booking/model/constants.ts`:
   - Add `PRESET_FACILITIES` array: catalog of all possible facility options with { value, label } format
     Presets: POOL (Swimming Pool), GYM (Gym/Fitness Center), COMMUNITY_CENTER (Community Center/Hall), TENNIS (Tennis Court), BBQ_AREA (BBQ Area), MEETING_ROOM (Meeting Room), GARDEN_PLOT (Garden Plot/Allotment), LAUNDRY (Laundry Room), PARKING (Parking Area), ROOFTOP (Rooftop Terrace), SPORTS_COURT (Sports Court), SAUNA (Sauna/Spa), PLAYGROUND (Playground), LIBRARY (Library/Reading Room), COWORKING (Co-working Space)
   - Keep existing FACILITIES, FACILITY_LABELS, VALID_FACILITIES as backward-compatible defaults
   - Add `TenantFacility` type: `{ value: string; label: string }` (not tied to Facility enum)
   - Add `DEFAULT_FACILITIES` — the 5 original ones (POOL, GYM, COMMUNITY_CENTER, TENNIS, BBQ_AREA) for backward compatibility
   - Change `Facility` type from union to `string` (since it's tenant-configurable, can't be a fixed union)

2. Create `src/entities/maintenance/model/constants.ts`:
   - Add `PRESET_CATEGORIES` array: catalog of all possible maintenance category options
     Presets: plumbing (Plumbing), electrical (Electrical), hvac (HVAC/Climate), structural (Structural), landscaping (Landscaping), common_area (Common Area), security (Security), water_emergency (Water Emergency), pest_control (Pest Control), elevator (Elevator/Lift), fire_safety (Fire Safety), noise (Noise Complaint), parking (Parking Issue), other (Other)
   - Add `TenantCategory` type: `{ value: string; label: string }`
   - Add `DEFAULT_CATEGORIES` — the 8 original ones for backward compat

3. Update `src/features/maintenance/model/useMaintenanceForm.ts`:
   - Remove hardcoded `categories` array (move to constants import)
   - Import `PRESET_CATEGORIES, DEFAULT_CATEGORIES` from the new constants file
   - Add `useTenantCategories()` hook that:
     a. Fetches `/api/settings?key=maintenance_categories`
     b. Returns the tenant's configured categories or falls back to DEFAULT_CATEGORIES
   - Update form to use `useTenantCategories()` for the category dropdown

4. Create or update `src/app/api/settings/[key]/route.ts`:
   - GET: Fetch a single setting by key for the current tenant (using withTenant())
   - PATCH: Upsert a setting value for the current tenant (require auth + hasPermission('admin'))
   - Settings are stored as JSON strings in the existing `Setting` table (key = 'booking_facilities' or 'maintenance_categories', value = JSON array of {value, label})

5. Update `src/shared/api/schemas.ts`:
   - Change `bookingSchema` facility field from `z.enum([...])` to `z.string().min(1)` since facilities are now dynamic per-tenant
   - Remove the hardcoded facility enum from bookingSchema

6. Update `src/entities/booking/model/types.ts`:

- Change `Facility` type from `'POOL' | 'GYM' | ...` to `string`
- Keep BookingFormData.facility as `string` instead of `Facility`

7. Update `prisma/schema.prisma`:

- Change the `Facility` enum to a `String` type for the Booking model's `facility` field
- Remove the `Facility` enum definition if no other models reference it
- Run `npx prisma db push` to apply the migration (column type changes from enum to text)
  </action>
  <verify>
  <automated>cd /home/ubuntupunk/Projects/soralia-village && pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  <manual>Preset catalogs exist for both facilities and categories. Settings API supports key-based CRUD. Facility type changed from enum to string throughout.</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>PRESET_FACILITIES and PRESET_CATEGORIES catalogs exist with 15 and 14 options respectively. Settings API supports GET/PATCH by key for tenant-specific configuration. Facility type changed from fixed enum to string. Maintenance form hook supports tenant-configurable categories.</done>
  </task>

<task type="auto">
<name>Task 2: Onboarding facility + maintenance config steps</name>
<files>
src/features/onboarding/ui/steps/FacilitiesStep.tsx
src/features/onboarding/ui/steps/MaintenanceStep.tsx
src/features/onboarding/model/useOnboarding.ts
src/features/onboarding/ui/steps/ModulesStep.tsx
</files>
<action>
1. Create `src/features/onboarding/ui/steps/FacilitiesStep.tsx`:
- OnboardingStep component showing PRESET_FACILITIES as checkboxes (multi-select)
- Each preset shows label + description, pre-checked if in DEFAULT_FACILITIES
- "Add Custom Facility" section at bottom: input for name, "Add" button
- Custom facilities appear in the list with an "X" remove button (presets can't be removed, only unchecked)
- On save: calls `saveStep(3, { booking_facilities: selectedFacilities })` which persists to the Setting table via the onboarding API
- Add as NEW step 3 between Modules (2) and Pages (4), shifting Pages→4, Invites→5, Launch→6

2. Create `src/features/onboarding/ui/steps/MaintenanceStep.tsx`:

- Same pattern as FacilitiesStep but for maintenance categories
- Shows PRESET_CATEGORIES as checkboxes
- "Add Custom Category" input at bottom
- On save: calls `saveStep(4, { maintenance_categories: selectedCategories })`
- This becomes step 4, shifting Pages→5, Invites→6, Launch→7

3. Update `src/features/onboarding/model/useOnboarding.ts`:

- Add `facilities` and `maintenanceCategories` fields to OnboardingFormData type
- Update step count from 5 to 7
- Update step numbering in saveStep calls

4. Update `src/features/onboarding/ui/steps/ModulesStep.tsx`:

- Add a note at the bottom: "You'll configure your facilities and maintenance categories in the next steps"
  </action>
  <verify>
  <automated>cd /home/ubuntupunk/Projects/soralia-village && pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  <manual>Onboarding wizard has 7 steps including Facilities and Maintenance config steps. Both steps show preset options with custom input.</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Onboarding wizard has FacilitiesStep and MaintenanceStep with preset options + custom input. useOnboarding updated to 7-step flow with facilities/maintenanceCategories fields. ModulesStep has teaser note about upcoming config steps.</done>
  </task>

<task type="auto">
<name>Task 3: Booking + maintenance forms read tenant config from settings</name>
<files>
src/app/api/bookings/route.ts
src/app/(tenant)/admin/bookings/page.tsx
</files>
<action>
1. Update `src/app/api/bookings/route.ts`:
- Remove `validFacilities` hardcoded array check (line 81)
- Instead, fetch tenant facilities from settings: `GET /api/settings?key=booking_facilities`
- Validate that the submitted facility value exists in the tenant's configured facilities
- If no tenant facilities configured, fall back to DEFAULT_FACILITIES
- Import from updated constants

2. Update booking form (wherever the facility dropdown is rendered) to:

- Fetch tenant facilities from settings on mount
- Render dropdown from tenant's configured facilities
- Fall back to DEFAULT_FACILITIES if no tenant config found
- Note: If the booking form is in `src/app/(tenant)/admin/bookings/page.tsx` or a booking component, update it there. Search for the facility select/dropdown component.
  </action>
  <verify>
  <automated>cd /home/ubuntupunk/Projects/soralia-village && pnpm exec tsc --noEmit --pretty 2>&1 | head -30</automated>
  <manual>Booking form dropdown shows tenant-specific facilities from settings API. Booking API validates against tenant's configured facilities.</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Booking API validates against tenant's configured facilities (not hardcoded enum). Booking form dropdown shows tenant-specific facilities from settings, falls back to DEFAULT_FACILITIES.</done>
  </task>
  </tasks>

<verification>
- Booking form shows tenant-configured facilities, not hardcoded list
- Maintenance form shows tenant-configured categories
- Onboarding wizard has facilities and maintenance config steps
- Settings API stores/retrieves booking_facilities and maintenance_categories
- TypeScript compiles with zero errors
</verification>

<success_criteria>

- Tenant can select from preset facilities/categories and add custom ones during onboarding
- Booking API validates against tenant-specific facilities, not hardcoded enum
- Maintenance category dropdown reads from tenant settings
- Backward compatibility: tenants without custom config fall back to defaults
  </success_criteria>

<output>
After completion, create `.planning/phases/27-tenant-config-and-gaps/27-01-SUMMARY.md`
</output>
