---
phase: 46.1-platform-saas-billing-foundation
plan: 01
subsystem: database
tags: [prisma, drizzle, postgresql, billing, schema, seed]

# Dependency graph
requires:
  - phase: 46-provider-platform
    provides: ProviderSubscription, PaymentTransaction, ProviderInvoice billing patterns
provides:
  - 10 billing domain tables in PostgreSQL
  - 5 billing enum types (TenantSubscriptionStatus, BillingEventType, BillingPlanInterval, CouponDiscountType, BillingAdjustmentType)
  - Drizzle schema files for all billing tables with relations
  - Idempotent default billing plan seed (STANDARD/PREMIUM/ENTERPRISE)
affects: [46.1-02 subscription lifecycle, 46.1-03 platform admin, 46.1-04 tenant portal]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 'TDD schema migration — RED test queries information_schema.tables before migration, GREEN validates 10 tables exist after'
    - 'Drizzle pgTable declarations follow existing provider-subscriptions.ts single-line pattern'
    - 'Seed function follow Phase 46 getOrCreateDefaultSubscriptionTiers idempotent pattern'
    - 'BillingPlan seed mirrors Phase 104 AI pool quota tiers (50k/200k/500k)'

key-files:
  created:
    - prisma/migrations/20260625143753_add_tenant_billing_models/migration.sql
    - src/db/schema/billing-plan.ts
    - src/db/schema/billing-plan-enums.ts
    - src/db/schema/tenant-subscription.ts
    - src/db/schema/tenant-invoice.ts
    - src/db/schema/tenant-payment.ts
    - src/db/schema/billing-adjustment.ts
    - src/db/schema/billing-adjustment-enums.ts
    - src/db/schema/billing-event.ts
    - src/db/schema/billing-event-enums.ts
    - src/db/schema/coupon.ts
    - src/db/schema/coupon-enums.ts
    - src/db/schema/coupon-redemption.ts
    - src/db/schema/tax-rate.ts
    - src/db/schema/tax-jurisdiction.ts
    - src/db/schema/billing-relations.ts
    - src/shared/lib/billing/seed-plans.ts
    - src/shared/lib/billing/index.ts
    - src/test/billing-migration.test.ts
  modified:
    - prisma/schema.prisma
    - src/db/schema/schema.ts
    - scripts/seed-drizzle.ts

key-decisions:
  - 'Used prisma db push instead of prisma migrate dev due to pre-existing shadow database Role enum drift (BD soralia-village-fjyw not yet resolved)'
  - 'Created 5 billing enums (not 4 as originally planned) — added BillingAdjustmentType (CREDIT/DEBIT/DISCOUNT) for type-safe adjustment queries'
  - 'Manually authored Drizzle files instead of using prisma-generator-drizzle output — plan-specified naming conventions differ from Prisma conventions'
  - 'seed-plans.ts idempotent via name-check — STANDARD/PREMIUM/ENTERPRISE plans created once per tenant, never duplicated'

patterns-established:
  - 'Pattern 1: TDD schema migration — vitest test queries information_schema.tables, runs RED (0 tables) then GREEN (10 tables) after prisma db push'
  - 'Pattern 2: Default billing plan seed — getOrCreateDefaultBillingPlans(db, tenantId) following Phase 46 tier seed pattern'

requirements-completed:
  - BILL-01

# Metrics
duration: 19 min
completed: 2026-06-25
---

# Phase 46.1 Plan 01: Platform SaaS Billing Foundation Schema Summary

**10 Prisma models + 5 enums + Drizzle schemas + idempotent default plan seed — billing domain foundation for tenant SaaS plans**

## Performance

- **Duration:** 19 min
- **Started:** 2026-06-25T12:33:05Z
- **Completed:** 2026-06-25T12:52:06Z
- **Tasks:** 3 (1 TDD, 2 auto)
- **Files modified:** 22 (19 created, 3 modified)

## Accomplishments

- 10 billing domain tables created in PostgreSQL: BillingPlan, TenantSubscription, TenantInvoice, TenantPayment, BillingAdjustment, BillingEvent, Coupon, CouponRedemption, TaxRate, TaxJurisdiction
- 5 PostgreSQL enum types: TenantSubscriptionStatus (6 values), BillingEventType (7 values), BillingPlanInterval, CouponDiscountType, BillingAdjustmentType
- Drizzle schemas authored with type-safe pgEnum + pgTable following existing provider billing conventions
- Default STANDARD/PREMIUM/ENTERPRISE plans seeded with correct AI quotas (50k/200k/500k) matching Phase 104 tiers
- Seed function is idempotent — re-running produces 3 plans per tenant, never duplicates

## Task Commits

Each task was committed atomically:

1. **Task 1 (TDD): Prisma Schema — 10 Models + 5 Enums + Migration** — `4f25fea4` (test/RED), `e599ae66` (feat/GREEN)
2. **Task 2: Drizzle Schema Files — 10 Tables + 4 Enums + Relations + Schema Re-exports** — `9d81e2bb` (feat)
3. **Task 3: Seed Default Billing Plans + Wire Into Seed Orchestrator** — `14153b88` (feat)

**Plan metadata:** (to be committed with STATE.md update)

_Note: Task 1 (TDD) has 2 commits — RED phase test + GREEN phase implementation. No REFACTOR needed._

## Files Created/Modified

- `prisma/schema.prisma` — Added 10 models (BillingPlan through TaxJurisdiction) + 5 enums + TaxJurisdiction.taxRates reverse relation
- `prisma/migrations/20260625143753_add_tenant_billing_models/migration.sql` — Full DDL: 5 enum types, 10 tables with FK constraints and indexes
- `src/db/schema/billing-plan.ts` — Drizzle pgTable for BillingPlan (17 columns, tierEnum, billingPlanIntervalEnum)
- `src/db/schema/billing-plan-enums.ts` — billingPlanIntervalEnum + tenantSubscriptionStatusEnum
- `src/db/schema/tenant-subscription.ts` — Drizzle pgTable for TenantSubscription (14 columns)
- `src/db/schema/tenant-invoice.ts` — Drizzle pgTable for TenantInvoice (16 columns)
- `src/db/schema/tenant-payment.ts` — Drizzle pgTable for TenantPayment (13 columns)
- `src/db/schema/billing-adjustment.ts` — Drizzle pgTable for BillingAdjustment (10 columns)
- `src/db/schema/billing-adjustment-enums.ts` — billingAdjustmentTypeEnum
- `src/db/schema/billing-event.ts` — Drizzle pgTable for BillingEvent (7 columns)
- `src/db/schema/billing-event-enums.ts` — billingEventTypeEnum
- `src/db/schema/coupon.ts` — Drizzle pgTable for Coupon (14 columns)
- `src/db/schema/coupon-enums.ts` — couponDiscountTypeEnum
- `src/db/schema/coupon-redemption.ts` — Drizzle pgTable for CouponRedemption (7 columns)
- `src/db/schema/tax-rate.ts` — Drizzle pgTable for TaxRate (9 columns)
- `src/db/schema/tax-jurisdiction.ts` — Drizzle pgTable for TaxJurisdiction (6 columns)
- `src/db/schema/billing-relations.ts` — Drizzle relations for all billing tables
- `src/db/schema/schema.ts` — Re-exports all new billing tables, enums, and relations (replaced auto-generated imports)
- `src/shared/lib/billing/seed-plans.ts` — getOrCreateDefaultBillingPlans() idempotent seed function
- `src/shared/lib/billing/index.ts` — Barrel export for billing helpers
- `scripts/seed-drizzle.ts` — Wired billing plan seed into tenant seeding loop
- `src/test/billing-migration.test.ts` — RED/GREEN test validating 10 tables exist in PostgreSQL

## Decisions Made

- Used `prisma db push` instead of `prisma migrate dev` due to pre-existing shadow database `Role` enum drift — migration SQL manually generated for version control
- Created 5 billing enums (added BillingAdjustmentType beyond the planned 4) for type-safe adjustment queries
- Manually authored Drizzle files matching plan-specified naming (e.g., `billing-plan.ts` vs auto-generated `billing-plans.ts`)
- seed-plans.ts uses name-based deduplication (not onConflict) for cross-DB compatibility

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Prisma shadow database Role enum drift prevented migrate dev**

- **Found during:** Task 1 (GREEN phase — migration)
- **Issue:** `prisma migrate dev` failed because shadow database didn't have Role enum; pre-existing migration `20260624000000_add_user_role_and_seat_lifecycle` couldn't apply
- **Fix:** Used `prisma db push` to sync schema directly, manually authored migration.sql for version control, generated Drizzle schemas via `prisma generate`
- **Files modified:** prisma/schema.prisma, prisma/migrations/20260625143753_add_tenant_billing_models/migration.sql
- **Verification:** `prisma validate` passes, 10 tables confirmed in PostgreSQL via `information_schema.tables` query, vitest GREEN test passes
- **Committed in:** e599ae66 (Task 1 GREEN commit)

**2. [Rule 2 - Missing Critical] Added BillingAdjustmentType enum (CREDIT/DEBIT/DISCOUNT)**

- **Found during:** Task 2 (Drizzle schemas)
- **Issue:** Plan mentioned BillingAdjustmentType in Task 2 Step 1 but didn't include it in Task 1's enum list; Prisma model used String field instead of enum
- **Fix:** Added `BillingAdjustmentType` enum to Prisma schema (5 total new enums) and corresponding Drizzle `billingAdjustmentTypeEnum` pgEnum
- **Files modified:** prisma/schema.prisma, src/db/schema/billing-adjustment-enums.ts, src/db/schema/billing-adjustment.ts
- **Verification:** Enum type confirmed in PostgreSQL via `SELECT typname FROM pg_type`, Drizzle schema compiles without errors
- **Committed in:** e599ae66 (Task 1) and 9d81e2bb (Task 2)

**3. [Rule 3 - Blocking] server-only import broke tsx seed execution**

- **Found during:** Task 3 (seed verification)
- **Issue:** `import 'server-only'` and `import { now } from '@api/server'` in seed-plans.ts caused `tsx` execution to throw "This module cannot be imported from a Client Component module"
- **Fix:** Removed `server-only` and `@api/server` imports; inlined `now()` as `() => new Date()`
- **Files modified:** src/shared/lib/billing/seed-plans.ts
- **Verification:** `pnpm db:seed -- --only=soralia` succeeds, 3 plans created, re-running remains idempotent (still 3 plans)
- **Committed in:** 14153b88 (Task 3 commit)

---

**Total deviations:** 3 auto-fixed (2 blocking, 1 missing critical)
**Impact on plan:** All fixes necessary for successful schema creation, Drizzle generation, and seed execution. No scope creep.

## Issues Encountered

- Shadow database drift with Role enum is a pre-existing condition (BD soralia-village-fjyw tracks the underlying issue). Worked around with `prisma db push` + manual migration SQL — does not block Phase 46.1. Future phases may also hit this; the root cause is that `prisma migrate dev` creates a fresh shadow DB from the current schema baseline, and the `ALTER TYPE "Role" ADD VALUE IF NOT EXISTS 'USER'` statement in migration `20260624000000` fails because `USER` already exists in the current schema.
- Full `tsc --noEmit` times out (>120s) on this project — scoped checks confirm no billing-related type errors introduced

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Billing domain models are ready for Plan 02 (checkout & subscription lifecycle)
- Default billing plans seeded with correct tier → AI quota mapping
- Drizzle schemas available for `@schema/billing-plan`, `@schema/tenant-subscription`, etc.
- Seed function ready for Plan 03 (admin) to create/update plans
- No blockers — Plan 02 can begin immediately

## TDD Gate Compliance

| Plan    | RED          | GREEN        | REFACTOR | Status |
| ------- | ------------ | ------------ | -------- | ------ |
| 46.1-01 | ✓ (4f25fea4) | ✓ (e599ae66) | —        | Pass   |

- RED test: `src/test/billing-migration.test.ts` — queries `information_schema.tables` for 10 billing tables, failed with 0 found before migration
- GREEN: 10 tables created, test passes with all 10 found
- No REFACTOR needed — schema is clean and matches plan specification

## Self-Check: PASSED

- [x] `src/test/billing-migration.test.ts` — vitest passes (10 tables confirmed)
- [x] `npx prisma validate` — passes
- [x] `SELECT count(*) FROM "BillingPlan"` — returns 3
- [x] `pnpm db:seed` — succeeds, idempotent (re-run produces 3 plans)
- [x] Commits exist: `4f25fea4`, `e599ae66`, `9d81e2bb`, `14153b88`

---

_Phase: 46.1-platform-saas-billing-foundation_
_Completed: 2026-06-25_
