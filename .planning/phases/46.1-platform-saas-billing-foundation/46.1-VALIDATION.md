# Phase 46.1: Platform SaaS Billing Foundation — Validation Strategy

**Phase:** 46.1-platform-saas-billing-foundation
**Date:** 2026-06-25
**Status:** Pending execution

## Test Framework

**Runner:** Vitest (configured in project)
**Coverage target:** 80% for billing business logic (tenant-billing.ts, helpers)

## Requirement → Test Map

| Requirement | Test                                                                                           | Type        |
| ----------- | ---------------------------------------------------------------------------------------------- | ----------- |
| BILL-01     | Schema migration applies cleanly — all models and enums present in DB                          | Migration   |
| BILL-02     | `getTenantBillingSnapshot()` returns subscription, invoices, payments for authenticated tenant | Integration |
| BILL-02     | `syncTierToTenant()` updates `tenants.tier` on subscription activation                         | Unit        |
| BILL-03     | Platform admin can list all subscriptions via `/api/admin/platform/billing/subscriptions`      | Integration |
| BILL-04     | Tenant can view their billing snapshot via `/api/tenant/billing/snapshot`                      | Integration |

## Sampling Rates

Per Phase 104's established `pool.ts` patterns:

- Quota checks: 100% sampling (every call logged via AiUsageEvent)
- Subscription events: 100% sampling (immutable BillingEvent stream)

## Wave 0 Gaps

- Payment gateway integration tests require Paystack/PayPal test keys (not committed)
- PDF invoice generation deferred — no PDF tests in scope
