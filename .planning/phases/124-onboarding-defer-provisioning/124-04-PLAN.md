---
phase: 124-onboarding-defer-provisioning
plan: 04
type: execute
wave: 4
depends_on:
  - 124-03
files_modified:
  - src/features/auth/ui/CommunitySetupForm.tsx
  - src/features/auth/ui/index.ts
  - src/app/(platform)/create-community/page.tsx
autonomous: false
requirements:
  - TENANT-04
user_setup: []

must_haves:
  truths:
    - 'Community-naming wizard (name + slug + plan) is a single form behind authentication'
    - 'Wizard is re-invocable from /home landing, account menu, and Setup Center'
    - 'Form submits to POST /api/platform/tenants with session cookie'
    - 'Slug conflict (409) shows inline error with retry'
    - 'On success, redirects to tenant dashboard'
  artifacts:
    - src/features/auth/ui/CommunitySetupForm.tsx (extracted community form)
    - src/app/(platform)/create-community/page.tsx (auth-gated page hosting the form)
  key_links:
    - "CommunitySetupForm submission: fetch('/api/platform/tenants', { credentials: 'include' })"
    - 'Route /create-community requires authenticated session (plan 124-05 guards the API)'
    - "Entry points: NullTenantLanding 'Create Community' CTA, account menu item, Setup Center link"
---

<objective>
Relocate the community-naming wizard (community name, subdomain, plan) from the sign-up flow to behind authentication. The same UI is re-served as a re-invocable, single-page form accessible from the null-tenant landing, account menu, and Setup Center.

Purpose: Separate identity creation (sign-up) from community creation (provisioning). Users can create their community at any time after verification — no one-shot step, no expiry.

Output: Reusable `CommunitySetupForm` component + `/create-community` route that calls the authenticated `POST /api/platform/tenants` (plan 124-05).
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-onboarding-defer-provisioning/124-CONTEXT.md
@.planning/phases/124-onboarding-defer-provisioning/124-RESEARCH.md
@.planning/phases/124-onboarding-defer-provisioning/124-PATTERNS.md
@.planning/phases/124-onboarding-defer-provisioning/124-UI-SPEC.md
@.planning/phases/124-onboarding-defer-provisioning/124-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CommunitySetupForm component (community name + slug + plan)</name>
  <files>
    src/features/auth/ui/CommunitySetupForm.tsx
    src/features/auth/ui/index.ts
  </files>
  <read_first>src/app/(platform)/signup/page.tsx:54-151 (old Step 1 community fields — being extracted); PATTERNS.md §10; src/features/auth/ui/SignupFormSection.tsx (form section wrapper); src/features/auth/ui/SignupCTA.tsx:44-67 (loading button pattern); UI-SPEC.md §4 (Relocated Community-Naming Wizard)</read_first>
  <action>
    Create `src/features/auth/ui/CommunitySetupForm.tsx` — a single-form component extracted from the community-details fields in the old sign-up Step 1 (PATTERNS.md §10). This is a client component.

    **Form fields:**
    - **Community Name:** Text input with label "Community Name", placeholder "e.g. Soralia Village HOA". Bound to `communityName` field. Same styling as signup page: `w-full px-4 py-3 border border-slate-300 rounded-lg focus:ring-2 focus:ring-indigo-500`.
    - **Subdomain:** Text input with `.netbones.co.za` suffix (same as old signup). Bound to `subdomain` field. Placeholder "soralia". Suffix styled `bg-slate-100 border border-l-0 border-slate-300 rounded-r-lg text-slate-500`.
    - **Plan Selection:** Radio card group fetching plans from `/api/pricing` (same as old signup lines 26-42: `useEffect` fetch, `setPlans`, `setPlansLoading`). Render plan cards with `border-2 rounded-lg p-4` — selected card has `border-indigo-600`, unselected has `border-slate-200`. Show plan name, price, features list per pricing API response.

    **Validation:** Use `communitySetupSchema` from `@entities/tenant` (created in plan 124-02 Task 2A). Use `useForm` from `react-hook-form` with `zodResolver` — same pattern as old signup.

    **Submission:** On submit, call `POST /api/platform/tenants` with `{ name, slug, plan }` + `credentials: 'include'` (sends session cookie). Per PATTERNS.md §10:
    ```
    const res = await fetch('/api/platform/tenants', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: data.communityName, slug: data.subdomain, plan: data.plan }),
      credentials: 'include',
    });
    if (!res.ok) {
      const err = await res.json();
      throw new Error(err.message || 'Failed to create community');
    }
    // On success, redirect to dashboard
    router.push('/dashboard');
    ```

    **Error handling:**
    - Slug conflict (409): Show error banner "This subdomain is already taken. Please choose another." — user can edit subdomain and re-submit.
    - Network error: Show error banner "Something went wrong creating your community. Please try again." + "If this persists, contact support." per UI-SPEC Copywriting.
    - Use the error banner pattern from signup page: `bg-red-50 border-red-200 text-red-700 px-4 py-3 rounded-lg mb-6`.

    **Loading state:** Button shows spinner + "Creating Community..." while submitting. Use the existing spinner pattern from `SignupCTA.tsx:44-67`.

    **Header:** No progress indicator. Instead: `<h2 className="text-3xl font-bold text-bark mb-2">Tell us about your community</h2>` with `<p className="text-lg text-slate-600">Set up your community's online presence</p>`.

    **Props:** The component is self-contained — no props needed. It handles its own form state, validation, submission, and redirect.

    **Wrapping:** Use `SectionLayout` wrapper (same as `SignupFormSection.tsx` pattern) — `size="lg"`, `className="flex-1"`. Inner container `max-w-2xl mx-auto`.

    Add barrel export: in `src/features/auth/ui/index.ts`, add `export * from './CommunitySetupForm';`.

  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>CommunitySetupForm exports from @features/auth/ui barrel, renders community name + subdomain + plan, submits to POST /api/platform/tenants with credentials, handles 409 + network errors, typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create /create-community route with auth gate</name>
  <files>src/app/(platform)/create-community/page.tsx</files>
  <read_first>src/app/(platform)/home/page.tsx (auth gate pattern from plan 124-03); PATTERNS.md §10 (CommunitySetupForm); UI-SPEC.md §4 (Relocated Wizard, §Caller contexts)</read_first>
  <action>
    Create `src/app/(platform)/create-community/page.tsx` — a client component that gates the CommunitySetupForm behind authentication.

    **Auth check pattern (same as /home from plan 124-03):**
    1. `const { data: session, isPending } = authClient.useSession()`.
    2. If `isPending`: render `LoadingSkeleton` (single card shape).
    3. If `!session`: redirect to `/sign-in`.
    4. If `session.user.tenantId !== null`: redirect to `/dashboard` (user already has a community).
    5. If `session.user.tenantId === null`: render `<CommunitySetupForm />`.

    **Page wrapper:** Use the same layout pattern as the signup page:
    ```
    <PageLayout background="fieldstone">
      <PlatformHeader variant="light" />
      <main className="flex-1 flex flex-col">
        <CommunitySetupForm />
      </main>
      <PlatformFooter />
    </PageLayout>
    ```

    Import `PageLayout`, `PlatformHeader`, `PlatformFooter` from existing paths (matching signup page imports). Import `CommunitySetupForm` from `@features/auth/ui` (Task 1 barrel export).

    **Entry points (wired in this task or documented for plan 124-06):**
    - `/home` NullTenantLanding "Create Community" CTA → `router.push('/create-community')` — this is wired in NullTenantLanding (plan 124-03 Task 1). Verify the `onClick` handler points to `/create-community`.
    - Account menu item: document for plan 124-06 cleanup task.
    - Setup Center entry: document for future integration (plan 124-06).

    This page just needs to exist and gate correctly; entry points are spread across plans as documented.

  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint 2&gt;&amp;1 | head -30</automated>
  </verify>
  <done>/create-community route gated behind auth: unauthenticated → /sign-in, existing tenant → /dashboard, null-tenant → CommunitySetupForm. Typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>CommunitySetupForm component extracted from old signup Step 1 + /create-community route with auth gate. The wizard is a single form (no multi-step) — community name, subdomain, plan selection. Submits to authenticated POST /api/platform/tenants.</what-built>
  <how-to-verify>
    1. Sign up (plan 124-02), verify email, land on /home (plan 124-03).
    2. Click "Create Community" → navigate to /create-community.
    3. Enter community name, subdomain, select a plan. Submit.
    4. Confirm: tenant is created (check DB), user.tenantId is set, role = ADMIN.
    5. After creation, confirm redirect to /dashboard.
    6. Test error: try a taken subdomain → confirm 409 error banner "already taken".
    7. Test auth: visit /create-community in incognito → confirm redirect to /sign-in.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                      | Description                                                                                                                                                                          |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Client→/create-community                      | Page is auth-gated; session token checked client-side via `authClient.useSession()`. API route (POST /api/platform/tenants) is the real enforcement — page gate is UX, not security. |
| CommunitySetupForm→POST /api/platform/tenants | Form body (name, slug, plan) validated by Zod before fetch; API re-validates with its own Zod schema.                                                                                |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                            | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                                  |
| ----------- | ---------------------- | ---------------------------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-124-04-01 | Spoofing               | Client-side auth gate bypass                         | low      | accept      | The client-side `authClient.useSession()` check is UX, not security. The real enforcement is plan 124-05's API route (session check + email-verified guard). Bypassing the page gate just shows a form that will fail on submit. |
| T-124-04-02 | Tampering              | Slug injection via form                              | medium   | mitigate    | Form validates slug against `communitySetupSchema` Zod regex before submission. API route validates again (plan 124-05). DB unique constraint is the backstop.                                                                   |
| T-124-04-03 | Elevation of Privilege | Already-provisioned user accessing /create-community | low      | accept      | Page redirects `tenantId !== null` users to `/dashboard`. If they somehow reach the form, the API route still succeeds (they'd create a second tenant — valid multi-tenant behavior, not a security issue).                      |
| T-124-04-SC | Tampering              | npm/pnpm installs                                    | high     | mitigate    | No new packages. RESEARCH.md Package Legitimacy Audit confirms all dependencies are installed and verified.                                                                                                                      |

</threat_model>

<verification>
- `pnpm typecheck` passes.
- `/create-community` gates on auth (session check + tenantId check).
- `CommunitySetupForm` submits to authenticated `POST /api/platform/tenants`.
</verification>

<success_criteria>

1. `CommunitySetupForm` renders community name, subdomain, plan fields.
2. `/create-community` redirects unauthenticated users to `/sign-in` and already-provisioned users to `/dashboard`.
3. Form submission creates tenant + redirects to dashboard.
4. Slug conflict shows inline error with retry.
5. Entry point from `/home` landing CTA → `/create-community` is wired.
   </success_criteria>

<output>
Create `.planning/phases/124-onboarding-defer-provisioning/124-04-SUMMARY.md` when done
</output>
