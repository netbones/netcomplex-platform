---
phase: 124-onboarding-defer-provisioning
plan: 03
type: execute
wave: 3
depends_on:
  - 124-01
files_modified:
  - src/app/(platform)/home/page.tsx
  - src/features/platform/ui/NullTenantLanding.tsx
  - src/features/platform/ui/index.ts
autonomous: false
requirements:
  - TENANT-03
user_setup: []

must_haves:
  truths:
    - 'Authenticated user with tenantId = null lands on /home and sees a two-card choice: Try Demo + Create Community'
    - 'Unauthenticated visitor to /home is redirected to /sign-in'
    - 'Authenticated user with tenantId != null is redirected to their dashboard'
    - 'Demo card is conditionally rendered behind feature flag (G2 unresolved)'
    - 'Loading state shows skeleton cards while session is checked'
  artifacts:
    - src/app/(platform)/home/page.tsx (auth-gated landing page)
    - src/features/platform/ui/NullTenantLanding.tsx (landing UI component)
  key_links:
    - 'authClient.useSession() gate: no session → /sign-in redirect'
    - 'session.user.tenantId !== null → tenant dashboard redirect'
    - 'session.user.tenantId === null → render NullTenantLanding'
    - "'enable-demo-tenant' feature flag gates demo card visibility (G2 assumption)"
---

<objective>
Build the post-verification null-tenant landing page at `/home`. Authenticated users with `tenantId: null` see a two-card choice: "Try a live demo" (conditionally feature-flagged per unresolved G2) and "Create a community." Unauthenticated visitors are redirected to sign-in; users with an existing tenant are redirected to their dashboard.

Purpose: Give verified, unprovisioned users a clear, non-blocking landing page where they can explore or create their community at their own pace — no expiry, no pressure.

Output: Auth-gated `/home` route with session check, tenantId check, and two-card landing UI per UI-SPEC.md §3.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-onboarding-defer-provisioning/124-CONTEXT.md
@.planning/phases/124-onboarding-defer-provisioning/124-RESEARCH.md
@.planning/phases/124-onboarding-defer-provisioning/124-PATTERNS.md
@.planning/phases/124-onboarding-defer-provisioning/124-UI-SPEC.md
@.planning/phases/124-onboarding-defer-provisioning/124-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NullTenantLanding component with two-card choice UI</name>
  <files>
    src/features/platform/ui/NullTenantLanding.tsx
    src/features/platform/ui/index.ts
  </files>
  <read_first>UI-SPEC.md §3 (/home — Null-Tenant Landing); UI-SPEC.md §Color, §Typography, §Copywriting; PATTERNS.md §9 (home page auth patterns); src/widgets/dashboard/ui/HomeLayer.tsx:601-613 (LoadingSkeleton pattern, tenant guard)</read_first>
  <action>
    Create `src/features/platform/ui/NullTenantLanding.tsx` — a client component that renders the two-card landing UI per UI-SPEC.md §3. Conforms to the Design System in UI-SPEC.md §Design System.

    **Layout:** Hero heading + subtext + two-card grid (2 columns on desktop, stack on mobile).

    **Hero:**
    - Heading: "Welcome to NetComplex" (text-4xl font-bold, centered, text-bark).
    - Subtext: "Your account is verified. What would you like to do next?" (text-lg text-slate-600, centered).
    - On return visit (detected via localStorage key `visited-landing`): heading changes to "Welcome back! Ready to create your community?" per UI-SPEC Copywriting.

    **Cards (2-column grid, full-width on mobile):**
    - **Demo card (left):** Lucide `Eye` icon in `bg-lapis-azure/10` circle container. Title: "Try a live demo". Body: "Explore a read-only community to see how NetComplex works before creating your own." CTA: outline/ghost button "Explore demo" (lapis-deep text, lapis-deep border). Card styling: `bg-white rounded-xl shadow-card border border-gold-vein/20` with 24px internal padding. This card is conditionally rendered behind a feature flag — use a prop `showDemo` (boolean, default false) so the parent page controls visibility per G2 assumption. When `showDemo=false`, the create card fills full width.

    - **Create card (right):** Lucide `Building2` icon in `bg-gold-vein/10` circle container. Title: "Create a community". Body: "Set up your own community with a custom subdomain, branding, and features." CTA: filled button "Create Community" (`bg-gold-vein text-white hover:bg-gold-vein/90`). Card styling: `bg-white rounded-xl shadow-card border border-lapis-azure/30` with 24px internal padding. CTA `onClick` → navigates to `/create-community` (route built in plan 124-04).

    **Sign-out link:** Below the cards, centered: "Sign out" link that calls `authClient.signOut()` (import from `@api/client` per PATTERNS.md §16).

    **Loading state:** Accept a `loading` prop. When true, render two `Skeleton` card shapes with `animate-pulse` (use the existing `LoadingSkeleton` component or simple div animation — match `HomeLayer.tsx:601` Skeleton pattern).

    **Props interface:**
    ```
    interface NullTenantLandingProps {
      loading?: boolean;
      showDemo?: boolean;
      isReturnVisit?: boolean;
    }
    ```

    Add barrel export: in `src/features/platform/ui/index.ts`, add `export * from './NullTenantLanding';`.

    Import `authClient` from `@api/client` (PATTERNS.md §16), `Eye` and `Building2` from `lucide-react`.

  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>NullTenantLanding component renders two cards (demo + create), sign-out link, loading skeleton state. Barrel export added.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /home page as auth-gated landing with session + tenantId checks</name>
  <files>src/app/(platform)/home/page.tsx</files>
  <read_first>src/app/(platform)/home/page.tsx (current — full file); PATTERNS.md §9 (auth pattern + guard pattern); src/app/(auth)/verify-email/page.tsx:4 (authClient import); UI-SPEC.md §3</read_first>
  <action>
    Rewrite `src/app/(platform)/home/page.tsx` as a client component with auth gating per PATTERNS.md §9. The existing page is a public marketing landing (HeroSection, MissionSection, etc.) — it should still serve as the UNAUTHENTICATED page (public visitors see marketing). But AUTHENTICATED users with `tenantId: null` should see the NullTenantLanding instead.

    **Auth flow:**
    1. Import `authClient` from `@api/client` and `NullTenantLanding` from `@features/platform` (already barrel-exported in Task 1).
    2. Use `const { data: session, isPending } = authClient.useSession()` to check auth state.
    3. **If `isPending`** (loading): render `<NullTenantLanding loading={true} />`.
    4. **If `!session`** (unauthenticated): render the existing public marketing page (HeroSection, MissionSection, FeaturesSection, CTASection). This preserves the public-facing marketing site.
    5. **If `session.user.tenantId !== null`** (already has a community): redirect to their dashboard — `router.push('/dashboard')`.
    6. **If `session.user.tenantId === null`** (null-tenant user): render `<NullTenantLanding showDemo={/* feature flag */} />`.

    **Feature flag for demo card (G2 assumption):** G2 (demo path — shared read-only tenant vs fully mocked) is unresolved per CONTEXT.md:95. Per UI-SPEC.md:249, the demo card renders behind a feature flag `enable-demo-tenant`. Since the flag system may not have this key yet, hardcode `showDemo={false}` for now with a comment: `// TODO(G2): wire to feature flag enable-demo-tenant when demo tenant exists`. The create card takes full width when demo is hidden — this is accounted for in NullTenantLanding.

    **Return-visit detection:** Check `localStorage.getItem('visited-landing')` before first render. Set it after first render (`localStorage.setItem('visited-landing', 'true')`). Pass `isReturnVisit` prop to NullTenantLanding.

    **Routing note (PATTERNS.md §15):** `/home` is already in `isPlatformRoute` (middleware.ts:100, line 732). The route is already a platform-plane route — `withTenant()` is NOT invoked here, so null-tenant users won't hit tenant resolution errors.

    **Keep existing imports:** `PlatformHeader`, `PlatformFooter`, `HeroSection`, `MissionSection`, `FeaturesSection`, `CTASection` — all preserved for the unauthenticated view.

    Use `useRouter` from `next/navigation` for redirects (not `redirect()` — client component).

  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint 2&gt;&amp;1 | head -30</automated>
  </verify>
  <done>/home route: unauthenticated → marketing page, tenantId != null → dashboard redirect, tenantId === null → NullTenantLanding, loading → skeleton. Typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Post-verification null-tenant landing at /home. Authenticated users with tenantId=null see two-card choice (Demo + Create Community). Unauthenticated visitors see the existing marketing page. Users with an existing tenant are redirected to their dashboard.</what-built>
  <how-to-verify>
    1. Sign up via the new 2-step wizard (plan 124-02 must be complete), verify email.
    2. After verification, confirm you land on /home — should show "Welcome to NetComplex" heading + two cards (demo card may be hidden per G2 flag).
    3. Click "Create Community" — should navigate to /create-community (built in plan 124-04; accept 404 if plan 124-04 is not yet complete).
    4. Visit /home in incognito (unauthenticated) — should show the marketing page (HeroSection, Features, etc.).
    5. Sign in as a user with an existing tenant — should redirect to /dashboard.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                                |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Unauthenticated→/home       | Public route — serves marketing page to visitors, no session required                                                      |
| Authenticated session→/home | Session token checked via `authClient.useSession()`; redirects based on tenantId state                                     |
| TenantId state→redirect     | `tenantId !== null` → dashboard redirect; `tenantId === null` → landing. No tenant-scoped data leaked to null-tenant users |

## STRIDE Threat Register

| Threat ID   | Category               | Component                                              | Severity | Disposition             | Mitigation Plan                                                                                                                                                                                                     |
| ----------- | ---------------------- | ------------------------------------------------------ | -------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-124-03-01 | Information Disclosure | `/home` leaking tenant data to null-tenant users       | low      | accept                  | The `/home` route is a platform-plane route — no `withTenant()` resolution, no tenant-scoped data queries. Null-tenant users see only the landing UI, never another tenant's data.                                  |
| T-124-03-02 | Elevation of Privilege | `tenantId !== null` → dashboard redirect bypass        | low      | accept                  | If a user somehow lands on `/home` with `tenantId` set, they're redirected to `/dashboard` — the correct behavior. No privilege escalation possible.                                                                |
| T-124-03-03 | Spoofing               | Demo tenant (G2 unresolved) access to real tenant data | medium   | accept (deferred to G2) | Demo card is hidden (`showDemo=false`) until G2 is resolved and a demo tenant exists. When enabled, the demo tenant must be read-only and tenant-isolated. This threat is explicitly deferred to the G2 resolution. |
| T-124-03-SC | Tampering              | npm/pnpm installs                                      | high     | mitigate                | No new packages. Existing dependencies verified in RESEARCH.md Package Legitimacy Audit.                                                                                                                            |

</threat_model>

<verification>
- `pnpm typecheck` passes.
- Unauthenticated → marketing page, null-tenant → landing, tenant-user → dashboard redirect.
- Demo card hidden behind feature flag per G2 assumption.
</verification>

<success_criteria>

1. Authenticated user with `tenantId: null` sees NullTenantLanding at `/home`.
2. Unauthenticated visitor sees marketing page at `/home`.
3. User with `tenantId !== null` is redirected to `/dashboard`.
4. Demo card is hidden (`showDemo=false`) until G2 resolved.
5. Loading state shows skeleton cards during session check.
   </success_criteria>

<output>
Create `.planning/phases/124-onboarding-defer-provisioning/124-03-SUMMARY.md` when done
</output>
