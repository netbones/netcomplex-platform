---
phase: 124-onboarding-defer-provisioning
plan: 06
type: execute
wave: 5
depends_on:
  - 124-02
  - 124-04
  - 124-05
files_modified:
  - src/features/auth/model/useSignupForm.ts
  - src/app/(platform)/signup/page.tsx
  - src/shared/api/auth.ts
  - src/features/auth/ui/index.ts
autonomous: false
requirements:
  - TENANT-06
user_setup: []

must_haves:
  truths:
    - 'No code path in sign-up creates a Tenant row'
    - 'Old community-details fields are removed from sign-up UI'
    - 'Unused imports (tenantConfig.defaultSlug as fallback, pricing plans in signup) are cleaned'
    - 'No dead code remains referencing eager-provisioning'
    - 'No reaper, TTL, or cron job is added (explicitly out of scope)'
  artifacts:
    - Clean sign-up wizard (no residual tenant-creation code)
    - Removed unused imports and dead references
    - Documented removal of eager-provisioning path
  key_links:
    - 'useSignupForm: no fetch to /api/platform/tenants'
    - 'signup/page.tsx: no communityName, subdomain, plan fields'
    - 'auth.ts: no remaining references to tenantConfig.defaultSlug in hook path'
    - 'No reaper/TTL added (ADVISORY-031 Option C explicitly rejects)'
---

<objective>
Retire the dead eager-provisioning code path — remove any residual references to tenant creation during sign-up, unused imports, and orphaned helper functions. No reaper/TTL (explicitly rejected per ADVISORY-031 Option C).

Purpose: Ensure no dead code remains that could mislead future developers or accidentally create tenants during sign-up. Clean separation: identity creation (sign-up) is fully decoupled from community creation (provisioning).

Output: Clean, lean sign-up flow with zero references to tenant creation. No reaper — null-tenant state is durable and indefinite by design.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/124-onboarding-defer-provisioning/124-CONTEXT.md
@.planning/phases/124-onboarding-defer-provisioning/124-RESEARCH.md
@.planning/phases/124-onboarding-defer-provisioning/124-PATTERNS.md
@.planning/phases/124-onboarding-defer-provisioning/124-UI-SPEC.md
@.planning/phases/124-onboarding-defer-provisioning/124-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove residual tenant-creation code from sign-up flow</name>
  <files>
    src/features/auth/model/useSignupForm.ts
    src/app/(platform)/signup/page.tsx
    src/shared/api/auth.ts
  </files>
  <read_first>src/features/auth/model/useSignupForm.ts (current post-plan-124-02 state); src/app/(platform)/signup/page.tsx (current post-plan-124-02 state); src/shared/api/auth.ts:243-265 (databaseHooks, post-plan-124-01 state); PATTERNS.md §7-8 (what was already changed)</read_first>
  <action>
    Audit and clean three files for any residual eager-provisioning code left over from plans 124-01 and 124-02:

    **A. src/features/auth/model/useSignupForm.ts — remove dead code:**
    - Verify no `fetch('/api/platform/tenants', ...)` call remains — this was replaced in 124-02 Task 2B with `authClient.signUp.email()`. If any stale comment or import references the old path, remove.
    - Remove `handleSubdomainChange` if it still exists (should have been removed in 124-02, confirm).
    - Remove unused imports: any reference to `signupSchema` that only included community fields (should be `identitySignupSchema` now).
    - Remove any stale TypeScript types referencing community fields (`communityName`, `subdomain`, `plan`) from the `defaultValues` or `SignupFormData` type usage.

    **B. src/app/(platform)/signup/page.tsx — remove dead code:**
    - Remove unused pricing plan state (`plans`, `setPlans`, `plansLoading`, `fetchPlans` useEffect) — plans were only used by the old Step 1 plan selector. The signup page no longer needs pricing data.
    - Remove the `fetch('/api/pricing')` import or inline call.
    - Remove any residual community-summary card code (old Step 3 summary — should have been removed in 124-02, confirm).
    - Remove `useEffect` import if it was only used for plan fetching.

    **C. src/shared/api/auth.ts — remove dead references:**
    - In the `user.create.before` hook (line 256): confirm the fallback is `tenant?.id ?? rawTenantId ?? null` (no `tenantConfig.defaultSlug`). This was changed in 124-01 Task 2 — verify it stuck.
    - If `tenantConfig.defaultSlug` is no longer referenced anywhere in auth.ts, consider removing the import (only if no other reference exists — check lines 87-93 email branding for its usage).
    - DO NOT remove `tenantConfig` itself — it may be used by email branding (line 117: `rawUser.tenantId as string | undefined`) and the `cookiePrefix` (line 215).

    **D. Verify across the phase:**
    - Run `rg 'POST /api/platform/tenants' src/features/auth/ src/app/\(platform\)/signup/ ` — should match ZERO results (the sign-up flow no longer touches this endpoint).
    - Run `rg 'tenantConfig\.defaultSlug' src/shared/api/auth.ts` — should match only in the `tenantId` additionalField definition? No — that was removed in 124-01. Verify zero results for `defaultSlug` in the hook body.
    - Run `pnpm typecheck` — confirm no unused variable warnings.

    **No reaper/TTL:** Per ADVISORY-031 Option C (explicitly rejected), do NOT add any reservation table, TTL column, cron job, or cleanup reaper. The null-tenant state is durable and indefinite by design.

  </action>
  <verify>
    <automated>pnpm typecheck && rg 'POST /api/platform/tenants' src/features/auth/ src/app/\(platform\)/signup/ --count && echo "---" && sed -n '240,265p' src/shared/api/auth.ts | rg 'tenantConfig\.defaultSlug' --count</automated>
  </verify>
  <done>Zero references to `POST /api/platform/tenants` in sign-up flow. Zero `tenantConfig.defaultSlug` references in the databaseHook. Unused pricing state removed from signup page. No reaper/TTL added. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Document entry points for re-invocable wizard and verify no dead exports</name>
  <files>src/features/auth/ui/index.ts</files>
  <read_first>src/features/auth/ui/index.ts (current barrel); PATTERNS.md §10 (CommunitySetupForm barrel); UI-SPEC.md §4 (caller contexts)</read_first>
  <action>
    **Entry point documentation (no code changes — audit only):**
    - `/home` NullTenantLanding "Create Community" CTA → `/create-community` — verified in plan 124-03 Task 1. Confirm the `onClick` handler.
    - Account menu "Create Community" item — add a comment/TODO in the account menu component (or in this plan's SUMMARY) noting the entry point for future wiring.
    - Setup Center — referenced in CONTEXT.md:88; document that this is a future integration, not part of Phase 124. The Setup Center's internal sections are owned by Phase 123 (CONTEXT.md:111).

    **Barrel export audit:**
    - `src/features/auth/ui/index.ts`: confirm `CommunitySetupForm` is exported (added in plan 124-04 Task 1).
    - Run `pnpm typecheck` to confirm no broken imports.
    - Check that old signup-step exports (if any were moved) don't break consumers. Since `useSignupForm` was modified in-place (not moved), no barrel change needed for the hook itself.

    **Final cross-plan verification:**
    - `pnpm lint` — confirm no unused variables/imports warnings from cleanup.
    - `pnpm typecheck` — must pass.
    - Run `rg 'eager.provision'` or `rg 'createTenant'` across `src/features/auth/` — verify no stale comments reference the old flow.

  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>Barrel exports clean, no broken imports, entry points documented. Typecheck + lint pass with zero warnings from phase-124 files.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------------- |
| N/A      | This is a cleanup plan — no new trust boundaries introduced. Existing boundaries from plans 124-01 through 124-05 remain. |

## STRIDE Threat Register

| Threat ID   | Category  | Component                                         | Severity | Disposition | Mitigation Plan                                                                                                                                                                                                   |
| ----------- | --------- | ------------------------------------------------- | -------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-124-06-01 | Tampering | Accidental tenant creation via residual dead code | low      | accept      | Cleanup removes all references to `POST /api/platform/tenants` from sign-up path. Risk is low since the endpoint is now authentication-gated (plan 124-05) — even if dead code called it, it would fail with 401. |
| T-124-06-SC | Tampering | npm/pnpm installs                                 | high     | mitigate    | No new packages. RESEARCH.md Package Legitimacy Audit confirms all dependencies are installed and verified.                                                                                                       |

</threat_model>

<verification>
- `pnpm typecheck` + `pnpm lint` pass with zero warnings from Phase 124 files.
- `rg 'POST /api/platform/tenants' src/features/auth/ src/app/(platform)/signup/` returns zero results.
- No reaper, TTL, or cron job added.
</verification>

<success_criteria>

1. Zero references to `POST /api/platform/tenants` in sign-up flow (`src/features/auth/`, `src/app/(platform)/signup/`).
2. Zero references to `tenantConfig.defaultSlug` in the `user.create.before` hook fallback.
3. Unused pricing-plan state removed from signup page.
4. `pnpm typecheck` + `pnpm lint` pass clean.
5. No reaper, TTL, or reservation table added.
   </success_criteria>

<output>
Create `.planning/phases/124-onboarding-defer-provisioning/124-06-SUMMARY.md` when done
</output>
