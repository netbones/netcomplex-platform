---
phase: 108-csos-export-package
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/dispute/ui/CSOSExportButton.tsx
  - src/entities/dispute/ui/__tests__/CSOSExportButton.test.tsx
autonomous: true
requirements:
  - DISPUTE-08
must_haves:
  truths:
    - 'CSOSExportButton downloads a PDF file (not JSON) when export succeeds'
    - 'Button extracts filename from Content-Disposition response header'
    - "Button shows loading spinner with 'Exporting...' text during fetch"
    - 'Button shows error toast on 429 (rate limited) and disables itself'
    - 'Button shows success toast and increments local export count on success'
    - 'Button is disabled with tooltip when local export count >= 3'
    - "Button aria-label updated from 'JSON' to 'PDF'"
  artifacts:
    - path: 'src/entities/dispute/ui/CSOSExportButton.tsx'
      provides: 'Updated button with binary PDF download'
      contains: 'res.blob()'
    - path: 'src/entities/dispute/ui/__tests__/CSOSExportButton.test.tsx'
      provides: 'Component tests for binary download behavior'
      min_lines: 80
  key_links:
    - from: 'CSOSExportButton.tsx'
      to: '/api/disputes/[id]/csos-export'
      via: 'fetch then res.blob()'
      pattern: "res\\.blob\\(\\)"
    - from: 'CSOSExportButton.tsx'
      to: 'Content-Disposition header'
      via: "res.headers.get('Content-Disposition')"
      pattern: 'Content-Disposition.*filename'
---

<objective>
Update the CSOSExportButton component (built in Phase 105, composed in Phase 107-04) to handle binary PDF downloads instead of JSON blob downloads. The button currently calls `res.json()` and creates a JSON file — Phase 108 changes the route to return `application/pdf`, so the button must use `res.blob()`, extract the filename from Content-Disposition, and trigger a proper PDF file download.

Purpose: Complete the client side of the CSOS export package. The server route now returns certified PDF — the button must correctly download it as a .pdf file.
Output: Updated CSOSExportButton component with binary PDF download handling. Component test verifying download behavior.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-csos-export-package/108-RESEARCH.md
@src/entities/dispute/ui/CSOSExportButton.tsx

# Understanding existing patterns — component test patterns

@src/entities/dispute/ui/**tests**/
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update CSOSExportButton for binary PDF download</name>
  <files>src/entities/dispute/ui/CSOSExportButton.tsx</files>
  <behavior>
    - Test 1: Button calls fetch with correct URL, handles binary PDF response (res.blob()), creates ObjectURL, triggers download
    - Test 2: Button extracts filename from Content-Disposition header — "attachment; filename=\"csos-export-DSP-2026-0001.pdf\"" → download filename is "csos-export-DSP-2026-0001.pdf"
    - Test 3: On 429 response, button shows error toast, sets exportCount to MAX_EXPORTS_PER_DAY (3), does NOT call res.blob()
    - Test 4: On non-ok non-429 response (e.g., 403), button attempts res.json() for error body, shows error toast, does NOT download
    - Test 5: On successful download, exportCount increments by 1 and success toast shown
    - Test 6: When exportCount >= 3, button is disabled with title "Daily export limit reached (3 per day)"
    - Test 7: During fetch, button shows spinner and "Exporting..." text, is not disabled (unless exportCount limit reached)
    - Test 8: Download uses URL.createObjectURL + hidden <a> click + URL.revokeObjectURL cleanup
  </behavior>
  <action>
    Update src/entities/dispute/ui/CSOSExportButton.tsx per RESEARCH.md §Code Examples — CSOSExportButton Binary Download Update.

    **Core changes (handleExport function):**
    - Replace `const json = await res.json();` (line 41) + JSON blob creation (lines 42-53) with binary PDF download:
    ```
    // Binary PDF download (was: JSON blob)
    const blob = await res.blob();
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;

    // Extract filename from Content-Disposition header
    const disposition = res.headers.get('Content-Disposition');
    const filenameMatch = disposition?.match(/filename="?(.+?)"?$/);
    a.download = filenameMatch?.[1] ?? `csos-export-${disputeId}.pdf`;

    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(url);
    ```
    - The `json.data ?? json` envelope unwrap (line 42) is no longer needed — binary response has no envelope.

    **Button text and accessibility:**
    - Update aria-label from `"Export dispute case as CSOS JSON"` to `"Export dispute case as CSOS PDF"` (line 75)
    - Update title (lines 72-73): change "JSON" references to "PDF" — the file name shown in tooltip should say "PDF" not "JSON"

    **Maintain existing behaviors (do NOT change):**
    - 429 handling: `res.status === 429` branch stays. Sets `setExportCount(MAX_EXPORTS_PER_DAY)` and shows error toast. Does NOT call `res.blob()` (avoid parsing binary error body).
    - Non-ok error handling: `!res.ok` branch stays. Attempts `res.json()` for error message, throws with `body.error?.message || 'Export failed'`. The error response from the route is JSON (the route returns `apiError` for auth/access/rate-limit errors — only the 200 response is PDF).
    - Loading state: `setExporting(true)` at start, `setExporting(false)` in finally.
    - Toast lifecycle: `toast.loading('Generating CSOS export...')` → dismiss on completion/error.
    - Local export count: `exportCount` state + `isDisabled` logic remains (client-side UX convenience, server-side is authoritative).
    - Export count toast: `toast.success('CSOS export downloaded')` on success.
    - CSV icon SVG: keep the existing download arrow icon.

    **Files NOT modified:** CSOSExportButton props interface (`{ disputeId, userId }`) stays the same. No prop changes. The `userId` prop is unused in the current component — leave it as-is (it was added in Phase 105 for future use and doesn't cause issues).

  </action>
  <verify>
    <automated>MISSING — Wave 0 must create src/entities/dispute/ui/__tests__/CSOSExportButton.test.tsx first. After test creation: `npx vitest run src/entities/dispute/ui/__tests__/CSOSExportButton.test.tsx`</automated>
  </verify>
  <done>CSOSExportButton downloads PDF files (not JSON). Filename extracted from Content-Disposition header. All button states (loading, success, error, rate-limited, disabled) work correctly. aria-label updated to "PDF". Component test file created and passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CSOSExportButton component tests</name>
  <files>src/entities/dispute/ui/__tests__/CSOSExportButton.test.tsx</files>
  <behavior>
    - Test 1: Successful download — mock fetch returns 200 with application/pdf blob + Content-Disposition header. Verify: download triggered with correct filename, success toast shown, export count incremented
    - Test 2: Rate limited (429) — mock fetch returns 429. Verify: error toast shown, exportCount set to 3 (MAX), no blob/download attempted
    - Test 3: Auth error (403) — mock fetch returns 403 with JSON error body. Verify: error toast with message, no download
    - Test 4: Disabled state — render with exportCount=3. Verify: button has disabled attribute, title includes "Daily export limit reached"
    - Test 5: Loading state — during fetch, verify spinner rendered with "Exporting..." text
    - Test 6: Download cleanup — after successful download, verify URL.revokeObjectURL called with the created ObjectURL
    - Test 7: Fallback filename — when Content-Disposition header is missing, filename falls back to "csos-export-{disputeId}.pdf"
  </behavior>
  <action>
    Create src/entities/dispute/ui/__tests__/CSOSExportButton.test.tsx using vitest + @testing-library/react (already in project devDependencies).

    **Setup:**
    - Use `vi.mock('sonner')` to mock the toast module: `toast.loading`, `toast.dismiss`, `toast.error`, `toast.success`
    - Mock `URL.createObjectURL` and `URL.revokeObjectURL` via `vi.stubGlobal` or `vi.spyOn(URL, 'createObjectURL').mockReturnValue('blob:mock-url')` — restore in afterEach
    - Mock `document.createElement('a')` to spy on `click()` and verify `download` attribute
    - Use `vi.stubGlobal('fetch', vi.fn())` to mock the global fetch
    - Import and render CSOSExportButton with test props: `{ disputeId: 'test-dispute-123', userId: 'user-456' }`

    **Test patterns (use @testing-library/react):**
    - Render: `render(<CSOSExportButton disputeId="test-dispute-123" userId="user-456" />)`
    - Click: `fireEvent.click(screen.getByRole('button'))`
    - Assertions: `expect(fetch).toHaveBeenCalledWith('/api/disputes/test-dispute-123/csos-export')`
    - Blob mock: `new Blob(['%PDF-1.4 mock pdf content'], { type: 'application/pdf' })`
    - Response mock: `{ status: 200, ok: true, blob: async () => mockBlob, headers: new Headers({ 'Content-Disposition': 'attachment; filename="csos-export-DSP-2026-0001.pdf"' }) }`
    - For 429: `{ status: 429, ok: false, blob: async () => { throw new Error('should not call blob') } }`
    - For error body: `{ status: 403, ok: false, json: async () => ({ error: { message: 'Forbidden' } }) }`

    **Test file must not import server-only modules.** The CSOSExportButton is a 'use client' component that only uses browser APIs (fetch, URL.createObjectURL, document.createElement). The test should test pure client behavior. No @api/server mocks needed.

    **Cleanup:** In afterEach, restore all mocks: `vi.restoreAllMocks()`, `URL.createObjectURL` and `URL.revokeObjectURL` globals, and clear any appended DOM elements.

    Minimum 7 tests (one per behavior above), with descriptive test names using the pattern: `it('downloads PDF file with correct filename on success', async () => { ... })`

  </action>
  <verify>
    <automated>npx vitest run src/entities/dispute/ui/__tests__/CSOSExportButton.test.tsx</automated>
  </verify>
  <done>All 7 component tests pass. CSOSExportButton test file created with tests covering: successful PDF download, rate limit handling, auth error, disabled state, loading state, URL cleanup, fallback filename.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary            | Description                                                                         |
| ------------------- | ----------------------------------------------------------------------------------- |
| Client → API        | fetch() call to /api/disputes/[id]/csos-export — browser handles CORS, cookies      |
| API Response → Blob | Binary PDF blob created in browser memory from response                             |
| Blob → Download     | ObjectURL created for blob — must be revoked after download to prevent memory leaks |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                          |
| --------- | ---------------------- | ---------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-108-10  | Information Disclosure | CSOSExportButton (object URL)      | mitigate    | `URL.revokeObjectURL(url)` called after download completes. Object URL does not persist beyond page lifetime.                                            |
| T-108-11  | Denial of Service      | CSOSExportButton (repeated clicks) | mitigate    | Button disabled during fetch (`exporting` state). Client-side counter disables after 3 exports. Server-side rate limiting is authoritative (3/case/day). |
| T-108-12  | Spoofing               | CSOSExportButton (filename)        | mitigate    | Filename extracted from server's Content-Disposition header — not client-controlled. Fallback uses `disputeId` from props (not user input).              |
| T-108-13  | Information Disclosure | CSOSExportButton (error handling)  | mitigate    | On non-ok responses, error message from server JSON body displayed via toast. No raw PDF or server internals leaked.                                     |

</threat_model>

<verification>
Run component tests:
```bash
npx vitest run src/entities/dispute/ui/__tests__/CSOSExportButton.test.tsx
```
All 7 tests must pass. Manual verification: in a running dev server, navigate to a dispute detail page, click "Export for CSOS", verify a .pdf file downloads with proper content.
</verification>

<success_criteria>

- [ ] CSOSExportButton calls `res.blob()` instead of `res.json()` for the PDF response
- [ ] Filename extracted from Content-Disposition header — uses fallback "csos-export-{disputeId}.pdf" if header missing
- [ ] Object URL created for blob and revoked after download
- [ ] Hidden `<a>` element triggers download via click()
- [ ] 429 handling unchanged — error toast, client counter set to max
- [ ] Non-ok error handling unchanged — reads JSON error body, shows toast
- [ ] Loading spinner with "Exporting..." text during fetch
- [ ] aria-label updated to say "PDF" instead of "JSON"
- [ ] Button tooltip updated to say "PDF" instead of "JSON"
- [ ] All 7 component tests pass
- [ ] No TypeScript errors in modified files
      </success_criteria>

<output>
Create `.planning/phases/108-csos-export-package/108-02-SUMMARY.md` when done
</output>
