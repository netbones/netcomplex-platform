---
phase: 40-maintenance-ticketing
plan: 03
type: execute
wave: 2
depends_on: [40-02]
files_modified:
  - src/app/(tenant)/admin/requests/page.tsx
  - src/entities/maintenance/ui/MaintenanceCard.tsx
  - src/entities/maintenance/ui/StatusBadge.tsx
  - src/features/maintenance/ui/MaintenanceStatusFilter.tsx
  - src/features/maintenance/model/useMaintenanceFilter.ts
  - src/widgets/maintenance/index.ts
autonomous: true
requirements: MAINT-TICKET-04, MAINT-TICKET-06
user_setup: []
must_haves:
  truths:
    - 'Admin can add/edit/remove maintenance categories inline on the requests page'
    - 'Each request card displays ticket number prominently'
    - 'Detail drawer shows assignment panel with team/provider dropdowns'
    - "Detail drawer has 'Hand off to provider' action with reason field"
    - 'Detail drawer shows visual progress timeline of status changes'
    - 'Status workflow controls show only valid transitions from current state'
    - 'Teams, providers, and categories are fetched from new API endpoints'
  artifacts:
    - path: 'src/app/(tenant)/admin/requests/page.tsx'
      provides: 'Updated admin requests page with all new UI'
      min_lines: 900
    - path: 'src/entities/maintenance/ui/MaintenanceCard.tsx'
      provides: 'Updated card showing ticket number'
  key_links:
    - from: 'admin/requests/page.tsx'
      to: '/api/maintenance/teams'
      via: 'fetch for team dropdown'
    - from: 'admin/requests/page.tsx'
      to: '/api/maintenance/providers'
      via: 'fetch for provider dropdown'
    - from: 'admin/requests/page.tsx'
      to: '/api/maintenance/categories'
      via: 'fetch for category management'
---

<objective>
**Admin UI — inline category management, assignment panel, progress timeline, ticket display**

Purpose: Update the admin requests page with all new ticketing UI features — inline category management, assignment to teams/providers, progress timeline, ticket number display, and workflow controls. This makes the admin page a proper ticket management interface.

Output: Updated admin requests page with full ticketing UI.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-maintenance-ticketing/40-CONTEXT.md
@.planning/phases/40-maintenance-ticketing/40-02-SUMMARY.md
@src/app/(tenant)/admin/requests/page.tsx
@src/entities/maintenance/ui/MaintenanceCard.tsx
@src/entities/maintenance/ui/StatusBadge.tsx
@src/features/maintenance/ui/MaintenanceStatusFilter.tsx
@src/features/maintenance/model/useMaintenanceFilter.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Update MaintenanceCard and StatusBadge for ticket info</name>
<files>
  src/entities/maintenance/ui/MaintenanceCard.tsx
  src/entities/maintenance/ui/StatusBadge.tsx
  src/entities/maintenance/index.ts
</files>
<action>
**MaintenanceCard.tsx:**
- Add `ticketNumber` display at the top right of the card (small monospace text like `SRV-2026-0001`)
- Add `assignedTeam?.name` and `assignedProvider?.companyName` display when present (small text below category)
- Use a grid layout to show: LEFT = category + description + user info, RIGHT = ticket number + status + priority
- Keep the `onClick` and `showUser` props interface — maintain backward compatibility
- Ensure the card still renders without ticketNumber (graceful fallback for old data)

**StatusBadge.tsx:**

- Already has all 7 statuses with correct colors — verify existing config covers all
- No changes needed unless colors need adjustment for the new statuses

**entities/maintenance/index.ts:**

- Export any new types needed by consumers (MaintenanceTeam, ServiceProvider, etc.)
  </action>
  <verify>tsc --noEmit 2>&1 | head -20</verify>
  <done>MaintenanceCard shows ticket number and assignment info. StatusBadge covers all 7 statuses.</done>
  </task>

<task type="auto">
<name>Task 2: Update admin requests page with ticketing UI</name>
<files>src/app/(tenant)/admin/requests/page.tsx</files>
<action>
Rewrite the admin requests page (`src/app/(tenant)/admin/requests/page.tsx`) with these additions. Keep the existing structure (search, filters, list, detail drawer) but enhance each section:

**1. Data fetching — add new API calls alongside existing:**

```typescript
// In the fetch functions area (around line 200-213), add:
const [teamsRes, providersRes, categoriesRes] = await Promise.all([
  fetch('/api/maintenance/teams'),
  fetch('/api/maintenance/providers'),
  fetch('/api/maintenance/categories'),
]);
// Store in state: teams, providers, categories
```

**2. Ticket number display in list items (line ~470-524):**

- Each request card in the list shows `ticketNumber` prominently (e.g., `#SRV-2026-0001`)
- If missing (old data), show a fallback like `#---`

**3. Inline category management (add near the category filter section, around line ~413-440):**

- Add a small "Manage Categories" button next to the category filter pills
- When clicked, opens an inline expandable section (not a modal) with:
  - List of current categories with edit/delete buttons
  - "Add Category" form: value (slug), label, description fields
  - Uses fetch to POST/PATCH/DELETE `/api/maintenance/categories`
  - After changes, refresh the category list state
- Keep it compact — collapsible, not taking too much space

**4. Detail drawer assignment panel (around line ~654-701):**
Replace the current "Assigned To" select (line 663-674) with a proper assignment section:

- **In-house Team dropdown** — populated from `/api/maintenance/teams` response, shows team name + trade
- **Service Provider dropdown** — populated from `/api/maintenance/providers`, shows company name + trade
- Both can be set independently (either/both/none)
- When changing, call `POST /api/maintenance/[id]/assign` with `{ teamId, providerId }`

**5. "Hand off to Provider" action (add below the assignment section):**

- Button labeled "Hand off to Provider" — only visible when a team is currently assigned
- When clicked, shows a text field for "Reason for handoff" plus a provider selector
- On submit: calls assign endpoint with `{ providerId, reason, teamId: null }` (unassigns team, assigns provider)
- Creates a history entry recording the handoff reason

**6. Progress timeline in detail drawer (add between Cost Tracking and Images sections, around line ~745):**

- Shows a vertical timeline of all status changes for this request
- Each entry: status icon, status label, date/time, who changed it
- Data source: `/api/maintenance/[id]/history` (already fetched)
- Filter history entries to only `field === 'status'` for the timeline
- Style like a GitHub commit timeline: vertical line with dots at each change
- Color-code each status dot to match the StatusBadge colors

**7. Status workflow controls (in the detail drawer status section, line ~578-590):**

- Replace the simple status dropdown with a workflow-aware control
- Show only valid next statuses based on current state:
  - SUBMITTED → ASSIGNED, CANCELLED
  - ASSIGNED → SCHEDULED, IN_PROGRESS, CANCELLED
  - SCHEDULED → IN_PROGRESS, CANCELLED
  - IN_PROGRESS → PENDING_PARTS, COMPLETED, CANCELLED
  - PENDING_PARTS → IN_PROGRESS, CANCELLED
  - COMPLETED → (terminal)
  - CANCELLED → (terminal)
- Use a row of labeled buttons rather than a dropdown for valid transitions
- Disabled/visual-only indication for invalid transitions
- On status change: call PATCH, update local state, refresh history for timeline

Keep all existing functionality (search, filters, images, notes, cost tracking, email notifications). The file will grow but stay under ~1100 lines.
</action>
<verify>tsc --noEmit 2>&1 | head -30</verify>
<done>Admin requests page shows ticket numbers, inline category management, assignment panel, progress timeline, and workflow-aware status controls.</done>
</task>

<task type="auto">
<name>Task 3: Update filter components for new statuses</name>
<files>
  src/features/maintenance/ui/MaintenanceStatusFilter.tsx
  src/features/maintenance/model/useMaintenanceFilter.ts
</files>
<action>
**MaintenanceStatusFilter.tsx:**
- Verify all 7 statuses appear as filter pills (they should already — all 7 were in the UI before DB supported them)
- Add a "Ticket Number" search field alongside the existing text search
- Ensure the filter sends correct params to the API

**useMaintenanceFilter.ts:**

- Remove the locally-duplicated `MaintenanceStatus` type — import from `@entities/maintenance` instead
- Ensure filter logic supports all 7 statuses for toggle behavior
- Add `ticketNumber` to the filter state interface

**widgets/maintenance/index.ts:**

- Export any new components that should be available to consumers
  </action>
  <verify>tsc --noEmit 2>&1 | head -20</verify>
  <done>Filter components support all 7 statuses and ticket number search. Type duplication resolved.</done>
  </task>

</tasks>

<verification>
1. Admin requests page loads with all existing data
2. Ticket numbers visible on each request card
3. Inline category management adds/edits/removes categories
4. Assignment panel shows teams and providers
5. "Hand off to provider" creates reassignment
6. Progress timeline shows status change history
7. Status workflow buttons show only valid transitions
8. TypeScript compiles
</verification>

<success_criteria>

- Ticket numbers displayed on all request cards
- Inline category management works end-to-end
- Assignment to team/provider works
- Handoff flow works with history tracking
- Progress timeline visible in detail drawer
- Status workflow respects lifecycle constraints
  </success_criteria>

<output>
After completion, create `.planning/phases/40-maintenance-ticketing/40-03-SUMMARY.md`
</output>
