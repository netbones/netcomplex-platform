---
phase: 40-maintenance-ticketing
plan: 04
type: execute
wave: 2
depends_on: [40-02]
files_modified:
  - src/page-modules/maintenance/ui/MaintenancePage.tsx
  - src/widgets/dashboard/ui/HomeLayer.tsx
  - src/entities/maintenance/model/types.ts
  - prisma/seed.ts
  - scripts/seed-drizzle.ts
autonomous: true
requirements: MAINT-TICKET-05, MAINT-TICKET-08, MAINT-TICKET-12
user_setup: []
must_haves:
  truths:
    - "User's /maintenance page shows ticket numbers, status, assigned team/provider"
    - "User's HomeLayer activity zone shows maintenance status changes for their requests"
    - '7 seed maintenance requests exist in the database with realistic data'
    - 'Seed requests cover all statuses and assignment scenarios'
    - 'Data can be verified via API without crashing'
  artifacts:
    - path: 'prisma/seed.ts'
      provides: '7 maintenance request seed records'
      min_lines: 100 (maintenance section)
    - path: 'scripts/seed-drizzle.ts'
      provides: 'Drizzle-compatible seed records'
  key_links:
    - from: 'HomeLayer.tsx'
      to: '/api/maintenance?userId={id}'
      via: "fetch user's own maintenance requests for activity zone"
    - from: 'prisma/seed.ts'
      to: 'prisma/schema.prisma'
      via: 'Uses all new models (MaintenanceTeam, ServiceProvider, MaintenanceCategory)'
---

<objective>
**User tracking, activity zone integration, and seed data**

Purpose: Enhance the user-facing maintenance page with ticket info and assignment details. Wire maintenance activity into the user's HomeLayer ActivityZone so users see their maintenance updates on the dashboard. Create 7 realistic seed maintenance requests with proper teams, providers, and statuses.

Output: Enhanced user tracking page, active activity zone, 7 seed requests.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-maintenance-ticketing/40-CONTEXT.md
@.planning/phases/40-maintenance-ticketing/40-02-SUMMARY.md
@src/page-modules/maintenance/ui/MaintenancePage.tsx
@src/widgets/dashboard/ui/HomeLayer.tsx
@prisma/seed.ts
@scripts/seed-drizzle.ts
@src/entities/maintenance/model/types.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Enhance user maintenance tracking page</name>
<files>
  src/page-modules/maintenance/ui/MaintenancePage.tsx
  src/entities/maintenance/model/types.ts
</files>
<action>
Update the user-facing `/maintenance` page to show proper ticket information:

**MaintenancePage.tsx:**

- Update the `MaintenanceRequest` type usage to include new fields from the API response:
  - `ticketNumber` — display as `#SRV-2026-0001`
  - `assignedTeam` — show team name when assigned
  - `assignedProvider` — show provider company name when assigned
- The page already fetches from `/api/maintenance` and renders via `MaintenanceList` — enhance what's passed

**Replace the current simple list with an enhanced view:**
Instead of `MaintenanceList`, build inline rendering (or update `MaintenanceList` component) to show:

- **Ticket card layout:**
  - Left: Ticket number (monospace, prominent), category, description
  - Right: Status badge, priority badge, created date
  - Bottom (if assigned): "Assigned to: [Team/Provider name]" in small text
- **Click to expand** — clicking a card shows more detail in an inline section below it:
  - Full description
  - Assigned team/provider with contact info if available
  - Status timeline (simple vertical timeline from history data)
  - Preferred date/time if set
- **Polling** — add a 30-second polling interval via `useEffect` + `setInterval` to refresh the request list so users see near-real-time status updates without manual refresh

**entities/maintenance/model/types.ts:**

- Verify `MaintenanceRequest` entity interface includes `ticketNumber`, `assignedTeam`, `assignedProvider`, `preferredDate`, `preferredTime`
- The DTO layer already provides these — ensure the frontend type matches

Keep the existing toggle between "View My Requests" and "New Request" form. Keep the existing form functionality untouched.
</action>
<verify>tsc --noEmit 2>&1 | head -20</verify>
<done>User maintenance page shows ticket numbers, assignments, and status timeline with auto-refresh.</done>
</task>

<task type="auto">
<name>Task 2: Wire maintenance activity into HomeLayer ActivityZone</name>
<files>src/widgets/dashboard/ui/HomeLayer.tsx</files>
<action>
Update the HomeLayer component to show maintenance activity in the user's ActivityZone:

**Current state (line ~458-487):**

- `recentActivity` currently only filled from announcements (`/api/announcements?limit=5`)
- No maintenance activity shown

**Changes:**

1. Add a new fetch call in the `Promise.all` array (around line ~458):

```typescript
// User maintenance activity (their own requests' status updates)
fetchJson<ActivityItem>('/api/maintenance?limit=5&activity=true'),
```

If the existing `/api/maintenance` endpoint doesn't have an `activity` mode, add a query param handler to `maintenance/route.ts` GET:

- When `activity=true` and user is a resident, return the user's requests ordered by `updatedAt DESC`, limited to 5
- Each item includes status, ticketNumber, category for display

But actually, a simpler approach is to just fetch the user's own maintenance requests and show them as activity items. Use the existing endpoint:

```typescript
fetchJson<MaintenanceRequest>(`/api/maintenance?limit=5`);
```

2. After the existing `Promise.all`, merge maintenance items into `recentActivity`:

```typescript
// Transform maintenance items into activity items
const maintenanceActivity = (maintenanceItems as MaintenanceRequest[]).map(m => ({
  id: m.id,
  title: `${m.ticketNumber || 'Request'} — ${m.status.replace('_', ' ')}`,
  type: 'Maintenance',
  createdAt: m.updatedAt || m.createdAt,
  summary: m.category,
}));
```

3. Merge with announcements, sort by date, take top 5:

```typescript
const allActivity = [...recentActivity, ...maintenanceActivity]
  .sort((a, b) => new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime())
  .slice(0, 5);
```

4. Update the `ActivityZone` component (line ~253):

- Add a mapping for maintenance activity items to use the `Wrench` icon (already imported from lucide-react)
- Link maintenance items to `/dashboard/services` (not `/dashboard/community` like announcements)

5. Update the `HomeLayerData` interface to include `maintenanceItems` if needed, or just fold them into `recentActivity` post-fetch.

The maintenance item card should link to `/maintenance` (the user's own page). The wrench icon and "Maintenance" type badge should be distinctive.

For the `OverdueMaintenance` in the UrgencyZone (line ~104-111), ensure it still works — it currently reads `overdue` from `/api/maintenance?overdue=true`. Verify the API still supports this filter.
</action>
<verify>N/A — visual verification. Check HomeLayer renders without errors in browser console.</verify>
<done>User dashboard ActivityZone shows maintenance status changes alongside announcements.</done>
</task>

<task type="auto">
<name>Task 3: Create 7 seed maintenance requests</name>
<files>
  prisma/seed.ts
  scripts/seed-drizzle.ts
</files>
<action>
Add 7 seed maintenance requests to both `prisma/seed.ts` and `scripts/seed-drizzle.ts`. Follow the existing seed patterns in each file.

First, in both seed files, add seed data for the new tables:

**MaintenanceCategory seed entries** (for the soralia tenant):

```
PLUMBING → "Plumbing"
ELECTRICAL → "Electrical"
HVAC → "HVAC"
LANDSCAPING → "Landscaping"
STRUCTURAL → "Structural"
NETWORK → "Network"
WASTE → "Waste Management"
SECURITY → "Security"
OTHER → "Other"
```

**MaintenanceTeam seed entries** (for soralia tenant):

- `team-plumbing`: "Plumbing Team" (trade: PLUMBING, contact: "Thabo Mokoena")
- `team-electrical`: "Electrical Team" (trade: ELECTRICAL, contact: "Sarah van der Merwe")
- `team-landscaping`: "Landscaping Team" (trade: LANDSCAPING, contact: "David Nkosi")

**ServiceProvider seed entries** (for soralia tenant):

- `prov-pipe-burst`: "PipeBurst Solutions Inc." (trade: PLUMBING, phone: "+27 82 555 0101")
- `prov-tree-felling`: "TreeCare Pros" (trade: LANDSCAPING, phone: "+27 82 555 0102")
- `prov-electrical`: "VoltSafe Electrical" (trade: ELECTRICAL, phone: "+27 82 555 0103")
- `prov-network`: "FibreConnect ISP" (trade: NETWORK, phone: "+27 82 555 0104")
- `prov-waste`: "WasteWise Collectors" (trade: WASTE, phone: "+27 82 555 0105")

**7 MaintenanceRequest seed entries:**

1. **Lawn irrigation malfunction**
   - id: "mr-irrigation-01", ticketNumber: "SRV-2026-0001"
   - userId: first RESIDENT user, category: LANDSCAPING
   - priority: MEDIUM, status: SUBMITTED
   - Description: "The automated sprinkler system in the common garden area has been malfunctioning for the past week. Zones 3 and 4 aren't turning on, and zone 2 runs continuously."
   - created: 3 days ago

2. **Burst pipe in unit 12B**
   - id: "mr-burst-pipe", ticketNumber: "SRV-2026-0002"
   - userId: second RESIDENT user, category: PLUMBING
   - priority: EMERGENCY, status: IN_PROGRESS
   - assignedTeamId: team-plumbing
   - Description: "Water pipe burst in the kitchen wall. Water damage spreading to ground floor. Need immediate shutdown of water supply to building B."
   - created: 12 hours ago

3. **Tree felling request**
   - id: "mr-tree-felling", ticketNumber: "SRV-2026-0003"
   - userId: third RESIDENT user, category: LANDSCAPING
   - priority: HIGH, status: SUBMITTED
   - Description: "The old oak tree near the children's playground has a large dead branch hanging over the play area. Risk of it falling during the next storm."
   - created: 1 day ago

4. **Network outage**
   - id: "mr-network-outage", ticketNumber: "SRV-2026-0004"
   - userId: first RESIDENT user, category: NETWORK
   - priority: HIGH, status: IN_PROGRESS
   - assignedProviderId: prov-network
   - Description: "Fiber optic cable cut by contractors during roadworks on the main access road. All units in Blocks A and B have no internet connectivity since yesterday. FibreConnect has been notified."
   - created: 2 days ago

5. **Electrical mains problem**
   - id: "mr-electrical-mains", ticketNumber: "SRV-2026-0005"
   - userId: fourth RESIDENT user, category: ELECTRICAL
   - priority: HIGH, status: ASSIGNED
   - assignedTeamId: team-electrical
   - Description: "Flickering lights and intermittent power surges in Block C units 20-30. Affects all rooms. Neighbors in adjacent units report similar issues. Seems to be related to the main supply line."
   - created: 4 days ago

6. **Bin removal request**
   - id: "mr-bin-removal", ticketNumber: "SRV-2026-0006"
   - userId: second RESIDENT user, category: WASTE
   - priority: LOW, status: CANCELLED
   - Description: "Requesting removal of bulky waste (old furniture, mattresses) from unit 8. Normal bin collection can't handle these items."
   - created: 14 days ago
   - resolution: null (cancelled by user)

7. **Garage door stuck (longstanding)**
   - id: "mr-garage-door", ticketNumber: "SRV-2026-0007"
   - userId: third RESIDENT user, category: STRUCTURAL
   - priority: LOW, status: SUBMITTED
   - Description: "The garage door at unit 17B has been sticking intermittently for the past 3 months. It makes a loud grinding noise when opening and sometimes gets stuck halfway. Have reported twice before but issue persists."
   - created: 90 days ago

Use proper UUIDs for all IDs. Use existing tenant and user IDs from the seed data. Reference the actual Prisma/Drizzle context (user IDs from the seeded users).

Also create a `Setting` entry for `ticket_number_format` with value `"SRV-{YYYY}-{NNNN}"` for the soralia tenant.

For `scripts/seed-drizzle.ts` — mirror the same data using Drizzle's insert syntax. Follow the existing patterns in that file.
</action>
<verify># Verify seed works without errors
node -e "console.log('Seed data definition complete — run with npx prisma db seed')"
</verify>
<done>7 seed maintenance requests with teams, providers, and categories exist in both seed files.</done>
</task>

</tasks>

<verification>
1. `/maintenance` page renders with ticket numbers and assignment info
2. HomeLayer ActivityZone shows maintenance activity items with correct icons
3. Maintenance items link to `/maintenance` or `/dashboard/services`
4. 30-second poll timer refreshes maintenance status
5. Seed data can be verified: `npx prisma db seed` runs without errors
6. All 7 requests are distinct, cover different statuses, priorities, and assignment scenarios
</verification>

<success_criteria>

- User can see ticket numbers, status, and assignment on their maintenance page
- HomeLayer shows maintenance activity in the Recent Activity zone
- 7 realistic seed records exist covering all major states
- Seed data includes teams, providers, and categories
  </success_criteria>

<output>
After completion, create `.planning/phases/40-maintenance-ticketing/40-04-SUMMARY.md`
</output>
