---
phase: 06-maintenance-requests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/maintenance/ui/MaintenanceForm.tsx
  - src/app/api/maintenance/route.ts
  - src/app/api/maintenance/[id]/route.ts
  - src/app/api/maintenance/[id]/notify/route.ts
autonomous: true
requirements:
  - MAINT-01
  - MAINT-02
  - MAINT-03

must_haves:
  truths:
    - Users can upload photos to maintenance requests via Supabase Storage
    - Status change notifications sent via in-app + email
    - Admin can view all requests in queue
  artifacts:
    - path: src/features/maintenance/ui/MaintenanceForm.tsx
      provides: Photo upload with Supabase Storage
      min_lines: 50
    - path: src/app/api/maintenance/[id]/notify/route.ts
      provides: Email notifications via MailerSend
      min_lines: 30
  key_links:
    - from: src/features/maintenance/ui/MaintenanceForm.tsx
      to: Supabase Storage
      via: storage.upload() for images
      pattern: storage\\.from.*upload
    - from: src/app/api/maintenance/[id]/notify/route.ts
      to: MailerSend API
      via: email.send() for status changes
      pattern: mailersend|email\\.send
---

<objective>
**Complete maintenance request system** — Wire image uploads to Supabase Storage and add transactional email notifications. Model, API, and basic UI already exist. Focus on gaps: photo storage and email notifications.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-maintenance-requests/06-CONTEXT.md
@src/app/api/maintenance/route.ts
@src/features/maintenance/ui/MaintenanceForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire photo uploads to Supabase Storage</name>
  <files>src/features/maintenance/ui/MaintenanceForm.tsx</files>
  <action>
Update MaintenanceForm to upload images to Supabase Storage:

1. Check for existing Supabase storage client in src/lib/supabase.ts
2. If no bucket exists, use Supabase MCP to create 'maintenance-images' bucket
3. Update form to:
   - Accept multiple image files (max 5)
   - Upload each to storage bucket on file select
   - Store returned URLs in images[] array
   - Show image previews with remove option
4. Update POST /api/maintenance to store image URLs (already handles images array)

Use existing Supabase client. Upload pattern:

```typescript
const { data, error } = await storage
  .from('maintenance-images')
  .upload(`${userId}/${Date.now()}-${file.name}`, file, {
    contentType: file.type,
    upsert: false,
  });
```

  </action>
  <verify>grep -n "storage.from.*maintenance" src/features/maintenance/ui/MaintenanceForm.tsx</verify>
  <done>Form uploads images to Supabase Storage, URLs stored in request.images</done>
</task>

<task type="auto">
  <name>Task 2: Add MailerSend for transactional emails</name>
  <files>src/app/api/maintenance/[id]/notify/route.ts</files>
  <action>
Add MailerSend integration for status change notifications:

1. Check if MAILERSEND_API_KEY exists in .env
2. If not, add to .env.example:
   ```
   MAILERSEND_API_KEY=your-api-key
   MAILERSEND_FROM_EMAIL=notifications@soralia.co.za
   ```
3. Install mailerSend-ts if needed: `npm install mailerchange` or use HTTP
4. Update notify/route.ts to:
   - Send email on status change
   - Include request details in email
   - Use template for consistent styling

Email triggers:

- On status change (any status)
- On new assignment (IN_PROGRESS)
- On completion (COMPLETED)

Use existing notify endpoint structure. Add email send after DB update.
</action>
<verify>grep -n "mailersend\|email.send" src/app/api/maintenance/[id]/notify/route.ts</verify>
<done>Email sent via MailerSend on status changes</done>
</task>

<task type="auto">
  <name>Task 3: Verify admin queue functionality</name>
  <files>src/widgets/maintenance/ui/MaintenanceList.tsx</files>
  <action>
Verify admin can view all requests:

1. Check existing MaintenanceList component
2. Verify it shows all requests when user.role === 'ADMIN' or 'BOARD'
3. Ensure status filters work for admin view
4. Add admin-only actions if missing (assign, cancel, add notes)

Admin should see:

- All tenant requests (not just their own)
- Status change dropdown
- Priority indicator
- Notes internal flag

Reference existing route.ts - canViewAll logic already exists.
</action>
<verify>grep -n "canViewAll\|role.\*ADMIN" src/widgets/maintenance/ui/MaintenanceList.tsx</verify>
<done>Admin sees all requests with management actions</done>
</task>

</tasks>

<verification>
- [ ] Photo upload works to Supabase Storage
- [ ] Images stored in maintenance request record
- [ ] Email notifications sent on status change
- [ ] Admin can view/manage all requests
- [ ] Existing form and list still work
</verification>

<success_criteria>

- Users can attach photos to requests (stored in Supabase)
- Status changes trigger email notifications
- Admin has full queue view with actions
  </success_criteria>

<output>
After completion, create `.planning/phases/06-maintenance-requests/06-01-SUMMARY.md`
</output>
