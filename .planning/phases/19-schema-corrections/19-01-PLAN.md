---
phase: 19-schema-corrections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/db/schema/tenants.ts
  - src/db/schema/settings.ts
  - src/db/schema/users.ts
  - src/db/schema/index.ts
autonomous: true
requirements: [SCHEMA-01, SCHEMA-02, SCHEMA-03]

must_haves:
  truths:
    - 'Setting table allows same key for different tenants (composite unique on tenantId+key)'
    - 'Tenant table has ownerId field referencing user'
    - 'user table has isPlatformAdmin boolean field defaulting to false'
  artifacts:
    - path: 'prisma/schema.prisma'
      provides: 'Setting @@unique([tenantId, key]), Tenant.ownerId, user.isPlatformAdmin'
      contains: '@@unique([tenantId, key])'
    - path: 'src/db/schema/settings.ts'
      provides: 'Drizzle schema with composite unique constraint'
    - path: 'src/db/schema/tenants.ts'
      provides: 'Drizzle schema with ownerId field'
    - path: 'src/db/schema/users.ts'
      provides: 'Drizzle schema with isPlatformAdmin field'
    - path: 'prisma/migrations/'
      provides: 'Migration file applying all three changes'
  key_links:
    - from: 'prisma/schema.prisma'
      to: 'src/db/schema/'
      via: 'prisma generate (drizzle generator)'
      pattern: 'prisma generate'
---

<objective>
Apply three critical schema changes and run migration: fix Setting key uniqueness, add ownerId to Tenant, add isPlatformAdmin to user.

Purpose: These are foundational fixes that must exist before any self-service signup or multi-tenant work can proceed safely. The Setting uniqueness bug will break immediately with a second tenant. Without ownerId there is no verifiable tenant ownership. Without isPlatformAdmin there is no clean separation of platform staff from tenant users.

Output: Applied migration, regenerated Prisma client, regenerated Drizzle schema.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DISCUSSION.md — Sections 2, 3 (schema changes)
@.planning/phases/00-multi-tenant-foundation/00-01-SUMMARY.md — prior tenant infrastructure
@prisma/schema.prisma — current schema (Setting at line 300, Tenant at line 50, user at line 820)
@src/db/schema/ — Drizzle schema generated from Prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Setting key uniqueness and add ownerId to Tenant</name>
  <files>prisma/schema.prisma</files>
  <action>
    Edit prisma/schema.prisma with two changes:

    1. Fix Setting model (currently line 300-305):
       - Remove `@unique` from `key String @unique`
       - Change to `key   String`
       - Add `@@unique([tenantId, key])` after the fields

    2. Add ownerId to Tenant model (currently line 50-76):
       - Add `ownerId  String?` field after `updatedAt DateTime?`
       - Add `owner    user?  @relation("TenantOwner", fields: [ownerId], references: [id])`
       - The user model already has an `id` field, so this relation references it

    IMPORTANT: Do NOT modify any other models. These are surgical changes only.

    After editing, run:
    - `pnpm prisma generate` — regenerates Prisma client AND Drizzle schema (via prisma-generator-drizzle)
    - Verify the generated files updated: src/db/schema/tenants.ts should have ownerId, src/db/schema/settings.ts should have the composite unique

  </action>
  <verify>
    <automated>pnpm prisma generate && echo "Generate OK"</automated>
    <manual>Check src/db/schema/tenants.ts has ownerId field, src/db/schema/settings.ts has @@unique([tenantId, key])</manual>
  </verify>
  <done>Prisma schema edited, prisma generate succeeds, Drizzle schema reflects both changes</done>
</task>

<task type="auto">
  <name>Task 2: Add isPlatformAdmin to user model</name>
  <files>prisma/schema.prisma, src/db/schema/users.ts</files>
  <action>
    Edit prisma/schema.prisma user model (currently line 820-873):
    - Add `isPlatformAdmin  Boolean  @default(false)` field after `emailVerified Boolean @default(false)`
    - This is a separate flag from the Role enum — it identifies NetComplex staff independently of tenant roles

    After editing, run:
    - `pnpm prisma generate` — regenerates Prisma client AND Drizzle schema

    IMPORTANT: Do NOT add this to the Role enum. Platform Admin is a boolean flag on user, never a role value. This is a key architectural decision from DISCUSSION.md.

  </action>
  <verify>
    <automated>pnpm prisma generate && echo "Generate OK"</automated>
    <manual>Check src/db/schema/users.ts has isPlatformAdmin boolean field with default false</manual>
  </verify>
  <done>User model has isPlatformAdmin Boolean @default(false), Prisma and Drizzle schemas regenerated</done>
</task>

<task type="auto">
  <name>Task 3: Create and apply database migration</name>
  <files>prisma/migrations/</files>
  <action>
    Create a Prisma migration that applies all three schema changes:
    1. ALTER TABLE "Setting" — drop unique constraint on key, add composite unique (tenantId, key)
    2. ALTER TABLE "Tenant" — ADD COLUMN "ownerId" TEXT (nullable)
    3. ALTER TABLE "user" — ADD COLUMN "isPlatformAdmin" BOOLEAN NOT NULL DEFAULT false

    Run: `pnpm prisma migrate dev --name add-ownerId-isPlatformAdmin-fix-setting-uniqueness`

    This will:
    - Generate the migration SQL
    - Apply it to the development database
    - Confirm success

    If the migration prompts about data loss, review carefully — none of these changes should cause data loss:
    - Setting uniqueness change: constraint modification only
    - ownerId: nullable column, no existing data affected
    - isPlatformAdmin: non-nullable with default false, all existing users get false

  </action>
  <verify>
    <automated>pnpm prisma migrate status && echo "Migration status OK"</automated>
    <manual>Verify migration file exists in prisma/migrations/ with descriptive name</manual>
  </verify>
  <done>Migration applied successfully, prisma migrate status shows database is in sync</done>
</task>

</tasks>

<verification>
- Prisma schema has all three changes (Setting @@unique, Tenant.ownerId, user.isPlatformAdmin)
- `pnpm prisma generate` succeeds without errors
- `pnpm prisma migrate status` shows database is in sync
- Drizzle schema files in src/db/schema/ reflect all three changes
- No data loss from migration
</verification>

<success_criteria>

- Setting model uses @@unique([tenantId, key]) — two tenants can share setting keys
- Tenant model has ownerId String? with relation to user
- user model has isPlatformAdmin Boolean @default(false)
- Migration file created and applied
- Prisma client and Drizzle schema regenerated
  </success_criteria>

<output>
After completion, create `.planning/phases/19-schema-corrections/19-01-SUMMARY.md`
</output>
