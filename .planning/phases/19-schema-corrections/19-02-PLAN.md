---
phase: 19-schema-corrections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ui/Header.tsx
autonomous: true
requirements: [SCHEMA-04]

must_haves:
  truths:
    - 'Admin users see admin link in header (role comparison uses uppercase ADMIN)'
    - 'No lowercase role string comparisons exist in Header.tsx'
  artifacts:
    - path: 'src/shared/ui/Header.tsx'
      provides: 'Header component with isAdmin() helper import and correct role checks'
      contains: 'isAdmin(session'
  key_links:
    - from: 'src/shared/ui/Header.tsx'
      to: 'src/entities/tenant/api/permissions.ts'
      via: 'import isAdmin'
      pattern: 'import.*isAdmin.*permissions'
---

<objective>
Fix Header role case-sensitivity bug where admin link fails to display.

Purpose: Header.tsx checks `session?.user?.role === 'admin'` (lowercase) but the Role enum stores `'ADMIN'` (uppercase). This means the admin-specific UI never renders for users with the ADMIN role. This is a tiny fix but blocks the admin user journey.

Output: Header.tsx using the existing `isAdmin()` helper from permissions.ts instead of manual string comparison.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/DISCUSSION.md — Section 2, Tenant Admin Authentication & Access table
@src/shared/ui/Header.tsx — lines 96-97 contain the bug
@src/entities/tenant/api/permissions.ts — isAdmin() helper at line 167
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace manual role string comparison with isAdmin() helper</name>
  <files>src/shared/ui/Header.tsx</files>
  <action>
    In src/shared/ui/Header.tsx:

    1. Add import at the top of the file (with other imports):
       `import { isAdmin } from '@entities/tenant/api/permissions';`

    2. Replace line 96:
       FROM: `const isAdmin = session?.user?.role === 'admin';`
       TO:   `const isAdminUser = isAdmin(session?.user?.role);`

       NOTE: Rename the local variable to `isAdminUser` to avoid shadowing the imported function.

    3. Find all usages of the local `isAdmin` variable in the component and replace with `isAdminUser`.
       - Search for `isAdmin` in the file — there should be at least one usage (likely conditional rendering of admin link)
       - Replace each occurrence with `isAdminUser`

    4. Also fix line 97 which has the same bug:
       FROM: `const isBoard = session?.user?.role === 'board';`
       TO:   `const isBoardUser = session?.user?.role === 'BOARD';`

       There is no `isBoard()` helper in permissions.ts, so use direct comparison with the correct uppercase value.

    Do NOT change any other logic in Header.tsx. This is a surgical fix for the case-sensitivity bug only.

  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify Header.tsx imports isAdmin from permissions, uses isAdminUser variable (not isAdmin local), and checks 'BOARD' uppercase</manual>
  </verify>
  <done>Header.tsx uses isAdmin() helper for admin check, uses uppercase 'BOARD' for board check, TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
- Header.tsx imports `isAdmin` from `@entities/tenant/api/permissions`
- Local variable renamed to `isAdminUser` to avoid shadowing
- Board check uses uppercase `'BOARD'`
- TypeScript type check passes
- Admin link renders correctly for users with ADMIN role
</verification>

<success_criteria>

- No lowercase role string comparisons in Header.tsx
- Uses imported isAdmin() helper for admin check
- TypeScript compiles without errors
- Admin users see admin link in header
  </success_criteria>

<output>
After completion, create `.planning/phases/19-schema-corrections/19-02-SUMMARY.md`
</output>
