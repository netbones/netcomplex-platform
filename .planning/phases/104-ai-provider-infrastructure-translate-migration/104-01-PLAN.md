---
phase: 104-ai-provider-infrastructure-translate-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/tenant/api/settings-service.ts
  - src/entities/tenant/lib/modules/get-tenant-module.ts
  - src/entities/tenant/lib/modules/index.ts
  - src/entities/tenant/api/settings.ts
  - src/entities/tenant/index.server.ts
  - src/shared/api/ai/encryption.ts
  - src/shared/api/ai/__tests__/encryption.test.ts
  - prisma/seed/modules.ts
  - .env.example
autonomous: true
requirements:
  - AI-PROV-02

must_haves:
  truths:
    - "getTenantModule(tenantId, 'ai-provider') returns the full TenantModule record or null"
    - 'getSettingValue(tenantId, key) returns the Setting.value string or null'
    - 'encryptKey(plaintext) returns hex-encoded ciphertext with iv:authTag:ciphertext format'
    - 'decryptKey(encrypted) round-trips to the original plaintext'
    - 'ai-provider PlatformModule exists in seed data with defaultEnabled: false'
    - 'AI_SETTINGS_ENCRYPTION_KEY appears in .env.example with generation instructions'
  artifacts:
    - path: 'src/entities/tenant/api/settings-service.ts'
      provides: 'getSettingValue, setSettingValue, deleteSettingValue helpers'
      exports: ['getSettingValue', 'setSettingValue', 'deleteSettingValue']
    - path: 'src/entities/tenant/lib/modules/get-tenant-module.ts'
      provides: 'getTenantModule(tenantId, moduleKey): TenantModule | null'
      exports: ['getTenantModule']
    - path: 'src/shared/api/ai/encryption.ts'
      provides: 'AES-256-GCM encryptKey/decryptKey/getEncryptionKey'
      exports: ['encryptKey', 'decryptKey', 'getEncryptionKey']
    - path: 'src/shared/api/ai/__tests__/encryption.test.ts'
      provides: 'Round-trip encryption test'
      min_cases: 3
    - path: 'prisma/seed/modules.ts'
      provides: 'ai-provider PlatformModule seed entry'
      contains: "key: 'ai-provider'"
    - path: '.env.example'
      provides: 'AI_SETTINGS_ENCRYPTION_KEY env var template'
      contains: 'AI_SETTINGS_ENCRYPTION_KEY'
  key_links:
    - from: 'src/entities/tenant/index.server.ts'
      to: 'src/entities/tenant/api/settings-service.ts'
      via: 'barrel re-export'
      pattern: 'export.*settings-service'
    - from: 'src/entities/tenant/index.server.ts'
      to: 'src/entities/tenant/lib/modules/get-tenant-module.ts'
      via: 'barrel re-export'
      pattern: 'export.*getTenantModule'
---

<read_first>

# Must read these files before implementing:

@/home/ubuntupunk/Projects/soralia-village/src/entities/tenant/lib/modules/assert-module-enabled.ts — existing module helpers, Drizzle query patterns
@/home/ubuntupunk/Projects/soralia-village/src/entities/tenant/lib/modules/index.ts — current barrel exports
@/home/ubuntupunk/Projects/soralia-village/src/entities/tenant/api/settings.ts — current SETTINGS_KEYS
@/home/ubuntupunk/Projects/soralia-village/src/entities/tenant/index.server.ts — current barrel exports
@/home/ubuntupunk/Projects/soralia-village/prisma/seed/modules.ts — module seed pattern (upsert + tierMap)
@/home/ubuntupunk/Projects/soralia-village/src/shared/api/server/index.ts — Drizzle table re-exports pattern (tenantModules, settings)
@/home/ubuntupunk/Projects/soralia-village/.planning/phases/104-ai-provider-infrastructure-translate-migration/104-RESEARCH.md — research sections 1.4, 2, 4.4
</read_first>

<objective>
Create the entity-layer foundation (getTenantModule, settings-service, AI setting keys), the AES-256-GCM encryption module, seed data, and env var template. Every other plan in this phase depends on these artifacts.

Purpose: Establish the lowest-level primitives — tenant module lookup, encrypted key storage, settings CRUD — that providers, admin routes, and the translate migration all consume.
Output: 4 new files, 5 modified files. getTenantModule() available at @entities/tenant/server. Encrypt/decrypt round-trip verified by test.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-ai-provider-infrastructure-translate-migration/104-RESEARCH.md

# Patterns to replicate:

- Module helpers use `import { db, tenantModules, platformModules } from '@api/server'` (NOT from `@/lib/db`)
- Barrel exports in index.server.ts export functions by name (not `export *`)
- SETTINGS_KEYS uses `as const` with a SettingsKey type
- Seed modules use `prisma.platformModule.upsert({ where: { key }, update: m, create: m })`
- Entity helpers use `server-only` import
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Entity helpers + settings service + AI keys + barrel updates</name>
  <files>
    src/entities/tenant/api/settings-service.ts
    src/entities/tenant/lib/modules/get-tenant-module.ts
    src/entities/tenant/lib/modules/index.ts
    src/entities/tenant/api/settings.ts
    src/entities/tenant/index.server.ts
  </files>
  <read_first>
    @src/entities/tenant/api/settings.ts — current keys, pattern
    @src/entities/tenant/lib/modules/assert-module-enabled.ts — Drizzle query style, db import
    @src/entities/tenant/lib/modules/index.ts — barrel exports
    @src/entities/tenant/index.server.ts — barrel exports
    @src/shared/api/server/index.ts — settings and tenantModules Drizzle table exports
  </read_first>
  <behavior>
    - Test 1: getTenantModule(validTenantId, 'dashboard') returns TenantModule record with .config field
    - Test 2: getTenantModule(validTenantId, 'nonexistent-module') returns null
    - Test 3: getSettingValue(validTenantId, SETTINGS_KEYS.AI_PROVIDER_DEFAULT) returns stored string
    - Test 4: setSettingValue + getSettingValue round-trip persists and retrieves correctly
    - Test 5: deleteSettingValue removes the record; subsequent getSettingValue returns null
    - Test 6: SETTINGS_KEYS contains AI_PROVIDER_DEFAULT, AI_ANTHROPIC_KEY, AI_OPENAI_KEY, AI_DEEPSEEK_KEY
    - Test 7: modules/index.ts re-exports getTenantModule
    - Test 8: index.server.ts re-exports getSettingValue, setSettingValue, deleteSettingValue
  </behavior>
  <action>
    per AI-PROV-02 (encryption infrastructure requires settings CRUD helpers).

    **1. Create src/entities/tenant/api/settings-service.ts:**
    - Import `db, settings` from `@api/server`, `eq` from `drizzle-orm`
    - Import `server-only`
    - Export `getSettingValue(tenantId: string, key: string): Promise<string | null>` — queries settings table with eq(settings.tenantId, tenantId) AND eq(settings.key, key), returns `.value` or null
    - Export `setSettingValue(tenantId: string, key: string, value: string): Promise<void>` — upserts into settings via `db.insert(settings).values({id: crypto.randomUUID(), tenantId, key, value}).onConflictDoUpdate({target: [settings.tenantId, settings.key], set: {value}})` — NOTE: settings table has @@unique([tenantId, key]), NOT @id so use the composite unique for upsert
    - Export `deleteSettingValue(tenantId: string, key: string): Promise<void>` — deletes with eq(settings.tenantId, tenantId) AND eq(settings.key, key)
    - Add `import 'server-only'` at top

    **2. Create src/entities/tenant/lib/modules/get-tenant-module.ts:**
    - Import `db, tenantModules` from `@api/server`, `and, eq` from `drizzle-orm`
    - Import `server-only`
    - Export `async function getTenantModule(tenantId: string, moduleKey: string): Promise<typeof tenantModules.$inferSelect | null>`
    - Query: `db.select().from(tenantModules).where(and(eq(tenantModules.tenantId, tenantId), eq(tenantModules.moduleKey, moduleKey))).limit(1)` then return `rows[0] ?? null`
    - Add `import 'server-only'` at top

    **3. Update src/entities/tenant/lib/modules/index.ts:**
    - Add `export { getTenantModule } from './get-tenant-module';`

    **4. Update src/entities/tenant/api/settings.ts:**
    - Add these keys BEFORE the closing `} as const;`:
      ```
      // AI Provider — tenant-managed API keys (stored encrypted)
      AI_PROVIDER_DEFAULT: 'ai.provider.default',
      AI_ANTHROPIC_KEY: 'ai.anthropic.key',
      AI_OPENAI_KEY: 'ai.openai.key',
      AI_DEEPSEEK_KEY: 'ai.deepseek.key',
      ```
    - Keep existing TRANSLATION_PROVIDER and TRANSLATION_API_KEY unchanged (backward compat)

    **5. Update src/entities/tenant/index.server.ts:**
    - Add: `export { getTenantModule } from './lib/modules/get-tenant-module';`
    - Add: `export { getSettingValue, setSettingValue, deleteSettingValue } from './api/settings-service';`

  </action>
  <verify>
    <automated>pnpm typecheck -- src/entities/tenant/api/settings-service.ts src/entities/tenant/lib/modules/get-tenant-module.ts 2>&1 | head -5</automated>
  </verify>
  <done>
    getTenantModule() is importable from @entities/tenant/server. settings-service.ts has getSettingValue/setSettingValue/deleteSettingValue. SETTINGS_KEYS has 4 new AI keys. index.server.ts barrel includes all new exports. TypeScript compiles clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encryption module + round-trip test</name>
  <files>
    src/shared/api/ai/encryption.ts
    src/shared/api/ai/__tests__/encryption.test.ts
  </files>
  <read_first>
    @src/entities/tenant/api/settings-service.ts — getSettingValue pattern
    @.planning/phases/104-ai-provider-infrastructure-translate-migration/104-RESEARCH.md — §2 for AES-256-GCM algorithm details
  </read_first>
  <behavior>
    - Test 1: encryptKey('sk-ant-test-key-12345') returns a string matching /^[0-9a-f]+:[0-9a-f]+:[0-9a-f]+$/ (iv:authTag:ciphertext hex format)
    - Test 2: decryptKey(encryptKey('my-secret-api-key')) === 'my-secret-api-key' (round-trip)
    - Test 3: decryptKey(encryptKey('')) === '' (empty string round-trips)
    - Test 4: decryptKey(encryptKey('a'.repeat(200))) === 'a'.repeat(200) (long key round-trips)
    - Test 5: decryptKey with a tampered authTag throws an error (GCM auth verification)
    - Test 6: Each call to encryptKey produces different output (unique IV per encryption)
    - Test 7: getEncryptionKey() throws when AI_SETTINGS_ENCRYPTION_KEY env var is missing
    - Test 8: getEncryptionKey() returns 32-byte Buffer when AI_SETTINGS_ENCRYPTION_KEY is valid hex (64 chars)
  </behavior>
  <action>
    per AI-PROV-02.

    **1. Create src/shared/api/ai/encryption.ts:**
    - Import `crypto` from `node:crypto`, `server-only`
    - Define constants: `ALGORITHM = 'aes-256-gcm'`, `IV_LENGTH = 12` (96-bit IV for GCM)
    - Export `getEncryptionKey(): Buffer` — reads `process.env.AI_SETTINGS_ENCRYPTION_KEY`, throws descriptive error if missing. Accepts hex (64 chars) or base64 (44 chars). Decode hex via `Buffer.from(env, 'hex')`, base64 via `Buffer.from(env, 'base64')`. Validate resulting Buffer is exactly 32 bytes.
    - Export `encryptKey(plaintext: string): string` — generate random 12-byte IV via `crypto.randomBytes(IV_LENGTH)`, create cipher `crypto.createCipheriv(ALGORITHM, encryptionKey, iv)`, encrypt plaintext, get auth tag, return `${iv.toString('hex')}:${authTag.toString('hex')}:${ciphertextHex}`.
    - Export `decryptKey(encrypted: string): string` — split on `:`, expect 3 parts (ivHex, authTagHex, ciphertext), decode each from hex, create decipher, setAuthTag, decrypt, return utf8 string. Throw descriptive error on format mismatch or auth failure.
    - Add `import 'server-only'` at top

    **2. Create src/shared/api/ai/__tests__/encryption.test.ts:**
    - Use `vitest` — `import { describe, it, expect, beforeEach } from 'vitest'`
    - In `beforeEach`, set `process.env.AI_SETTINGS_ENCRYPTION_KEY` to a valid 64-char hex string for reproducibility
    - Test round-trip with common API key format strings
    - Test empty string
    - Test long strings
    - Test tampered ciphertext throws
    - Test unique IV per call
    - Test missing env var throws
    - Run with `pnpm vitest run src/shared/api/ai/__tests__/encryption.test.ts`

  </action>
  <verify>
    <automated>pnpm vitest run src/shared/api/ai/__tests__/encryption.test.ts</automated>
  </verify>
  <done>
    encryption.test.ts passes all 8 cases. encryptKey/decryptKey round-trip verified. GCM auth failure detected. Unique IV per call confirmed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Seed ai-provider PlatformModule + .env.example</name>
  <files>
    prisma/seed/modules.ts
    .env.example
  </files>
  <read_first>
    @prisma/seed/modules.ts — existing module entries, upsert pattern
    @.env.example — current env var format
    @.planning/phases/104-ai-provider-infrastructure-translate-migration/104-RESEARCH.md — §10 for env var format, §1.5 for seed entry
  </read_first>
  <acceptance_criteria>
    - prisma/seed/modules.ts contains an ai-provider entry with key='ai-provider', minTier=tierMap.STANDARD, defaultEnabled=false
    - The entry is in alphabetical position (before 'auth' since 'ai' < 'au')
    - .env.example contains AI_SETTINGS_ENCRYPTION_KEY="" with comment describing 32-byte requirement and generation command
    - .env.example contains commented-out PLATFORM_ANTHROPIC_KEY/PLATFORM_OPENAI_KEY/PLATFORM_DEEPSEEK_KEY placeholders
  </acceptance_criteria>
  <action>
    **1. Update prisma/seed/modules.ts:**
    - Insert new entry BEFORE the 'auth' module (alphabetical — 'ai-provider' comes before 'auth'):
    ```typescript
    {
      key: 'ai-provider',
      label: 'AI Provider',
      description: 'Connect an AI provider (Anthropic, OpenAI, or DeepSeek) to enable AI-powered features across the platform',
      minTier: tierMap.STANDARD,
      defaultEnabled: false,
    },
    ```

    **2. Update .env.example:**
    - Add new section after the existing env vars, before any closing section:
    ```
    # AI Provider — Platform encryption key for tenant API key storage
    # Required for AI features. Generate with: openssl rand -hex 32
    # Must be exactly 32 bytes (64 hex characters or 44 base64 characters)
    AI_SETTINGS_ENCRYPTION_KEY=""

    # Optional: Platform-owned fallback keys (for future metered pool)
    # If set, used when tenant has not configured their own key
    # PLATFORM_ANTHROPIC_KEY=""
    # PLATFORM_OPENAI_KEY=""
    # PLATFORM_DEEPSEEK_KEY=""
    ```

  </action>
  <verify>
    <automated>grep -c "ai-provider" prisma/seed/modules.ts</automated>
  </verify>
  <done>
    Seed entry present. .env.example has AI_SETTINGS_ENCRYPTION_KEY with generation instructions and commented platform key placeholders.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary          | Description                                                                    |
| ----------------- | ------------------------------------------------------------------------------ |
| env var → process | AI_SETTINGS_ENCRYPTION_KEY loaded at startup — must never be logged or exposed |

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition | Mitigation Plan                                                  |
| --------- | ---------------------- | ------------------------------ | ----------- | ---------------------------------------------------------------- |
| T-104-01  | Information Disclosure | encryption.ts:encryptKey       | mitigate    | Plaintext never logged; encryptKey returns ciphertext only       |
| T-104-02  | Tampering              | encryption.ts:decryptKey       | mitigate    | GCM auth tag verification — tampered ciphertext throws           |
| T-104-03  | Elevation of Privilege | settings-service.ts            | mitigate    | Only server-side access; no client import path via `server-only` |
| T-104-04  | Spoofing               | encryption.ts:getEncryptionKey | mitigate    | Key must be exactly 32 bytes; validate at startup                |

</threat_model>

<verification>
```bash
pnpm vitest run src/shared/api/ai/__tests__/encryption.test.ts
pnpm typecheck
grep -c "ai-provider" prisma/seed/modules.ts
grep "AI_SETTINGS_ENCRYPTION_KEY" .env.example
```
</verification>

<success_criteria>

- [ ] getTenantModule(tenantId, moduleKey) compiles and is re-exported from @entities/tenant/server
- [ ] getSettingValue/setSettingValue/deleteSettingValue available from @entities/tenant/server
- [ ] SETTINGS_KEYS has AI_PROVIDER_DEFAULT, AI_ANTHROPIC_KEY, AI_OPENAI_KEY, AI_DEEPSEEK_KEY
- [ ] encryption.test.ts passes all 8 cases (round-trip, tamper detection, unique IV)
- [ ] ai-provider PlatformModule in seed with defaultEnabled: false
- [ ] AI_SETTINGS_ENCRYPTION_KEY in .env.example with generation instructions
- [ ] pnpm typecheck passes for all files
      </success_criteria>

## Artifacts This Phase Produces

### Exported Symbols

| Symbol               | Kind           | File                                                 | Description                                                                    |
| -------------------- | -------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------ |
| `getTenantModule`    | async function | src/entities/tenant/lib/modules/get-tenant-module.ts | Returns TenantModule record or null for a given tenant + module key            |
| `getSettingValue`    | async function | src/entities/tenant/api/settings-service.ts          | Returns setting value string or null                                           |
| `setSettingValue`    | async function | src/entities/tenant/api/settings-service.ts          | Upserts a setting value                                                        |
| `deleteSettingValue` | async function | src/entities/tenant/api/settings-service.ts          | Deletes a setting record                                                       |
| `encryptKey`         | function       | src/shared/api/ai/encryption.ts                      | AES-256-GCM encrypts a plaintext string; returns iv:authTag:ciphertext hex     |
| `decryptKey`         | function       | src/shared/api/ai/encryption.ts                      | AES-256-GCM decrypts ciphertext; throws on auth failure                        |
| `getEncryptionKey`   | function       | src/shared/api/ai/encryption.ts                      | Reads and validates AI_SETTINGS_ENCRYPTION_KEY env var; returns 32-byte Buffer |

### Setting Keys

| Key                                 | Value                   |
| ----------------------------------- | ----------------------- |
| `SETTINGS_KEYS.AI_PROVIDER_DEFAULT` | `'ai.provider.default'` |
| `SETTINGS_KEYS.AI_ANTHROPIC_KEY`    | `'ai.anthropic.key'`    |
| `SETTINGS_KEYS.AI_OPENAI_KEY`       | `'ai.openai.key'`       |
| `SETTINGS_KEYS.AI_DEEPSEEK_KEY`     | `'ai.deepseek.key'`     |

### Environment Variables

| Variable                     | Required              | Description                                                         |
| ---------------------------- | --------------------- | ------------------------------------------------------------------- |
| `AI_SETTINGS_ENCRYPTION_KEY` | Yes (for AI features) | 32-byte hex/base64 encryption key; absent = AI features unavailable |

### New Files

| File                                                   | Type   |
| ------------------------------------------------------ | ------ |
| `src/entities/tenant/api/settings-service.ts`          | Create |
| `src/entities/tenant/lib/modules/get-tenant-module.ts` | Create |
| `src/shared/api/ai/encryption.ts`                      | Create |
| `src/shared/api/ai/__tests__/encryption.test.ts`       | Create |

### Modified Files

| File                                       | Change                                                                            |
| ------------------------------------------ | --------------------------------------------------------------------------------- |
| `src/entities/tenant/lib/modules/index.ts` | Add getTenantModule re-export                                                     |
| `src/entities/tenant/api/settings.ts`      | Add 4 AI settings keys                                                            |
| `src/entities/tenant/index.server.ts`      | Add getTenantModule, getSettingValue, setSettingValue, deleteSettingValue exports |
| `prisma/seed/modules.ts`                   | Add ai-provider PlatformModule entry                                              |
| `.env.example`                             | Add AI_SETTINGS_ENCRYPTION_KEY + commented platform keys                          |

<output>
Create `.planning/phases/104-ai-provider-infrastructure-translate-migration/104-01-SUMMARY.md` when done
</output>
