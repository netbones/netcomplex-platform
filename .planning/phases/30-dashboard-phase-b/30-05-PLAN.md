---
phase: 30-dashboard-phase-b
plan: 05
type: execute
wave: 4
depends_on: ['30-03', '30-04']
files_modified:
  - src/widgets/dashboard/ui/SpaceLauncher.tsx
  - src/widgets/dashboard/ui/MobileSpaceBar.tsx
  - src/app/(tenant)/dashboard/layout.tsx
  - src/widgets/dashboard/model/spaces.ts
  - src/page-modules/dashboard/ui/DashboardPage.tsx
  - src/widgets/dashboard/ui/DashboardTabs.tsx
  - src/entities/widget/model/widget-store.ts
autonomous: false
requirements:
  - FOCUS-12
  - FOCUS-13
  - FOCUS-14
must_haves:
  truths:
    - "Mobile bottom bar shows up to 5 space icons, with overflow in 'More' menu"
    - 'SpaceLauncher collapses to icon-only sidebar on desktop'
    - 'Feature flag toggle switches between old tab UI and new space UI'
    - 'DashboardTabs is deprecated — feature flag toggle is the mechanism that enables eventual removal once flag is permanently enabled'
    - 'Old tab-keyed widget data migrates cleanly to space-keyed format'
  artifacts:
    - path: 'src/widgets/dashboard/ui/MobileSpaceBar.tsx'
      provides: 'Mobile bottom navigation bar for spaces'
      min_lines: 60
    - path: 'src/app/(tenant)/dashboard/layout.tsx'
      provides: 'Updated layout with mobile bottom bar + feature flag toggle'
      min_lines: 50
  key_links:
    - from: 'src/widgets/dashboard/ui/MobileSpaceBar.tsx'
      to: 'src/widgets/dashboard/model/spaces.ts'
      via: 'getVisibleSpaces for mobile bar items'
      pattern: 'getVisibleSpaces'
    - from: 'src/app/(tenant)/dashboard/layout.tsx'
      to: 'NEXT_PUBLIC_FOCUS_SPACES'
      via: 'feature flag check for old vs new UI'
      pattern: 'FOCUS_SPACES'
---

<objective>
Complete the mobile experience, feature flag integration, and cleanup of legacy tab UI.

Purpose: Delivers the mobile bottom nav bar (Q1: 5 slots with overflow), finalizes the feature flag toggle for safe rollout, and deprecates the old DashboardTabs component (removal happens after flag is permanently enabled).

Output: MobileSpaceBar component, updated layout with responsive SpaceLauncher, feature flag fully integrated, old tab UI deprecated.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/architecture/DASHBOARD-PHASE-B-DISCUSSION.md
@src/widgets/dashboard/ui/SpaceLauncher.tsx
@src/app/(tenant)/dashboard/layout.tsx
@src/page-modules/dashboard/ui/DashboardPage.tsx
@src/widgets/dashboard/ui/DashboardTabs.tsx
</context>

<tasks>

<task type="auto">
<name>Task 1: Build MobileSpaceBar + responsive layout update</name>
<files>src/widgets/dashboard/ui/MobileSpaceBar.tsx, src/widgets/dashboard/ui/SpaceLauncher.tsx, src/app/(tenant)/dashboard/layout.tsx</files>
<action>
1. **Create `src/widgets/dashboard/ui/MobileSpaceBar.tsx`**:
   - Mobile bottom navigation bar (fixed bottom, full width)
   - Shows up to 5 space items: Home, Services, Community, Messages, Admin (per Q1 decision — 5 mobile slots)
   - Each item: icon + short label, active state with indigo-600 color
   - If more than 5 spaces visible (unlikely with current model), overflow into "More" sheet
   - **Overflow guard (per advisory A9):** If `getVisibleSpaces()` returns more than 5 items, log a console warning and slice to 5. Add a `// TODO: implement "More" overflow sheet when 6th space is added` comment. This prevents silent data loss if a future space is added before the overflow UI is built.
   - Icon badges: unread message count on Messages, overdue count on Services
   - **Safe-area handling (per advisory A3):**
     - Bar height: `h-16` + safe-area padding at bottom: `pb-[env(safe-area-inset-bottom,0px)]`
     - Full class: `fixed bottom-0 left-0 right-0 h-16 pb-[env(safe-area-inset-bottom,0px)] md:hidden`
     - This ensures the bar extends behind the home indicator on iPhone notch/Dynamic Island devices
   - Only renders on mobile (md:hidden)

2. **Update `src/widgets/dashboard/ui/SpaceLauncher.tsx`**:
   - Desktop only (hidden on mobile: hidden md:flex)
   - Collapsible: icon-only (w-16) by default, expands to icon+label (w-56) on hover or toggle
   - Smooth transition on collapse/expand (transition-all duration-200)
   - Active space has left border accent (border-l-2 border-indigo-600) instead of bg highlight
   - Bottom: collapse/expand toggle button (chevron icon)

3. **Update `src/app/(tenant)/dashboard/layout.tsx`**:
   - Feature flag check: `NEXT_PUBLIC_FOCUS_SPACES === 'true'`
   - If ON: render SpaceLauncher (desktop sidebar) + MobileSpaceBar (mobile bottom) + children
   - If OFF: render old DashboardPage layout (no sidebar, no bottom bar)
   - Layout structure when flag ON:
     ```
     <div className="flex">
       <SpaceLauncher /> {/* desktop sidebar, hidden on mobile */}
       <main className="flex-1 md:pb-0" style={{ paddingBottom: 'calc(4rem + env(safe-area-inset-bottom, 0px))' }}>{children}</main> {/* safe-area-aware padding for mobile bottom bar */}
     </div>
     <MobileSpaceBar /> {/* mobile only */}
     ```
   - SpaceLauncher and MobileSpaceBar both use getVisibleSpaces(role, flags)
   - Active space determined from pathname via usePathname()
   - **Prerequisite (per advisory A3):** Check `src/app/layout.tsx` for `viewport-fit=cover` in the viewport meta tag. If missing, add `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />` — without this, `env(safe-area-inset-bottom)` returns 0 on iOS and the safe-area padding will be ignored.
     </action>
     <verify>
     <automated>npx tsc --noEmit --pretty 2>&1 | grep -E "(MobileSpaceBar|SpaceLauncher)" || echo "No type errors"</automated>
     <manual>On mobile viewport, verify bottom nav bar with 5 space icons. On desktop, verify collapsible sidebar. Toggle between spaces.</manual>
     <sampling_rate>run after this task commits, before next task begins</sampling_rate>
     </verify>
     <done>MobileSpaceBar renders on mobile with 5 space slots. SpaceLauncher is desktop-only, collapsible. Layout switches between old/new UI based on feature flag.</done>
     </task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 2: Verify full Focus Space UI on mobile + desktop</name>
<files>none</files>
<action>Present the complete Focus Space UI to the user for manual verification across desktop and mobile viewports, including feature flag toggle and role-based visibility checks.</action>
<verify>
<automated>echo "Checkpoint: human verification required — no automated command for visual/interactive check"</automated>
<manual>Follow the how-to-verify steps below and confirm the UI works correctly</manual>
</verify>
<done>User approves all Focus Space UI flows on both mobile and desktop viewports, feature flag toggle works, and role-based visibility is correct</done>
<what-built>
Complete Focus Space architecture:
- Desktop: collapsible SpaceLauncher sidebar + space pages with widget grids + HomeLayer
- Mobile: bottom nav bar (5 slots) + space pages + HomeLayer
- Admin: sub-launcher icon grid with domain pages
- My Home: property/household/profile consolidation
- Announcements: accessible from Messages space
- Feature flag: NEXT_PUBLIC_FOCUS_SPACES toggles between old tabs and new spaces
</what-built>
<how-to-verify>
1. Set NEXT_PUBLIC_FOCUS_SPACES=true in .env
2. Run `pnpm dev`
3. **Desktop tests:**
   - Visit /dashboard → HomeLayer renders (Urgency/Today/Activity zones)
   - Click Services in sidebar → /dashboard/services shows maintenance + service widgets
   - Click Community → /dashboard/community shows events + content widgets
   - Click Messages → /dashboard/messages shows messages + notifications widgets
   - Click Admin → icon grid sub-launcher appears, click Users → admin-user widget renders
   - Click Home → My Home space with property/household/profile sections
   - Collapse sidebar to icon-only → icons still clickable, labels hidden
   - Add Widget modal filters by current space
4. **Mobile tests (use browser devtools responsive mode):**
   - Bottom nav bar visible with 5 space icons
   - Tap each space → navigates to correct URL
   - Widget grid renders as stacked cards
   - HomeLayer renders in single-column
5. **Backward compat test:**
   - Set NEXT_PUBLIC_FOCUS_SPACES to empty/false
   - Visit /dashboard → old tab UI renders
6. **Role tests:**
   - As resident: Admin space hidden, non-admin widgets only
   - As admin: All spaces visible, admin widgets render
</how-to-verify>
<resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. MobileSpaceBar renders on mobile with 5 space items
2. SpaceLauncher desktop sidebar is collapsible (icon-only ↔ icon+label)
3. Feature flag toggles between old tabs and new spaces
4. All space pages render correctly on mobile (stacked cards) and desktop (grid)
5. HomeLayer renders at /dashboard
6. Admin sub-launcher works on both mobile and desktop
7. No regression when feature flag is off
</verification>

<success_criteria>

- Mobile bottom bar with 5 space slots per Q1 decision
- Desktop collapsible sidebar with icon-only mode
- Feature flag (NEXT_PUBLIC_FOCUS_SPACES) fully integrated
- Responsive layout: mobile bottom bar + desktop sidebar
- Full user flow verified on both mobile and desktop
- Old tab UI preserved when flag is off (Q5 decision: build alongside, switch with flag)
  </success_criteria>

<output>
After completion, create `.planning/phases/30-dashboard-phase-b/30-05-SUMMARY.md`
</output>
