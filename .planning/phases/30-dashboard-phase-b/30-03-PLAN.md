---
phase: 30-dashboard-phase-b
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/widgets/dashboard/model/spaces.ts
  - src/widgets/dashboard/model/widgets.ts
  - src/widgets/dashboard/ui/SpaceLayout.tsx
  - src/app/(tenant)/dashboard/[space]/page.tsx
  - src/features/dashboard/ui/AddWidgetModal.tsx
  - src/entities/widget/model/dashboard-config.ts
autonomous: true
requirements:
  - FOCUS-06
  - FOCUS-07
  - FOCUS-08
must_haves:
  truths:
    - "Each space page renders its assigned widgets in the widget grid"
    - "AddWidgetModal filters available widgets by current space"
    - "Widget manifests declare which spaces they belong to via spaces field"
    - "Navigating to /dashboard/services shows maintenance + booking + service widgets"
    - "Navigating to /dashboard/admin shows admin-only widgets"
  artifacts:
    - path: "src/widgets/dashboard/ui/SpaceLayout.tsx"
      provides: "Space-specific widget grid layout with edit mode"
      min_lines: 80
    - path: "src/widgets/dashboard/model/widgets.ts"
      provides: "Widget manifests updated with spaces field"
      min_lines: 50
    - path: "src/features/dashboard/ui/AddWidgetModal.tsx"
      provides: "Space-filtered widget picker"
      min_lines: 40
  key_links:
    - from: "src/widgets/dashboard/ui/SpaceLayout.tsx"
      to: "src/entities/widget/model/widget-store.ts"
      via: "useWidgetStore for space-keyed layout data"
      pattern: "useWidgetStore"
    - from: "src/app/(tenant)/dashboard/[space]/page.tsx"
      to: "src/widgets/dashboard/ui/SpaceLayout.tsx"
      via: "renders SpaceLayout for current space"
      pattern: "SpaceLayout"
    - from: "src/features/dashboard/ui/AddWidgetModal.tsx"
      to: "src/widgets/dashboard/model/spaces.ts"
      via: "filters widgets by current space's widgetIds"
      pattern: "SPACES\[.*\]\.widgetIds"
---

<objective>
Wire widgets to spaces — update widget manifests with space assignments, build SpaceLayout component, filter AddWidgetModal by current space.

Purpose: This is the core migration from tab-based to space-based widget layout. Without it, space pages are just placeholders. This plan makes each space render its actual widgets with the existing DraggableWidget/WidgetRenderer infrastructure.

Output: SpaceLayout component rendering per-space widget grids, widget manifests declaring space membership, AddWidgetModal filtering by space.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/architecture/DASHBOARD-PHASE-B-DISCUSSION.md
@src/widgets/dashboard/model/widgets.ts
@src/widgets/dashboard/model/spaces.ts
@src/entities/widget/model/widget-store.ts
@src/page-modules/dashboard/ui/DashboardPage.tsx
@src/features/dashboard/ui/AddWidgetModal.tsx
</context>

<tasks>

<task type="auto">
<name>Task 1: Assign widgets to spaces in manifest + update spaces.ts widgetIds</name>
<files>src/widgets/dashboard/model/widgets.ts, src/widgets/dashboard/model/spaces.ts</files>
<action>
1. **Update `src/widgets/dashboard/model/widgets.ts`** — add `spaces` field to each registry.register() call:

Space assignments (derived from discussion doc table + user decisions):

- **home** space: stats, quick-actions, recent-activity, notifications, solo-seat, properties
- **services** space: maintenance-requests, maintenance-list, maintenance-analytics, my-services, service-inquiries, events (bookings)
- **community** space: events, announcements-stream, my-content, my-album, media, bookshelf, admin-events, admin-surveys, group-moderation, admin-announcements, admin-competitions, admin-resources, admin-content
- **messages** space: messages, notifications
- **admin** space: admin-stats, admin-activity, admin-quick-links, admin-user, admin-system, page-settings, admin-announcements

For each `registry.register()` call, add `spaces: ['spaceId1', 'spaceId2']` field. Example:

```typescript
registry.register({
  id: 'stats',
  // ... existing fields ...
  spaces: ['home'], // NEW
});
```

Some widgets belong to multiple spaces (e.g., `notifications` → ['home', 'messages'], `events` → ['services', 'community']).

2. **Update `src/widgets/dashboard/model/spaces.ts`** — make `widgetIds` in each SPACES entry derive from registry instead of being hardcoded:
   - Add a `getSpaceWidgets(spaceId: string): WidgetManifest[]` function that filters registry.list() by `manifest.spaces?.includes(spaceId)`
   - Keep the static widgetIds arrays as fallback / initial values
   - Export `getSpaceWidgetIds(spaceId: string): string[]` as convenience wrapper
   - **CRITICAL: These functions must ONLY be called inside React components or hooks (not at module initialization).** The registry is populated by `registerAllWidgets()` at component mount time — module-level calls return empty arrays. Add JSDoc:
     ```ts
     /**
      * Call only inside React components or hooks — registry is populated at runtime.
      * Module-level calls will return an empty array.
      */
     export function getSpaceWidgets(spaceId: SpaceId): WidgetManifest[] {
     ```
   - Add runtime guard: `if (registry.list().length === 0) { console.warn('getSpaceWidgets called before registerAllWidgets()'); }`

3. **Update `src/widgets/dashboard/model/types.ts`** — make the `spaces` field required in WidgetManifest:
   - Change `spaces?: SpaceId[]` to `spaces: SpaceId[]`
   - Define `SpaceId = 'home' | 'services' | 'community' | 'messages' | 'admin'` type
   - Default value: if a widget doesn't specify spaces, it goes to 'home' (but all should specify explicitly)

4. **Add space assignment comment block** at the top of `widgets.ts` (after imports, before `registerAllWidgets()`):
   ```ts
   /**
    * Space assignments (Focus Spaces architecture — Phase 30-B)
    *
    * home:        stats, quick-actions, recent-activity, notifications, solo-seat, properties
    * services:    maintenance-requests, maintenance-list, maintenance-analytics, my-services, service-inquiries, events
    * community:   events, announcements-stream, my-content, my-album, media, bookshelf,
    *              admin-events, admin-surveys, group-moderation, admin-announcements,
    *              admin-competitions, admin-resources, admin-content
    * messages:    messages, notifications
    * admin:       admin-stats, admin-activity, admin-quick-links, admin-user, admin-system, page-settings, admin-announcements
    *
    * Some widgets belong to multiple spaces:
    *   notifications → ['home', 'messages']
    *   events        → ['services', 'community']
    *   admin-announcements → ['community', 'admin']
    */
   ```
   This makes the intended assignments reviewable alongside the code, preventing silent misassignment bugs.
   </action>
   <verify>
   <automated>npx tsc --noEmit --pretty 2>&1 | grep -E "(widgets\.ts|spaces\.ts|types\.ts)" | head -10 || echo "No type errors in widget files"</automated>
   <automated-extra>node -e "
   const spaces = ['home','services','community','messages','admin'];
   // After registerAllWidgets is called, each space must have ≥1 widget
   // This runs as a post-build smoke test
   spaces.forEach(s => {
   // This will be validated manually after imports work —
   // the intent is: assert every space has ≥1 widget assigned
   console.log('Smoke: space ' + s + ' must have ≥1 widget (verify manually)');
   });
   " || echo "Smoke test placeholder — replace with registry-based check after module init works"</automated-extra>
   <manual>Run `node -e "const {registry} = require('./src/widgets/dashboard/model/registry'); console.log(registry.list().filter(w => w.spaces?.includes('services')).map(w => w.id))"` or check in browser</manual>
   <sampling_rate>run after this task commits, before next task begins</sampling_rate>
   </verify>
   <done>All 30+ widget manifests have explicit `spaces` field. SpaceId type defined. getSpaceWidgets/getSpaceWidgetIds functions work. No type errors.</done>
   </task>

<task type="auto">
<name>Task 2: Build SpaceLayout component and wire space pages</name>
<files>src/widgets/dashboard/ui/SpaceLayout.tsx, src/app/(tenant)/dashboard/[space]/page.tsx, src/features/dashboard/ui/AddWidgetModal.tsx</files>
<action>
1. **Create `src/widgets/dashboard/ui/SpaceLayout.tsx`**:
   - Client component ('use client')
   - Props: `spaceId: string`
   - Reads `userWidgets[spaceId]` from useWidgetStore for the widget list
   - Falls back to `roleDefaults.userWidgets[spaceId]` from getDefaultLayout
   - Falls back to `getSpaceWidgetIds(spaceId)` from spaces.ts (space registry default)
   - Renders the widget grid using existing DraggableWidget + WidgetRenderer (same pattern as DashboardPage)
   - Edit mode toggle (edit/remove widgets)
   - Add widget button opens AddWidgetModal filtered by space
   - Reset to defaults button per space
   - Mobile: WidgetCard stacked layout (same as DashboardPage mobile pattern)
   - Desktop: DraggableWidget grid (same as DashboardPage desktop pattern)
   - Uses `useWidgetStore` with space-keyed lookups (spaceId replaces tabId)
   - Handles empty state: "No widgets in this space" + Add Widget button

2. **Update `src/app/(tenant)/dashboard/[space]/page.tsx`**:
   - Replace placeholder content with `<SpaceLayout spaceId={space} />`
   - Validate space param against SPACE_SLUGS
   - Role-gate admin space (redirect non-admin to /dashboard)
   - Add breadcrumbs: Home > Dashboard > [Space Name]
   - Wrap in ErrorBoundary

3. **Update `src/features/dashboard/ui/AddWidgetModal.tsx`**:
   - Add optional `spaceId` prop
   - When `spaceId` is provided, filter available widgets to only those whose manifest includes this space
   - Use `getSpaceWidgetIds(spaceId)` to get the allowed widget IDs
   - Cross-reference with registry.list() to get widget metadata
   - When `spaceId` is NOT provided (backward compat), show all widgets (existing behavior)
   - Also filter out widgets already active in the current space
     </action>
     <verify>
     <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
     <manual>Navigate to /dashboard/services — should show maintenance + service widgets in grid. /dashboard/admin — should show admin widgets. Click "Add Widget" — modal shows only widgets for that space.</manual>
     <sampling_rate>run after this task commits, before next task begins</sampling_rate>
     </verify>
     <done>Space pages render widget grids with space-specific widgets. AddWidgetModal filters by space. Admin space role-gated. All existing widgets appear in appropriate spaces.</done>
     </task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. /dashboard/services renders maintenance + service widgets
3. /dashboard/community renders events + content + admin-content widgets (for admin)
4. /dashboard/messages renders messages + notifications widgets
5. /dashboard/admin renders admin widgets, redirects non-admin
6. AddWidgetModal shows only space-relevant widgets
7. Widget edit/add/remove works per space
8. Widget layout persists per space in widget-store
</verification>

<success_criteria>

- All widget manifests have explicit `spaces` field assignments
- SpaceLayout component renders per-space widget grids reusing DraggableWidget/WidgetRenderer
- AddWidgetModal filters by current space
- Dynamic route /dashboard/[space] renders SpaceLayout with validated space slug
- Admin space is role-gated
- Widget store lookups use spaceId instead of tabId
  </success_criteria>

<output>
After completion, create `.planning/phases/30-dashboard-phase-b/30-03-SUMMARY.md`
</output>
