---
phase: 22-page-flag-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/entities/tenant/api/settings.ts
  - src/entities/tenant/api/flags/platform-flags.ts
  - src/entities/tenant/api/flags/platform-flags.test.ts
  - src/app/api/admin/settings/page-flags/route.ts
  - src/app/api/flags/route.ts
  - src/shared/lib/hooks/usePageFlags.ts
autonomous: true
requirements: [FLAG-EXP-01, FLAG-EXP-02]

must_haves:
  truths:
    - 'SETTINGS_KEYS includes new keys for Groups, Services, Resources, Maintenance, Surveys, and Competitions'
    - 'getPlatformPageFlags returns new flags with default value true'
    - 'Admin API validKeys includes new flag keys'
    - 'Public API validFlags includes new flag keys'
    - 'Unit tests pass for all new flag logic'
  artifacts:
    - path: 'src/entities/tenant/api/settings.ts'
      provides: 'Expanded SETTINGS_KEYS'
    - path: 'src/entities/tenant/api/flags/platform-flags.ts'
      provides: 'Expanded PlatformPageFlags logic'
    - path: 'src/entities/tenant/api/flags/platform-flags.test.ts'
      provides: 'Nyquist-compliant test coverage for flags'
    - path: 'src/shared/lib/hooks/usePageFlags.ts'
      provides: 'Unified client-side flag fetching'
  key_links:
    - from: 'src/app/api/flags/route.ts'
      to: 'getPlatformPageFlags'
      via: 'API call'
---

<objective>
Establish the data foundation, API support, and testing for expanded page visibility flags.
</objective>

<execution_context>
@/home/ubuntupunk/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/entities/tenant/api/settings.ts
@src/entities/tenant/api/flags/platform-flags.ts
@src/app/api/admin/settings/page-flags/route.ts
@src/app/api/flags/route.ts
</context>

<tasks>

<task type="auto">
  <name>Expand Settings Logic and Add Tests</name>
  <files>
    - src/entities/tenant/api/settings.ts
    - src/entities/tenant/api/flags/platform-flags.ts
    - src/entities/tenant/api/flags/platform-flags.test.ts
  </files>
  <action>
    1. Update `SETTINGS_KEYS` in `src/entities/tenant/api/settings.ts` to include:
       - PAGE_GROUPS_ENABLED: 'page_groups_enabled'
       - PAGE_SERVICES_ENABLED: 'page_services_enabled'
       - PAGE_RESOURCES_ENABLED: 'page_resources_enabled'
       - PAGE_MAINTENANCE_ENABLED: 'page_maintenance_enabled'
       - PAGE_SURVEYS_ENABLED: 'page_surveys_enabled'
       - PAGE_COMPETITIONS_ENABLED: 'page_competitions_enabled'
    2. Update `PlatformPageFlags` interface in `src/entities/tenant/api/flags/platform-flags.ts` to include these 6 new boolean flags.
    3. Update `DEFAULT_PAGE_FLAGS` to set all new flags to `true`.
    4. Update `getPlatformPageFlags` switch statement to map the new `SETTINGS_KEYS` to the interface properties.
    5. Update `mapFlagToSettingKey` to include mappings for the new flags.
    6. Create `src/entities/tenant/api/flags/platform-flags.test.ts` with tests that:
       - Verify `getPlatformPageFlags` returns default values for empty settings.
       - Verify `getPlatformPageFlags` correctly overrides defaults with provided settings.
       - Verify `mapFlagToSettingKey` correctly maps all flag keys to their respective setting keys.
  </action>
  <verify>
    <automated>npx vitest src/entities/tenant/api/flags/platform-flags.test.ts --run</automated>
    <manual>Confirm that types compile without errors</manual>
    <sampling_rate>always</sampling_rate>
  </verify>
  <done>Flag logic supports 6 new page visibility settings and is fully tested</done>
</task>

<task type="auto">
  <name>Update Flag APIs and Create Shared Hook</name>
  <files>
    - src/app/api/admin/settings/page-flags/route.ts
    - src/app/api/flags/route.ts
    - src/shared/lib/hooks/usePageFlags.ts
  </files>
  <action>
    1. Update `validKeys` array in `src/app/api/admin/settings/page-flags/route.ts` (POST) to include the new flag names.
    2. Update `validFlags` array in `src/app/api/flags/route.ts` (GET) to include the new flag names.
    3. Create `src/shared/lib/hooks/usePageFlags.ts` that:
       - Uses `useState` and `useEffect` to fetch flags from `/api/flags`.
       - Provides a loading state and the flags object.
       - Should be a client-side hook.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/api/flags | jq '.flags'</automated>
    <manual>Check if the API response includes the new flags with value true</manual>
    <sampling_rate>always</sampling_rate>
  </verify>
  <done>APIs support new flags and a shared hook is available for UI components</done>
</task>

</tasks>

<success_criteria>

- All new settings keys are defined.
- getPlatformPageFlags returns the expanded flag set.
- Both public and admin APIs recognize the new flag keys.
- usePageFlags hook is ready for use in navigation components.
- Unit tests pass for the new flag logic.
  </success_criteria>

<output>
After completion, create .planning/phases/22-page-flag-expansion/22-01-SUMMARY.md
</output>
