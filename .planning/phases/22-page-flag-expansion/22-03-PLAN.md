---
phase: 22-page-flag-expansion
plan: 03
type: execute
wave: 3
depends_on: ['22-02']
files_modified:
  - src/shared/ui/Header.tsx
  - src/shared/ui/Footer.tsx
  - src/shared/ui/MobileMenu.tsx
autonomous: false
requirements: [FLAG-EXP-05]

must_haves:
  truths:
    - 'Header navigation items are hidden when their corresponding flag is disabled'
    - 'Footer navigation items are hidden when their corresponding flag is disabled'
    - 'MobileMenu items respect the page visibility flags'
  artifacts:
    - path: 'src/shared/ui/Header.tsx'
      provides: 'Flag-aware main navigation'
    - path: 'src/shared/ui/Footer.tsx'
      provides: 'Flag-aware footer links'
  key_links:
    - from: 'Header'
      to: 'usePageFlags'
      via: 'Custom hook'
    - from: 'Footer'
      to: 'usePageFlags'
      via: 'Custom hook'
---

<objective>
Implement navigation visibility logic across Header, Footer, and MobileMenu to respect the expanded set of page flags.
</objective>

<execution_context>
@/home/ubuntupunk/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-page-flag-expansion/22-02-SUMMARY.md
@src/shared/ui/Header.tsx
@src/shared/ui/Footer.tsx
@src/shared/ui/MobileMenu.tsx
</context>

<tasks>

<task type="auto">
  <name>Update Header and MobileMenu Navigation</name>
  <files>
    - src/shared/ui/Header.tsx
    - src/shared/ui/MobileMenu.tsx
  </files>
  <action>
    1. In `src/shared/ui/Header.tsx`:
       - Import `usePageFlags` hook.
       - Expand `BASE_NAV` to include: groups, services, resources, maintenance, surveys, competitions.
       - Replace local state and fetch for `pageFlags` with `usePageFlags()` hook.
       - Update `navItems` filtering logic to include checks for the 6 new flags.
    2. In `src/shared/ui/MobileMenu.tsx`:
       - Import `usePageFlags` hook.
       - Wrap the maintenance, dashboard, etc. links with flag checks (e.g., `{flags.maintenance && <Link ...>}`).
  </action>
  <verify>
    <automated>grep -E "groups|services|resources|maintenance|surveys|competitions" src/shared/ui/Header.tsx</automated>
    <manual>Check the main navigation bar in the browser; disable a flag in Admin and verify it disappears in Header</manual>
    <sampling_rate>always</sampling_rate>
  </verify>
  <done>Header and MobileMenu respect the new page visibility flags</done>
</task>

<task type="auto">
  <name>Update Footer Navigation</name>
  <files>
    - src/shared/ui/Footer.tsx
  </files>
  <action>
    1. In `src/shared/ui/Footer.tsx`:
       - Import `usePageFlags` hook.
       - Wrap the Quick Links (directory, services, resources, conservation) and Services Links (maintenance) with flag checks.
       - For example, only show the `<li>` containing `/directory` if `flags.directory` is true.
  </action>
  <verify>
    <automated>grep "usePageFlags" src/shared/ui/Footer.tsx</automated>
    <manual>Disable a flag in Admin and verify the corresponding link disappears from the Footer</manual>
    <sampling_rate>always</sampling_rate>
  </verify>
  <done>Footer navigation respects the new page visibility flags</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full end-to-end page visibility flag system</what-built>
  <how-to-verify>
    1. Go to Admin -> Page Settings.
    2. Toggle "Groups" to OFF.
    3. Check Header, Footer, and Mobile Menu; "Groups" link should be hidden.
    4. Toggle "Maintenance" to OFF.
    5. Check Header, Footer, and Mobile Menu; "Maintenance" links should be hidden.
    6. Repeat for other flags (Services, Resources, Surveys, Competitions).
    7. Toggle them all back to ON and verify they reappear.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>

- Main navigation in Header correctly filters items based on tenant settings.
- Footer links are conditionally rendered based on tenant settings.
- Mobile Menu correctly gates links based on tenant settings.
- All new flags are functional and responsive to Admin changes.
  </success_criteria>

<output>
After completion, create .planning/phases/22-page-flag-expansion/22-03-SUMMARY.md
</output>
