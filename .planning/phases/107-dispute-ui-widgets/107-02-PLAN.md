---
phase: 107-dispute-ui-widgets
plan: 02
type: execute
wave: 2
depends_on:
  - 107-01
files_modified:
  - src/features/dispute/ui/intake/EmotionCheckIn.tsx
  - src/features/dispute/ui/intake/SelfResolutionChecklist.tsx
  - src/features/dispute/ui/intake/FrivolityScreen.tsx
  - src/features/dispute/ui/intake/ConflictTipsPanel.tsx
  - src/features/dispute/ui/intake/ReviewScreen.tsx
  - src/features/dispute/ui/intake/DisputeIntakeWizard.tsx
  - src/features/dispute/ui/DisputeForm.tsx
  - src/features/dispute/model/useDisputeIntake.ts
  - src/features/dispute/ui/intake/__tests__/EmotionCheckIn.test.tsx
  - src/features/dispute/ui/intake/__tests__/SelfResolutionChecklist.test.tsx
  - src/features/dispute/ui/intake/__tests__/FrivolityScreen.test.tsx
  - src/features/dispute/ui/intake/__tests__/DisputeForm.test.tsx
autonomous: true
requirements:
  - DISPUTE-06

must_haves:
  truths:
    - "EmotionCheckIn renders 5 emoji buttons, highlights selected, shows soft gate banner on 'very_angry' or 'upset'"
    - 'SelfResolutionChecklist renders 4 checkboxes, surfaces contextual tip when <2 checked'
    - "FrivolityScreen renders AI results (toneScore, likelyFrivolous, deEscalationTip) from intake-screen API, always shows 'Proceed' button"
    - 'DisputeForm validates with Zod schema, submits to POST /api/disputes, navigates to /disputes/[id] on success'
    - 'DisputeIntakeWizard orchestrates 6 stages with forward-only transitions, auto-skips frivolity when aiEnabled=false'
  artifacts:
    - path: 'src/features/dispute/ui/intake/DisputeIntakeWizard.tsx'
      provides: 'Wizard orchestrator consuming useWorkflow with dispatchDisputeCreateSchema'
    - path: 'src/features/dispute/ui/intake/EmotionCheckIn.tsx'
      provides: 'Emoji-scale emotion selection with soft gate on upset/angry'
      exports: ['EmotionCheckIn']
    - path: 'src/features/dispute/ui/intake/FrivolityScreen.tsx'
      provides: 'AI frivolity assessment display calling /api/disputes/intake-screen'
      exports: ['FrivolityScreen']
    - path: 'src/features/dispute/ui/DisputeForm.tsx'
      provides: 'React Hook Form + Zod dispute creation form'
      exports: ['DisputeForm']
    - path: 'src/features/dispute/model/useDisputeIntake.ts'
      provides: 'Intake wizard state management hook wrapping useWorkflow + autoSave'
      exports: ['useDisputeIntake']
  key_links:
    - from: 'src/features/dispute/ui/intake/DisputeIntakeWizard.tsx'
      to: 'src/shared/lib/workflow/useWorkflow.ts'
      via: 'import useWorkflow from @shared/lib/workflow'
      pattern: "import.*useWorkflow.*from '@shared/lib/workflow'"
    - from: 'src/features/dispute/ui/intake/FrivolityScreen.tsx'
      to: 'POST /api/disputes/intake-screen'
      via: 'fetch() in useEffect on mount'
      pattern: 'fetch.*api/disputes/intake-screen'
    - from: 'src/features/dispute/ui/DisputeForm.tsx'
      to: 'POST /api/disputes'
      via: 'React Hook Form handleSubmit → fetch'
      pattern: 'fetch.*api/disputes'
---

<objective>
Build the 6-stage psychological intake wizard (DISPUTE-06) — the de-escalation gate that residents complete before filing a dispute. Covers the 4 wizard stage components plus the DisputeForm, wizard orchestrator, intake state management hook, auto-save integration, and component tests.

Purpose: This is the primary gatekeeping UX for dispute resolution — reduces frivolous filings through emotional check-in, self-resolution prompts, AI frivolity screening, and conflict education before the dispute form even appears.

Output: 6 wizard stage components, DisputeForm (React Hook Form + Zod), DisputeIntakeWizard orchestrator, useDisputeIntake hook, 4 test files. All wizard stages render inline within the my-disputes widget frame (widget in Plan 03).
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-dispute-ui-widgets/107-CONTEXT.md
@.planning/phases/107-dispute-ui-widgets/107-RESEARCH.md
@.planning/phases/107-dispute-ui-widgets/107-UI-SPEC.md
@src/entities/dispute/model/types.ts
@src/entities/dispute/model/constants.ts
@src/entities/dispute/model/schemas.ts
@src/shared/lib/dispute/intake-screen-output.ts

<interfaces>
<!-- Types consumed by wizard components -->
From src/entities/dispute/model/types.ts:
export type DisputeCategory = 'NOISE' | 'PETS' | 'PARKING' | 'BOUNDARIES' | ... | 'OTHER';
export type DisputeSeverity = 'MINOR' | 'MODERATE' | 'SERIOUS' | 'URGENT';
export type DisputeRespondent = 'RESIDENT' | 'HOA' | 'BOARD_MEMBER' | 'TENANT_PROVIDER';

From src/entities/dispute/model/schemas.ts:
export const disputeCreateSchema = z.object({
category: z.enum(ALL_DISPUTE_CATEGORIES),
title: z.string().min(5).max(200),
description: z.string().min(10).max(5000),
respondentId: z.string().uuid().optional(),
respondentType: z.enum(['RESIDENT','HOA','BOARD_MEMBER','TENANT_PROVIDER']).default('RESIDENT'),
desiredOutcome: z.string().max(2000).optional(),
severity: z.enum(['MINOR','MODERATE','SERIOUS','URGENT']).default('MODERATE'),
}).strip();
export type DisputeCreateInput = z.infer<typeof disputeCreateSchema>;

From src/entities/dispute/model/constants.ts:
export const CATEGORY_LABELS: Record<DisputeCategory, string>;
export const SEVERITY_LABELS: Record<DisputeSeverity, string>;
export const RESPONDENT_LABELS: Record<DisputeRespondent, string>;

From src/shared/lib/dispute/intake-screen-output.ts:
export type IntakeScreenOutput = {
toneScore: number;
issueClarity: number;
likelyFrivolous: boolean;
suggestedCategory: string;
deEscalationTip: string | null;
};
export function parseIntakeScreenOutput(raw: string): IntakeScreenOutput;

<!-- Workflow engine API (built in Plan 01) -->

From src/shared/lib/workflow/useWorkflow.ts:
export function useWorkflow<S extends string, C extends Record<string, unknown>>(
config: WorkflowConfig<S, C>
): {
currentStep: S;
currentStepConfig: WorkflowStep<S, C>;
ctx: C;
updateCtx: (patch: Partial<C>) => void;
next: () => void;
completedSteps: S[];
isComplete: boolean;
validationError: string | null;
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build wizard stage components — EmotionCheckIn, SelfResolutionChecklist, FrivolityScreen, ConflictTipsPanel, ReviewScreen</name>
  <files>src/features/dispute/ui/intake/EmotionCheckIn.tsx, src/features/dispute/ui/intake/SelfResolutionChecklist.tsx, src/features/dispute/ui/intake/FrivolityScreen.tsx, src/features/dispute/ui/intake/ConflictTipsPanel.tsx, src/features/dispute/ui/intake/ReviewScreen.tsx</files>
  <read_first>
@src/features/dispute/ui/intake/EmotionCheckIn.tsx (will be created)
@src/features/dispute/ui/intake/SelfResolutionChecklist.tsx (will be created)
@src/features/dispute/ui/intake/FrivolityScreen.tsx (will be created)
@src/features/dispute/ui/intake/ConflictTipsPanel.tsx (will be created)
@src/features/dispute/ui/intake/ReviewScreen.tsx (will be created)
@.planning/phases/107-dispute-ui-widgets/107-CONTEXT.md Search for "Stage 1 — EmotionCheckIn" through "Stage 6 — ReviewScreen" — exact behavior specs for all 5 stages
@.planning/phases/107-dispute-ui-widgets/107-UI-SPEC.md Search for "EmotionCheckIn", "SelfResolutionChecklist", "FrivolityScreen", "ConflictTipsPanel", "ReviewScreen" — copywriting contract and visual specs
@src/entities/dispute/ui/DisputeStatusBadge.tsx (pattern reference: badge rendering, color map)
@src/shared/lib/dispute/intake-screen-output.ts (IntakeScreenOutput type and parseIntakeScreenOutput)
  </read_first>
  <action>
Create 5 wizard stage components under src/features/dispute/ui/intake/. Each is a 'use client' component that receives props from the DisputeIntakeWizard orchestrator (Task 2). Every component follows the Tailwind spacing/typography/color contracts from UI-SPEC.md.

Component 1 — EmotionCheckIn.tsx:
Props: { selected: string | null, onSelect: (emotion: string) => void }

- Render 5 emoji buttons as specified in UI-SPEC.md "Intake Wizard" copywriting contract and RESEARCH.md §Code Examples EmotionCheckIn:
  😤 Very angry, 😟 Upset, 😐 Neutral, 🙂 Calm, 😌 Resolved (values: 'very_angry', 'upset', 'neutral', 'calm', 'resolved')
- Highlight selected button with `border-soralia-primary bg-indigo-50 scale-110` (accent color #4F46E5 per UI-SPEC §Color)
- Heading: "How are you feeling about this situation?" (text-lg font-semibold per UI-SPEC §Typography)
- Subtitle: "This helps us understand your perspective. Your emotional state is not stored — only that you completed this check-in." (text-sm text-gray-500)
- Soft gate: if selected is 'very_angry' or 'upset', render amber-50 banner with copy from UI-SPEC.md "Stage 1 soft gate"
- aria-pressed on selected button

Component 2 — SelfResolutionChecklist.tsx:
Props: { checked: Record<string, boolean>, onToggle: (id: string) => void }

- Render 4 checkboxes with copy from UI-SPEC.md "Stage 2 checklist items":
  1. "I have spoken to the other party directly"
  2. "I have checked the community rules"
  3. "I have given reasonable time for resolution"
  4. "I believe third-party help is needed"
- Use input[type=checkbox] with Tailwind accent-soralia-primary. Minimum 44px clickable area on mobile (per UI-SPEC §Spacing touch targets): h-11, flex items-center
- Count checked items. If <2 checked: surface inline tip with copy from UI-SPEC.md "Stage 2 tip" using amber-50 border-amber-200 style (matching EmotionCheckIn soft gate pattern)
- Link "conflict resolution tips" to ConflictTipsPanel via an anchor or context-based scroll (use a simple ref passed from wizard)

Component 3 — FrivolityScreen.tsx:
Props: { description: string, onResult: (result: IntakeScreenOutput) => void, onProceed: () => void }

- On mount, call POST /api/disputes/intake-screen with JSON body { description } using fetch()
- Show loading state: "Analyzing your dispute..." with LoadingSkeleton from @shared/ui
- On success: parse response via parseIntakeScreenOutput(). Pass result to onResult.
- Render the AI results:
  - If likelyFrivolous is true: show AlertTriangle icon (lucide-react), heading "Our system flagged some concerns", body "Please review the following before proceeding."
  - If likelyFrivolous is false: show CheckCircle icon (lucide-react), heading "Your dispute does not appear frivolous."
  - Always show toneScore, deEscalationTip (if non-null)
  - Always show advisory note: "This is an automated assessment. You may still file your dispute regardless of this result." (text-xs text-gray-500)
- Show "Proceed" button (bg-soralia-primary text-white) always — per CONTEXT.md "Always advisory — user can always proceed"
- On error (fetch failure): show degraded state "Assessment unavailable" with message from UI-SPEC.md Error States — still show Proceed button
- Props contract: FrivolityScreen does NOT check aiEnabled — the wizard orchestrator (Task 2) skips this component entirely when aiEnabled=false (via WorkflowStep.condition). This component assumes it's been invoked.

Component 4 — ConflictTipsPanel.tsx:
Props: { emotionScore: string } (the selected emotion value from Stage 1)

- Render static, collapsible conflict tips content as specified in UI-SPEC.md "Stage 4 heading" and "Stage 4 content"
- Group tips into three categories: heated disputes (noise, pets, parking), HOA rule disputes, escalation-ready situations
- Use <details>/<summary> for collapsible sections (no JavaScript needed). Default: first section open.
- Contextual per emotion score: if 'very_angry' or 'upset', open the "heated disputes" section by default and highlight with amber left border
- Include a "Continue to Dispute Form" CTA button at the bottom (bg-soralia-primary text-white)

Component 5 — ReviewScreen.tsx:
Props: { formData: DisputeCreateInput, aiResult: IntakeScreenOutput | null, aiEnabled: boolean, onSubmit: () => void, isSubmitting: boolean }

- Display all form fields in a read-only summary layout: category (with label from CATEGORY_LABELS), title, description (truncated to 150 chars with "..."), desiredOutcome, respondentType (from RESPONDENT_LABELS), severity (from SEVERITY_LABELS)
- If aiEnabled and aiResult non-null: show AI warning compact banner — "AI noted: tone appears emotional. Consider focusing on facts." (amber-50 left border) with toneScore badge
- Show "Estimated process" timeline: Submit → Moderator Review → Mediation → Resolution (avg 7-14 days) — simple horizontal step indicator using Tailwind flex/gap, not an interactive component
- Submit button: "Submit Dispute" (bg-soralia-primary text-white) with loading spinner when isSubmitting
- Cancel button: "Back to list" (text-gray-600 hover:text-gray-800)
- Show final file count: "N attachments included" if form has attachments (placeholder for now — evidence uploader is on the detail page, not the intake wizard; show 0 for this phase)
  </action>
  <verify>
  <automated>pnpm tsc --noEmit --pretty src/features/dispute/ui/intake/EmotionCheckIn.tsx src/features/dispute/ui/intake/SelfResolutionChecklist.tsx src/features/dispute/ui/intake/FrivolityScreen.tsx src/features/dispute/ui/intake/ConflictTipsPanel.tsx src/features/dispute/ui/intake/ReviewScreen.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria> - All 5 components exist at the specified paths with 'use client' directive - EmotionCheckIn renders 5 emoji buttons with aria-pressed, highlights selected, shows amber soft gate for upset/angry - SelfResolutionChecklist renders 4 checkboxes with min-h-11 touch targets, surfaces tip when <2 checked - FrivolityScreen fetches POST /api/disputes/intake-screen on mount, renders toneScore/likelyFrivolous/deEscalationTip, always shows Proceed button even on error - ConflictTipsPanel renders 3 collapsible sections with contextual default expansion per emotion score - ReviewScreen renders all form fields as read-only summary, AI banner if applicable, estimated timeline, Submit + Cancel buttons - All copy matches UI-SPEC.md §Copywriting Contract exactly - Tailwind classes use the spacing/color/typography contracts from UI-SPEC.md (no custom pixel values) - `pnpm tsc --noEmit` on all 5 files → zero errors
  </acceptance_criteria>
  <done>5 wizard stage components rendering with correct copy, Tailwind styling per UI-SPEC, and prop interfaces ready for wizard orchestrator</done>
  </task>

<task type="auto">
  <name>Task 2: Build DisputeForm, DisputeIntakeWizard orchestrator, and useDisputeIntake hook</name>
  <files>src/features/dispute/ui/DisputeForm.tsx, src/features/dispute/ui/intake/DisputeIntakeWizard.tsx, src/features/dispute/model/useDisputeIntake.ts</files>
  <read_first>
@src/features/dispute/ui/DisputeForm.tsx (will be created)
@src/features/dispute/ui/intake/DisputeIntakeWizard.tsx (will be created)
@src/features/dispute/model/useDisputeIntake.ts (will be created)
@.planning/phases/107-dispute-ui-widgets/107-RESEARCH.md Search for "Pattern 3: Widget with Content Replacement" and "Pattern 1: Workflow Engine"
@.planning/phases/107-dispute-ui-widgets/107-CONTEXT.md Search for "State machine:" and "Stage 5 — Dispute Form" and "Stage 6 — ReviewScreen"
@src/entities/dispute/model/schemas.ts (disputeCreateSchema, DisputeCreateInput)
@src/entities/dispute/model/types.ts (DisputeCaseDTO for return type)
@src/shared/lib/workflow/useWorkflow.ts (useWorkflow hook API from Plan 01)
@src/shared/lib/useAutoSave.ts (useAutoSave hook API from Plan 01)
  </read_first>
  <action>
Create 3 files that wire the wizard stages together into a functional intake flow:

File 1 — src/features/dispute/ui/DisputeForm.tsx ('use client'):

- Props: { onComplete: (disputeId: string) => void, onCancel: () => void }
- Use React Hook Form with zodResolver(disputeCreateSchema):
  - Import { useForm } from 'react-hook-form', { zodResolver } from '@hookform/resolvers/zod'
  - Infer DisputeCreateInput from disputeCreateSchema
- Fields (all with labels from UI-SPEC.md "DisputeForm"):
  - category: <select> with options from ALL_DISPUTE_CATEGORIES, labels from CATEGORY_LABELS
  - title: <input type="text">, placeholder "Brief title describing the issue"
  - description: <textarea>, placeholder "Describe what happened, when, and who was involved"
  - desiredOutcome: <textarea>, placeholder "What resolution would you like to see?"
  - respondentType: <select> with options: RESIDENT, HOA, BOARD_MEMBER, TENANT_PROVIDER (labels from RESPONDENT_LABELS)
  - respondentId: <input type="text"> (optional UUID — simple text for MVP, no user search)
  - severity: <select> with options from MINOR/MODERATE/SERIOUS/URGENT, labels from SEVERITY_LABELS, default MODERATE
- Validation errors: show inline beneath each field in text-red-600 text-xs (per UI-SPEC §Typography form-specific)
- Submit button: "Submit Dispute" (bg-soralia-primary text-white), loading state: "Submitting..."
- On submit (handleSubmit): POST to /api/disputes with JSON body → parse response JSON → extract dispute.id → call onComplete(disputeId)
- On error: show toast via sonner (import { toast } from 'sonner') with message from UI-SPEC.md "Form submission failure"
- Form layout: stacked single column, max-w-lg mx-auto, gap-4 between fields, labels in text-sm font-medium text-gray-700
- Cancel button at bottom: "Back to list" calls onCancel()

File 2 — src/features/dispute/ui/intake/DisputeIntakeWizard.tsx ('use client'):
Props: { aiEnabled: boolean, onComplete: (disputeId: string) => void, onCancel: () => void }

- Define wizard step IDs as const type: 'emotion' | 'checklist' | 'frivolity' | 'tips' | 'form' | 'review'
- Define IntakeContext: { selectedEmotion: string | null, checklistItems: Record<string, boolean>, description: string, aiResult: IntakeScreenOutput | null, aiEnabled: boolean, formData: DisputeCreateInput | null }
- Use the useDisputeIntake hook (File 3) which wraps useWorkflow + useAutoSave
- Render the current step component based on workflow.currentStep:
  - 'emotion' → <EmotionCheckIn> (from Task 1)
  - 'checklist' → <SelfResolutionChecklist> (from Task 1)
  - 'frivolity' → <FrivolityScreen> (from Task 1)
  - 'tips' → <ConflictTipsPanel> (from Task 1)
  - 'form' → <DisputeForm> (File 1 above)
  - 'review' → <ReviewScreen> (from Task 1)
- Wizard progress display: numbered inline steps per CONTEXT.md — "1. How are you feeling?" etc. (copy from UI-SPEC.md "Intake Wizard" step labels). Completed steps show checkmark (CheckCircle icon from lucide-react in soralia-primary). Current step highlighted with font-semibold text-soralia-primary.
- Breadcrumb at top: "← Back to My Disputes ({N} active)" — N is placeholder 0 for now (Plan 03 MyDisputesWidget provides the actual count)
- On completion (workflow.isComplete): navigates to /disputes/[id] via onComplete prop
- On cancel: calls onCancel (returns to widget list view)

File 3 — src/features/dispute/model/useDisputeIntake.ts ('use client'):

- Export function useDisputeIntake(aiEnabled: boolean, onComplete: (disputeId: string) => void, onCancel: () => void)
- Define IntakeStep literal type and IntakeContext type (as above)
- Build workflow config:
  - steps: emotion → checklist → frivolity (condition: ctx.aiEnabled) → tips → form (validate: ensure formData not null) → review
  - transitions: emotion→checklist, checklist→frivolity (guard: ctx.aiEnabled), checklist→tips (guard: !ctx.aiEnabled), frivolity→tips, tips→form, form→review, review→**complete**
  - initialStep: 'emotion'
  - onComplete: construct formData from ctx, POST to /api/disputes, call onComplete with returned dispute.id
- Use auto-save via useAutoSave hook from Plan 01: key="dispute-intake-wizard", data=ctx, delay=2000 (default)
- Restore from localStorage on mount: if savedData exists, use it as initial ctx (pre-populate emotion, checklist, description, aiResult)
- Show "saved N seconds ago" indicator using secondsSinceSave from useAutoSave
- Return the workflow state + autoSave state for the wizard component to render
- Export type { IntakeStep, IntakeContext } for the wizard component to use
  </action>
  <verify>
  <automated>pnpm tsc --noEmit --pretty src/features/dispute/ui/DisputeForm.tsx src/features/dispute/ui/intake/DisputeIntakeWizard.tsx src/features/dispute/model/useDisputeIntake.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria> - DisputeForm.tsx uses React Hook Form with zodResolver(disputeCreateSchema), all 7 fields with labels - DisputeForm submits POST /api/disputes, calls onComplete with returned dispute.id on success, shows sonner toast on error - DisputeIntakeWizard renders the correct stage component per workflow.currentStep, shows numbered inline progress indicator - useDisputeIntake builds WorkflowConfig with 6 steps, auto-skips frivolity when aiEnabled=false, forward-only transitions - useDisputeIntake integrates useAutoSave with key "dispute-intake-wizard", restores saved state on mount - "saved N seconds ago" indicator renders when secondsSinceSave is not null - `pnpm tsc --noEmit` on all 3 files → zero errors
  </acceptance_criteria>
  <done>DisputeForm, DisputeIntakeWizard, and useDisputeIntake hook wired with workflow engine and auto-save, ready for widget integration in Plan 03</done>
  </task>

<task type="auto">
  <name>Task 3: Create wizard stage and form tests</name>
  <files>src/features/dispute/ui/intake/__tests__/EmotionCheckIn.test.tsx, src/features/dispute/ui/intake/__tests__/SelfResolutionChecklist.test.tsx, src/features/dispute/ui/intake/__tests__/FrivolityScreen.test.tsx, src/features/dispute/ui/intake/__tests__/DisputeForm.test.tsx</files>
  <read_first>
@src/features/dispute/ui/intake/EmotionCheckIn.tsx (created in Task 1)
@src/features/dispute/ui/intake/SelfResolutionChecklist.tsx (created in Task 1)
@src/features/dispute/ui/intake/FrivolityScreen.tsx (created in Task 1)
@src/features/dispute/ui/DisputeForm.tsx (created in Task 2)
@.planning/phases/107-dispute-ui-widgets/107-RESEARCH.md Search for "REQ-DISPUTE-06" table — exact test commands and behaviors to verify
@src/features/dispute/ui/intake/__tests__/ (will be created)
  </read_first>
  <action>
Create 4 test files in src/features/dispute/ui/intake/__tests__/ per the RESEARCH.md test map. Use vitest + @testing-library/react (render, screen, fireEvent, waitFor). Mock fetch with vi.fn() for FrivolityScreen and DisputeForm.

Test file 1 — EmotionCheckIn.test.tsx:

- Test: renders 5 emoji buttons with correct labels
- Test: clicking an emoji calls onSelect with correct value
- Test: selected button gets highlighted class (bg-indigo-50)
- Test: selecting 'very_angry' shows soft gate banner with "We understand this is difficult"
- Test: selecting 'upset' shows soft gate banner
- Test: selecting 'neutral' does NOT show soft gate banner
- Assert on aria-pressed attribute

Test file 2 — SelfResolutionChecklist.test.tsx:

- Test: renders 4 checkboxes with correct labels
- Test: toggling a checkbox calls onToggle with correct id
- Test: when 0 or 1 boxes checked, contextual tip is visible ("Consider speaking directly")
- Test: when 2+ boxes checked, contextual tip is NOT visible
- Assert min-h-11 touch target class on checkbox containers

Test file 3 — FrivolityScreen.test.tsx:

- Test: on mount, calls fetch to /api/disputes/intake-screen with correct JSON body
- Test: on successful response with likelyFrivolous=true, renders "flagged" message and toneScore
- Test: on successful response with likelyFrivolous=false, renders "not frivolous" message
- Test: "Proceed" button is always rendered (even on error state)
- Test: on fetch error, renders "Assessment unavailable" degraded state and still shows Proceed button
- Test: calls onProceed when "Proceed" button is clicked

Test file 4 — DisputeForm.test.tsx:

- Test: renders all 7 form fields (category, title, description, desiredOutcome, respondentType, respondentId, severity)
- Test: shows validation errors when submitted with empty required fields (title min 5, description min 10)
- Test: on successful submit, calls fetch to /api/disputes with correct body
- Test: on fetch success (201 + { id: 'uuid' }), calls onComplete with the dispute id
- Test: on fetch error (500), shows sonner toast (verify toast import from 'sonner' — mock if needed or just assert fetch was called)
- Test: "Back to list" cancel button calls onCancel

All test files must use the vitest alias @features for imports. Use vi.hoisted() for top-level mocks per the project's established test pattern (see STATE.md Phase 35 decision: "API test infrastructure uses hoisted mock state pattern").
</action>
<verify>
<automated>pnpm vitest run src/features/dispute/ui/intake/**tests**/EmotionCheckIn.test.tsx src/features/dispute/ui/intake/**tests**/SelfResolutionChecklist.test.tsx src/features/dispute/ui/intake/**tests**/FrivolityScreen.test.tsx src/features/dispute/ui/intake/**tests**/DisputeForm.test.tsx 2>&1 | tail -30</automated>
</verify>
<acceptance_criteria> - 4 test files exist at the specified paths - EmotionCheckIn.test.tsx: 6 tests pass - SelfResolutionChecklist.test.tsx: 4 tests pass - FrivolityScreen.test.tsx: 5 tests pass - DisputeForm.test.tsx: 6 tests pass - All test files use @features import alias (not relative paths beyond ../../) - `pnpm vitest run src/features/dispute/ui/intake/__tests__/` → all tests pass
</acceptance_criteria>
<done>4 test files covering wizard stages and form with 21 tests passing — EmotionCheckIn (6), SelfResolutionChecklist (4), FrivolityScreen (5), DisputeForm (6)</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                                                     |
| --------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| client→POST /api/disputes/intake-screen | Untrusted user description text sent to AI screening API — sanitized server-side (Phase 106)                    |
| client→POST /api/disputes               | Form data including respondent selection and severity — validated client-side (Zod) and server-side (Phase 106) |
| client→localStorage                     | Wizard form state including description draft — accessible to any JS on origin                                  |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                       |
| --------- | ---------------------- | ---------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-107-04  | Spoofing               | FrivolityScreen AI response        | accept      | AI output is advisory-only per CONTEXT.md — user can always proceed regardless of result. Server validates response via Zod (Phase 106). Client uses parseIntakeScreenOutput() safe-defaults on parse failure.                                                                                        |
| T-107-05  | Tampering              | DisputeForm client-side validation | mitigate    | Client uses disputeCreateSchema with Zod for immediate UX feedback. Server re-validates via the same schema (Phase 106). Client-side validation is a UX convenience, not a security control.                                                                                                          |
| T-107-06  | Information Disclosure | EmotionCheckIn emotional state     | accept      | Emotional state is NOT persisted per CONTEXT.md ("Emotional state NOT persisted — only intakeCompletedAt timestamp"). The wizard ctx stores the selection only for the current session's ConflictTipsPanel contextualization. localStorage auto-save stores the emotion value but it's non-sensitive. |
| T-107-07  | Elevation of Privilege | DisputeIntakeWizard aiEnabled prop | mitigate    | aiEnabled is resolved server-side and passed as a static prop per RESEARCH.md Pattern 1. Client components never fetch AI configuration. Workflow condition gates the frivolity step synchronously.                                                                                                   |
| T-107-SC  | Tampering              | (no new packages)                  | accept      | Zero new package installs. All deps pre-audited in Phase 107 RESEARCH.md §Package Legitimacy Audit.                                                                                                                                                                                                   |

</threat_model>

<verification>
- [ ] `pnpm vitest run src/features/dispute/ui/intake/__tests__/EmotionCheckIn.test.tsx` — 6 tests pass
- [ ] `pnpm vitest run src/features/dispute/ui/intake/__tests__/SelfResolutionChecklist.test.tsx` — 4 tests pass
- [ ] `pnpm vitest run src/features/dispute/ui/intake/__tests__/FrivolityScreen.test.tsx` — 5 tests pass
- [ ] `pnpm vitest run src/features/dispute/ui/intake/__tests__/DisputeForm.test.tsx` — 6 tests pass
- [ ] `pnpm tsc --noEmit --pretty src/features/dispute/ui/intake/ src/features/dispute/ui/DisputeForm.tsx src/features/dispute/model/useDisputeIntake.ts 2>&1 | head -20` — zero errors
- [ ] All copy matches UI-SPEC.md §Copywriting Contract verbatim
</verification>

<success_criteria>
All 6 wizard stages render correctly: 5 stage components (EmotionCheckIn, SelfResolutionChecklist, FrivolityScreen, ConflictTipsPanel, ReviewScreen) with correct Tailwind styling per UI-SPEC, plus DisputeForm with React Hook Form + Zod validation. DisputeIntakeWizard orchestrator drives forward-only transitions via the Workflow engine, auto-skips frivolity when AI disabled. useDisputeIntake hook integrates auto-save with localStorage persistence and "saved N seconds ago" indicator. 21 component tests pass. TypeScript compiles with zero errors across all wizard files.
</success_criteria>

## Artifacts This Phase Produces

| Symbol                    | Kind      | File                                                         | Exported     |
| ------------------------- | --------- | ------------------------------------------------------------ | ------------ |
| `EmotionCheckIn`          | component | `src/features/dispute/ui/intake/EmotionCheckIn.tsx`          | named export |
| `SelfResolutionChecklist` | component | `src/features/dispute/ui/intake/SelfResolutionChecklist.tsx` | named export |
| `FrivolityScreen`         | component | `src/features/dispute/ui/intake/FrivolityScreen.tsx`         | named export |
| `ConflictTipsPanel`       | component | `src/features/dispute/ui/intake/ConflictTipsPanel.tsx`       | named export |
| `ReviewScreen`            | component | `src/features/dispute/ui/intake/ReviewScreen.tsx`            | named export |
| `DisputeForm`             | component | `src/features/dispute/ui/DisputeForm.tsx`                    | named export |
| `DisputeIntakeWizard`     | component | `src/features/dispute/ui/intake/DisputeIntakeWizard.tsx`     | named export |
| `useDisputeIntake`        | hook      | `src/features/dispute/model/useDisputeIntake.ts`             | named export |
| `IntakeStep`              | type      | `src/features/dispute/model/useDisputeIntake.ts`             | named export |
| `IntakeContext`           | type      | `src/features/dispute/model/useDisputeIntake.ts`             | named export |

<output>
Create `.planning/phases/107-dispute-ui-widgets/107-02-SUMMARY.md` when done
</output>
