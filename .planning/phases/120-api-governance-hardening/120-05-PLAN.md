---
phase: 120-api-governance-hardening
plan: 05
type: execute
wave: 2
depends_on:
  - 120-01
files_modified:
  - src/server/routers/chat/shared.ts
  - src/server/routers/chat/conversations.ts
  - src/server/routers/chat/messaging.ts
  - src/server/routers/maintenance/shared.ts
  - src/server/routers/maintenance/*.ts
  - src/server/routers/marketplace/shared.ts
  - src/server/routers/marketplace/*.ts
  - src/server/routers/surveys/shared.ts
  - src/server/routers/surveys/*.ts
autonomous: true
requirements:
  - GOV-01
  - GOV-03

must_haves:
  truths:
    - All sub-router shared.ts modules export toEnvelope alongside existing re-exports
    - All sub-router procedures adopt tenantProcedure/privilegedProcedure (not protectedProcedure/adminProcedure)
    - Inline ctx.tenantId null checks removed where tenantProcedure enforces
    - chat/ sub-routers (conversations.ts, messaging.ts): returns wrapped in toEnvelope(dto.parse(row))
    - maintenance/ sub-routers (5 files): returns wrapped in toEnvelope() with maintenance DTOs
    - marketplace/ sub-routers (9 files): returns wrapped in toEnvelope() with marketplace DTOs
    - surveys/ sub-routers (5 files): returns wrapped in toEnvelope() with survey DTOs
    - Zero raw InferSelectModel returns in any sub-router
    - JSDoc @tenant/@privileged tags on all procedures
    - pnpm tsc --noEmit passes across all sub-router files
  artifacts:
    - src/server/routers/chat/ (3 files migrated)
    - src/server/routers/maintenance/ (5 files migrated)
    - src/server/routers/marketplace/ (9 files migrated)
    - src/server/routers/surveys/ (5 files migrated)
  key_links:
    - chat/shared.ts → @server/dto/chat (conversationDto, messageDto, etc.)
    - maintenance/shared.ts → @server/dto/maintenance (maintenanceRequestDto)
    - marketplace/shared.ts → @server/dto/marketplace (listingDto, reviewDto, serviceBookingDto)
    - surveys/shared.ts → @server/dto/surveys (surveyDto, questionDto, responseDto)
---

<objective>
Migrate 22 sub-router files across 4 modules (chat/, maintenance/, marketplace/, surveys/) to use DTOs, tenantProcedure/privilegedProcedure, and toEnvelope() wrapping. The shared.ts pattern in each module centralizes imports — add DTO and toEnvelope to shared modules, then apply mechanical changes to each sub-router.

Purpose: Sub-routers follow a uniform shared.ts pattern (PATTERNS.md §Pattern E). Migration is mechanical: add DTOs + toEnvelope to shared re-exports, then replace inline schemas and raw returns in each sub-router.
Output: 22 sub-router files with correct tiers, DTO-mapped returns, and envelope wrapping.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-api-governance-hardening/120-PATTERNS.md (§Pattern E: Sub-Router Shared Module, §No Analog Needed — sub-routers)
@.planning/phases/120-api-governance-hardening/120-RESEARCH.md (§Architecture Patterns — sub-routers)

# Shared module patterns

@src/server/routers/chat/shared.ts (62 lines — re-export pattern, add DTOs + toEnvelope)
@src/server/routers/maintenance/shared.ts (re-export pattern)
@src/server/routers/marketplace/shared.ts (re-export pattern)
@src/server/routers/surveys/shared.ts (re-export pattern)

# DTO files

@src/server/dto/chat.ts (conversationDto, conversationDetailDto, messageDto, unreadCountsDto)
@src/server/dto/maintenance.ts (maintenanceRequestDto, maintenanceRequestDetailDto)
@src/server/dto/marketplace.ts (listingDto, reviewDto, serviceBookingDto, urgencyDto)
@src/server/dto/surveys.ts (surveyDto, questionDto, responseDto, externalSurveyDto)
</context>

<rationale>
## CONTEXT.md Override Note

**D-01 Override (envelope at middleware level):** CONTEXT.md D-01 specifies envelope wrapping at the tRPC middleware layer. This is technically infeasible in tRPC v11 — no success-response transformation middleware exists. The per-procedure `toEnvelope()` approach applied throughout this plan follows the tRPC v11 constraint confirmed in RESEARCH.md §Alternatives Considered (line 52) and Plan 01 rationale block.
</rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Shared Modules and Migrate chat/ Sub-Routers</name>
  <files>src/server/routers/chat/shared.ts, src/server/routers/chat/conversations.ts, src/server/routers/chat/messaging.ts</files>
  <behavior>
    - Test 1: chat/shared.ts exports toEnvelope + chat DTOs alongside existing re-exports
    - Test 2: conversations.ts returns toEnvelope(conversationDto.parse(row)) — not raw rows
    - Test 3: messaging.ts returns toEnvelope(messageDto.parse(row))
    - Test 4: conversations.ts uses tenantProcedure (not protectedProcedure + inline check)
    - Test 5: messaging.ts uses tenantProcedure for tenant-scoped message ops
    - Test 6: JSDoc @tenant tags on conversation/message procedures
    - Test 7: pnpm tsc --noEmit passes on chat/ files
  </behavior>
  <action>
Update chat/shared.ts and migrate conversations.ts + messaging.ts.

**Step 1: Update chat/shared.ts** (PATTERNS.md Pattern E):

- Add to shared exports (after existing re-exports): `export { toEnvelope } from '@api/server';`
- Add DTO exports: `export { conversationDto, conversationDetailDto, messageDto, unreadCountsDto } from '@server/dto';`
- shared.ts already re-exports z, protectedProcedure, adminProcedure, db, TRPCError, hasPermission, Drizzle helpers — add DTOs and toEnvelope to complete the shared toolkit

**Step 2: Migrate conversations.ts:**

- Replace inline zod schemas in .output() with DTOs from shared
- Migrate protectedProcedure → tenantProcedure where tenant-scoped
- Migrate adminProcedure → privilegedProcedure
- Wrap all returns in toEnvelope(dto.parse(row))
- Remove inline tenantId checks
- Add JSDoc classification tags

**Step 3: Migrate messaging.ts:**

- Same pattern as conversations.ts
- Message queries are tenant-scoped → tenantProcedure
- Message send/receive use toEnvelope(messageDto.parse(row))

Per PATTERNS.md §No Analog Needed: "Already has inline zod schemas and envelope-optional returns. Just needs toEnvelope() wrap + tenantProcedure." The migration is straightforward — add envelope wrapping, adopt tiers, import DTOs from shared.ts.
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/chat/shared.ts src/server/routers/chat/conversations.ts src/server/routers/chat/messaging.ts 2>&1 | head -30</automated>
<automated>grep -c 'toEnvelope(' src/server/routers/chat/conversations.ts; echo "envelope calls in conversations.ts"</automated>
</verify>
<done>
chat/ shared module exports DTOs + toEnvelope. conversations.ts and messaging.ts use correct tiers, envelope wrapping, and DTO mapping. TypeScript compiles clean.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate maintenance/ Sub-Routers</name>
  <files>src/server/routers/maintenance/shared.ts, src/server/routers/maintenance/*.ts</files>
  <behavior>
    - Test 1: maintenance/shared.ts exports toEnvelope + maintenance DTOs
    - Test 2: All 5 maintenance sub-routers use tenantProcedure/privilegedProcedure
    - Test 3: All returns wrapped in toEnvelope(maintenanceRequestDto.parse(row))
    - Test 4: Inline ctx.tenantId checks removed where tenantProcedure enforces
    - Test 5: JSDoc @tenant/@privileged tags on all procedures
    - Test 6: pnpm tsc --noEmit passes on maintenance/ files
  </behavior>
  <action>
Migrate maintenance/ sub-routers (5 files). This task handles ONLY the maintenance module — marketplace and surveys are separate tasks.

**Step 1: Update maintenance/shared.ts:**

- maintenance/shared.ts: add `export { toEnvelope } from '@api/server';` + `export { maintenanceRequestDto, maintenanceRequestDetailDto } from '@server/dto';`

**Step 2: Migrate each sub-router file:**

- Each sub-router imports from ./shared.ts — inherits new DTO + toEnvelope exports
- For each procedure: migrate adminProcedure → privilegedProcedure, protectedProcedure → tenantProcedure
- Wrap returns: toEnvelope(maintenanceRequestDto.parse(row))
- Remove inline tenantId checks
- Use ctx.db for DB queries
- Add JSDoc tags

**Mechanical check:** After migration, grep for `adminProcedure` in any maintenance/ file — should return zero hits (or only justified exceptions). grep for `protectedProcedure` — only user-scoped operations remain.
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/maintenance/ 2>&1 | head -30</automated>
<automated>for f in src/server/routers/maintenance/\*.ts; do echo "$f: $(grep -c 'toEnvelope(' "$f") envelopes"; done</automated>
</verify>
<done>
maintenance/ sub-routers migrated. Shared module re-exports DTOs + toEnvelope. All returns envelope-wrapped with DTO parsing. TypeScript compiles clean.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate marketplace/ Sub-Routers</name>
  <files>src/server/routers/marketplace/shared.ts, src/server/routers/marketplace/*.ts</files>
  <behavior>
    - Test 1: marketplace/shared.ts exports toEnvelope + marketplace DTOs
    - Test 2: All 9 marketplace sub-routers use tenantProcedure/privilegedProcedure
    - Test 3: All returns wrapped in toEnvelope(dto.parse(row)) — listingDto, reviewDto, serviceBookingDto, urgencyDto
    - Test 4: Inline ctx.tenantId checks removed
    - Test 5: JSDoc @tenant/@privileged tags on all procedures
    - Test 6: pnpm tsc --noEmit passes on marketplace/ files
  </behavior>
  <action>
Migrate marketplace/ sub-routers (9 files). This task handles ONLY the marketplace module — maintenance is handled by Task 2.

**Step 1: Update marketplace/shared.ts:**

- marketplace/shared.ts: add `export { toEnvelope } from '@api/server';` + `export { listingDto, reviewDto, serviceBookingDto, urgencyDto } from '@server/dto';`

**Step 2: Migrate each sub-router file:**

- Same pattern: import from ./shared.ts
- Marketplace listings, reviews, bookings → tenantProcedure
- Admin marketplace management → privilegedProcedure
- Wrap returns: toEnvelope(listingDto.parse(row)), toEnvelope(reviewDto.parse(row)), toEnvelope(serviceBookingDto.parse(row)), toEnvelope(urgencyDto.parse(row)) as appropriate
- Add JSDoc tags

**Mechanical check:** After migration, grep for `adminProcedure` in any marketplace/ file — should return zero hits (or only justified exceptions).

Per RESEARCH.md: marketplace DTOs (marketplace.ts, 123 lines, 4 DTOs) are the most complete exemplar. Follow their field names exactly.
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/marketplace/ 2>&1 | head -30</automated>
<automated>for f in src/server/routers/marketplace/\*.ts; do echo "$f: $(grep -c 'toEnvelope(' "$f") envelopes"; done</automated>
</verify>
<done>
marketplace/ sub-routers migrated. Shared module re-exports DTOs + toEnvelope. All returns envelope-wrapped with DTO parsing. TypeScript compiles clean.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Migrate surveys/ Sub-Routers</name>
  <files>src/server/routers/surveys/shared.ts, src/server/routers/surveys/*.ts</files>
  <behavior>
    - Test 1: surveys/shared.ts exports toEnvelope + survey DTOs
    - Test 2: All 5 survey sub-routers use tenantProcedure/privilegedProcedure
    - Test 3: Survey procedures return toEnvelope(surveyDto.parse(row))
    - Test 4: External survey procedures (surveys/external.ts) use publicProcedure for public-facing endpoints
    - Test 5: Question/response procedures use toEnvelope(questionDto.parse(row)) / toEnvelope(responseDto.parse(row))
    - Test 6: JSDoc @public on external survey endpoints, @tenant/@privileged on internal
    - Test 7: pnpm tsc --noEmit passes on surveys/ files
  </behavior>
  <action>
Migrate surveys/ sub-routers (5 files).

**Step 1: Update surveys/shared.ts:**

- Add: `export { toEnvelope } from '@api/server';`
- Add: `export { surveyDto, questionDto, responseDto, externalSurveyDto } from '@server/dto';`

**Step 2: Migrate each file:**

**surveys/external.ts** — public-facing survey endpoints:

- publicProcedure for unauthenticated survey access (respondents)
- Already uses toEnvelope() per RESEARCH.md
- Ensure externalSurveyDto is used for public-facing responses (fewer fields exposed)
- JSDoc @public tags

**Other survey sub-routers** — internal admin/management:

- Migrate adminProcedure → privilegedProcedure
- Migrate protectedProcedure → tenantProcedure
- Wrap returns in toEnvelope(dto.parse(row))
- Add JSDoc @tenant/@privileged tags

**Key consideration:** surveys/external.ts is the only router with external-facing procedures accessed without auth. Ensure unauthenticated procedures use publicProcedure (not protectedProcedure) and expose only externalSurveyDto (not the full surveyDto with internal fields).
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/surveys/ 2>&1 | head -30</automated>
</verify>
<done>
Survey sub-routers migrated. External procedures use publicProcedure + externalSurveyDto. Internal procedures use tenantProcedure/privilegedProcedure. TypeScript compiles clean.
</done>
</task>

</tasks>

<threat_model>

## Plan-Specific Threats — 22 Sub-Router Files (chat, maintenance, marketplace, surveys)

These sub-routers handle real-time messaging (chat), service requests (maintenance), commerce listings (marketplace), and public-facing surveys — diverse threat surfaces.

| Threat ID   | Category               | Component               | Severity | Disposition | Mitigation Plan                                                                                                                                           |
| ----------- | ---------------------- | ----------------------- | -------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-120-05-01 | Information Disclosure | chat/conversations.ts   | high     | mitigate    | Conversation and message DTOs via drizzle-zod `.pick()` expose only safe fields; conversation participant PII gated by tenantProcedure                    |
| T-120-05-02 | Elevation of Privilege | chat/messaging.ts       | medium   | mitigate    | Message send/receive use tenantProcedure (not protectedProcedure); cross-tenant message access blocked by middleware                                      |
| T-120-05-03 | Spoofing               | surveys/external.ts     | medium   | mitigate    | External survey endpoints use publicProcedure — no auth, but externalSurveyDto exposes only minimal fields (no internal survey metadata)                  |
| T-120-05-04 | Information Disclosure | marketplace/listings.ts | medium   | mitigate    | Listing DTO `.pick()` exposes safe commerce fields; financial details (pricing) are public by design for marketplace listings                             |
| T-120-05-05 | Tampering              | maintenance/\*.ts       | low      | accept      | Maintenance request creation is tenant-scoped via tenantProcedure; admin override via privilegedProcedure. Risk accepted — tenant membership gates access |

</threat_model>

<verification>
Post-plan:
- [ ] `pnpm tsc --noEmit` passes across all 22 sub-router files
- [ ] `grep -rn 'adminProcedure' src/server/routers/{chat,maintenance,marketplace,surveys}/` — zero hits
- [ ] `grep -rn 'protectedProcedure' src/server/routers/{chat,maintenance,marketplace,surveys}/` — only user-scoped endpoints remain
- [ ] Manual: call a chat procedure and a survey procedure via tRPC panel, confirm envelope shape
</verification>

<success_criteria>

1. All 4 shared.ts modules export toEnvelope + DTOs
2. All 22 sub-router files use correct procedure tiers
3. All returns envelope-wrapped with DTO parsing
4. Zero raw InferSelectModel returns
5. JSDoc classification tags present
6. pnpm tsc --noEmit passes
   </success_criteria>

<output>
Create `.planning/phases/120-api-governance-hardening/120-05-SUMMARY.md` when done
</output>
