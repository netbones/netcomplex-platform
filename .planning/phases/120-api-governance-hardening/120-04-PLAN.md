---
phase: 120-api-governance-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 120-01
files_modified:
  - src/server/routers/dwallet.ts
  - src/server/routers/competitions.ts
  - src/server/routers/disputes.ts
  - src/server/routers/resources.ts
autonomous: true
requirements:
  - GOV-01
  - GOV-03

must_haves:
  truths:
    - All 4 flat routers migrated to tenantProcedure/privilegedProcedure tiers
    - Inline ctx.tenantId null checks removed
    - Every return wrapped in toEnvelope() with DTO parsing
    - dwallet.ts: existing walletDto/walletTransactionDto/consentDto/payoutDto used (already imported)
    - competitions.ts: inline output schemas replaced; toEnvelope() added where missing
    - disputes.ts: disputeCaseDto/disputeEventDto/disputeEvidenceDto/disputeMessageDto from new @server/dto/disputes imported
    - resources.ts: resourceDto from new @server/dto/resources imported; raw DB row returns replaced with dto.parse()
    - Zero raw InferSelectModel returns
    - JSDoc @tenant/@privileged tags on all procedures
    - pnpm tsc --noEmit passes across all 4 files
  artifacts:
    - src/server/routers/dwallet.ts (migrated)
    - src/server/routers/competitions.ts (migrated — currently has no DTOs)
    - src/server/routers/disputes.ts (migrated — uses new disputes.ts DTO)
    - src/server/routers/resources.ts (migrated — uses new resources.ts DTO)
  key_links:
    - competitions.ts → @server/dto/misc (no dedicated competition DTO — check if one exists or competition fields are in misc.ts)
    - disputes.ts → @server/dto/disputes (created in Plan 01)
    - resources.ts → @server/dto/resources (created in Plan 01)
    - dwallet.ts → @server/dto/dwallet (already exists)
---

<objective>
Migrate the remaining 4 flat tRPC routers: dwallet.ts (576 lines), competitions.ts (894 lines), disputes.ts (406 lines), resources.ts (406 lines). These routers have the least governance compliance — competitions.ts and resources.ts have NO DTO imports, disputes.ts and resources.ts return raw DB rows. Two of them depend on the new DTO files created in Plan 01 (disputes.ts, resources.ts DTOs).

Purpose: Close the remaining flat-router governance gaps. Resources and competitions are the lowest-compliance routers per RESEARCH.md (§Wave 3 Router Migration table).
Output: 4 routers with correct tiers, DTO-mapped returns, and envelope wrapping.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-api-governance-hardening/120-RESEARCH.md (§Wave 3 Router Migration — competitions, resources, disputes, dwallet)
@.planning/phases/120-api-governance-hardening/120-PATTERNS.md (§Router-Specific: competitions.ts lines 546-552, resources.ts lines 556-561)
@.planning/phases/120-api-governance-hardening/CONTEXT.md (D-09)

# DTO files (created in Plan 01 or pre-existing)

@src/server/dto/dwallet.ts (walletDto, walletTransactionDto, consentDto, payoutDto)
@src/server/dto/disputes.ts (created in Plan 01 — disputeCaseDto, disputeEventDto, disputeEvidenceDto, disputeMessageDto)
@src/server/dto/resources.ts (created in Plan 01 — resourceDto)

# Source routers

@src/server/routers/dwallet.ts
@src/server/routers/competitions.ts
@src/server/routers/disputes.ts
@src/server/routers/resources.ts
</context>

<rationale>
## CONTEXT.md Override Note

**D-01 Override (envelope at middleware level):** CONTEXT.md D-01 specifies envelope wrapping at the tRPC middleware layer. This is technically infeasible in tRPC v11 — no success-response transformation middleware exists. The per-procedure `toEnvelope()` approach applied throughout this plan follows the tRPC v11 constraint confirmed in RESEARCH.md §Alternatives Considered (line 52) and Plan 01 rationale block.
</rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate dWallet Router</name>
  <files>src/server/routers/dwallet.ts</files>
  <behavior>
    - Test 1: dwallet tenant-scoped procedures use tenantProcedure
    - Test 2: dwallet admin procedures use privilegedProcedure
    - Test 3: All returns use toEnvelope(dto.parse(row)) — walletDto, walletTransactionDto, consentDto, payoutDto
    - Test 4: ctx.db replaces global db
    - Test 5: Inline ctx.tenantId checks removed
    - Test 6: JSDoc @tenant/@privileged tags present
    - Test 7: pnpm tsc --noEmit passes
  </behavior>
  <action>
Migrate dwallet.ts (576 lines). Per RESEARCH.md: this router already imports toEnvelope() and DTOs (walletDto, etc.) but uses protectedProcedure/adminProcedure instead of tenantProcedure/privilegedProcedure.

**Steps:**

1. Verify DTO imports: walletDto, walletTransactionDto, consentDto, payoutDto from @server/dto/dwallet
2. Import toEnvelope from @api/server
3. Migrate procedure tiers:
   - protectedProcedure with tenantId check → tenantProcedure
   - adminProcedure → privilegedProcedure
4. Remove inline `if (!ctx.tenantId)` blocks
5. Ensure all query returns wrap in toEnvelope(dto.parse(row))
6. Replace global db with ctx.db
7. Add JSDoc tags: @tenant on wallet queries, @privileged on admin ops

**Verify consistency:** grep for bare `return` statements without toEnvelope() — there should be none after migration. DTOs from dwallet.ts already exist and are pre-verified.
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/dwallet.ts 2>&1 | head -30</automated>
<automated>grep -c 'toEnvelope(' src/server/routers/dwallet.ts; echo "toEnvelope calls in dwallet.ts"</automated>
</verify>
<done>
dWallet router uses tenantProcedure/privilegedProcedure, all returns envelope-wrapped with DTO parsing. TypeScript compiles clean.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Competitions and Resources Routers</name>
  <files>src/server/routers/competitions.ts, src/server/routers/resources.ts</files>
  <behavior>
    - Test 1: competitions.ts imports toEnvelope (currently missing per RESEARCH.md) and wraps returns
    - Test 2: competitions.ts inline output schemas replaced
    - Test 3: competitions.ts admin procedures use privilegedProcedure, public list uses publicProcedure
    - Test 4: resources.ts imports resourceDto from @server/dto/resources and parses returned rows
    - Test 5: resources.ts raw DB row returns (toEnvelope(await db.select()...)) replaced with toEnvelope(rows.map(r => resourceDto.parse(r)))
    - Test 6: JSDoc @public on listPublicCompetitions, @tenant/@privileged on others
    - Test 7: pnpm tsc --noEmit passes for both files
  </behavior>
  <action>
Migrate competitions.ts (894 lines) and resources.ts (406 lines) — the two routers with lowest governance compliance per RESEARCH.md.

**Competitions Router (competitions.ts):**
Per RESEARCH.md: "Uses toEnvelope() and toEnvelopeSchema() on returns. Some inline output schemas. No DTO imports."

No competition-specific DTO exists in src/server/dto/misc.ts (confirmed via grep). Handle as follows:

1. Inspect competitions.ts to identify the Drizzle table references used in query returns. Search for `from(` or `.select().from(` and map each to its DTO:
   - If the router queries `communityServiceListings` table → import and use `listingDto` from @server/dto/marketplace
   - If the router queries `events` table → import and use `eventDto` from @server/dto/misc
   - If the router queries tables with no existing DTO: keep the inline output schema BUT add a BD issue reference for creating the DTO in a follow-up. Format the TODO as:
     ```
     // TODO(BD): Create competitionDto in src/server/dto/misc.ts — tracked in BD-XXXX
     // Current approach: inline Zod schema until dedicated DTO exists
     ```
   - Do NOT create a new DTO file as part of this task — that is out of scope for a router migration task.

2. Import toEnvelope from @api/server (verify — may already be imported per PATTERNS.md line 546-553)
3. Replace inline output schemas with DTO references where available; mark remaining inline schemas with BD issue TODO as specified above
4. Migrate tiers: adminProcedure → privilegedProcedure, protectedProcedure → tenantProcedure
5. Ensure toParticipantDTO() mapper output is DTO-validated if a matching DTO exists; otherwise leave the mapper as-is with a TODO
6. Add JSDoc: @public on listPublicCompetitions, @privileged on admin CRUD

**Resources Router (resources.ts):**
Per RESEARCH.md: "Uses toEnvelope() but returns raw DB rows. No DTO parsing." PATTERNS.md: "line 142-148, toEnvelope(await db.select()...)."

1. Import resourceDto from @server/dto/resources (created in Plan 01)
2. Replace `toEnvelope(await db.select()...)` with `toEnvelope(rows.map(r => resourceDto.parse(r)))`
3. Migrate tiers: protectedProcedure → tenantProcedure for tenant-scoped, adminProcedure → privilegedProcedure
4. Remove inline tenantId checks
5. Use ctx.db for queries
6. Add JSDoc tags

Per RESEARCH.md Pitfall 3: these routers currently import NEITHER toEnvelope nor DTOs (per §Current State). Verify actual state before starting — some toEnvelope usage may already exist. Add imports where missing.
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/competitions.ts src/server/routers/resources.ts 2>&1 | head -30</automated>
<automated>grep -c 'resourceDto' src/server/routers/resources.ts; echo "resourceDto references (should be >0 after migration)"</automated>
</verify>
<done>
Competitions and resources routers migrated. All returns DTO-parsed and envelope-wrapped. Correct procedure tiers. TypeScript compiles clean.
</done>
</task>

</tasks>

<threat_model>

## Plan-Specific Threats — dWallet, Competitions, Disputes, Resources

These 4 routers cover financial transactions (dWallet), dispute arbitration, and resource management — higher sensitivity than typical CRUD routers.

| Threat ID   | Category               | Component       | Severity | Disposition | Mitigation Plan                                                                                                                                                                                   |
| ----------- | ---------------------- | --------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-120-04-01 | Information Disclosure | dwallet.ts      | high     | mitigate    | walletDto, walletTransactionDto, consentDto, payoutDto via drizzle-zod `.pick()` expose only safe financial fields; raw DB row returns replaced with dto.parse()                                  |
| T-120-04-02 | Elevation of Privilege | dwallet.ts      | high     | mitigate    | Wallet admin operations use privilegedProcedure (not adminProcedure); COMMITTEE role included for financial oversight                                                                             |
| T-120-04-03 | Information Disclosure | disputes.ts     | medium   | mitigate    | disputeCaseDto/disputeEventDto/disputeEvidenceDto/disputeMessageDto created in Plan 01; dispute evidence fileUrls exposed but file access gated by Supabase storage RLS                           |
| T-120-04-04 | Information Disclosure | resources.ts    | low      | mitigate    | resourceDto via drizzle-zod `.pick()` exposes safe resource metadata fields; raw DB rows replaced with dto.parse()                                                                                |
| T-120-04-05 | Tampering              | competitions.ts | low      | accept      | Competition DTO coverage is incomplete (no dedicated competitionDto); BD issue tracked for DTO completion. Risk accepted for Phase 120 — competitions are public-facing with minimal PII exposure |

</threat_model>

<verification>
Post-plan:
- [ ] `pnpm tsc --noEmit` passes on dwallet.ts, competitions.ts, resources.ts, disputes.ts
- [ ] `grep -c 'toEnvelope(' src/server/routers/competitions.ts` > 0
- [ ] `grep -c 'resourceDto' src/server/routers/resources.ts` > 0
- [ ] `grep -c 'disputeCaseDto' src/server/routers/disputes.ts` > 0
- [ ] Manual: call competitions.listPublicCompetitions without auth, confirm success + envelope shape
</verification>

<success_criteria>

1. All 4 routers use correct procedure tiers
2. All returns envelope-wrapped with DTO parsing
3. Resources router uses resourceDto from Plan 01 DTO
4. Disputes router uses dispute DTOs from Plan 01
5. Competitions router uses toEnvelope() on all returns
6. pnpm tsc --noEmit passes
   </success_criteria>

<output>
Create `.planning/phases/120-api-governance-hardening/120-04-SUMMARY.md` when done
</output>
