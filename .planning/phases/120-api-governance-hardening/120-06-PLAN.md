---
phase: 120-api-governance-hardening
plan: 06
type: execute
wave: 3
depends_on:
  - 120-02
  - 120-03
  - 120-04
  - 120-05
files_modified:
  - src/server/routers/*.ts
  - src/server/routers/chat/*.ts
  - src/server/routers/maintenance/*.ts
  - src/server/routers/marketplace/*.ts
  - src/server/routers/surveys/*.ts
  - docs/STEERING/API.md
  - docs/STEERING/API_ARCHITECT.md
autonomous: true
requirements:
  - GOV-07
  - GOV-08

must_haves:
  truths:
    - Every procedure across all 20 routers has a JSDoc @public, @tenant, or @privileged classification tag matching its procedure tier
    - @public tags on all publicProcedure endpoints
    - @tenant tags on all tenantProcedure and protectedProcedure endpoints
    - @privileged tags on all privilegedProcedure, adminProcedure, and agentProcedure endpoints
    - docs/STEERING/API.md updated with Phase 120 completion status and verified governance rules
    - docs/STEERING/API_ARCHITECT.md updated with current procedure tier definitions and usage
    - OpenAPI meta audit completed — procedures with .meta({ openapi }) verified to have correct method/path/tags
    - pnpm lint passes (no new lint violations from JSDoc additions)
  artifacts:
    - JSDoc classification tags across all 20+ router files
    - docs/STEERING/API.md (updated — completion status)
    - docs/STEERING/API_ARCHITECT.md (updated — procedure tier documentation)
  key_links:
    - JSDoc tags → procedure tier (e.g., @privileged → privilegedProcedure)
    - API.md → Phase 120 completion status
    - API_ARCHITECT.md → tenantProcedure / privilegedProcedure definitions
---

<objective>
Add API classification JSDoc tags to all procedures across all 20 routers, audit OpenAPI metadata on external procedures, and update governance documentation to reflect Phase 120 completion.

Purpose: JSDoc classification tags enable doc-generation tooling and manual audit of API governance compliance. Updated docs ensure the governance standard reflects the actual implemented state.
Output: JSDoc-tagged procedures, audited OpenAPI metadata, updated governance docs.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-api-governance-hardening/CONTEXT.md (D-09 — JSDoc tags)
@.planning/phases/120-api-governance-hardening/120-PATTERNS.md (§6: Classification JSDoc Tags, §7: Router Registration)
@.planning/phases/120-api-governance-hardening/120-RESEARCH.md (§Wave 4: Classification + OpenAPI)

# Documentation targets

@docs/STEERING/API.md (update with Phase 120 completion)
@docs/STEERING/API_ARCHITECT.md (update procedure tier documentation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Classification JSDoc Tags to All Procedures</name>
  <files>All 20+ router files in src/server/routers/</files>
  <behavior>
    - Verify 1: Every publicProcedure endpoint has /** @public */ JSDoc
    - Verify 2: Every tenantProcedure endpoint has /** @tenant */ JSDoc
    - Verify 3: Every privilegedProcedure endpoint has /** @privileged */ JSDoc
    - Verify 4: Every adminProcedure endpoint has /** @privileged */ JSDoc (includes ADMIN/BOARD, not COMMITTEE)
    - Verify 5: Every agentProcedure endpoint has /** @privileged */ JSDoc (restricted agent access)
    - Verify 6: Every protectedProcedure endpoint has /** @tenant */ JSDoc (all protected procedures are tenant-scoped in this project)
    - Verify 7: pnpm lint passes (no JSDoc-related lint violations)
  </behavior>
  <action>
Add JSDoc classification tags to every procedure across all 20+ router files. Per D-09: tags are @public, @tenant, @privileged.

**Tag mapping** (from PATTERNS.md §6):

- publicProcedure → `/** @public */`
- tenantProcedure → `/** @tenant */`
- protectedProcedure → `/** @tenant */` (all protected procedures are authenticated + tenant-scoped in this project)
- privilegedProcedure → `/** @privileged */`
- adminProcedure → `/** @privileged */` (includes ADMIN/BOARD, excludes COMMITTEE — note this distinction in the JSDoc description)
- agentProcedure → `/** @privileged */` (restricted to AGENT role)

**JSDoc format** (PATTERNS.md lines 666-689):

```typescript
/**
 * List all properties in the current tenant.
 * @tenant
 */
listProperties: tenantProcedure
  .meta({ ... })
  .query(async ({ ctx }) => { ... }),
```

Add a one-line description above the tag summarizing the procedure's purpose.

**Execution strategy — batch by router:**

1. identity.ts — already partially documented, add @tenant/@privileged tags (most impactful)
2. content.ts, achievements.ts, events.ts, bookings.ts, groups.ts — 5 routers
3. merits, notifications, invitations, settings, agents — 5 routers
4. dwallet, competitions, disputes, resources — 4 routers
5. chat/conversations.ts, chat/messaging.ts — 2 files
6. maintenance/\*.ts — 5 files
7. marketplace/\*.ts — 9 files
8. surveys/\*.ts — 5 files

**Audit check:** After tagging, run `grep -rn '@tenant\|@privileged\|@public' src/server/routers/ | wc -l` to verify tag count roughly matches total procedure count across all routers.

Per CONTEXT.md D-09: tags should be "parseable by a doc generator or manual audit." No automation enforcement required in this phase — just consistent tagging.
</action>
<verify>
<automated>grep -rn '@tenant\|@privileged\|@public' src/server/routers/ | wc -l; echo "JSDoc tags found (compare to expected procedure count ~170)"</automated>
<automated>pnpm lint 2>&1 | tail -5</automated>
</verify>
<done>
All procedures across 20+ routers have JSDoc classification tags. Tags match procedure tiers: @public on public, @tenant on tenant/protected, @privileged on privileged/admin/agent. pnpm lint passes.
</done>
</task>

<task type="auto">
  <name>Task 2: Audit OpenAPI Metadata and Update Governance Docs</name>
  <files>docs/STEERING/API.md, docs/STEERING/API_ARCHITECT.md</files>
  <behavior>
    - Verify 1: API.md includes Phase 120 completion status and verified governance rules
    - Verify 2: API_ARCHITECT.md documents tenantProcedure and privilegedProcedure definitions with their middleware chain
    - Verify 3: API_ARCHITECT.md documents canonical error code mapping via errorFormatter
    - Verify 4: API_ARCHITECT.md documents DTO layer location and derivation pattern
    - Verify 5: OpenAPI meta audit confirms external procedures have .meta({ openapi }) with valid method/path/tags
  </behavior>
  <action>
Audit OpenAPI metadata on external procedures and update governance documentation.

**Part A: OpenAPI Meta Audit (GOV-08 — deferred, minimal audit only):**

1. grep for `.meta({ openapi` across all router files to find external procedures
2. Verify each has: method (GET/POST/PATCH/DELETE), path (unique), tags ([domain name]), and protect (boolean) or summary/description
3. Focus on surveys/external.ts and identity procedures with @public tags — these are the most likely candidates for missing meta
4. Do NOT add new .meta() declarations — only audit existing ones for correctness. Adding new OpenAPI metadata is deferred per CONTEXT.md.

**Part B: Update docs/STEERING/API.md:**

1. Add a "Phase 120 Completion" section documenting:
   - Error code mapping: tRPC errorFormatter rewrites native codes → canonical via TRPC_TO_CANONICAL
   - Response envelope: all tRPC procedures return {success, data, meta} via toEnvelope()
   - DTO layer: 13 DTO files covering all domain entities, derived from Drizzle tables via drizzle-zod
   - Procedure tiers: tenantProcedure, privilegedProcedure, adminProcedure, agentProcedure
   - Auth middleware: 5-step check (session → tenant → role → suspension → feature flag), with steps 4-5 now enforced
2. Mark governance rules 4, 5, 6, 7 as VERIFIED post-Phase 120

**Part C: Update docs/STEERING/API_ARCHITECT.md:**

1. Document the full procedure tier hierarchy with middleware chains:
   - publicProcedure → (no middleware)
   - protectedProcedure → session check
   - tenantProcedure → session + tenant membership
   - privilegedProcedure → session + tenant + role (ADMIN/BOARD/COMMITTEE) + suspension
   - adminProcedure → session + tenant + role (ADMIN/BOARD)
   - agentProcedure → session + tenant + role (AGENT/ADMIN/BOARD)
2. Document the errorFormatter canonical code mapping mechanism
3. Document DTO layer: src/server/dto/ directory, drizzle-zod derivation pattern
4. Reference Phase 120 as the implementation milestone

**Doc update scope:** Minimal updates to existing sections — do NOT rewrite the entire document. Add completion status and reference Phase 120.
</action>
<verify>
<automated>grep -c 'Phase 120' docs/STEERING/API.md docs/STEERING/API_ARCHITECT.md; echo "Phase 120 references in docs (should be >0 in each)"</automated>
</verify>
<done>
API.md and API_ARCHITECT.md updated with Phase 120 completion status. OpenAPI metadata audited. Governance documentation reflects implemented state.
</done>
</task>

</tasks>

<verification>
Post-plan:
- [ ] `grep -rn '@tenant\|@privileged\|@public' src/server/routers/ | wc -l` — tag count matches procedure count
- [ ] No procedure lacks a classification JSDoc tag
- [ ] `pnpm lint` passes
- [ ] `docs/STEERING/API.md` contains Phase 120 completion reference
- [ ] `docs/STEERING/API_ARCHITECT.md` contains current procedure tier definitions
</verification>

<success_criteria>

1. All ~170 procedures across 20+ routers have JSDoc classification tags
2. Tag type matches procedure tier (@public, @tenant, @privileged)
3. docs/STEERING/API.md updated with Phase 120 completion
4. docs/STEERING/API_ARCHITECT.md updated with procedure tier documentation
5. pnpm lint passes
   </success_criteria>

<output>
Create `.planning/phases/120-api-governance-hardening/120-06-SUMMARY.md` when done
</output>
