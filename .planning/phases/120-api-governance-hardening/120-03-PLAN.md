---
phase: 120-api-governance-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 120-01
files_modified:
  - src/server/routers/content.ts
  - src/server/routers/achievements.ts
  - src/server/routers/events.ts
  - src/server/routers/bookings.ts
  - src/server/routers/groups.ts
  - src/server/routers/merits.ts
  - src/server/routers/notifications.ts
  - src/server/routers/invitations.ts
  - src/server/routers/settings.ts
  - src/server/routers/agents.ts
autonomous: true
requirements:
  - GOV-01
  - GOV-03

must_haves:
  truths:
    - All 10 flat routers migrated to tenantProcedure/privilegedProcedure (not protectedProcedure/adminProcedure)
    - Inline ctx.tenantId null checks removed where tenantProcedure now enforces
    - Every procedure returns toEnvelope(data) with DTO-mapped rows
    - content.ts: inline schemas replaced with contentDto/announcementDto from @server/dto
    - achievements.ts, events.ts, bookings.ts, groups.ts: existing toEnvelope() calls preserved, inline schemas replaced with DTOs
    - merits.ts, notifications.ts, invitations.ts, settings.ts, agents.ts: envelope wrapping added where missing, DTOs imported
    - Zero raw InferSelectModel returns (all rows pass through .dto.parse())
    - pnpm tsc --noEmit passes across all 10 files
  artifacts:
    - src/server/routers/content.ts (migrated — tiers + DTOs + envelope)
    - src/server/routers/achievements.ts (migrated — tier change only, already clean)
    - src/server/routers/events.ts (migrated — tiers + DTOs)
    - src/server/routers/bookings.ts (migrated)
    - src/server/routers/groups.ts (migrated)
    - src/server/routers/merits.ts (migrated)
    - src/server/routers/notifications.ts (migrated)
    - src/server/routers/invitations.ts (migrated)
    - src/server/routers/settings.ts (migrated)
    - src/server/routers/agents.ts (migrated)
  key_links:
    - Each router → tenantProcedure / privilegedProcedure from server.ts
    - Each router → DTOs from @server/dto/{content, misc, more}
    - Each router → toEnvelope from @api/server
---

<objective>
Migrate 10 flat tRPC routers to use DTOs, tenantProcedure/privilegedProcedure tiers, and consistent toEnvelope() wrapping. These routers range from partially compliant (content, achievements, events) to minimally compliant (merits, settings, agents). All fit the flat-router pattern — single file, no sub-directories.

Purpose: These 10 routers represent the medium-complexity flat routers. Most already import toEnvelope() and some DTOs. The migration is mechanical: adopt tiers, replace inline schemas, ensure DTO parsing.
Output: 10 routers with correct procedure tiers, DTO-mapped returns, envelope wrapping, and zero inline output schemas.
</objective>

<execution_context>
@/home/ubuntupunk/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/ubuntupunk/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/120-api-governance-hardening/120-RESEARCH.md (§Wave 3: Router Migration, §Common Pitfalls 3-5)
@.planning/phases/120-api-governance-hardening/120-PATTERNS.md (§Router Migration: Canonical Clean Pattern, §Procedure Tier Selection table)

# Source files for patterns

@src/server/routers/achievements.ts (cleanest exemplar — copy import structure)
@src/server/routers/content.ts (1055 lines — imports toEnvelope + DTOs, needs tier migration)
@src/server/routers/events.ts (entity service + envelope pattern)
@src/server/dto/content.ts (contentDto, announcementDto, contentAuthorDto)
@src/server/dto/misc.ts (eventDto, bookingDto, groupDto, groupDetailDto, meritDto, notificationDto)
@src/server/dto/more.ts (achievementDto, achievementProgressDto, invitationDto, settingDto, agentProfileDto)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate Content, Achievements, Events Routers</name>
  <files>src/server/routers/content.ts, src/server/routers/achievements.ts, src/server/routers/events.ts</files>
  <behavior>
    - Test 1: content router staff procedures use privilegedProcedure (not adminProcedure)
    - Test 2: content router tenant-scoped procedures use tenantProcedure (not protectedProcedure)
    - Test 3: content router inline output schemas replaced with contentDto/announcementDto
    - Test 4: achievements router procedures use tenantProcedure/privilegedProcedure (currently uses protectedProcedure with inline checks)
    - Test 5: events router procedures use tenantProcedure/privilegedProcedure
    - Test 6: All returns use toEnvelope(dto.parse(row)) pattern
    - Test 7: JSDoc @tenant/@privileged tags on all procedures
    - Test 8: pnpm tsc --noEmit passes for these 3 files
  </behavior>
  <action>
Migrate content.ts (1055 lines, partially compliant), achievements.ts (86 lines, cleanest exemplar), and events.ts (entity service pattern).

**Content Router (content.ts):**

- Replace inline Zod output schemas with contentDto, announcementDto, contentAuthorDto from @server/dto/content
- Migrate adminProcedure → privilegedProcedure (staff CRUD operations)
- Migrate protectedProcedure with tenant-scoped ops → tenantProcedure
- Remove inline `if (!ctx.tenantId) throw` blocks
- Wrap all returns in toEnvelope(dto.parse(row))
- Use ctx.db for DB queries
- Add JSDoc classification tags

**Achievements Router (achievements.ts):**

- Already the cleanest exemplar — uses toEnvelope(), DTOs, and clean import structure
- Only change: migrate protectedProcedure → tenantProcedure/privilegedProcedure
- Remove inline `if (!tenantId) throw TRPCError` blocks (tenantProcedure middleware handles this)
- Add JSDoc tags

**Events Router (events.ts):**

- Already uses toEnvelope() and delegates to entity services
- Migrate adminProcedure → privilegedProcedure
- Migrate protectedProcedure → tenantProcedure where tenant-scoped (remove inline checks)
- Ensure all returns DTO-parse through eventDto from @server/dto/misc
- Add JSDoc tags

Per PATTERNS.md §Procedure Tier Selection: "adminProcedure (where COMMITTEE should have access) → privilegedProcedure." All content/achievements/events staff operations should include COMMITTEE.
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/content.ts src/server/routers/achievements.ts src/server/routers/events.ts 2>&1 | head -30</automated>
<automated>grep -c 'tenentProcedure' src/server/routers/content.ts; grep -c 'privilegedProcedure' src/server/routers/content.ts; echo "content.ts tier usage"</automated>
</verify>
<done>
Content, achievements, and events routers use correct procedure tiers. All returns wrapped in toEnvelope() with DTO parsing. Zero inline output schemas. JSDoc tags present. TypeScript compiles clean.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Bookings, Groups, Merits Routers</name>
  <files>src/server/routers/bookings.ts, src/server/routers/groups.ts, src/server/routers/merits.ts</files>
  <behavior>
    - Test 1: bookings.ts procedures use tenantProcedure for tenant-scoped booking CRUD
    - Test 2: groups.ts admin procedures use privilegedProcedure
    - Test 3: merits.ts procedures use tenantProcedure/privilegedProcedure
    - Test 4: All three routers return toEnvelope(dto.parse(row))
    - Test 5: DTOs imported from @server/dto/misc (bookingDto, groupDto, groupDetailDto, meritDto)
    - Test 6: JSDoc @tenant/@privileged tags present
    - Test 7: pnpm tsc --noEmit passes
  </behavior>
  <action>
Migrate bookings.ts, groups.ts, and merits.ts — three medium-size routers (200-600 lines each) that already use toEnvelope() partially but need tier migration and DTO enforcement.

**Common migration steps for each:**

1. Import corresponding DTOs from @server/dto/misc: bookingDto, groupDto, groupDetailDto, meritDto
2. Import toEnvelope from @api/server (verify it's already present)
3. Audit all procedure tiers:
   - adminProcedure → privilegedProcedure (COMMITTEE included)
   - protectedProcedure + tenant-scoped → tenantProcedure
   - protectedProcedure + user-scoped → keep as protectedProcedure
4. Remove inline `if (!ctx.tenantId)` checks where tenantProcedure takes over
5. Ensure every return wraps in toEnvelope(): toEnvelope(rows.map(r => dto.parse(r))) or toEnvelope(dto.parse(row))
6. Replace inline output schemas with DTO references
7. Use ctx.db for DB queries
8. Add JSDoc tags

**Bookings router specifics:**

- Facility booking queries are tenant-scoped → tenantProcedure
- Admin booking override → privilegedProcedure
- Existing toEnvelope() usage confirmed by RESEARCH.md (§Current State)

**Groups router specifics:**

- Group listing is tenant-scoped → tenantProcedure
- Group moderation → privilegedProcedure
- Group join/leave → protectedProcedure (user action)

**Merits router specifics:**

- Merit listing → tenantProcedure
- Merit creation → privilegedProcedure
- Uses existing toEnvelope() + DTO pattern per RESEARCH.md

Per RESEARCH.md §Current State Table: these routers "import toEnvelope" but "none apply it consistently." The migration makes it consistent.
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/bookings.ts src/server/routers/groups.ts src/server/routers/merits.ts 2>&1 | head -30</automated>
</verify>
<done>
Bookings, groups, and merits routers — correct tiers, consistent envelope wrapping, DTO-mapped returns. TypeScript compiles clean.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate Notifications, Invitations, Settings, Agents Routers</name>
  <files>src/server/routers/notifications.ts, src/server/routers/invitations.ts, src/server/routers/settings.ts, src/server/routers/agents.ts</files>
  <behavior>
    - Test 1: notifications.ts user procedures use protectedProcedure, admin use privilegedProcedure
    - Test 2: invitations.ts tenant-scoped procedures use tenantProcedure
    - Test 3: settings.ts procedures use tenantProcedure/privilegedProcedure
    - Test 4: agents.ts procedures use agentProcedure/privilegedProcedure
    - Test 5: All returns toEnvelope(dto.parse(row)) — notificationDto, invitationDto, settingDto, agentProfileDto
    - Test 6: JSDoc @tenant/@privileged tags present
    - Test 7: pnpm tsc --noEmit passes
  </behavior>
  <action>
Migrate notifications.ts, invitations.ts, settings.ts, and agents.ts — four smaller routers (100-400 lines) that need full migration: tier adoption, DTO wiring, envelope wrapping.

**Notifications router:**

- DTO: notificationDto from @server/dto/misc
- User notification list → protectedProcedure (user-scoped)
- Admin notification create → privilegedProcedure
- Ensure all returns use toEnvelope(notificationDto.parse(row))

**Invitations router:**

- DTO: invitationDto from @server/dto/more
- Invitation create/accept → tenantProcedure (tenant-scoped)
- Admin invitation management → privilegedProcedure
- Existing toEnvelope() usage confirmed

**Settings router:**

- DTO: settingDto from @server/dto/more
- Settings read/write → tenantProcedure (tenant-scoped)
- Admin settings → privilegedProcedure

**Agents router:**

- DTO: agentProfileDto from @server/dto/more
- Agent-scoped procedures → agentProcedure (keep, this tier is correct for agent endpoints)
- Admin agent management → privilegedProcedure

**Common steps:**

1. Import DTOs from @server/dto
2. Import toEnvelope from @api/server
3. Apply tier migration per table above
4. Remove inline tenantId checks
5. Wrap all returns in toEnvelope()
6. DTO-parse all returned rows
7. Use ctx.db
8. JSDoc tags

Per D-00b: agentProcedure is correct for agent-scoped endpoints — keep it. Only migrate adminProcedure → privilegedProcedure where COMMITTEE should have access.
</action>
<verify>
<automated>pnpm tsc --noEmit --pretty src/server/routers/notifications.ts src/server/routers/invitations.ts src/server/routers/settings.ts src/server/routers/agents.ts 2>&1 | head -30</automated>
</verify>
<done>
Notifications, invitations, settings, and agents routers migrated. Correct tiers, envelope wrapping, DTO mapping. TypeScript compiles clean.
</done>
</task>

</tasks>

<verification>
Post-plan:
- [ ] `pnpm tsc --noEmit` passes across all 10 router files
- [ ] `grep -rn 'adminProcedure' src/server/routers/{content,achievements,events,bookings,groups,merits,notifications,invitations,settings,agents}.ts` — zero hits (or accompanied by justification comment)
- [ ] `grep -rn 'protectedProcedure' src/server/routers/{content,achievements,events,bookings,groups,merits,notifications,invitations,settings,agents}.ts` — only user-scoped endpoints remain
- [ ] Manual spot-check: call 3 random procedures from these routers via tRPC panel, confirm envelope shape { success, data }
</verification>

<success_criteria>

1. All 10 routers use correct procedure tiers (tenantProcedure/privilegedProcedure/protectedProcedure as appropriate)
2. All returns wrapped in toEnvelope() with DTO parsing
3. Zero inline output Zod schemas
4. ctx.db used for DB queries
5. JSDoc classification tags on all procedures
6. pnpm tsc --noEmit passes
   </success_criteria>

<output>
Create `.planning/phases/120-api-governance-hardening/120-03-SUMMARY.md` when done
</output>
