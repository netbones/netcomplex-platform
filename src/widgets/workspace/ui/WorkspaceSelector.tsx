/**
 * WorkspaceSelector — STUB
 *
 * Replaced in P1a-04 (plan 122-04). DO NOT consume before then.
 *
 * Hierarchical tree selector with search/recent/favorites/pinned/all
 * (UI-SPEC §'Hierarchical Workspace Selector', RESEARCH §10.1).
 */

/** Stub — replaced in P1a-04 (plan 122-04). DO NOT consume before then. */
export function WorkspaceSelector() {
  return null;
}
