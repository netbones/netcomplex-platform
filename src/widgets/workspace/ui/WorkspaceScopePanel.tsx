/**
 * WorkspaceScopePanel — STUB
 *
 * Replaced in P1a-06 (plan 122-06). DO NOT consume before then.
 *
 * First-class workspace object showing scope, delegator, permissions, expiry.
 * Renders in the app shell as a persistent element (UI-SPEC §'Surface 3').
 */

/** Stub — replaced in P1a-06 (plan 122-06). DO NOT consume before then. */
export function WorkspaceScopePanel() {
  return null;
}
