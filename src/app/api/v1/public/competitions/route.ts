// Re-export from canonical route location
// Public read-only endpoint for competitions
// This route exists to establish the /api/v1/public/ namespace.
export { GET } from '@/app/api/competitions/route';
