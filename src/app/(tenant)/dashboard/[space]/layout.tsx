/**
 * Space sub-route layout — simple pass-through.
 * Future: space-specific header/breadcrumbs can be added here.
 */
export default function SpaceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
